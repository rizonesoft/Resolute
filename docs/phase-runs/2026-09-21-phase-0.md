# Phase run: Phase 0 -- Gates, proof, and the bar (2026-09-21)

Guard id: a4535213 (`*/10 * * * *`, Run-guard heartbeat, skips while active). Plan owns the guard.

## Audit (process-plan 2026-09-20T23:35Z)

1. Graph: 1 fatal, 0 warnings, 100 adjacency advisories (all WARN lines carry the advisory tag; count matches the prior run).
2. Plan currency: `plan --check` was stale (progress JSON, retargeted by uncommitted §24 items); `plan --sync` regenerated it (216 rows, 0 boxes changed); re-check green, 38/216 sections complete (18%).
3. Ready rows: 34 runnable now, 0 runnable elsewhere. First per phase: Phase 0 D00 T04 §23, Phase 1 D01 T03 §1, Phase 2 D03 T02 §1; Phases 3 and 4 have no ready rows.
4. Blocked rows: 145 waiting. Whole-TODO gates dominate: the sampled head shows D00 T02 §6-§12 waiting on cpp-toolchain-and-gates (whole TODO).
5. Starting Phase 0: first phase in plan order with runnable-now rows (11 runnable here).

Prior run file: docs/phase-runs/2026-09-20-phase-0.md. It shipped D00 T04 §12-§22 (11 sections) with no closeout; the operator killed the campaign and its guard 1fcf370b is gone (cron store empty, delete confirms unknown job). Carryovers: D00 T04 §24 in flight (Started 2026-09-20T22:13Z, 4/24 items, uncommitted) and the S22 plan-review gpt-rung retry-owed (owner Derick Payne, due 2026-09-27), which is operator-owned, not this run's.

## Phase repairs

- The 1 FATAL is residue, not history: TODO-04 §9's `Commit: "workspace: review-input integrity"` prefix-matches 2 commits (2b4b493e the §9 ship, 734e5c08 the §21 implementation), and the uncommitted §24 strictness fails multi-match while HEAD's rule blesses it. HEAD is derived 0-fatal: both commits predate the §21 and §22 stamps that recorded 0 fatal, and HEAD adds only a run-record commit.
- The FATAL's owner is the in-flight §24 (its OID item owns the live-line migration), so §24 resumes first as the repair vehicle and table order restarts at §23 after it lands. No other row can pass gates on a red tree.
- Resolve sweep over all 26 open Phase 0 rows, no exit 1/2. Exit 0 (11): D00 T04 §23, §24, §25; D07 T01 §2, §3; D09 T01 §1, §2; D00 T01 §8, §9; D00 T05 §1; D00 T06 §1. Exit 4 leftovers (15): D07 T01 §4; D09 T01 §3; D00 T02 §6-§12; D00 T05 §2, §3; D00 T06 §2-§5.
- Step 1.4 delta: no committed change since the prior run's full read touches TODO-01, TODO-02, TODO-05, TODO-06, D07 T01, D09 T01, or README (verified by log; every commit since 2026-09-20T00:33Z touches TODO-04 §§20-25 only). Re-read owed: TODO-04 §§23-25 plus the uncommitted §24 hunks.

## Shipped-row verification

- Spine (shipped rows the 11 runnable rows depend on): D00 T04 §18, §20, §21, §22; D00 T01 §3, §4, §5; D00 T02 §3; D07 T01 §1. All resolve exit 3, stamps cover.
- Checkpoint re-runs deferred until §24 lands the tree green: every spine checkpoint gates on `validate`/check-all, which the live FATAL fails today. Re-running red gates now would prove only the known FATAL.

## Gap audit

- Pending: runs after §24 lands and before §23 ships, so the shipping loop starts from a green tree and a filed gap set.

## Sections

### D00 T04 §24 -- Review-tooling follow-ups -- RESUMED (was in flight at the kill)

- Adopted state: Started 2026-09-20T22:13Z (kept), 4/24 items ticked with Done notes, 6 files uncommitted (scripts/todo-graph.py, todo-runs.py, todo-validate.py; TODO-03 §1, TODO-04 §24, README contract).
- Residue baseline re-measured: `validate` 1 FATAL (§9 multi-match, the OID item's own fallout); graph self-test 567 cases with 2 failed (`bound prefix passes`, `ticked bound passes` — the half-wired ancestry/touch gates reject the brepo fixtures); runs self-test 111 green.
- Resume point: finish item 5 (OID binding: representation, ancestry plus section identity, duplicate-subject fixture, bound/unbound/duplicate legs) including the 5-live-line migration, then items 6-23 in order.
- Item 5 shipped: resolved-and-pinned; §9 requoted (binds `2b4b493e`), other 4 live lines unchanged; duplicate fixture §§5-6; three completion fixes (git-read UTF-8, diff-tree `--root`, absolute-path relativize). Tree green: `validate` 0 fatal, graph suite 569 green, report `5 Commit, 0 failures`. §24 at 5/24.
- Item 6 shipped: proof requires `(item: ...)` verbatim-equal to the deferral's item; §26 proof re-pointed; fixtures §§37-42 (section-only fails, item-citing passes, wrong-item fails); no live migration; README rule 3 carries the grammar (+3 graph cases). §24 at 6/24.
- Item 7 implemented, ticks at ship: count discipline written into process-todo-section steps 4 and 7; items 1-6 Dones converted to delta form (+0/+4/+1/+2/+2/+3); totals quote once at §24's commit.
- Item 8 implemented, drive owed at §24's plan review: plan-review gpt rung rides stdin like the panel runner (argv form gone from the skill); ticks when a fenced plan prompt over 48KB runs quoted (the formal review carries the drive; no throwaway round).
- Item 9 shipped: attest runs the panel check itself (witness sha over manifest plus runner bytes); `--checker-output` refused; skill handoff dropped; legs refused/witnessed/runs (+3 review-prompt cases); old attestations read. §24 at 7/24 ticked (+2 implemented).
- Item 10 shipped: ordered coverage via `_seq_partitioned` (diminishing subsequences per F13, declaration order then permutations); parsers refactored to ordered generators; rearranged-changes/markers fail quoted, ordered/concatenation/interleaved pass (+5 review-prompt cases); live cross-check over `734e5c08` exit 0. §24 at 8/24 ticked (+2 implemented). Correction: contiguous tiling was built first from a misremembered item text (C1/freeze-window confabulation; the tick anchor missed and HEAD proved the true round-2 A text); reworked to the prescribed subsequence. No concurrent writer: status/log clean, HEAD unchanged.
- Item 11 shipped: 3 §1 cites re-pointed to scripts/, 4 cites marked (2 historical, 1 ephemeral, 1 machine-local); marker grammar in the anchors gate (+7 review-prompt cases); sweep 38 sections 0 failures quoted. §24 at 9/24 ticked (+2 implemented).
- Item 12 shipped: declaration-pair binding (option b, pair bounds declaration); foreign-outside-pair fails quoted in cross-check and attest; full OIDs ride schema-2 `commits`; §12 regression re-passes (+9 review-prompt cases). §24 at 10/24 ticked (+2 implemented).
- Item 13 shipped: `model_provenance` marker (derived vs trusted, fail-closed rejected for banner-less codex flows); trusted-pin read-back quoted; stderr-derived re-passes (+9 review-prompt cases). §24 at 11/24 ticked (+2 implemented).
- Item 14 shipped: `clock` in the round record plus required `--run-clock` (no mtime fallback, `--timestamp` removed); touched-mtime drive keeps the clock quoted; envelope clock deferred, neither runner reports one (+5 review-prompt cases). §24 at 12/24 ticked (+2 implemented).
- Item 15 shipped: central `GIT_NO_REPLACE_OBJECTS` guard (tool-level assignment plus skill export in the four git fences); item-6 drill re-passes unflagged quoted; non-vacuity control scrubs the guard (+2 review-prompt cases). §24 at 13/24 ticked (+2 implemented).
- Item 16 shipped: anchor-candidate cross-binding (ancestry-or-self for contiguous, declared-equal for assemblies; `cwd` threaded); stranger fails quoted naming membership; §21 seeds re-pass (+5 review-prompt cases). §24 at 14/24 ticked (+2 implemented).
- Item 17 shipped: content-hash line cites (`#hash12`, `cite-hash` CLI); drifted line fails quoted naming the stale cite; D00 T01 re-passes; tree sweep shows only the 2 pre-existing §§16/18 membership reds (+5 review-prompt cases). §24 at 15/24 ticked (+2 implemented).
- Item 18 shipped: rule 28b round correspondence (heading-number mapping, uniqueness enforced, ordering free; pre-cutoff exempt, missing findings stays rule 16's); mistagged and duplicate rounds fail quoted, §21 tags 1-5 resolve over recorded [1-5] (+5 graph cases, +1 review-prompt pin); README mirror row plus ratchet-unlink hygiene for TODO-12. §24 at 16/24 ticked (+2 implemented).
- Item 19 shipped: pinned push URL (`get-url --push --all` re-read before every push against the session-recorded canonical, confirmed against the known canonical never the clone config); all four redirect shapes plus multi-pushurl refuse quoted, threat demo shows the silent fork landing first, bare-remote e2e re-passes (+1 review-prompt pin). §24 at 17/24 ticked (+2 implemented).
- Item 20 shipped: portable review bundle (`bundle` emits one deterministic zip with manifest/body/runner/findings/attestation/transcripts/tools/graph/receipt/verify-command; `bundle-verify` replays all bindings offline, `--recheck-*` resolve where available); WSL second-machine replay green with no repo, tampered member fails quoted, no-key-signing decision recorded (+13 review-prompt cases). §24 at 18/24 ticked (+2 implemented).
- Item 21 shipped: per-attempt stamp outputs (`stamp-N.out/.err`, failover to `stamp-N-opus.out/.err`, stderr newly captured); two-attempt drill preserves both cost figures quoted (1234 + 5678tokens) with attempt 1 byte-distinct (+1 review-prompt pin). §24 at 19/24 ticked (+2 implemented).
- Item 22 shipped: single-dash usage refusal in the runs checker (both gates narrow to `startswith("-")`, mode-aware lines kept); all three refusals plus the findings `-check` pin quoted; double-dash legs unmodified (+3 runs cases to 114, +1 findings case to 46). §24 at 20/24 ticked (+2 implemented).
- Item 23 shipped: `--format` docstring carve-outs (runs report/export, findings interleave) plus the refusal membership sentence; interleave leg pins prose/array/closing order quoted, runs prose leg pins no-array (+1 findings case to 47, +1 runs case to 115). §24 at 21/24 ticked (+2 implemented); all 23 work items done, Commit item plus §24 review/stamp remain.
- §24 self-review fix 1: items 12-23 Dones converted to delta form (in-flight suite totals stripped, `+N` deltas kept), enforcing the item-7 discipline the section itself implements; validator no-false-fire words on items 5/6/18 stay as behavioral evidence per the item-7 conversion precedent. Totals quote once in the commit body.
- §24 self-review fix 2: bundle verify hardened against hostile bytes (role str-type checks, 64MB file / 16MB member caps, duplicate-name refusal) with 4 new legs (review-prompt 280 cases, 0 failed); item 20's delta amended to +17 with the split noted.
- Flake watch: one review-prompt suite run printed a zipfile-internal traceback yet reported 280/0 (exit 1); three surrounding runs clean exit 0. Suspected transient temp-file locking; watch for recurrence.

## Critical events

- 2026-09-20T23:35Z run opened; guard a4535213 created (no prior guard survived).
- Prior campaign killed by operator ~2026-09-20T23:00Z; 6 files carry its uncommitted §24 partial work, adopted not reverted.

## Lessons

- Re-read each item's exact text immediately before building it, never from the session's first read: item 10 was implemented from a confabulated C1/freeze-window text (contiguous tiling) and reworked to the true round-2 A text (diminishing subsequence) after the tick anchor missed. Exact-match ticks verify text, but only at tick time; the read-before-build closes the gap for the implementation itself.
