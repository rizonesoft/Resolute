Proves: D00 T01 §9's inventory of the Linux and WSL execution surface before the purge and what remains after it, each kept hit with its reason in the section's Done notes.
Produced by: git grep (git 2.55.0.windows.2), commands below, run on Windows Version 10.0.26200.9457, 2026-09-26T10:53:02Z, against commits 73dbcf9104e303e6a433e69e8c31ca9a52046a54 (before) and 0ec273d4239e74a851b99267252454b45297c1fb (after). Complete matching lines, untruncated; the only edit is the leading '<commit>:' prefix git prints, removed. Text output; it travels.

=== $ git grep -n -I -i -E 'wsl|ubuntu|linux|python3 ' 73dbcf9104e303e6a433e69e8c31ca9a52046a54 -- . :!resolute_au3 :!docs/reviews :!docs/phase-runs :!docs/captures :!docs/brainstorm :!todo/implementation-plan.md
.claude/skills/review-todo-section/SKILL.md:96:Every review mints one private directory and stages every prompt file under it: fixed `/tmp` names collide across concurrent sessions on one machine, proven when a `D00 T04 §9` round fenced another session's contract as its own and voided the round. Mint once per review, reuse for every round, plan review, and stamp review of that review, and keep the directory as evidence (no trap-delete; the OS scrubs `/tmp`). On Windows, run every `bash` block in this skill under Git Bash at `C:\Program Files\Git\bin\bash.exe`, never the `bash` on PATH (that is WSL's, and it resolves `python3` to Ubuntu's interpreter against the wrong tree). Git Bash carries `mktemp`, `timeout`, `sed`, and `date`, and both `python` and `python3` there resolve to the same Windows interpreter (probed 2026-09-19), so the blocks below run verbatim, with one translation: MSYS2 converts bare path arguments to Windows paths but not paths embedded in `TITLE=path`, so every fence command first translates its chunk paths with `cygpath -w` (untranslated, python resolves Git Bash `/tmp` to the wrong place and fence exits 2 with `cannot read`).
.github/workflows/plan.yml:15:    runs-on: ubuntu-24.04
.github/workflows/plan.yml:23:        run: python3 scripts/todo-graph.py self-test
.github/workflows/plan.yml:25:        run: python3 scripts/todo-graph.py validate
.github/workflows/plan.yml:28:          python3 scripts/todo-graph.py plan --sync
README.md:35:From WSL, the build itself still runs on the Windows side: the pinned toolchain is Windows binaries and Windows CMake cannot read WSL paths. `scripts/wsl.sh` translates the repository path and re-invokes the named script under Windows PowerShell 7, so every path downstream is native.
README.md:38:scripts/wsl.sh cpp-env.ps1        # check the toolchain resolves
README.md:39:scripts/wsl.sh build.ps1 -All     # same build command, via Windows PowerShell
README.md:40:scripts/wsl.sh check-all.ps1      # every gate, the same way
scripts/campaign_guard.py:2216:        # Git Bash by path: the `bash` on PATH can be WSL's, which runs a
scripts/check-all.ps1:78:# under WSL and CI, `py` as the Windows launcher fallback. Resolve once so a
scripts/review_prompt.py:11:WSL checkouts agree), and reviewer output is validated whole (item 15: one
scripts/review_prompt.py:1848:        # GitHub's defaults: `bash -e {0}` on Linux and macOS runners,
scripts/review_prompt.py:1850:        if runner.startswith(("ubuntu", "macos")):
scripts/review_prompt.py:6929:            plan_text = ("jobs:\n  plan-gates:\n    runs-on: ubuntu-24.04\n    steps:\n      - uses: actions/checkout@abc\n"
scripts/review_prompt.py:6930:                         "      - name: Self-test the TODO graph tool\n        run: python3 scripts/todo-graph.py self-test\n"
scripts/review_prompt.py:6931:                         "      - name: Validate the TODO tree\n        run: python3 scripts/todo-graph.py validate\n"
scripts/review_prompt.py:6933:                         "          python3 scripts/todo-graph.py plan --sync\n          if true; then\n"
scripts/review_prompt.py:7059:            ctx_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: ctx\n        working-directory: tools\n"
scripts/review_prompt.py:7071:            q_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: q\n"
scripts/review_prompt.py:7079:            for label, yml in (("flow env", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        env: {MODE: x}\n        run: make\n"),
scripts/review_prompt.py:7080:                               ("block env", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        env:\n          MODE: |\n            x\n        run: make\n"),
scripts/review_prompt.py:7081:                               ("flow workflow env", "env: {X: 1}\njobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        run: make\n"),
scripts/review_prompt.py:7083:                               ("block working-directory", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        working-directory: >-\n          tools\n        run: make\n"),
scripts/review_prompt.py:7084:                               ("block shell", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        shell: |\n          bash {0}\n        run: make\n"),
scripts/review_prompt.py:7085:                               ("container job", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    container: node:22\n    steps:\n      - name: a\n        run: make\n"),
scripts/review_prompt.py:7092:                    "  second:\n    steps:\n      - name: s\n        run: make\n    runs-on: ubuntu-24.04\n"
scripts/review_prompt.py:7093:                    "  third:\n    steps:\n      - name: t\n        run: make\n    runs-on: ubuntu-24.04\n    container: node:22\n")
scripts/review_prompt.py:7095:            commented = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n# a column-zero comment inside the job\n"
scripts/review_prompt.py:7098:            inline_c = "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps: # build\n    - name: B\n      run: make\n"
scripts/review_prompt.py:7103:                  and got_c[0]["context"]["runs-on"] == "ubuntu-24.04", str(got_c))
scripts/review_prompt.py:7105:                  by["s"]["context"]["runs-on"] == "ubuntu-24.04" and by["f"]["context"]["runs-on"] == "windows-2025",
scripts/review_prompt.py:7109:            quoted = ('jobs:\n  "j":\n    "runs-on": ubuntu-24.04\n    "steps":\n      - "name": q\n'
scripts/review_prompt.py:7115:                  and qs["q"]["run"] == "make" and qs["q"]["context"]["runs-on"] == "ubuntu-24.04", str(qs["q"]))
scripts/review_prompt.py:7118:            flow = "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - {name: f, run: make}\n"
scripts/review_prompt.py:7122:            uses_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - uses: actions/checkout@abc\n"
scripts/review_prompt.py:7129:            fake = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: Run actions/checkout@prepare\n"
scripts/review_prompt.py:7136:            flow_with = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - uses: actions/checkout@abc\n"
scripts/review_prompt.py:7299:            wf_env = "env:\n  X: 1\njobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        run: make\n"
scripts/review_prompt.py:7304:                      ("Self-test the TODO graph tool", "python3 scripts/todo-graph.py self-test"),
scripts/review_prompt.py:7305:                      ("Validate the TODO tree", "python3 scripts/todo-graph.py validate"),
scripts/review_prompt.py:7307:                       "python3 scripts/todo-graph.py plan --sync\nif true; then\n  test -z x\nfi")],
scripts/review_prompt.py:7312:                  and f"rerun locally at {c1[:12]}: Validate the TODO tree: python3 scripts/todo-graph.py validate" in line
scripts/review_prompt.py:7428:                         "jobs:\n  x:\n    runs-on: ubuntu-24.04\n")
scripts/review_prompt.py:7684:            sec_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: s\n        env:\n"
scripts/review_prompt.py:7864:            cred_step = workflow_steps("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: c\n"
scripts/todo-graph.py:268:    nowhere else -- in particular never under WSL, whose
scripts/todo-graph.py:5382:                  detect_context(platform="linux", environ={}),
scripts/wsl.sh:3:# Run one of this repository's PowerShell scripts on the Windows side, from WSL.
scripts/wsl.sh:6:# cannot read WSL paths, so the .ps1 scripts cannot run under Linux pwsh.
scripts/wsl.sh:7:# This wrapper translates the script path with wslpath and re-invokes it
scripts/wsl.sh:12:#   scripts/wsl.sh <script.ps1> [args...]
scripts/wsl.sh:13:#   scripts/wsl.sh build.ps1 -All -Config Release
scripts/wsl.sh:14:#   scripts/wsl.sh check-all.ps1
scripts/wsl.sh:22:# Windows PowerShell 7. Override the interpreter with WSL_PWSH.
scripts/wsl.sh:30:    echo "usage: scripts/wsl.sh <script.ps1> [args...]" >&2
scripts/wsl.sh:31:    echo "  runs a repository PowerShell script on the Windows side, from WSL" >&2
scripts/wsl.sh:32:    echo "  example: scripts/wsl.sh build.ps1 -All -Config Release" >&2
scripts/wsl.sh:35:# Outside WSL this wrapper has no job: the .ps1 runs directly.
scripts/wsl.sh:36:if ! command -v wslpath >/dev/null 2>&1; then
scripts/wsl.sh:37:    echo "wsl.sh: not running under WSL (wslpath not found)" >&2
scripts/wsl.sh:53:        echo "wsl.sh: not a .ps1 script: '$name'" >&2
scripts/wsl.sh:65:    echo "wsl.sh: unknown script '$name'" >&2
scripts/wsl.sh:76:        echo "wsl.sh: refusing a script outside the repository: $resolved" >&2
scripts/wsl.sh:81:WIN_REPO="$(wslpath -w "$REPO_ROOT")"
scripts/wsl.sh:84:        echo "wsl.sh: warning: the checkout is not on a Windows drive ($WIN_REPO)" >&2
scripts/wsl.sh:85:        echo "  Windows tools reach it over the WSL network share, which is slow" >&2
scripts/wsl.sh:89:WIN_SCRIPT="$(wslpath -w "$resolved")"
scripts/wsl.sh:93:PWSH="${WSL_PWSH:-}"
scripts/wsl.sh:99:        echo "wsl.sh: warning: Windows PowerShell 7 not found, using 5.1" >&2
scripts/wsl.sh:101:        echo "wsl.sh: no Windows PowerShell found" >&2
scripts/wsl.sh:104:        echo "  fix: install PowerShell 7 on the Windows side, or set WSL_PWSH" >&2
todo/00-workspace/TODO-01-toolchain-and-gates.md:53:|   9   |   §9    | Remove the Linux execution surface         | §5         |  [ ]   |
todo/00-workspace/TODO-01-toolchain-and-gates.md:860:## 9. Remove the Linux Execution Surface
todo/00-workspace/TODO-01-toolchain-and-gates.md:862:Resolute is a Windows-only suite, per the operator's 2026-09-20 decision, but the tree still carries a Linux-development surface: `scripts/wsl.sh` bridges WSL to the Windows scripts, and CI jobs, tests, or docs may assume a Linux interpreter. Every such surface is untested ground that rots: nobody runs it, so nobody notices when it breaks, and its presence invites the next contributor to lean on it. This section inventories the surface and removes it, leaving the Windows-native tooling untouched.
todo/00-workspace/TODO-01-toolchain-and-gates.md:866:- [ ] Inventory every Linux/WSL-execution surface: `scripts/wsl.sh`, CI jobs on Linux runners, scripts requiring a Linux interpreter, tests that fork Linux tooling, and docs or plan text instructing WSL flows. Done when: the inventory lists each item with its references (what calls or cites it), quoted from grep, and names what stays: Git Bash at `C:\Program Files\Git\bin\bash.exe`, which the review skill requires; the llvm-mingw Windows-targeting toolchain; the stdlib Python tooling that runs on Windows.
todo/00-workspace/TODO-01-toolchain-and-gates.md:867:- [ ] Remove the bridge and its callers: delete `scripts/wsl.sh` and re-point or delete everything referencing it. Done when: no reference to `wsl.sh` or WSL execution remains outside this section's own record, quoted from grep.
todo/00-workspace/TODO-01-toolchain-and-gates.md:868:- [ ] Remove the remaining inventoried surface: Linux-runner CI jobs, Linux-gated tests, Linux-conditional code paths. Done when: the step-1 inventory reads empty item by item, each quoted, and nothing removed has a live caller left behind (grep for each removed name returns nothing outside this section's record).
todo/00-workspace/TODO-01-toolchain-and-gates.md:870:- [ ] Settle the review-prompt self-test's POSIX status: under WSL `python3 scripts/review_prompt.py --self-test` fails `anchors-untracked-fires` (a fixture cite written with a Windows separator, `todo\README.md`), found by the D00 T04 §39 independent review and present before it (the pre-§39 suite failed three legs under WSL, the post-§39 suite one). Done when: either the self-test is declared Windows-only with the rest of the Linux surface this section removes (and the AGENTS.md line saying the TODO tooling runs anywhere says so), or the fixture is separator-neutral and the suite passes under a POSIX interpreter, quoted.
todo/00-workspace/TODO-01-toolchain-and-gates.md:871:- [ ] Scope the purge to what executes: the removal criteria name every WSL reference outside this section's record, which would rewrite stamped review evidence and historical run notes, and removing Linux-gated paths would take the Linux-runner oracle captures and the portable template checks D00 T04 §37 and D00 T04 §39 rely on (plan review of the D00 T04 §39 candidate, PR16 and PR17; needs D00 T04 §39 shipped). Done when: the inventory separates executable paths and active guidance (removed) from immutable review evidence, historical notes, captured drill logs, and Git Bash dependencies (kept, each with its reason), the supported host and interpreter matrix is decided per script before anything is removed, and the grep criteria exempt the kept records, quoted.
todo/00-workspace/TODO-01-toolchain-and-gates.md:872:- [ ] Commit: `"workspace: remove the Linux execution surface"`
todo/00-workspace/TODO-01-toolchain-and-gates.md:876:-> SOURCE: operator-2026-09-20-no-linux
todo/00-workspace/TODO-01-toolchain-and-gates.md:878:-> XREF: D00 T04 §39 -- the WSL self-test failure its independent review found, filed as an item here
todo/00-workspace/TODO-04-self-correction.md:1064:- [x] Emit one signed portable review bundle: operators need one independently verifiable artifact rather than convention-linked files (plan review of the §21 candidate, PR20, premium win). Done when: one command emits the bundle (manifests, raw outputs, findings, attestations, checker transcripts, tool versions, candidate graph, push receipt, verification command), the verification command replays green quoted on a second machine, and the self-test pins bundle schema plus one tampered-bundle leg. Cheaper substitute that fails the checkpoint: the rundir tarball, which is portable files, not a verifiable bundle. Done: `bundle` emits one deterministic zip (fixed date/order/attrs, so identical inputs emit identical bytes) carrying the sign-off round's manifest, fenced body, runner output, findings, attestation, and checker transcripts, plus the tool record (python, git, script sha), the candidate graph (base/head/trees, declared commits), the push receipt (URL, ref, OID, readback match), and the verification command text (`review-bundle/1`); `bundle-verify` replays member hashes plus the exact member set, the attestation rebinds (findings, runner, witness), the panel recheck with reason match, the body byte/sha replay, transcript PASS lines, and the push/candidate shapes, all offline (no git, no network, no repo), with `--recheck-graph` resolving OIDs/trees/declaration and `--recheck-remote` re-reading the landing where they exist. Emit and verify share one binding authority, so an inconsistent bundle never ships (schema-1 attestations refuse: nothing to rebind). Second-machine replay quoted: script plus bundle copied into WSL /tmp with no repo (`git rev-parse` exit 128), `python3 review_prompt.py bundle-verify` exits 0 with the same digest (`8 member(s) agree, attestation approve (codex-panel/gpt-5.6-sol), panel recheck PASS four lenses, one verdict each, transcripts pass 3/3`). Tampered bundle fails quoted: `member findings.md hashes 18184e63fe9d..., manifest binds 57de9b9bde4d...` exit 1 (drive: `%TEMP%/s24-item20-drive.py`, re-runnable; rechecks quote `trees match` and `confirms`). No key signs the bundle (stated in the skill): tamper-evidence rides the manifest hashes plus the exact member set, attribution rides the push receipt the remote confirms (residual, self-review fix 8: a wholesale rewrite with consistent hashes passes default verify; the run-record digest quote plus `--recheck-remote` close it), and `gpg --detach-sign` over the deterministic bytes stays a one-liner. Skill wired: post-push fence reruns the three checker transcripts deterministically, emits, verifies before handing off, and quotes the digest in the run record. Legs: roundtrip emit/verify, determinism, tampered member, member set, emit-refuses-rebind, emit-refuses-transcript, recheck graph/mismatch, recheck remote/mismatch, plus the `skill-bundle` pin (+28 review-prompt cases: 13 at implementation, 4 hostile-input hardening at self-review, 4 caps plus single-PASS at self-review, 5 holding shapes at round 4, 2 witnessed shapes at round 5). Self-review fix 5 (adversarial): the verifier bounds the member total (`BUNDLE_MAX_TOTAL`) and count (`BUNDLE_MAX_COUNT`) beside the per-member bounds, since per-member caps alone admit a thousand small members expanding past memory (`bundle-cap-total`, `bundle-cap-count`). Self-review fix 6 (adversarial): a carried transcript must be exactly one PASS-led line (`_bundle_transcript_ok`), so a failures-plus-injected-PASS record no longer verifies as passing; the emit and verify legs pin the injected shape at both ends and the old containing-PASS leg is retargeted. Round-4 fix (F15): single-PASS-led admitted the checker's own blocking output (`PASS N naming(s) to answer` exits 0 by the branch-on-reason design), so transcripts must now match one checker's holding shape (panel exact, plan alternation, stamp holding-only; disjoint, no role plumbing): blocking and forged PASS lines refuse at emit and fail at verify, the parameterized plan shape accepts, and the item-20 drive re-passes green. Round-5 fix (F21): the plan and stamp holding patterns are witnessed (real checker stdout piped into the transcripts), so a reason drift fails the legs instead of every suite staying green; the hand-written plan leg stays for parameterized-count generality.
todo/00-workspace/TODO-04-self-correction.md:1152:- [ ] Replay the bundle verify on a genuinely separate machine: the WSL proof shares the host, so portability past the hypervisor rides assertion (plan review of the §24 candidate, PR12; needs §24 shipped: it extends §24's bundle proof). Done when: the verify replays green quoted on independent hardware or a CI worker when either exists (until then the WSL proof stands with its scope -- separate kernel and filesystem, no repo -- stated), and the item records which substrate ran it. Cheaper substitute that fails the checkpoint: a second same-host replay, which re-proves the same scope.
todo/00-workspace/TODO-04-self-correction.md:1425:- [x] Decode `run:` scalars the way GitHub does, or state the forms read: `workflow_step_commands` in `scripts/review_prompt.py` returns raw scalar text, so `run: "python3 scripts/check.py"` prints with its quotes and `run: >` prints unfolded (panel round 3 of the D00 T04 §33 review, F8; needs §33 shipped: it rethinks §33's command fallback). Done when: plain, single-quoted, double-quoted (with escapes), literal `|`, and folded `>` scalars, each with `-`/`+` chomping, decode to the command text GitHub runs, one fixture leg per form quoted; any form still unsupported prints `ci-wait: rerun locally: <step>: unsupported run: form, read the workflow` instead of a guessed command, quoted from a leg; and the three earlier block legs re-pass. Cheaper substitute that fails the checkpoint: a fourth ad hoc patch for the two forms the round named. Done: `workflow_steps` in `scripts/review_prompt.py` decodes plain (comment stripped), single-quoted (`''`), double-quoted (every YAML escape, `\x`, `\u`, `\U`), literal `|` and folded `>` blocks with `-`/`+` chomping and an indentation indicator, per YAML 1.2; anchors, aliases, tags, flow collections, multi-line quoted or plain scalars, and unknown escapes refuse as `ci-wait: rerun locally at <sha>: <step>: unsupported run: form (<why>), read the workflow`. The expected outputs come from GitHub: a disposable `scalar-echo.yml` ran every form under `shell: cat {0}` (run 36175449390), and its eleven step outputs are `SCALAR_ECHO_GITHUB`. Legs (+23): `scalar-decodes-as-github: <form>` for all eleven, `scalar-set-covers-every-github-step`, `scalar-chomping-clip-strip-keep`, and five `scalar-refuses` plus five `rerun-refuses-unsupported` (anchor, multi-line double, multi-line plain, unknown escape, flow sequence); dropping the double-quote decoding fails `scalar-decodes-as-github: double quoted` by name. The earlier block legs re-pass.
todo/00-workspace/TODO-04-self-correction.md:1429:- [x] Print a re-run command with its context or refuse it: a decoded `run:` line still depends on the step's `shell`, `working-directory`, `env`, `${{ }}` expressions, matrix values, and earlier steps (plan review of the D00 T04 §33 candidate, PR5, PR6, and PR7; needs §33 shipped: it extends the command fallback beside the scalar item above). Done when: the supported YAML subset is stated (scalar forms, chomping, indentation indicators) with every other form refused; a step whose context the fallback cannot reproduce prints `cannot reproduce locally: <reason>` rather than a command; and the decoder's expected outputs come from a disposable workflow that echoes each decoded fixture on GitHub, not from self-authored strings, quoted. Done: the supported subset is stated in `workflow_steps`' docstring; `rerun_lines` prints a one-line command with its `cd <working-directory> &&` and `KEY=value` env prefix and a `(shell: ...; earlier steps assumed run)` line, or a multi-line script as written under the same context line, and refuses `cannot reproduce locally: <reason>` for `${{ }}` expressions (matrix, secrets, contexts) in the command, env, or directory, and for workflow or job `env` or `defaults`. The decoder's expectations are GitHub's (item 1). Legs (+3): `rerun-prints-its-context`, `rerun-refuses-expressions`, `rerun-refuses-workflow-env`. **Corrected 2026-09-25 by the independent review (F2, F3, F4):** env forms the parser does not read (a flow mapping, a block or empty value, workflow-level flow `env`) were dropped or misread, context values were interpolated unquoted and the env reached only the first command, and the default shell was assumed to be Bash. The context now renders as a quoted script (`export KEY='value'` lines and `cd 'dir'` ahead of the command, so the env reaches the whole script), unreadable env forms refuse, and the shell comes from the job's `runs-on` (`bash -e {0}` on Linux and macOS, `pwsh` on Windows, refused when the runner is unknown or a non-POSIX shell would need env rendering). Legs (+7): `rerun-quotes-values-and-scopes-env-to-the-script`, five `rerun-refuses-unreadable-context` (flow env, block env, flow workflow env, unknown runner, windows env), `rerun-names-the-windows-default-shell`; the context leg now expects the quoted script. **Corrected 2026-09-25 by panel round 1:** a block-valued `shell` or `working-directory` was read as its header (`cd '>-'`); both refuse now. Legs `: block working-directory`, `: block shell` (+2). **Corrected 2026-09-25 by panel round 2:** a job running in a `container:` (or with `services:`) was advertised with the host's default shell; it refuses now, the container's shell and filesystem not being the host's. Leg `: container job` (+1).
todo/00-workspace/TODO-04-self-correction.md:1616:- [x] Keep the oracle's evidence durable: the scalar and context expectations came from workflows that lived only on deleted branches (plan review PR16; needs §37 shipped). Done when: the drill workflow definitions, the runner image and version, and the captured outputs live under `docs/captures/` with their run ids, and the self-test names that file, quoted. Done: `docs/captures/ci-oracle/` holds, per drill, the workflow file at its head commit (fetched from GitHub by commit), the run record, and the full log, with a README and `oracle.json` listing the four drills (runner image and version per drill); the in-code `SCALAR_ECHO_*` and `CONTEXT_ECHO_*` constants are gone, replaced by `oracle_drills`, `oracle_workflow`, and `oracle_log`, whose expectations are read from GitHub's logs, and the self-test also compares each printed template with the log's own `shell:` line (`normalize_template`). Legs (+8): `oracle-evidence-is-durable:` per drill (4), `rerun-template-is-githubs:` for the four Linux steps (4). **Corrected 2026-09-26 by the independent review:** the Windows execution legs broke the self-test off Windows (a quoted command line passed as one executable under WSL, and `powershell` and `cmd` required everywhere) (P2); Windows shells execute only on a Windows host, PowerShell through an argument vector, while the template and durability legs run everywhere. Under WSL the suite now fails one leg, `anchors-untracked-fires`, which also failed before this section (the pre-§39 suite failed three under WSL); it is filed in D00 T01 §9, which owns the Linux execution surface.
todo/00-workspace/TODO-04-self-correction.md:1622:-> XREF: D00 T01 §9 -- the WSL-only self-test failure this section's independent review found, filed there
todo/00-workspace/TODO-04-self-correction.md:1643:> **Review:** round 5 GPT depth, candidates `f122556e`(round 1) `f5c4bc06`(round 2) `96221264`(round 3) `2d337c78`(round 4) `897d6426`(round 5) -- `adversarial` needs-attention at round 5, filed: F18 in D00 T04 §41 (the hard cap; a block scalar as a trigger-list pattern), closed earlier: 8 fixed (redaction and the flow-list reader each rethought after three rounds) · `consistency` approves at round 5, closed: none raised · `integration` needs-attention at round 5, filed: F19 in D00 T04 §41 as major (a hand-written foreign transition in the campaign's own run file), closed earlier: 5 fixed · `record` approves at round 5, closed: 1 fixed · `source-defect` approves in-session (GitHub's semantics read from its own drill logs) · `design` not owed. Independent pass on the implementation commit 34bc5ecb (`independent` slot, gpt-6-astra high): one P1 and five P2s, all right, fixed in f122556e before round 1; the WSL-only failure it surfaced predates the section, filed in D00 T01 §9. Raw findings: docs/reviews/00-workspace/D00-T04-s39.md Attestation: docs/reviews/00-workspace/D00-T04-s39.attest.json
todo/00-workspace/TODO-05-repo-face.md:87:- [ ] Make it easy to read at length: a short table of contents, one heading level per idea, the long reference parts (WSL, layout, gates) folded into `<details>` blocks, no em dashes, and one line per paragraph as `AGENTS.md` requires. Done when: a markdown lint plus a link and anchor check over `README.md`, `CONTRIBUTING.md`, and the templates runs in `scripts/check-all.ps1` and fails a broken relative link or anchor by name, quoted.
tools/githooks/pre-commit:9:  for c in python3 python py; do
(git grep exit 0)

=== $ git grep -n -I -i -E 'wsl|ubuntu|linux|python3 ' 0ec273d4239e74a851b99267252454b45297c1fb -- . :!resolute_au3 :!docs/reviews :!docs/phase-runs :!docs/captures :!docs/brainstorm :!todo/implementation-plan.md
.claude/skills/review-todo-section/SKILL.md:96:Every review mints one private directory and stages every prompt file under it: fixed `/tmp` names collide across concurrent sessions on one machine, proven when a `D00 T04 §9` round fenced another session's contract as its own and voided the round. Mint once per review, reuse for every round, plan review, and stamp review of that review, and keep the directory as evidence (no trap-delete; the OS scrubs `/tmp`). On Windows, run every `bash` block in this skill under Git Bash at `C:\Program Files\Git\bin\bash.exe`, never the `bash` on PATH (that is WSL's, and it resolves `python3` to Ubuntu's interpreter against the wrong tree). Git Bash carries `mktemp`, `timeout`, `sed`, and `date`, and both `python` and `python3` there resolve to the same Windows interpreter (probed 2026-09-19), so the blocks below run verbatim, with one translation: MSYS2 converts bare path arguments to Windows paths but not paths embedded in `TITLE=path`, so every fence command first translates its chunk paths with `cygpath -w` (untranslated, python resolves Git Bash `/tmp` to the wrong place and fence exits 2 with `cannot read`).
scripts/campaign_guard.py:2216:        # Git Bash by path: the `bash` on PATH can be WSL's, which runs a
scripts/review_prompt.py:1848:        # GitHub's defaults: `bash -e {0}` on Linux and macOS runners,
scripts/review_prompt.py:1850:        if runner.startswith(("ubuntu", "macos")):
scripts/review_prompt.py:6929:            plan_text = ("jobs:\n  plan-gates:\n    runs-on: ubuntu-24.04\n    steps:\n      - uses: actions/checkout@abc\n"
scripts/review_prompt.py:6930:                         "      - name: Self-test the TODO graph tool\n        run: python3 scripts/todo-graph.py self-test\n"
scripts/review_prompt.py:6931:                         "      - name: Validate the TODO tree\n        run: python3 scripts/todo-graph.py validate\n"
scripts/review_prompt.py:6933:                         "          python3 scripts/todo-graph.py plan --sync\n          if true; then\n"
scripts/review_prompt.py:7059:            ctx_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: ctx\n        working-directory: tools\n"
scripts/review_prompt.py:7071:            q_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: q\n"
scripts/review_prompt.py:7079:            for label, yml in (("flow env", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        env: {MODE: x}\n        run: make\n"),
scripts/review_prompt.py:7080:                               ("block env", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        env:\n          MODE: |\n            x\n        run: make\n"),
scripts/review_prompt.py:7081:                               ("flow workflow env", "env: {X: 1}\njobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        run: make\n"),
scripts/review_prompt.py:7083:                               ("block working-directory", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        working-directory: >-\n          tools\n        run: make\n"),
scripts/review_prompt.py:7084:                               ("block shell", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        shell: |\n          bash {0}\n        run: make\n"),
scripts/review_prompt.py:7085:                               ("container job", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    container: node:22\n    steps:\n      - name: a\n        run: make\n"),
scripts/review_prompt.py:7092:                    "  second:\n    steps:\n      - name: s\n        run: make\n    runs-on: ubuntu-24.04\n"
scripts/review_prompt.py:7093:                    "  third:\n    steps:\n      - name: t\n        run: make\n    runs-on: ubuntu-24.04\n    container: node:22\n")
scripts/review_prompt.py:7095:            commented = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n# a column-zero comment inside the job\n"
scripts/review_prompt.py:7098:            inline_c = "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps: # build\n    - name: B\n      run: make\n"
scripts/review_prompt.py:7103:                  and got_c[0]["context"]["runs-on"] == "ubuntu-24.04", str(got_c))
scripts/review_prompt.py:7105:                  by["s"]["context"]["runs-on"] == "ubuntu-24.04" and by["f"]["context"]["runs-on"] == "windows-2025",
scripts/review_prompt.py:7109:            quoted = ('jobs:\n  "j":\n    "runs-on": ubuntu-24.04\n    "steps":\n      - "name": q\n'
scripts/review_prompt.py:7115:                  and qs["q"]["run"] == "make" and qs["q"]["context"]["runs-on"] == "ubuntu-24.04", str(qs["q"]))
scripts/review_prompt.py:7118:            flow = "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - {name: f, run: make}\n"
scripts/review_prompt.py:7122:            uses_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - uses: actions/checkout@abc\n"
scripts/review_prompt.py:7129:            fake = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: Run actions/checkout@prepare\n"
scripts/review_prompt.py:7136:            flow_with = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - uses: actions/checkout@abc\n"
scripts/review_prompt.py:7299:            wf_env = "env:\n  X: 1\njobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        run: make\n"
scripts/review_prompt.py:7304:                      ("Self-test the TODO graph tool", "python3 scripts/todo-graph.py self-test"),
scripts/review_prompt.py:7305:                      ("Validate the TODO tree", "python3 scripts/todo-graph.py validate"),
scripts/review_prompt.py:7307:                       "python3 scripts/todo-graph.py plan --sync\nif true; then\n  test -z x\nfi")],
scripts/review_prompt.py:7312:                  and f"rerun locally at {c1[:12]}: Validate the TODO tree: python3 scripts/todo-graph.py validate" in line
scripts/review_prompt.py:7428:                         "jobs:\n  x:\n    runs-on: ubuntu-24.04\n")
scripts/review_prompt.py:7684:            sec_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: s\n        env:\n"
scripts/review_prompt.py:7864:            cred_step = workflow_steps("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: c\n"
scripts/todo-graph.py:268:    nowhere else -- in particular never under WSL, whose
scripts/todo-graph.py:5382:                  detect_context(platform="linux", environ={}),
todo/00-workspace/TODO-01-toolchain-and-gates.md:53:|   9   |   §9    | Remove the Linux execution surface         | §5         |  [ ]   |
todo/00-workspace/TODO-01-toolchain-and-gates.md:860:## 9. Remove the Linux Execution Surface
todo/00-workspace/TODO-01-toolchain-and-gates.md:864:Resolute is a Windows-only suite, per the operator's 2026-09-20 decision, but the tree still carries a Linux-development surface: `scripts/wsl.sh` bridges WSL to the Windows scripts, and CI jobs, tests, or docs may assume a Linux interpreter. Every such surface is untested ground that rots: nobody runs it, so nobody notices when it breaks, and its presence invites the next contributor to lean on it. This section inventories the surface and removes it, leaving the Windows-native tooling untouched.
todo/00-workspace/TODO-01-toolchain-and-gates.md:868:- [x] Inventory every Linux/WSL-execution surface: `scripts/wsl.sh`, CI jobs on Linux runners, scripts requiring a Linux interpreter, tests that fork Linux tooling, and docs or plan text instructing WSL flows. Done when: the inventory lists each item with its references (what calls or cites it), quoted from grep, and names what stays: Git Bash at `C:\Program Files\Git\bin\bash.exe`, which the review skill requires; the llvm-mingw Windows-targeting toolchain; the stdlib Python tooling that runs on Windows. **Done 2026-09-26:** `git grep -i -E "wsl|ubuntu|linux|python3 "` outside `resolute_au3/`, the review records, the run files, the captures, and the brainstorm found five executable or active items: `scripts/wsl.sh` (cited by `README.md:35-40`, its only caller), the README's WSL flow, `plan-gates` on `ubuntu-24.04` calling `python3` (`.github/workflows/plan.yml:15-28`), the `under WSL and CI` interpreter comment in `scripts/check-all.ps1:78`, and the `WSL checkouts` line in `scripts/review_prompt.py:11`. No test forks Linux tooling and no code path is Linux-only. What stays, each with its reason: Git Bash at `C:\Program Files\Git\bin\bash.exe` (`.claude/skills/review-todo-section/SKILL.md:96` and `scripts/campaign_guard.py:2216` name WSL only to say why the `bash` on PATH must not be used); `detect_context` in `scripts/todo-graph.py:268` and its `display-session fails off Windows` leg (a fail-closed guard, not a Linux path); the workflow parser's model of GitHub's Linux runners in `scripts/review_prompt.py` (it reads what GitHub runs, whatever the runner); the `#!/usr/bin/env python3` shebang of `scripts/profile-check.py` and the `python3` probe in `tools/githooks/pre-commit` (inert or Git Bash, both on Windows); the llvm-mingw toolchain; and the stdlib Python tooling. **Corrected 2026-09-26 by panel round 1:** the inventory and removal results are now quoted in docs/phase-runs/2026-09-25-phase-0.md: the exact command with its exclusions, the 45 hits before and the 15 kept hits after, and the per-name greps for `wsl.sh`, `ubuntu-24.04`, and `python3 scripts`.
todo/00-workspace/TODO-01-toolchain-and-gates.md:869:- [x] Remove the bridge and its callers: delete `scripts/wsl.sh` and re-point or delete everything referencing it. Done when: no reference to `wsl.sh` or WSL execution remains outside this section's own record, quoted from grep. **Done 2026-09-26:** `scripts/wsl.sh` is deleted and the README's WSL paragraph, command block, and wrapper note with it; `git grep -n -i wsl` outside the kept records returns only the Git Bash guards, the `detect_context` rationale, this section, its reciprocal XREF in D00 T04 §39, the stamped history of D00 T04 §24 and D00 T04 §39, a D00 T04 §26 item naming a past proof, and the D00 T05 §4 correction note.
todo/00-workspace/TODO-01-toolchain-and-gates.md:870:- [x] Remove the remaining inventoried surface: Linux-runner CI jobs, Linux-gated tests, Linux-conditional code paths. Done when: the step-1 inventory reads empty item by item, each quoted, and nothing removed has a live caller left behind (grep for each removed name returns nothing outside this section's record). **Done 2026-09-26:** `plan-gates` now runs on `windows-2025` through the runner's Git Bash (`defaults: run: shell: bash`) with `python`, a recorded default: the gates run on the host family they are developed on, at about twice the runner minutes per read-back, and reverting is a two-line edit. **Corrected 2026-09-26 by self-review, before the independent review:** the first version set `shell: bash` as job defaults, which `ci-wait`'s re-run reader does not read, so a red would have printed "cannot reproduce locally: the job sets run defaults" for every step; each step now names `shell: bash`, and its printed template `bash --noprofile --norc -eo pipefail {0}` normalizes to the `shell:` line GitHub logged on the first `windows-2025` run (`C:\Program Files\Git\bin\bash.EXE --noprofile --norc -e -o pipefail {0}`, run 36236461973). The two comments are reworded (`python` usually, `python3` on some installs; CRLF and LF checkouts). No Linux-gated test or Linux-conditional code path existed, so none was removed.
todo/00-workspace/TODO-01-toolchain-and-gates.md:872:- [x] Settle the review-prompt self-test's POSIX status: under WSL `python3 scripts/review_prompt.py --self-test` fails `anchors-untracked-fires` (a fixture cite written with a Windows separator, `todo\README.md`), found by the D00 T04 §39 independent review and present before it (the pre-§39 suite failed three legs under WSL, the post-§39 suite one). Done when: either the self-test is declared Windows-only with the rest of the Linux surface this section removes (and the AGENTS.md line saying the TODO tooling runs anywhere says so), or the fixture is separator-neutral and the suite passes under a POSIX interpreter, quoted. **Done 2026-09-26:** declared Windows-only with the rest of the tooling: `AGENTS.md` now says the TODO tooling runs on the Windows development host and CI's `windows-2025` runner and nothing tests it elsewhere, replacing "runs anywhere". The separator-bound fixture stays as it is, since no POSIX interpreter runs it.
todo/00-workspace/TODO-01-toolchain-and-gates.md:873:- [x] Scope the purge to what executes: the removal criteria name every WSL reference outside this section's record, which would rewrite stamped review evidence and historical run notes, and removing Linux-gated paths would take the Linux-runner oracle captures and the portable template checks D00 T04 §37 and D00 T04 §39 rely on (plan review of the D00 T04 §39 candidate, PR16 and PR17; needs D00 T04 §39 shipped). Done when: the inventory separates executable paths and active guidance (removed) from immutable review evidence, historical notes, captured drill logs, and Git Bash dependencies (kept, each with its reason), the supported host and interpreter matrix is decided per script before anything is removed, and the grep criteria exempt the kept records, quoted. **Done 2026-09-26:** the host matrix, decided per script before removal: every `scripts/*.py` and `*.ps1` runs on Windows (the development host and the `windows-2025` runner), under Git Bash where a skill says bash, and no script is supported elsewhere. The inventory above separates what was removed from what stays and why; the kept records (stamped TODO text, `docs/reviews/`, `docs/phase-runs/`, the Linux-runner oracle captures under `docs/captures/ci-oracle/` that D00 T04 §37 and D00 T04 §39 read, and `resolute_au3/`) are exempt from the grep criteria, so nothing historical was rewritten.
todo/00-workspace/TODO-01-toolchain-and-gates.md:874:- [x] Commit: `"workspace: remove the Linux execution surface"`
todo/00-workspace/TODO-01-toolchain-and-gates.md:878:-> SOURCE: operator-2026-09-20-no-linux
todo/00-workspace/TODO-01-toolchain-and-gates.md:880:-> XREF: D00 T04 §39 -- the WSL self-test failure its independent review found, filed as an item here
todo/00-workspace/TODO-04-self-correction.md:1064:- [x] Emit one signed portable review bundle: operators need one independently verifiable artifact rather than convention-linked files (plan review of the §21 candidate, PR20, premium win). Done when: one command emits the bundle (manifests, raw outputs, findings, attestations, checker transcripts, tool versions, candidate graph, push receipt, verification command), the verification command replays green quoted on a second machine, and the self-test pins bundle schema plus one tampered-bundle leg. Cheaper substitute that fails the checkpoint: the rundir tarball, which is portable files, not a verifiable bundle. Done: `bundle` emits one deterministic zip (fixed date/order/attrs, so identical inputs emit identical bytes) carrying the sign-off round's manifest, fenced body, runner output, findings, attestation, and checker transcripts, plus the tool record (python, git, script sha), the candidate graph (base/head/trees, declared commits), the push receipt (URL, ref, OID, readback match), and the verification command text (`review-bundle/1`); `bundle-verify` replays member hashes plus the exact member set, the attestation rebinds (findings, runner, witness), the panel recheck with reason match, the body byte/sha replay, transcript PASS lines, and the push/candidate shapes, all offline (no git, no network, no repo), with `--recheck-graph` resolving OIDs/trees/declaration and `--recheck-remote` re-reading the landing where they exist. Emit and verify share one binding authority, so an inconsistent bundle never ships (schema-1 attestations refuse: nothing to rebind). Second-machine replay quoted: script plus bundle copied into WSL /tmp with no repo (`git rev-parse` exit 128), `python3 review_prompt.py bundle-verify` exits 0 with the same digest (`8 member(s) agree, attestation approve (codex-panel/gpt-5.6-sol), panel recheck PASS four lenses, one verdict each, transcripts pass 3/3`). Tampered bundle fails quoted: `member findings.md hashes 18184e63fe9d..., manifest binds 57de9b9bde4d...` exit 1 (drive: `%TEMP%/s24-item20-drive.py`, re-runnable; rechecks quote `trees match` and `confirms`). No key signs the bundle (stated in the skill): tamper-evidence rides the manifest hashes plus the exact member set, attribution rides the push receipt the remote confirms (residual, self-review fix 8: a wholesale rewrite with consistent hashes passes default verify; the run-record digest quote plus `--recheck-remote` close it), and `gpg --detach-sign` over the deterministic bytes stays a one-liner. Skill wired: post-push fence reruns the three checker transcripts deterministically, emits, verifies before handing off, and quotes the digest in the run record. Legs: roundtrip emit/verify, determinism, tampered member, member set, emit-refuses-rebind, emit-refuses-transcript, recheck graph/mismatch, recheck remote/mismatch, plus the `skill-bundle` pin (+28 review-prompt cases: 13 at implementation, 4 hostile-input hardening at self-review, 4 caps plus single-PASS at self-review, 5 holding shapes at round 4, 2 witnessed shapes at round 5). Self-review fix 5 (adversarial): the verifier bounds the member total (`BUNDLE_MAX_TOTAL`) and count (`BUNDLE_MAX_COUNT`) beside the per-member bounds, since per-member caps alone admit a thousand small members expanding past memory (`bundle-cap-total`, `bundle-cap-count`). Self-review fix 6 (adversarial): a carried transcript must be exactly one PASS-led line (`_bundle_transcript_ok`), so a failures-plus-injected-PASS record no longer verifies as passing; the emit and verify legs pin the injected shape at both ends and the old containing-PASS leg is retargeted. Round-4 fix (F15): single-PASS-led admitted the checker's own blocking output (`PASS N naming(s) to answer` exits 0 by the branch-on-reason design), so transcripts must now match one checker's holding shape (panel exact, plan alternation, stamp holding-only; disjoint, no role plumbing): blocking and forged PASS lines refuse at emit and fail at verify, the parameterized plan shape accepts, and the item-20 drive re-passes green. Round-5 fix (F21): the plan and stamp holding patterns are witnessed (real checker stdout piped into the transcripts), so a reason drift fails the legs instead of every suite staying green; the hand-written plan leg stays for parameterized-count generality.
todo/00-workspace/TODO-04-self-correction.md:1152:- [ ] Replay the bundle verify on a genuinely separate machine: the WSL proof shares the host, so portability past the hypervisor rides assertion (plan review of the §24 candidate, PR12; needs §24 shipped: it extends §24's bundle proof). Done when: the verify replays green quoted on independent hardware or a CI worker when either exists (until then the WSL proof stands with its scope -- separate kernel and filesystem, no repo -- stated), and the item records which substrate ran it. Cheaper substitute that fails the checkpoint: a second same-host replay, which re-proves the same scope.
todo/00-workspace/TODO-04-self-correction.md:1425:- [x] Decode `run:` scalars the way GitHub does, or state the forms read: `workflow_step_commands` in `scripts/review_prompt.py` returns raw scalar text, so `run: "python3 scripts/check.py"` prints with its quotes and `run: >` prints unfolded (panel round 3 of the D00 T04 §33 review, F8; needs §33 shipped: it rethinks §33's command fallback). Done when: plain, single-quoted, double-quoted (with escapes), literal `|`, and folded `>` scalars, each with `-`/`+` chomping, decode to the command text GitHub runs, one fixture leg per form quoted; any form still unsupported prints `ci-wait: rerun locally: <step>: unsupported run: form, read the workflow` instead of a guessed command, quoted from a leg; and the three earlier block legs re-pass. Cheaper substitute that fails the checkpoint: a fourth ad hoc patch for the two forms the round named. Done: `workflow_steps` in `scripts/review_prompt.py` decodes plain (comment stripped), single-quoted (`''`), double-quoted (every YAML escape, `\x`, `\u`, `\U`), literal `|` and folded `>` blocks with `-`/`+` chomping and an indentation indicator, per YAML 1.2; anchors, aliases, tags, flow collections, multi-line quoted or plain scalars, and unknown escapes refuse as `ci-wait: rerun locally at <sha>: <step>: unsupported run: form (<why>), read the workflow`. The expected outputs come from GitHub: a disposable `scalar-echo.yml` ran every form under `shell: cat {0}` (run 36175449390), and its eleven step outputs are `SCALAR_ECHO_GITHUB`. Legs (+23): `scalar-decodes-as-github: <form>` for all eleven, `scalar-set-covers-every-github-step`, `scalar-chomping-clip-strip-keep`, and five `scalar-refuses` plus five `rerun-refuses-unsupported` (anchor, multi-line double, multi-line plain, unknown escape, flow sequence); dropping the double-quote decoding fails `scalar-decodes-as-github: double quoted` by name. The earlier block legs re-pass.
todo/00-workspace/TODO-04-self-correction.md:1429:- [x] Print a re-run command with its context or refuse it: a decoded `run:` line still depends on the step's `shell`, `working-directory`, `env`, `${{ }}` expressions, matrix values, and earlier steps (plan review of the D00 T04 §33 candidate, PR5, PR6, and PR7; needs §33 shipped: it extends the command fallback beside the scalar item above). Done when: the supported YAML subset is stated (scalar forms, chomping, indentation indicators) with every other form refused; a step whose context the fallback cannot reproduce prints `cannot reproduce locally: <reason>` rather than a command; and the decoder's expected outputs come from a disposable workflow that echoes each decoded fixture on GitHub, not from self-authored strings, quoted. Done: the supported subset is stated in `workflow_steps`' docstring; `rerun_lines` prints a one-line command with its `cd <working-directory> &&` and `KEY=value` env prefix and a `(shell: ...; earlier steps assumed run)` line, or a multi-line script as written under the same context line, and refuses `cannot reproduce locally: <reason>` for `${{ }}` expressions (matrix, secrets, contexts) in the command, env, or directory, and for workflow or job `env` or `defaults`. The decoder's expectations are GitHub's (item 1). Legs (+3): `rerun-prints-its-context`, `rerun-refuses-expressions`, `rerun-refuses-workflow-env`. **Corrected 2026-09-25 by the independent review (F2, F3, F4):** env forms the parser does not read (a flow mapping, a block or empty value, workflow-level flow `env`) were dropped or misread, context values were interpolated unquoted and the env reached only the first command, and the default shell was assumed to be Bash. The context now renders as a quoted script (`export KEY='value'` lines and `cd 'dir'` ahead of the command, so the env reaches the whole script), unreadable env forms refuse, and the shell comes from the job's `runs-on` (`bash -e {0}` on Linux and macOS, `pwsh` on Windows, refused when the runner is unknown or a non-POSIX shell would need env rendering). Legs (+7): `rerun-quotes-values-and-scopes-env-to-the-script`, five `rerun-refuses-unreadable-context` (flow env, block env, flow workflow env, unknown runner, windows env), `rerun-names-the-windows-default-shell`; the context leg now expects the quoted script. **Corrected 2026-09-25 by panel round 1:** a block-valued `shell` or `working-directory` was read as its header (`cd '>-'`); both refuse now. Legs `: block working-directory`, `: block shell` (+2). **Corrected 2026-09-25 by panel round 2:** a job running in a `container:` (or with `services:`) was advertised with the host's default shell; it refuses now, the container's shell and filesystem not being the host's. Leg `: container job` (+1).
todo/00-workspace/TODO-04-self-correction.md:1616:- [x] Keep the oracle's evidence durable: the scalar and context expectations came from workflows that lived only on deleted branches (plan review PR16; needs §37 shipped). Done when: the drill workflow definitions, the runner image and version, and the captured outputs live under `docs/captures/` with their run ids, and the self-test names that file, quoted. Done: `docs/captures/ci-oracle/` holds, per drill, the workflow file at its head commit (fetched from GitHub by commit), the run record, and the full log, with a README and `oracle.json` listing the four drills (runner image and version per drill); the in-code `SCALAR_ECHO_*` and `CONTEXT_ECHO_*` constants are gone, replaced by `oracle_drills`, `oracle_workflow`, and `oracle_log`, whose expectations are read from GitHub's logs, and the self-test also compares each printed template with the log's own `shell:` line (`normalize_template`). Legs (+8): `oracle-evidence-is-durable:` per drill (4), `rerun-template-is-githubs:` for the four Linux steps (4). **Corrected 2026-09-26 by the independent review:** the Windows execution legs broke the self-test off Windows (a quoted command line passed as one executable under WSL, and `powershell` and `cmd` required everywhere) (P2); Windows shells execute only on a Windows host, PowerShell through an argument vector, while the template and durability legs run everywhere. Under WSL the suite now fails one leg, `anchors-untracked-fires`, which also failed before this section (the pre-§39 suite failed three under WSL); it is filed in D00 T01 §9, which owns the Linux execution surface.
todo/00-workspace/TODO-04-self-correction.md:1622:-> XREF: D00 T01 §9 -- the WSL-only self-test failure this section's independent review found, filed there
todo/00-workspace/TODO-04-self-correction.md:1643:> **Review:** round 5 GPT depth, candidates `f122556e`(round 1) `f5c4bc06`(round 2) `96221264`(round 3) `2d337c78`(round 4) `897d6426`(round 5) -- `adversarial` needs-attention at round 5, filed: F18 in D00 T04 §41 (the hard cap; a block scalar as a trigger-list pattern), closed earlier: 8 fixed (redaction and the flow-list reader each rethought after three rounds) · `consistency` approves at round 5, closed: none raised · `integration` needs-attention at round 5, filed: F19 in D00 T04 §41 as major (a hand-written foreign transition in the campaign's own run file), closed earlier: 5 fixed · `record` approves at round 5, closed: 1 fixed · `source-defect` approves in-session (GitHub's semantics read from its own drill logs) · `design` not owed. Independent pass on the implementation commit 34bc5ecb (`independent` slot, gpt-6-astra high): one P1 and five P2s, all right, fixed in f122556e before round 1; the WSL-only failure it surfaced predates the section, filed in D00 T01 §9. Raw findings: docs/reviews/00-workspace/D00-T04-s39.md Attestation: docs/reviews/00-workspace/D00-T04-s39.attest.json
todo/00-workspace/TODO-05-repo-face.md:87:- [ ] Make it easy to read at length: a short table of contents, one heading level per idea, the long reference parts (layout, gates) folded into `<details>` blocks (**Corrected 2026-09-26:** this listed WSL first; D00 T01 §9 removed the README's WSL flow with `scripts/wsl.sh`), no em dashes, and one line per paragraph as `AGENTS.md` requires. Done when: a markdown lint plus a link and anchor check over `README.md`, `CONTRIBUTING.md`, and the templates runs in `scripts/check-all.ps1` and fails a broken relative link or anchor by name, quoted.
tools/githooks/pre-commit:9:  for c in python3 python py; do
(git grep exit 0)

=== $ git grep -n -I -F 'wsl.sh' 0ec273d4239e74a851b99267252454b45297c1fb -- .github scripts README.md AGENTS.md tools
(git grep exit 1: no match)

=== $ git grep -n -I -F 'ubuntu-24.04' 0ec273d4239e74a851b99267252454b45297c1fb -- .github scripts README.md AGENTS.md tools
scripts/review_prompt.py:6929:            plan_text = ("jobs:\n  plan-gates:\n    runs-on: ubuntu-24.04\n    steps:\n      - uses: actions/checkout@abc\n"
scripts/review_prompt.py:7059:            ctx_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: ctx\n        working-directory: tools\n"
scripts/review_prompt.py:7071:            q_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: q\n"
scripts/review_prompt.py:7079:            for label, yml in (("flow env", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        env: {MODE: x}\n        run: make\n"),
scripts/review_prompt.py:7080:                               ("block env", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        env:\n          MODE: |\n            x\n        run: make\n"),
scripts/review_prompt.py:7081:                               ("flow workflow env", "env: {X: 1}\njobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        run: make\n"),
scripts/review_prompt.py:7083:                               ("block working-directory", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        working-directory: >-\n          tools\n        run: make\n"),
scripts/review_prompt.py:7084:                               ("block shell", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        shell: |\n          bash {0}\n        run: make\n"),
scripts/review_prompt.py:7085:                               ("container job", "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    container: node:22\n    steps:\n      - name: a\n        run: make\n"),
scripts/review_prompt.py:7092:                    "  second:\n    steps:\n      - name: s\n        run: make\n    runs-on: ubuntu-24.04\n"
scripts/review_prompt.py:7093:                    "  third:\n    steps:\n      - name: t\n        run: make\n    runs-on: ubuntu-24.04\n    container: node:22\n")
scripts/review_prompt.py:7095:            commented = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n# a column-zero comment inside the job\n"
scripts/review_prompt.py:7098:            inline_c = "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps: # build\n    - name: B\n      run: make\n"
scripts/review_prompt.py:7103:                  and got_c[0]["context"]["runs-on"] == "ubuntu-24.04", str(got_c))
scripts/review_prompt.py:7105:                  by["s"]["context"]["runs-on"] == "ubuntu-24.04" and by["f"]["context"]["runs-on"] == "windows-2025",
scripts/review_prompt.py:7109:            quoted = ('jobs:\n  "j":\n    "runs-on": ubuntu-24.04\n    "steps":\n      - "name": q\n'
scripts/review_prompt.py:7115:                  and qs["q"]["run"] == "make" and qs["q"]["context"]["runs-on"] == "ubuntu-24.04", str(qs["q"]))
scripts/review_prompt.py:7118:            flow = "jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - {name: f, run: make}\n"
scripts/review_prompt.py:7122:            uses_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - uses: actions/checkout@abc\n"
scripts/review_prompt.py:7129:            fake = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: Run actions/checkout@prepare\n"
scripts/review_prompt.py:7136:            flow_with = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - uses: actions/checkout@abc\n"
scripts/review_prompt.py:7299:            wf_env = "env:\n  X: 1\njobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: a\n        run: make\n"
scripts/review_prompt.py:7428:                         "jobs:\n  x:\n    runs-on: ubuntu-24.04\n")
scripts/review_prompt.py:7684:            sec_yml = ("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: s\n        env:\n"
scripts/review_prompt.py:7864:            cred_step = workflow_steps("jobs:\n  j:\n    runs-on: ubuntu-24.04\n    steps:\n      - name: c\n"
(git grep exit 0)

=== $ git grep -n -I -F 'python3 scripts' 0ec273d4239e74a851b99267252454b45297c1fb -- .github scripts README.md AGENTS.md tools
scripts/review_prompt.py:6930:                         "      - name: Self-test the TODO graph tool\n        run: python3 scripts/todo-graph.py self-test\n"
scripts/review_prompt.py:6931:                         "      - name: Validate the TODO tree\n        run: python3 scripts/todo-graph.py validate\n"
scripts/review_prompt.py:6933:                         "          python3 scripts/todo-graph.py plan --sync\n          if true; then\n"
scripts/review_prompt.py:7304:                      ("Self-test the TODO graph tool", "python3 scripts/todo-graph.py self-test"),
scripts/review_prompt.py:7305:                      ("Validate the TODO tree", "python3 scripts/todo-graph.py validate"),
scripts/review_prompt.py:7307:                       "python3 scripts/todo-graph.py plan --sync\nif true; then\n  test -z x\nfi")],
scripts/review_prompt.py:7312:                  and f"rerun locally at {c1[:12]}: Validate the TODO tree: python3 scripts/todo-graph.py validate" in line
(git grep exit 0)

