# Review -- D00 T04 §40, Campaign Guard Fence Completeness

**Reviewed:** 2026-09-26
**Stance:** post-implementation
**Rounds:** independent pass (3 findings: fixed); GPT 1 bulk (4 fixed, 1 refuted); GPT 2 bulk (4 fixed); GPT 3 sign-off (3 fixed); GPT 4 depth (2 fixed, the clearance unit rethought after four rounds); GPT 5 depth, the hard cap (1 filed in D00 T04 §42).
**Tier:** Full panel. The diff touches `scripts/campaign_guard.py` (a gate in `scripts/check-all.ps1`), the Stop hook, `scripts/review_prompt.py`, and three skills (`process-plan`, `process-phase`, `review-todo-section`: the run contract).
**Attestation:** `docs/reviews/00-workspace/D00-T04-s40.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `39ed6d3d` | implementation: legacy-state tagging before an acquisition publishes, `--run` required, generation rotation and `delete-check`, the workspace-digest header and listing states, startup draining with a startup acknowledgement, quarantine and recover, injected failures and the stated fsync default, a credential-free identity, the upgrade and in-section health drives, and D00 T04 §38's acceptance paragraph |
| `807f7840` | answers the independent pass: the rotation is a compare-and-swap, in-place migration keeps its state, quarantine names never collide |
| `6f3caafc` | answers panel round 1: a cleared job is never made current, no listing releases no job, phase advance creates before it deletes, a second upgrade drive from a guard with no identity |
| `a6feb28a` | answers panel round 2: recover refuses a cleared job, clearances never expire, an unreadable listing names no pending job, suite counts recorded |
| `3d39ffe0` | answers panel round 3: an unreadable clearance ledger refuses, startup rotates rather than adopting a survivor, the power-loss default stated accurately |
| `06209c98` | answers panel round 4: quarantine salvages the clearance ledger, startup rotates a same-generation twin |

Record commits (filings only, no reviewed content): `279980f0` (the hard-cap and plan-review findings filed in D00 T04 §42).

Range `39ed6d3d^..06209c98` (base `4770417c`), contiguous, no declaration owed. Each round fenced the range from `4770417c` to its own head; the fenced contract was checked to open with this section's heading. The rundir is session scratch outside the tree (/tmp/review-QZamCZuM, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation. Makes sense: each item maps to code (`_tag_legacy_state`, `_fence` with `run`, the rotation text in `whoami` and `delete_check`, `workspace_digest` and `listing_state`, the startup lines in `reconcile` and the `--startup` acknowledgement, `quarantine` and `recover`, `_fail` and `_unlink`, `credential_free`) or to skill text, with legs that fail without it. Logical: the one design choice the plan left open, how a firing proves it is its job, has no answer (`CronList` shows no firing id), so duplicates rotate the generation and every deletion is fenced; the drives exercised real jobs and kept both of the session's own mistakes as evidence. Broke nothing: `review_prompt.py`'s escalation pin moved to the `--run` form, `scripts/check-all.ps1` ran `19 gate(s) ok` at the SHIP commit, and the live heartbeat moved to the §40 prompt inside the health drive.

`source-defect`: not owed (no Win32 contract or other tool's behavior at stake; `CronList`'s own `No scheduled jobs.` sentinel and 80-character cut were read from the live tool). `design`: not owed (no rendered surface).

## Independent pass

`python scripts/panel_slots.py exec independent --commit 39ed6d3d...`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 213s, clock 2026-09-26T05:18:42Z, cost unresolved (the review runner printed no token figure). Output and stderr stay in the session scratch rundir outside the tree (ind40.out, ind40.err); the output is transcribed below.

### F1 -- a delayed rotation can resurrect an ended campaign -- adversarial -- FIXED in `807f7840` (independent) [major]

The rotation's `acquire` carries `--expect-generation` and `--expect-run` and refuses unless the guard is still the one it was named for.

### F2 -- in-place migration discards the same run's state -- correctness -- FIXED in `807f7840` (independent) [minor]

Migration tags the state after the guard; the hook honors an untagged state beside a guard migrated in place.

### F3 -- two quarantines in one second overwrite each other -- correctness -- FIXED in `807f7840` (independent) [minor]

Each destination carries a unique suffix.

```text
The 244 self-tests pass, but targeted fixtures expose lost legacy state, overwritten quarantine evidence, and an unfenced rotation that can recreate a stopped campaign.

Full review comments:

- [P1] Fence duplicate rotation against the original guard identity — R:\GitHub\Resolute\scripts\campaign_guard.py:733-736
  If the run ends or changes phases after a duplicate firing receives this instruction, its subsequent `acquire` still succeeds: acquisition checks session ownership but not the original run or generation, and creates a guard when none exists. A delayed rotation can therefore resurrect an operator-stopped campaign or overwrite the next phase's guard, contrary to [AGENTS.md:129](R:/GitHub/Resolute/AGENTS.md#L129). Make rotation conditional on the original identity still matching under the lock, and cancel the newly created job if that check fails.

- [P2] Tag legacy state on every guard migration path — R:\GitHub\Resolute\.claude\hooks\campaign-stop.ps1:216-219
  For a legacy guard, `whoami`, `reconcile`, and other readers call `_migrate_locked`, which publishes a run ID without tagging the existing state. If the Stop hook runs before the next `acquire`, this new condition discards that same campaign's state. A fixture with two trips and an unacknowledged legacy hook error becomes zero trips with the error missing after `whoami` followed by the hook. Tag the state during migration before publishing the migrated guard, rather than only during acquisition.

- [P2] Give each quarantine entry a collision-proof destination — R:\GitHub\Resolute\scripts\campaign_guard.py:891-892
  If the same file is recreated and quarantined twice within one second, both operations generate the same destination and `os.replace` silently destroys the first archived record. This loses the recovery evidence the command promises to preserve; a two-call fixture retains only the second file's bytes. Use a unique suffix and collision-safe publication so repeated recovery attempts cannot overwrite earlier quarantine entries.
```

## GPT panel Round 1

Round 1 over `39ed6d3d^..807f7840` with the independent pass appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 40s, clock 2026-09-26T05:27:23Z, cost 49906tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-1.out, panel-1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` needs-attention
`integration` needs-attention
`record` needs-attention

### F4 -- a job cleared by delete-check can be made current before its deletion -- adversarial -- FIXED in `6f3caafc` (independent) [major]

`delete-check` records the clearance under the lock; `acquire` refuses a cleared job.

### F5 -- whoami without a listing releases the job -- adversarial -- FIXED in `6f3caafc` (independent) [minor]

No `CronList` text, no `job=`.

### F6 -- phase advance deletes the old job before re-pointing -- consistency -- FIXED in `6f3caafc` (independent) [minor]

It now creates, re-points, then deletes after `delete-check`.

### F7 -- the header is 51 characters, not 52 -- integration -- REFUTED (the header is 27 + 12 + 1 + 12 = 52 characters; the length leg measures it and passes) (independent) [minor]

Refuted with the arithmetic and the passing leg.

### F8 -- the upgrade drive never migrated a guard's identity -- record -- FIXED in `6f3caafc` (independent) [minor]

A second drive started from a guard with no generation and no run id.

```text
RECEIPT sha=5c2013d0fac346e75fea4c329b3e495a5eee89304dd09b0c172f065c2c3f7f38 end=PANEL-8d53c1e5c7f27c22 nonce=a8ea2e2604d76fbb
**adversarial: needs-attention** (2)
1. `scripts/campaign_guard.py:886-896`: `delete_check` releases the lock before the scheduler deletion. An `acquire` can make the checked job current after `DELETE OK` and before `CronDelete`, allowing deletion of the live heartbeat. The new fixture covers a re-point before the check, leaving this race untested.
2. `scripts/campaign_guard.py:726-729`: `whoami` returns `OWNER ... job=...` when `cronlist is None`, bypassing the new unknown-listing check. Missing listing evidence therefore releases the identity used by fenced mutations, contrary to the contract requiring `UNKNOWN` and a withheld job.
**consistency: needs-attention** (1)
1. `.claude/skills/process-plan/SKILL.md:118`: Phase advancement still requires deleting the old job before creating its replacement and re-pointing the guard. The mandatory `delete-check` at line 74 refuses that deletion because the old job remains current. Create and acquire the replacement before deleting the old job.
**integration: needs-attention** (1)
1. `scripts/campaign_guard.py:136-145,2553-2556`: `HEARTBEAT_TAG_LENGTH` is 52, but the generated header is 51 characters: 26-character prefix, 12-character generation, one space, and 12-character digest. Consequently, `heartbeat-tag-has-a-fixed-length-and-no-collision` necessarily fails for all three fixture paths.
**record: needs-attention** (1)
1. `docs/phase-runs/2026-09-25-phase-0.md:230`: The upgrade drive starts with a guard already carrying both a generation and a run id. It therefore does not exercise legacy guard identity migration, despite marking complete the contract’s end-to-end upgrade from pre-§38 artifacts. Record a drive starting with the missing identity fields and verify their migration alongside the legacy state and heartbeat.
```

## GPT panel Round 2

Round 2 over `39ed6d3d^..6f3caafc` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 37s, clock 2026-09-26T05:38:22Z, cost 58124tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-2.out, panel-2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F9 -- recover can restore a cleared job -- adversarial -- FIXED in `a6feb28a` (independent) [major]

`recover` refuses a cleared job.

### F10 -- clearances expire after 200 later checks -- adversarial -- FIXED in `a6feb28a` (independent) [minor]

The ledger is never truncated.

### F11 -- an unreadable listing still names pending jobs for deletion -- integration -- FIXED in `a6feb28a` (independent) [minor]

Pending cancellations wait for a readable listing.

### F12 -- the run record lacks the suite counts -- record -- FIXED in `a6feb28a` (independent) [minor]

Counts recorded in the run file; the Live proof quotes the final ones.

```text
RECEIPT sha=5c9214a6b0877058cf82a351e23ac9cacc229b5094b8d875b29ac10cd48326d8 end=PANEL-49b87d9fd610ec93 nonce=e863b906fce985fa
**adversarial: needs-attention** (2)
1. `scripts/campaign_guard.py:961-990`: `recover` publishes the recorded guard without checking `_cleared_jobs`. After `delete-check` authorizes deletion, recovery can restore that same job as current before `CronDelete` executes, bypassing the new acquisition fence and allowing deletion of the live heartbeat.
2. `scripts/campaign_guard.py:912-913`: Clearance persistence retains only the last 200 jobs. Another 200 clearances evict an outstanding authorization, allowing `acquire` to make its job current before the delayed deletion executes. Clearance cannot expire merely because unrelated jobs were checked.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/campaign_guard.py:812-840`: `reconcile` appends pending-cancellation `CronDelete` instructions before validating the listing, then includes those instructions in its `UNKNOWN` return. An unreadable listing with pending records therefore still names jobs for deletion, contradicting the required refusal behavior. The new unknown-listing fixtures exercise only an empty pending queue.
**record: needs-attention** (1)
1. `docs/phase-runs/2026-09-25-phase-0.md:227-244`: The §40 evidence records the two required drives but contains no quoted `campaign_guard.py --self-test` result or suite count. The section’s Test checkpoint explicitly requires that execution evidence; listing added test names in Done notes does not establish a passing run.
```

## GPT panel Round 3

Round 3 over `39ed6d3d^..a6feb28a` the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 71s, clock 2026-09-26T05:47:20Z, cost 61272tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-3.out, panel-3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` needs-attention
`integration` needs-attention
`record` approve

### F13 -- an unreadable clearance ledger reads as empty -- adversarial -- FIXED in `3d39ffe0` (independent) [minor]

It refuses every guard change until quarantined.

### F14 -- the power-loss default overstates consistency -- consistency -- FIXED in `3d39ffe0` (independent) [minor]

The skill states that any subset of the last changes may survive, and what durable publication would need.

### F15 -- startup adopts a survivor of the lost job's generation -- integration -- FIXED in `3d39ffe0` (independent) [major]

Startup rotates with the compare-and-swap. Blocking (an in-flight firing could share the adopted identity), so round 4 ran.

```text
RECEIPT sha=59ec02987248bef60c0af5efa45c71f4cdde2aa62dfa3e5607148864cdd7ff24 end=PANEL-104b3812aea4c850 nonce=4bd31046b9318b1f
**adversarial: needs-attention** (1)
1. `scripts/campaign_guard.py:928-934`: `_cleared_jobs` treats malformed JSON, an invalid document shape, and every read error as an empty clearance ledger. After `delete-check` authorizes deletion, an unreadable ledger therefore lets `acquire` or `recover` make that job current before the delayed `CronDelete`, reopening the race the persistent clearance is meant to prevent. Refuse ownership publication when an existing ledger cannot be read and validated.
**consistency: needs-attention** (1)
1. `.claude/skills/process-plan/SKILL.md:76`: The assertion that power loss returns an “earlier consistent state” is unsupported by atomic replacement without durability barriers. The pending-record publication and subsequent guard deletion can persist independently, leaving no guard and no pending cancellation after reboot. State this limitation explicitly; one fsync per published file also does not establish durability or ordering of directory changes.
**integration: needs-attention** (1)
1. `.claude/skills/process-plan/SKILL.md:64`: Startup reconciliation still instructs the runner to re-point to a surviving carrier using the guard’s existing generation, bypassing the rotation required at line 74. If the missing original carrier has an in-flight firing, that firing can subsequently obtain the survivor’s identity through `whoami` and pass the mutation fences. Route this recovery through fresh-generation creation and the same compare-and-swap rotation used by the heartbeat.
**record: approve**
```

## GPT panel Round 4

Round 4 over `39ed6d3d^..3d39ffe0` with prior verdicts appended (rounds past the sign-off run for blocking findings only). Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 53s, clock 2026-09-26T05:54:17Z, cost 62850tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-4.out, panel-4.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F16 -- quarantining the ledger lifts every prohibition -- adversarial -- FIXED in `06209c98` (independent) [major]

The clearance unit, patched in four rounds, was rethought: quarantine rebuilds the ledger from every job id its bytes still carry.

### F17 -- startup deletes a same-generation twin -- integration -- FIXED in `06209c98` (independent) [major]

Startup rotates it with the compare-and-swap.

```text
RECEIPT sha=bef52bc2ba588e2e8b57c758feeb9b5545fc0790963a9d648f8a6638adcd618b end=PANEL-e65b78cec312f2cd nonce=0028cf1a1b947e19
**adversarial: needs-attention** (1)
1. `scripts/campaign_guard.py:941-942,963-984`: Quarantining an unreadable clearance ledger removes all deletion fences because the resulting missing ledger reads as empty. After `DELETE OK`, ledger corruption followed by `quarantine` lets `acquire` or `recover` make the cleared job current before its delayed `CronDelete`. Recovery must preserve the outstanding deletion prohibition.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/campaign_guard.py:883-892`: Startup reconciliation rotates only when the guard’s job is missing. When the current job and another carrier share its generation, reconciliation instead instructs deletion of the duplicate without rotating. An already-running duplicate can then obtain the surviving job’s identity through `whoami` and pass the mutation fences. Route this duplicate case through the same generation rotation and compare-and-swap.
**record: approve**
```

## GPT panel Round 5

Round 5 over `39ed6d3d^..06209c98` the hard cap, with prior verdicts appended. Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 47s, clock 2026-09-26T06:01:09Z, cost 77457tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-5.out, panel-5.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` approve

### F18 -- quarantine moves the ledger before publishing its replacement -- adversarial -- FILED to `D00 T04 §42` (independent) [minor]

Round 5 is the hard cap. Reaching it needs a crash or a failed write inside `quarantine`, between a `delete-check` and its `CronDelete`, in one session; it files with the prohibition item rather than shipping unreviewed code.

```text
RECEIPT sha=5a5251c139e21ff236415ff538fa9fe479b61733e4f36f3d05c3d64d08323783 end=PANEL-fdd140bf87820e29 nonce=3ca4fbc09369ef8b
**adversarial: needs-attention** (1)
1. `scripts/campaign_guard.py:1005-1007,951-952`: Quarantine moves the clearance ledger before publishing its replacement. A crash or failed `_publish` between these operations leaves the ledger absent, which `_cleared_jobs` treats as empty. Consequently, `acquire` or `recover` can restore a cleared job before its delayed `CronDelete`, allowing deletion of the live heartbeat despite the intact prohibitions in quarantine.
**consistency: approve**
**integration: approve**
**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §40, D00 T04 §38]; dependents [none]; bytes 44826; run 20260926-D00-T04-S40-astra

`bytes 44826` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency and XREF neighbor D00 T04 §38; no section depends on D00 T04 §40 yet. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 60s, clock 2026-09-26T06:02:27Z, cost 23757tokens. Check: `PASS 16 findings, one per line`.

Ledger:
- [D00-T04-S40-PR1] [minor] recover ignores a later stop, park, or closeout -> filed D00 T04 §42
- [D00-T04-S40-PR2] [minor] a rotation does not refuse an unfinished end -> filed D00 T04 §42
- [D00-T04-S40-PR3] [minor] salvage leaves partially lost prohibitions unprotected -> filed D00 T04 §42
- [D00-T04-S40-PR4] [minor] a quarantined pending record leaves its jobs unowned -> filed D00 T04 §42
- [D00-T04-S40-PR5] [minor] delete-check needs no positive evidence of ownership -> filed D00 T04 §42
- [D00-T04-S40-PR6] [minor] clearances have no identity scope, reuse policy, or compaction -> filed D00 T04 §42
- [D00-T04-S40-PR7] [minor] a valid but incomplete listing is not detected -> filed D00 T04 §42
- [D00-T04-S40-PR8] [minor] workspace canonicalization is unspecified -> filed D00 T04 §42
- [D00-T04-S40-PR9] [minor] the digest's collision assumption is unstated -> filed D00 T04 §42
- [D00-T04-S40-PR10] [minor] migrated_at acceptance has no expiry or crash fixture -> filed D00 T04 §42
- [D00-T04-S40-PR11] [minor] failure injection skips several operations -> filed D00 T04 §42
- [D00-T04-S40-PR12] [minor] a power loss can drop the stop intent -> filed D00 T04 §42
- [D00-T04-S40-PR13] [minor] the in-section health check cannot run during blocking work -> filed D00 T04 §42
- [D00-T04-S40-PR14] [minor] foreign pending records are reported forever -> filed D00 T04 §42
- [D00-T04-S40-PR15] [minor] §40 has no consolidated transition contract -> filed D00 T04 §42
- [D00-T04-S40-PR16] [minor] command outcomes are prose, not structured -> filed D00 T04 §42
End of ledger

16 findings, all filed in D00 T04 §42 (`279980f0`).

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=8df8e660f60c576049b26ed9614bc5756aa2f76de6a23b54b0d58cee30a75b26 end=PLAN-aba11637c37ebc8e nonce=85d6870809e2c42e
- Recovery uses the last `acquire:` line without a stated check for a later stop, park, or completed run; belongs in §40 recovery, because rebuilding ownership could resurrect an intentionally terminated campaign.
- Rotation checks the expected generation and run but does not explicitly reject a matching guard with an unfinished `end`; belongs in §40 rotation and §38 termination, because a crash after publishing stop intent must not permit a replacement heartbeat to resume work.
- Clearance salvage preserves only recoverable job-id tokens, leaving partially lost prohibitions unprotected as well as total loss; belongs in §40 quarantine, because recovering some identifiers cannot establish that another job is safe to acquire.
- Quarantining malformed pending records removes cancellation obligations from active processing without a specified reconstruction path; belongs in §40 recovery, because preserving corrupt bytes alone does not retire the orphaned scheduler jobs.
- `delete-check` is described as refusing only the current guard job, without requiring positive evidence that another job belongs to this workspace, run, and scheduler session; belongs in §40 deletion authorization, because a mistaken identifier could clear an unrelated job for deletion.
- Permanent deletion clearances have no stated identity scope, job-ID reuse policy, or safe compaction contract; belongs in §40 clearance storage, because indefinite retention can grow without bound or prohibit a legitimate future job if identifiers are reused.
- Listing validation does not specify how a syntactically valid but incomplete listing is distinguished from a complete one; belongs in §40 listing status, because an omitted duplicate can falsely establish the single-carrier condition.
- Windows workspace canonicalization and run-file normalization are unspecified beyond “canonical”; belongs in §40 prompt identity, because casing, junctions, relative paths, and alternate path spellings can split ownership across equivalent workspace identities.
- The twelve-hex digest test claims “no collision” although its examples establish only distinct outputs for selected inputs; belongs in §40 identity acceptance, because the plan needs an explicit collision assumption and ambiguity refusal policy.
- Migration publishes the guard before tagging its legacy state, while `migrated_at` permits accepting untagged state without a stated expiry condition; belongs in §40 legacy migration, because crash fixtures must prove both preservation of the original state and rejection of subsequently foreign untagged state.
- Failed-write evidence enumerates selected pending, guard, and state operations rather than every promised write, replace, link, and delete; belongs in §40 failure checkpoints, because migration, clearance publication, quarantine, recovery, and error acknowledgement remain outside the stated proof.
- Power-loss recovery treats the scheduler listing as truth without defining how it preserves stop intent when the pending record disappears; belongs in §40 durability recovery, because a live job proves existence but does not establish authorization to resume.
- The shortened health drive uses a stand-in wait without specifying how checks execute during genuinely blocking work; belongs in §40 health acceptance and the connected phase runner, because a wall-time promise needs an executable scheduling mechanism.
- Foreign-session cancellations are repeatedly reported without a defined escalation, retirement condition, or deduplicated incident record; belongs in §40 terminal draining, because otherwise unresolved obligations can accumulate indefinitely without actionable ownership.
- §40 now carries several superseding corrections but provides no consolidated current transition contract; belongs in §40 acceptance documentation, because a command matrix covering identity requirements, refusals, partial states, and recovery would make the final behavior reviewable and reduce another cycle of contradictory instructions.
- Command outcomes are largely prose that skills must interpret; belongs in §40 CLI integration, because versioned structured results with stable outcome codes would make automation more reliable and support a clear operator view of ownership, pending cleanup, and recovery status.
```

