# Review -- D00 T04 §38, Campaign Guard Identity and Recovery

**Reviewed:** 2026-09-26
**Stance:** post-implementation
**Rounds:** independent pass (4 findings: fixed); GPT 1 bulk (4 fixed); GPT 2 bulk (3 fixed); GPT 3 sign-off (2 fixed, one of them by a rethink after three rounds on the heartbeat's refusal handling); GPT 4 depth (2 fixed); GPT 5 depth, the hard cap (1 filed in D00 T04 §40).
**Tier:** Full panel. The diff touches `scripts/campaign_guard.py` (a gate in `scripts/check-all.ps1`), the Stop hook `.claude/hooks/campaign-stop.ps1`, `scripts/review_prompt.py`, and three skills (`process-plan`, `process-phase`, `review-todo-section`: the run contract).
**Attestation:** `docs/reviews/00-workspace/D00-T04-s38.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `7c0e4f3d` | implementation: identity fences on every guard mutation, `whoami` and `reconcile` over the `CronList` text with the identity-first prompt, scheduler-scoped pending cancellations, atomic publication in a stated order with crash points, legacy migration, identified hook errors, run-scoped state, the coverage summary, journal identity for repair restore, and D00 T04 §36's acceptance paragraph |
| `3349d4c2` | answers the independent pass: credential-free repository identity, backtick-safe journal fields, report lines as bookkeeping, `whoami` again after a re-point |
| `4ff640f4` | answers panel round 1: one live carrier before any fenced step, every journal identity checked before de-duplication, the stall step ends only its own run, the recovered start completes |
| `dcfccf64` | answers panel round 2: no carrier releases no job, a fenced ack needs a guard, plan-done deletes only after `end` succeeds |
| `8136e4b1` | answers panel round 3: one refusal rule for every fenced heartbeat step, a legacy state migrates at a same-run handover |
| `f3a6d485` | answers panel round 4: `acquire` migrates a legacy guard before comparing runs, the heartbeat finishes an incomplete end |

Record commits (filings only, no reviewed content): `02a0763c` (the hard-cap and plan-review findings filed in D00 T04 §40).

Range `7c0e4f3d^..f3a6d485` (base `e327a13a`), contiguous, no declaration owed. Each round fenced the range from `e327a13a` to its own head; the fenced contract was checked to open with this section's heading. The rundir is session scratch outside the tree (/tmp/review-z1mIFmhI, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation. Makes sense: every item maps to code (`_fence`, `_publish`, `_migrate_locked`, `parse_cronlist`, `classify_job`, `heartbeat_tag`, `_hook_errors`, `_clear_state_if_new_run`, `_journal_identities`) or to skill text, and each carries legs that fail without it. Logical: the one question the plan could not answer from its text was how a heartbeat learns its own job id; `CronList` shows about 80 characters of each prompt and no firing id, so the identity leads the prompt and the fence guarantees one live carrier instead of trusting an id. Broke nothing: `review_prompt.py`'s pin on the escalation line moved to the fenced form in the same commit, and `scripts/check-all.ps1` ran `19 gate(s) ok` at the SHIP commit. Os-level checks: `os.link` (the exclusive publish) was probed on the repository volume (`link ok True`). The drives (run file) exercised real `CronCreate` jobs; one drive slip (a wrong job id passed to `acquire`) was kept as evidence rather than redone.

`source-defect`: not owed (no Win32 contract, registry layout, or other tool's behavior at stake; the one external contract, `CronList`'s cut near 80 characters, was read from the live tool and pinned by `heartbeat-tag-fits-the-cronlist-cut`). `design`: not owed (no rendered surface).

## Independent pass

`python scripts/panel_slots.py exec independent --commit 7c0e4f3dd2bc...`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 314s, clock 2026-09-26T00:59:17Z, cost unresolved (the review runner printed no token figure). Output and stderr stay in the session scratch rundir outside the tree (ind38.out, ind38.err); the output is transcribed below.

### F1 -- attempt lines print credentials from the remote URL -- adversarial -- FIXED in `3349d4c2` (independent) [major]

An `origin` URL carrying userinfo printed it into attempt lines bound for tracked run files; the identity drops userinfo for serialization and comparison.

### F2 -- repeated report lines reset the stall breaker -- correctness -- FIXED in `3349d4c2` (independent) [minor]

A predecessor's `report` line repeats every firing; it is appended as `- bookkeeping:` now.

### F3 -- the heartbeat keeps a stale job id after a re-point -- correctness -- FIXED in `3349d4c2` (independent) [minor]

The prompt re-runs `whoami` after a re-point and keeps the job it prints then.

### F4 -- a backtick becomes part of the journal's workflow -- correctness -- FIXED in `3349d4c2` (independent) [minor]

Identity fields stop at a backtick, so a run file's inline code restores.

```text
The 173-case campaign guard self-test passes, but targeted reproductions expose identity-recovery and stall-breaker regressions. The new journal output also exposes credentials embedded in remote URLs.

Full review comments:

- [P1] Remove credentials from the journal's repository identity — R:\GitHub\Resolute\scripts\campaign_guard.py:879-882
  When `origin` contains HTTPS credentials, `_repo_identity()` preserves them and this suffix prints them verbatim. The runner records these attempt lines in tracked run files, exposing the credentials in logs and Git history. Use a credential-free repository identity consistently for serialization and restore comparisons, as required by [AGENTS.md:202](AGENTS.md#L202).

- [P2] Treat repeated cancellation reports as bookkeeping — R:\GitHub\Resolute\.claude\skills\process-plan\SKILL.md:94-94
  When a predecessor leaves a pending cancellation, the current session cannot clear it, so every heartbeat appends the same `report` line. These unmarked Critical events lines change the hook's progress fingerprint and reset its stall trips. Reproducing two such appends reset `trips` from 1 to 0 each time, preventing a stalled campaign from reaching its two-trip termination threshold. Record the report once or mark subsequent entries as `- bookkeeping:`.

- [P2] Refresh the heartbeat identity after re-pointing — R:\GitHub\Resolute\scripts\campaign_guard.py:602-606
  When the guard's job is missing but another job carries its generation, `whoami` prints `OWNER ... job=<old job>` before recommending this re-point. The canonical heartbeat retains that OWNER identity, performs the re-point, and continues using the old job ID for acknowledgements and `end`; those commands then reject it as obsolete. Require another `whoami` after the re-point before executing subsequent fenced mutations.

- [P2] Exclude Markdown delimiters from parsed workflow identities — R:\GitHub\Resolute\scripts\campaign_guard.py:875-876
  When an attempt line is recorded as inline code in the Markdown run file, `workflow=(\S+)` captures the closing backtick. After the episode file is lost, `repair restore --workflow plan-gates` consequently rejects the journal as belonging to `plan-gates\u0060`, blocking recovery despite matching identities. This reproduces with a generated attempt line wrapped in backticks; the unwrapped line restores successfully. Delimit or escape the serialized fields so Markdown punctuation cannot become part of the identity.
```

## GPT panel Round 1

Round 1 over `7c0e4f3d^..3349d4c2` with the independent pass appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 42s, clock 2026-09-26T01:07:48Z, cost 64216tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-1.out, panel-1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F5 -- whoami hands the job id to a duplicate carrier -- adversarial -- FIXED in `4ff640f4` (independent) [major]

A firing cannot learn its own job id, so `whoami` withholds `job=` while a duplicate or a stale id lives.

### F6 -- a mixed journal collapses before its identities are checked -- adversarial -- FIXED in `4ff640f4` (independent) [minor]

Every attempt line's identity is validated before de-duplication.

### F7 -- the stall step deletes the job when end refuses -- integration -- FIXED in `4ff640f4` (independent) [major]

The step requires this run's state and deletes only after `end` succeeds.

### F8 -- the post-CronCreate recovery ends with no heartbeat -- record -- FIXED in `4ff640f4` (independent) [minor]

The fixture and the drive complete the start to one heartbeat and a consistent record.

```text
RECEIPT sha=d73a30638782324b98285f28b011e5d3ca2995b0226809901d23f13a4d7dfab0 end=PANEL-e0347f03a6e8ca2d nonce=ab830901d4018174
**adversarial: needs-attention** (2)
1. scripts/campaign_guard.py:589-609: `whoami` authenticates only the session and generation, then supplies the guard's job ID to every matching caller. A duplicate-generation heartbeat therefore receives the current job's identity and can pass every subsequent mutation fence, including after deleting its own duplicate job. Listing duplicates does not establish the calling job's identity as §38 requires.
2. scripts/campaign_guard.py:914-917,978-998: Journal attempts are deduplicated by commit before restore validates their identities. Two records for the same commit carrying different repositories, branches, or workflows collapse into one record, so a mixed journal can be restored instead of refused. Validate every recorded identity before deduplicating attempts.
**consistency: approve**
**integration: needs-attention** (1)
1. .claude/skills/process-plan/SKILL.md:98 and scripts/campaign_guard.py:423-432: The heartbeat treats any state with two trips as its own, while `end --reason stall` correctly rejects state belonging to another run. After the explicitly supported crash between guard publication and state reset, this instruction proceeds to `CronDelete` despite that refusal, removing the live run's heartbeat. Check the state's run ID and require successful `end` before deleting the job.
**record: needs-attention** (1)
1. docs/phase-runs/2026-09-25-phase-0.md:184 and scripts/campaign_guard.py:1931-1940: The post-`CronCreate` interruption evidence stops after deleting the orphan, with no guard and no job. It never recreates a current heartbeat or verifies its run-file record, although §38 requires each startup interruption to recover to exactly one current heartbeat and a consistent record. The corresponding fixture likewise explicitly accepts zero jobs as completed recovery.
```

## GPT panel Round 2

Round 2 over `7c0e4f3d^..4ff640f4` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 40s, clock 2026-09-26T01:16:01Z, cost 61219tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-2.out, panel-2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F9 -- an empty listing releases the job id -- adversarial -- FIXED in `dcfccf64` (independent) [major]

`job=` is released only when exactly one live job carries the generation and it is the guard's; otherwise `NO LIVE CARRIER`.

### F10 -- a fenced ack passes without a guard -- adversarial -- FIXED in `dcfccf64` (independent) [minor]

The fence refuses when no guard lives.

### F11 -- plan-done deletes the job when end refuses -- integration -- FIXED in `dcfccf64` (independent) [major]

Plan-done deletes only after `end` succeeds and restarts at step 2 on a refusal.

```text
RECEIPT sha=7e1442dc541abf0f6e0d6cd46048f8350aeef4625fee21e771f053ce07b17f14 end=PANEL-0e825b299b559bf2 nonce=888e665c70ec59fb
**adversarial: needs-attention** (2)
1. scripts/campaign_guard.py:605-616: `whoami` releases `job=` when `carriers` is empty: both `repoint` and `extras` are false. A deleted heartbeat, an empty CronList, or an unparsed listing therefore receives an identity that passes subsequent mutation fences despite no verified live carrier. Require exactly one matching live carrier before releasing the job identity.
2. scripts/campaign_guard.py:845-852: `hook-error --ack` skips `_fence` when the guard is absent. A heartbeat whose guard was ended after `whoami` can therefore acknowledge and delete error records using obsolete session, generation, and job values. Missing ownership must refuse a fenced acknowledgement.
**consistency: approve**
**integration: needs-attention** (1)
1. .claude/skills/process-plan/SKILL.md:97: Heartbeat step 4 still instructs `CronDelete` after `end --reason plan-done` without requiring success. If ownership changes after step 2, the fence refuses `end`, but the instruction nevertheless deletes the heartbeat. Apply the success prerequisite already added to step 5 and re-check ownership on refusal.
**record: approve**
```

## GPT panel Round 3

Round 3 over `7c0e4f3d^..dcfccf64` the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 61s, clock 2026-09-26T01:24:54Z, cost 73876tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-3.out, panel-3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F12 -- a pre-run-id state is deleted at the first re-point -- adversarial -- FIXED in `8136e4b1` (independent) [minor]

A state without a run id migrates when the run id is unchanged.

### F13 -- a stall-step refusal resumes the campaign after a handover -- integration -- FIXED in `8136e4b1` (independent) [major]

The refusal handling had been patched in rounds 1, 2, and 3, so it was rethought: one rule in the prompt's preamble sends every fenced refusal back to step 2, and a `CronDelete` only follows a success. Blocking (two writers after a handover), so round 4 ran.

```text
RECEIPT sha=ecad813254b17958cd2fe806d368572d46b1e94bc9271d181f73cace6b114c5f end=PANEL-91c889ef06fd8965 nonce=e44722c647528376
**adversarial: needs-attention** (1)
1. scripts/campaign_guard.py:278-296: `_clear_state_if_new_run` deletes state lacking `run_id`, including state written by shipped §36. The first same-run re-point or handover therefore erases its stall trips and any unacknowledged legacy `hook_error`, violating the preservation contract. Migrate that state before applying the new-run deletion rule.
**consistency: approve**
**integration: needs-attention** (1)
1. .claude/skills/process-plan/SKILL.md:98-99: When `end --reason stall` refuses because ownership changed, step 5 continues into step 6 and resumes campaign writes without rechecking ownership. An obsolete heartbeat can consequently continue after handover. Restart at step 2 on identity refusal, as the plan-done branch already does.
**record: approve**
```

## GPT panel Round 4

Round 4 over `7c0e4f3d^..8136e4b1` with prior verdicts appended (rounds past the sign-off run for blocking findings only). Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 61s, clock 2026-09-26T01:30:24Z, cost 70316tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-4.out, panel-4.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F14 -- a legacy guard's state is lost at a same-run acquisition -- adversarial -- FIXED in `f3a6d485` (independent) [minor]

`acquire` migrates the previous guard before comparing run ids.

### F15 -- an incomplete end lets a stopped campaign resume -- integration -- FIXED in `f3a6d485` (independent) [major]

The `end incomplete` line names its reason and the heartbeat finishes that end; it never resumes while the line prints.

```text
RECEIPT sha=7a296fba7ac22ed78a635289fa6003ff8e8e117cb42f330333227fb0fd14f966 end=PANEL-450b8de94ee2990b nonce=e4972df7d361f6f5
**adversarial: needs-attention** (1)
1. scripts/campaign_guard.py:312,337-338,362: A same-run acquisition or handover of a legacy guard lacking `run_id` still deletes its legacy state. `acquire` reads without migration, mints an ID, and passes the previous missing ID into `_clear_state_if_new_run`, which cannot recognize continuity and deletes the stall trips and unacknowledged `hook_error`. Migrate the legacy guard and state together before applying the new-run reset.
**consistency: approve**
**integration: needs-attention** (1)
1. .claude/skills/process-plan/SKILL.md:94,99: The heartbeat logs `end incomplete` and proceeds without completing the interrupted end. If `end --reason operator-stop` crashes after publishing the pending cancellation but before deleting the guard, the next firing reaches step 6 and resumes the stopped campaign. Recover the recorded end before permitting campaign resumption.
**record: approve**
```

## GPT panel Round 5

Round 5 over `7c0e4f3d^..f3a6d485` the hard cap, with prior verdicts appended. Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 85s, clock 2026-09-26T01:35:48Z, cost 72260tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-5.out, panel-5.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` approve

### F16 -- a crash inside acquire leaves a legacy state the hook adopts -- adversarial -- FILED to `D00 T04 §40` (independent) [minor]

Round 5 is the hard cap. The residue needs a state written before D00 T04 §38, a crash between two steps of one acquisition, and two inherited trips; its worst effect is a breaker trip the heartbeat reports, not a system change, so it files rather than stamping unreviewed code.

```text
RECEIPT sha=21df0974fb311c5015185efd372ba4664a116b3e7c4621b7df5e9215e9515886 end=PANEL-0ab3f9fea751d4d2 nonce=4fc8282c050df581
**adversarial: needs-attention** (1)
1. scripts/campaign_guard.py:362-364,379-381 and .claude/hooks/campaign-stop.ps1:214-219: Acquisition publishes the new guard before clearing legacy state lacking `run_id`. A crash at either publication point leaves that state behind, and the hook unconditionally adopts its blocks, trips, and legacy error into the new run. Two inherited trips can consequently terminate the new campaign as stalled. Tag legacy state with its original run before publishing a replacement guard so interrupted acquisition cannot misattribute it.
**consistency: approve**
**integration: approve**
**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §38, D00 T04 §36, D00 T04 §37]; dependents [none]; bytes 58232; run 20260926-D00-T04-S38-astra

`bytes 58232` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency D00 T04 §36 and its XREF neighbor D00 T04 §37; no section depends on D00 T04 §38 yet. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 67s, clock 2026-09-26T01:37:35Z, cost 28077tokens. Check: `PASS 15 findings, one per line`.

Ledger:
- [D00-T04-S38-PR1] [minor] owner mutations accept a partial identity (`--run` optional) -> filed D00 T04 §40
- [D00-T04-S38-PR2] [minor] a duplicate firing already running can borrow the survivor's identity -> filed D00 T04 §40
- [D00-T04-S38-PR3] [minor] `CronDelete` sits outside every fence -> filed D00 T04 §40
- [D00-T04-S38-PR4] [minor] the prompt identity has no length or collision contract -> filed D00 T04 §40
- [D00-T04-S38-PR5] [minor] an unreadable listing reads as an empty one -> filed D00 T04 §40
- [D00-T04-S38-PR6] [minor] terminal pending cancellations have no drain owner -> filed D00 T04 §40
- [D00-T04-S38-PR7] [minor] orphan hook errors cannot be acknowledged by anyone -> filed D00 T04 §40
- [D00-T04-S38-PR8] [minor] the upgrade path is undriven end to end -> filed D00 T04 §40
- [D00-T04-S38-PR9] [minor] malformed guard files have no repair protocol -> filed D00 T04 §40
- [D00-T04-S38-PR10] [minor] failed writes and power-loss durability are unproven -> filed D00 T04 §40
- [D00-T04-S38-PR11] [minor] journal lines carry no episode id or closure boundary -> duplicate D00 T04 §39 item 3 (D00-T04-S37-PR3)
- [D00-T04-S38-PR12] [minor] an episode update and its journal line can disagree after a crash -> duplicate D00 T04 §39 item 3 (D00-T04-S37-PR4)
- [D00-T04-S38-PR13] [minor] credentials in URL query or fragment survive -> filed D00 T04 §40
- [D00-T04-S38-PR14] [minor] the in-section health interval is undriven -> filed D00 T04 §40
- [D00-T04-S38-PR15] [minor] D00 T04 §38 needs its own consolidated acceptance contract -> filed D00 T04 §40
End of ledger

15 findings: thirteen filed in D00 T04 §40 (`02a0763c`), PR11 and PR12 duplicates of D00 T04 §39's episode-lifecycle item.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=edbf59a73950061482902d86a5ee75b5b7e0b0aa34c3de7d0b0ffe998305cc3d end=PLAN-099fbb2ea690c4f2 nonce=9ad9af61f23c374d
- §38 promises run-identity checks on every mutation but describes `--run` as optional; require the complete expected identity on owner mutations so callers cannot accidentally weaken the fence.
- §38's single-live-carrier check does not distinguish an already-running duplicate whose scheduler entry was deleted; require generation rotation when resolving duplicate carriers and an overlapping-firings fixture, so an obsolete execution cannot borrow the surviving job's identity.
- §38 fences local commands but leaves scheduler deletion outside that protection; define fresh identity validation before `CronDelete` and test handover between listing and deletion, so stale recovery instructions cannot remove a successor's heartbeat.
- §38's identity header must fit an approximately 80-character scheduler preview, but no general length or collision contract is stated; use a bounded identity derived from the canonical workspace and run path, with long-path and same-basename fixtures, to prevent cross-workspace reconciliation.
- §38 does not specify how reconciliation distinguishes an empty scheduler from a failed, truncated, or unparseable listing; require an explicit unknown outcome that performs no destructive recovery, because missing evidence is not evidence that jobs are absent.
- §38 records failed cancellations but does not guarantee another execution will drain them after the guard and final heartbeat disappear; assign a recovery owner and retry trigger that survive campaign termination, so pending records cannot remain indefinitely unattended.
- §38 makes fenced error acknowledgement refuse without a guard, while errors can arrive after termination or before any owner is captured; define durable reporting and acknowledgement for orphan errors, so these records remain recoverable without borrowing a successor's identity.
- §38 migrates guards by minting missing generations while scoped reconciliation leaves legacy prompts alone; add a complete upgrade drive covering guard, state, scheduler prompt, and run record, so migration cannot strand the campaign without a recognized carrier.
- §38 handles malformed guards as a reporting branch but specifies no completed repair protocol for malformed guard, state, or pending-cancellation files; assign quarantine, recovery authority, and readback checks, so corruption has a bounded resolution that preserves ownership evidence.
- §38's crash-consistency proof focuses on process interruption around publication steps; add failed writes, failed replacements, and failed deletions, and state whether power-loss durability is promised, because atomic replacement alone does not establish every advertised recovery guarantee.
- §37 and §38 bind restored repair attempts to repository, branch, and workflow but record no explicit episode identity in the described journal lines; add episode IDs and closure boundaries, so a reused run file cannot merge separate repair budgets or resurrect a completed episode.
- §37 and §38 do not define recovery from a crash between updating the episode file and durably recording its attempt line; specify a recoverable publication order and interruption fixtures, so losing `build/` cannot silently erase the latest counted attempt.
- §38 strips URL userinfo but does not cover credentials in repository URL query parameters or fragments; define a credential-free repository identity before serialization and comparison, so tracked repair journals cannot expose authentication material.
- §38's 60-minute health requirement lacks a drive proving execution during a long section or recovery wait; specify how checks run during blocking work and test that path, so heartbeat loss is detected within the promised interval.
- §38 has accumulated several superseding corrections without the consolidated acceptance contract it adds to §36; publish its final command requirements, refusal outcomes, and recovery transitions in one place, so operators and future sections can follow the current lifecycle without reconstructing review history.
```

## Live proof

Section gates driven on the working tree at `ea0bb140` (the implementation `7c0e4f3d`, every review answer through `f3a6d485`, the D00 T04 §40 filing, and the recorded findings and run records), so the record totals count this section. The candidate names `ea0bb140` because a staged stamp tree cannot name itself. The drives' lines ride the run file (docs/phase-runs/2026-09-25-phase-0.md). A first sweep on this commit read `plan --check FAILED` because `build/todo-progress.json` was stale after the Commit item's tick; `plan --sync` rewrote only that ignored file (the tree did not move), and the sweep quoted here re-ran whole.

- `pwsh -NoProfile -File scripts/check-all.ps1`: `check-all: 19 gate(s) ok`

Provenance: candidate ea0bb140401ec97c39a8ef2394570bcfbfa7f75e; command pwsh -NoProfile -File scripts/check-all.ps1; exit 0; tool PowerShell 7.6.6; digest 89f6c24730c44d2ba079c33245e50b23d7b2bec537bf571b560e2e54e433e9c1; path docs/reviews/00-workspace/D00-T04-s38.md; run 20260926-D00-T04-S38-astra

- `python scripts/review_prompt.py --self-test`: `review-prompt self-test: 460 cases, 0 failed`

Provenance: candidate ea0bb140401ec97c39a8ef2394570bcfbfa7f75e; command python scripts/review_prompt.py --self-test; exit 0; tool CPython 3.14.4; digest 4cbcffd47027e3a9572cc8441b51f6b01a354373a31dc7842aa32b963edbd8d5; path docs/reviews/00-workspace/D00-T04-s38.md; run 20260926-D00-T04-S38-astra

- `python scripts/campaign_guard.py --self-test`: `campaign_guard self-test: 189 cases, 0 failed`

Provenance: candidate ea0bb140401ec97c39a8ef2394570bcfbfa7f75e; command python scripts/campaign_guard.py --self-test; exit 0; tool CPython 3.14.4; digest a2d1899530071b2f6b49e7efca91cffb21b7cbb92b73bd1b45966eec631eab98; path docs/reviews/00-workspace/D00-T04-s38.md; run 20260926-D00-T04-S38-astra

- `python scripts/todo-runs.py --check`: `50 runs, 151 rounds: all resolve, all covered, counts agree`

Provenance: candidate ea0bb140401ec97c39a8ef2394570bcfbfa7f75e; command python scripts/todo-runs.py --check; exit 0; tool CPython 3.14.4; digest 1369af0a36262930ace932dbae2be64aeb65f9ab9d3ebdffec92427539e3477d; path docs/reviews/00-workspace/D00-T04-s38.md; run 20260926-D00-T04-S38-astra

- `python scripts/todo-graph.py validate`: `30 todos, 234 sections -- 0 fatal, 0 warning(s), 98 adjacency advisory`

Provenance: candidate ea0bb140401ec97c39a8ef2394570bcfbfa7f75e; command python scripts/todo-graph.py validate; exit 0; tool CPython 3.14.4; digest 781cf10d893ea1b6de1b0941ce9b6a006fde47e9554b17a3acb7e2197bab18a3; path docs/reviews/00-workspace/D00-T04-s38.md; run 20260926-D00-T04-S38-astra

- `python scripts/todo-claims.py`: `todo-claims: 113 claim(s) -- 113 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`

Provenance: candidate ea0bb140401ec97c39a8ef2394570bcfbfa7f75e; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest 7356c6b957878f5f07de2506a4b114171ea860529bf505dbd0ee7046a7cd222d; path docs/reviews/00-workspace/D00-T04-s38.md; run 20260926-D00-T04-S38-astra

- `python scripts/todo-graph.py plan --check`: `implementation plan is current -- 49/231 sections complete (21%)`

Provenance: candidate ea0bb140401ec97c39a8ef2394570bcfbfa7f75e; command python scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.4; digest 43179de78b5b459ea9d0a0894965773aace79d7e0cd71ed2cdc23cd8e44eaa38; path docs/reviews/00-workspace/D00-T04-s38.md; run 20260926-D00-T04-S38-astra

- `python scripts/todo-findings.py --check`: `todo-findings: ledger current, 416 finding(s), transitions complete`

Provenance: candidate ea0bb140401ec97c39a8ef2394570bcfbfa7f75e; command python scripts/todo-findings.py --check; exit 0; tool CPython 3.14.4; digest 4dc56888478b5277b19c8baf723f160ae517446a89a52b30eba7759ed7069b6f; path docs/reviews/00-workspace/D00-T04-s38.md; run 20260926-D00-T04-S38-astra
