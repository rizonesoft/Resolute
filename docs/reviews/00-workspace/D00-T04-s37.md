# Review -- D00 T04 §37, CI Read-Back Hardening

**Reviewed:** 2026-09-25
**Stance:** post-implementation
**Rounds:** independent pass (3 findings: fixed); GPT 1 bulk (5 fixed); GPT 2 bulk (3 fixed, 1 cleared); GPT 3 sign-off (2 fixed, 1 cleared); GPT 4 depth (1 fixed, 1 filed in D00 T04 §38, 1 cleared); GPT 5 depth, the hard cap (1 filed in D00 T04 §39 as major, 1 advisory already filed).
**Tier:** Full panel. The diff touches `scripts/review_prompt.py` and `scripts/campaign_guard.py` (review tooling and gates in `scripts/check-all.ps1`) and two skills (`review-todo-section`, `process-phase`: the run contract).
**Attestation:** `docs/reviews/00-workspace/D00-T04-s37.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `9a9550ef` | implementation: the structural workflow parser, cause correlation, the reproducible or diagnostic label and uses steps, the GitHub-verified context oracle, the bound repair episode and ceiling allowance, the authorized no-run, and redaction |
| `894c9805` | answers the independent pass: comments and indentless sequences keep their steps; restored attempts compare by full sha, de-duplicated; restore needs its workflow |
| `6eec7be2` | answers panel round 1: quoted and secret-named values redact; triggers compare by content; checkout by identity; the authorization's time; sh proven, unproven shells refuse |
| `d3bce9e7` | answers panel round 2: quoted credential keys redact; the skills' no-run command carries `--authorized-at`; flow-mapped checkout inputs count |
| `5efb5ccf` | answers panel round 3: quoted secrets match by the full string grammar; an inline comment keeps an indentless sequence |
| `694e91a6` | answers panel round 4: every adjacent quoted segment of a secret is masked; suite counts recorded; restore identity filed |

Record commits (filings only, no reviewed content): `29b21597` (the hard-cap and plan-review findings filed in D00 T04 §39 and D00 T04 §38).

Range `9a9550ef^..694e91a6` (base `24e9a994`), contiguous, no declaration owed. Each round fenced the range from `24e9a994` to its own head; the fenced contract was checked to open with this section's heading. The rundir is session scratch outside the tree (/tmp/review-OuVVHkIF, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation. The context oracle is GitHub's, not the author's: two disposable drills (runs 36191008012 and 36194988672) printed four steps' directory, errexit, pipefail, and child-visible env, and each printed re-run executed locally under the shell it names matches line for line; the explicit-bash drill exposed a real gap (the printout had named plain `bash`), fixed before review. A VS Code restart killed one test command mid-run; the suites were re-run and passed. The architecture gate is not triggered (review tooling, no framework state). `source-defect` approves in-session: GitHub's shell templates and `gh run view` behaviour were probed live. `design` is not owed.

## Independent pass

`python scripts/panel_slots.py exec independent --commit 9a9550ef79f35c2c834cff9e8bf5378801422ba8`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 167s, cost unresolved (the review runner printed no token figure). Output and stderr stay in the session scratch rundir outside the tree (ind37.out, ind37.err); the output is transcribed below.

### F1 -- comments and indentless sequences drop a job's steps -- correctness -- FIXED in `894c9805` (independent) [major]

A column-zero comment and a valid indentless `steps:` sequence made the structural parser return no steps.

### F2 -- restored attempts compare by prefix and duplicate -- correctness -- FIXED in `894c9805` (independent) [minor]

Restored 12-character hashes let a full-sha retry count twice, and `already counted` lines restored as extra attempts.

### F3 -- restore without a workflow binds an empty identity -- correctness -- FIXED in `894c9805` (independent) [minor]

`restore` now needs `--workflow` and `--run-file`.

```text
The 447-case review-prompt self-test passes, but targeted reproductions demonstrate a workflow-parsing regression and repair-restoration failures that lose idempotency or block further repairs.

Full review comments:

- [P2] Preserve valid YAML children across comments and indentless lists — R:\GitHub\Resolute\scripts\review_prompt.py:1393-1396
  The child-block scan treats indentation alone as the boundary. A column-zero comment between `runs-on` and `steps` terminates the entire `jobs` block, and a valid indentless sequence beneath `steps:` is likewise excluded. Both cases previously returned the Build command but now make `workflow_steps()` return an empty list, so the failed-log fallback incorrectly reports no command to rerun. Handle comments and indentless sequences without silently dropping their enclosing workflow content.

- [P2] Restore unique attempts using canonical commit identities — R:\GitHub\Resolute\scripts\campaign_guard.py:667-668
  Restoration stores the 12-character hashes extracted from printed lines, but subsequent attempts compare those strings directly with the supplied commit SHA. Retrying the same commit with its full SHA therefore consumes another attempt after recovery. Additionally, recorded `already counted` lines match the same regex and are restored as separate attempts. Both cases prematurely exhaust the three-attempt bound. Resolve restored hashes to canonical commit identities and deduplicate the journal by commit before persisting it.

- [P2] Require workflow identity before persisting a restored episode — R:\GitHub\Resolute\scripts\campaign_guard.py:664-669
  The documented `repair restore --run-file F` invocation permits omitting `--workflow`, which writes an episode bound to an empty workflow. Every subsequent legitimate attempt supplying `plan-gates` then fails the identity check, while omitting the workflow also fails the required-arguments check. Repeating restore cannot fix this because an existing episode returns immediately. Require a nonempty workflow before writing restored state, or recover its verified identity from the journal.
```

## GPT panel Round 1

Round 1 over `9a9550ef^..894c9805` with the independent pass appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 42s, clock 2026-09-25T21:59:05Z, cost 44892tokens (latency and clock measured from the rundir file times, the round's prompt copy to its output). Output and stderr stay in the session scratch rundir outside the tree (panel-1.out, panel-1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F4 -- quoted and secret-named values pass redaction -- adversarial -- FIXED in `6eec7be2` (independent) [major]

`password='hunter22'` and `API_TOKEN=abcdefgh` printed unmasked.

### F5 -- triggers compare as raw text -- integration -- FIXED in `6eec7be2` (independent) [minor]

A comment in the `on:` block passed as a trigger change; triggers compare by content now.

### F6 -- checkout is recognised by display name -- integration -- FIXED in `6eec7be2` (independent) [minor]

Checkout is recognised by action identity, and its inputs are named as context.

### F7 -- the authorization records no time -- record -- FIXED in `6eec7be2` (independent) [minor]

`--authorized-at` is required and printed.

### F8 -- the context oracle proves only bash and skips silently -- record -- FIXED in `6eec7be2` (independent) [minor]

A second drill proved `sh`; unproven shells refuse as a recorded default; the oracle fails when bash is missing; the drills are in the run file.


```text
RECEIPT sha=4ef1e315d7100b4ea28ecc98edec63186c3d07944503c3a3c22f3db46e48f2b4 end=PANEL-b4843c28febf2d91 nonce=9adbe9d1a372a674
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:1678`: The credential-assignment regex excludes opening quotes, so workflow commands or logs containing `password='hunter22'` or `token="abcdefgh"` pass through `redact()` unchanged. Secret-named environment assignments such as `API_TOKEN=abcdefgh` also evade this pattern outside the structured env renderer, allowing credentials into records.
**consistency: approve**
**integration: needs-attention** (2)
1. `scripts/review_prompt.py:7400-7407`: `_triggers()` compares raw text, including comments and whitespace. Adding a comment inside the existing `on:` block satisfies the trigger-change check without changing any trigger, allowing `--expect-no-run` to authorize silence for an unchanged workflow.
2. `scripts/review_prompt.py:1629-1631`: Checkout prerequisites are identified from display names rather than action identity and inputs. A preceding command named `Run actions/checkout@prepare` is excluded from prerequisites and the next command is falsely labeled reproducible; an actual checkout with a different `ref` or `path` is likewise treated as sufficient without naming that required context.
**record: needs-attention** (2)
1. `scripts/review_prompt.py:7386-7397,7419-7421`; `todo/00-workspace/TODO-04-self-correction.md:1511`: The checked authorization requirement explicitly includes who, when, and the approved range, but implementation accepts and records only who and range. No authorization timestamp is required or preserved.
2. `scripts/review_prompt.py:6094-6117`; `todo/00-workspace/TODO-04-self-correction.md:1508,1515`; `docs/phase-runs/2026-09-25-phase-0.md:24`: The execution-context oracle exercises only default and explicit Bash, although the renderer also supports sh, PowerShell, Python, and cmd. These tests silently disappear when Bash is unavailable. The candidate also supplies neither quoted checkpoint suite counts nor the required execution-context workflow evidence in the run file, whose only added line is heartbeat bookkeeping.
```

## GPT panel Round 2

Round 2 over `9a9550ef^..6eec7be2` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 42s, clock 2026-09-25T22:09:24Z, cost 48363tokens (latency and clock measured from the rundir file times, the round's prompt copy to its output). Output and stderr stay in the session scratch rundir outside the tree (panel-2.out, panel-2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` needs-attention
`integration` needs-attention
`record` needs-attention

### F9 -- a quoted credential key passes redaction -- adversarial -- FIXED in `d3bce9e7` (independent) [major]

`"API_TOKEN": "..."` printed unmasked.

### F10 -- the skills' no-run command omits --authorized-at -- consistency -- FIXED in `d3bce9e7` (independent) [minor]

Both skills carry it now, and the pin names it.

### F11 -- flow-mapped checkout inputs are ignored -- integration -- FIXED in `d3bce9e7` (independent) [minor]

Any inline `with` names the checkout's inputs as context.

### F12 -- the checkpoint's suite counts are not quoted -- record -- CLEARED by the round-4 run-file record and this stamp's Live proof (independent) [minor]

The counts were quoted in the run file with the round-4 answer and again in the Live proof.


```text
RECEIPT sha=8966387e89c530edc9c29d7f1203a2744fb2a4b5aecec6017498cb833779532d end=PANEL-aa9f6f1b37ad4cb9 nonce=8a6969f0b403e261
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:1702`: The credential regex does not match quoted assignment keys. A log containing `{"API_TOKEN": "abcdefgh1234"}` passes through `redact()` unchanged, exposing a secret-named value in the recorded fallback.
**consistency: needs-attention** (1)
1. `.claude/skills/process-phase/SKILL.md:65`; `.claude/skills/review-todo-section/SKILL.md:286`; `scripts/review_prompt.py:6501`: Both documented no-run commands omit the newly required `--authorized-at`, and the skill fixture pins that incomplete invocation. Following either command now exits 2 instead of recording the authorized no-run outcome.
**integration: needs-attention** (1)
1. `scripts/review_prompt.py:1592-1596,1650-1656`: Checkout inputs are collected only from block children of `with`. Valid `with: {ref: other, path: alternate}` therefore produces an empty input list, and the following command is falsely labeled reproducible with only a plain checkout despite requiring a different revision and directory.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:1517`; `docs/phase-runs/2026-09-25-phase-0.md:157-162`: The checkpoint requires quoted results with suite counts for both self-tests. The added record supplies execution-context drill evidence but neither self-test result nor count, leaving this previously reported checkpoint requirement unfulfilled.
```

## GPT panel Round 3

Round 3 over `9a9550ef^..d3bce9e7`, the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 70s, clock 2026-09-25T22:13:03Z, cost 50372tokens (latency and clock measured from the rundir file times, the round's prompt copy to its output). Output and stderr stay in the session scratch rundir outside the tree (panel-3.out, panel-3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F13 -- an escaped quote leaks part of a secret -- adversarial -- FIXED in `5efb5ccf` (independent) [major]

Blocking (a partial credential could reach a record), so round 4 ran; after three patches the value was rethought to the full string grammar.

### F14 -- an inline comment hides an indentless sequence -- integration -- FIXED in `5efb5ccf` (independent) [minor]

`steps: # build` keeps its sequence now.

### F15 -- the checkpoint's suite counts are not quoted -- record -- CLEARED by the round-4 run-file record and this stamp's Live proof (independent) [minor]

A repeat of F12.


```text
RECEIPT sha=5cef7de42b5059299932fae1e28041cbadbcb7e76eac3ab6e0dd5c444224193f end=PANEL-f7b61067a448cf3f nonce=dff0d2675bf5c2b7
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:1705`: The credential-value regex stops at escaped quotes. Valid JSON such as `{"API_TOKEN": "abc\"defghi"}` becomes `{"API_TOKEN": ***defghi"}`, exposing part of the credential in recorded output. Quoted-value matching must consume escape sequences.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/review_prompt.py:1397-1401`: An inline comment makes an empty-valued key appear nonempty. Valid `steps: # build` followed by an indentless `- run: make` therefore excludes the sequence from the steps block, and `workflow_steps()` silently returns no steps. Strip the header comment before deciding whether an indentless sequence belongs to the key.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:1517`; `docs/phase-runs/2026-09-25-phase-0.md:157-162`: The checkpoint still requires quoted results and suite counts for both self-tests. The candidate records the execution-context drills but supplies neither self-test result nor count, leaving the previously reported proof requirement unfulfilled.
```

## GPT panel Round 4

Round 4 over `9a9550ef^..5efb5ccf` with prior verdicts appended (rounds past the sign-off run for blocking findings only). Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 74s, clock 2026-09-25T22:17:00Z, cost 51331tokens (latency and clock measured from the rundir file times, the round's prompt copy to its output). Output and stderr stay in the session scratch rundir outside the tree (panel-4.out, panel-4.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F16 -- adjacent quoted segments leak a suffix -- adversarial -- FIXED in `694e91a6` (independent) [major]

YAML's `'it''s'` and shell's `'abc'"def"` left a suffix; every adjacent segment is masked now.

### F17 -- restore rebinds the episode's identity -- integration -- FILED to `D00 T04 §38` (independent) [minor]

Below the blocking bar (the repair budget's bookkeeping, no user data); filed as an item in D00 T04 §38, which owns guard identity and recovery.

### F18 -- the checkpoint's suite counts are not quoted -- record -- CLEARED by the round-4 run-file record and this stamp's Live proof (independent) [minor]

The round-4 answer recorded both suite counts in the run file.


```text
RECEIPT sha=0cfb06977cf5711cfd641d1d462c4047d2024b021bad26561cde0d19295ec0f4 end=PANEL-d7a268f14c0dba27 nonce=20b92ddecd519c9b
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:1708`: Quoted-value redaction still exposes credential suffixes in valid YAML and shell quoting. `password: 'it''s-secret'` becomes `password: ***'s-secret'`, and `API_TOKEN='abc'"defghi"` becomes `API_TOKEN=***"defghi"`. Matching one quoted segment does not consume the complete credential.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/campaign_guard.py:685-687`: Restoration assigns the current repository and branch plus the caller-supplied workflow without recovering or verifying the original episode identity. After losing the state file, restoring the same journal on another branch or with another workflow silently rebinds its attempts, allowing subsequent calls that the original episode would reject.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:1517`; `docs/phase-runs/2026-09-25-phase-0.md:157-162`: The checkpoint requires quoted results and suite counts for both self-tests, but the candidate records only execution-context drill evidence. The previously reported checkpoint evidence remains missing; a planned future stamp does not supply it in this candidate.
```

## GPT panel Round 5

Round 5 over `9a9550ef^..694e91a6`, the hard cap, with prior verdicts appended. Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 47s, clock 2026-09-25T22:22:27Z, cost 46989tokens (latency and clock measured from the rundir file times, the round's prompt copy to its output). Output and stderr stay in the session scratch rundir outside the tree (panel-5.out, panel-5.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` advisory
`record` approve

### F19 -- a bare credential value stops at a comma or brace -- adversarial -- FILED to `D00 T04 §39` (independent) [major]

`API_TOKEN=abc,defghi` prints `***,defghi`. Round 5 is the hard cap, so the leftover files as a major finding rather than shipping unreviewed code; GitHub's own masking of registered secrets stays the first line, so the stamp is not invalidated.

### F20 -- restore still rebinds identity -- integration -- FILED to `D00 T04 §38` (independent) [minor]

Advisory, a repeat of F17, already filed.


```text
RECEIPT sha=7c0a61de48d1da0c9834284738aebae9c871acc734d39dccbf9f7486bfaf8614 end=PANEL-8cafa5a8563e24bb nonce=9f73de59f9c3d504
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:1708`: Bare credential matching stops at commas and closing braces, which are valid characters in shell assignments and YAML block-mapping plain values. `API_TOKEN=abc,defghi` becomes `API_TOKEN=***,defghi`, and `password: abc}defghi` becomes `password: ***}defghi`, exposing credential suffixes despite the adjacent-segment fix.
**consistency: approve**
**integration: advisory** (1)
1. `scripts/campaign_guard.py:685-687`: Restoration still assigns the caller's repository, branch, and workflow instead of recovering the original identity. Restoring the journal elsewhere silently rebinds its attempts. This live limitation is explicitly deferred to D00 T04 §38.
**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §37, D00 T04 §35, D00 T04 §38]; dependents [D00 T04 §38]; bytes 40044; run 20260925-D00-T04-S37-astra

`bytes 40044` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency D00 T04 §35 and its review dependent D00 T04 §38. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 52s, clock 2026-09-25T22:23:42Z, cost 22661tokens. Check: `PASS 17 findings, one per line`.

Ledger:
- [D00-T04-S37-PR1] [minor] close evidence is a pasted line, not re-read from GitHub -> filed D00 T04 §39
- [D00-T04-S37-PR2] [minor] the episode names a run file, not the campaign run -> filed D00 T04 §39
- [D00-T04-S37-PR3] [minor] restore can resurrect a closed episode -> filed D00 T04 §39
- [D00-T04-S37-PR4] [minor] state and journal writes can disagree after a crash -> filed D00 T04 §39
- [D00-T04-S37-PR5] [minor] a failed push leaves an attempt ambiguous -> filed D00 T04 §39
- [D00-T04-S37-PR6] [minor] the ceiling allowance has no recovery -> filed D00 T04 §39
- [D00-T04-S37-PR7] [minor] the ceiling key lacks repository and attempt -> filed D00 T04 §39
- [D00-T04-S37-PR8] [minor] several failures in one red are not aggregated -> filed D00 T04 §39
- [D00-T04-S37-PR9] [minor] reproducible says nothing about runner tools or revision -> filed D00 T04 §39
- [D00-T04-S37-PR10] [minor] Windows shells have no local reproduction owner -> filed D00 T04 §39
- [D00-T04-S37-PR11] [minor] authorized silence is not tied to the push -> filed D00 T04 §39
- [D00-T04-S37-PR12] [minor] exit 4 has no campaign disposition -> filed D00 T04 §39
- [D00-T04-S37-PR13] [minor] D00 T04 §35 still documents superseded commands -> filed D00 T04 §39
- [D00-T04-S37-PR14] [minor] a masked export still reads as runnable -> filed D00 T04 §39
- [D00-T04-S37-PR15] [minor] private-key masking needs the whole block -> filed D00 T04 §39
- [D00-T04-S37-PR16] [minor] the oracle's workflows lived only on deleted branches -> filed D00 T04 §39
- [D00-T04-S37-PR17] [minor] the restore-identity item has no checkpoint legs -> filed D00 T04 §38
End of ledger

17 findings, all filed (`29b21597`): sixteen in D00 T04 §39, PR17 in D00 T04 §38's checkpoint.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=9b0c40a8b7b762e39b7d31794ab5c6d12dc38738d39d576424c691ff5c428687 end=PLAN-772a9c62aeaedd6a nonce=0911f981631dc936
- §37 accepts a copied green output line without specifying its provenance; its close-evidence contract should verify repository, workflow identity, branch, run ID, run attempt, and commit against authoritative metadata, preventing unrelated or stale success from closing an episode.
- §37 binds episodes to a run-file path but omits an immutable campaign run ID; align episode identity with §38’s run fencing so reusing a journal cannot attach a new campaign to old repair state.
- §38 restores attempt identities but does not define episode boundaries or closure records; its recovery item should restore only the applicable open episode and reject ambiguous legacy journals, preventing completed episodes from being resurrected.
- No section owns crash consistency between repair-state updates and journal appends; extend §38’s publication contract to repair attempts, closures, and restoration, testing interruption between writes so recovery cannot lose or invent attempts.
- §37’s attempt accounting leaves failed pushes unresolved; define reservation, successful push, retry, and abandonment transitions so an unpublished repair neither silently consumes the budget nor permits an uncounted push.
- §37’s ceiling allowance lives in another disposable `build/` file without recovery rules; extend §38’s recovery scope to detect missing or corrupt ceiling state, preventing deletion from granting another wait.
- §37 keys ceiling allowances only by run ID; specify repository and run-attempt identity plus queued-to-running transitions, so a restarted wait preserves its count while a genuinely new execution receives the intended allowance.
- §37 correlates cause through textual job/step groups but lacks multiple-failure acceptance cases; require stable job and step identities, matrix-job fixtures, and an aggregation rule so one failure cannot hide another actionable failure.
- §37 calls a command reproducible when only checkout precedes it, without proving runner tools, operating system, checkout revision, or execution conditions; strengthen that classification or qualify its limits to avoid presenting a different local environment as equivalent.
- §37 intentionally refuses Windows shells while requiring Bash for its oracle; assign native Windows reproduction and its prerequisites an owner, since the Windows-only project otherwise lacks a supported local fallback on its primary platform.
- §37’s no-run authorization checks that triggers changed, not that the approved push is excluded; require evidence linking the event, branch, paths, and trigger change to expected silence so unrelated trigger edits cannot excuse missing CI.
- §37 gives authorized silence exit 4 and forbids closing a repair with it, but assigns no campaign disposition; define how an approved workflow retirement terminates or transfers an open episode without claiming green or leaving recovery permanently blocked.
- §35 still documents exit 0 for authorized silence and obsolete repair command signatures; add a dated final acceptance paragraph pointing to §37’s current commands and outcomes, preserving history while removing conflicting operational guidance.
- §37 masks secret environment values with executable `'***'` assignments but does not require downgrading reproducibility; mark redacted scripts incomplete and name required local inputs so a placeholder run cannot be mistaken for equivalent execution.
- §37 promises private-key-block masking while describing line-by-line output redaction; require end-to-end multiline, escaped-newline, and truncated-block fixtures so credential bodies cannot escape a mask that depends on seeing the complete block.
- §37 retains oracle expectations but leaves the generating workflows only on deleted branches; preserve sanitized workflow definitions, runner metadata, and captured outputs in a durable evidence location so future parser changes can reproduce and audit the comparison.
- §38’s restore-identity item is absent from its explicit checkpoint inventory; add cross-repository, cross-branch, cross-workflow, mixed-journal, and legacy-record refusal cases so its principal connection to §37 has a measurable acceptance gate.
```

## Live proof

Section gates driven on the working tree at `efbcd7c6` (the implementation `9a9550ef`, every review answer through `694e91a6`, the D00 T04 §39 and D00 T04 §38 filings, and the recorded findings and run records) with this stamp's own records added, so the record totals count this section. The candidate names `efbcd7c6` because a staged stamp tree cannot name itself. The drills' lines ride the run file, quoted in the Verified line.

- `pwsh -NoProfile -File scripts/check-all.ps1`: `check-all: 19 gate(s) ok`

Provenance: candidate efbcd7c6ebdfe5bb0142956b75394e7aec71b73a; command pwsh -NoProfile -File scripts/check-all.ps1; exit 0; tool PowerShell 7.6.6; digest 796b1dae028ce8e4acfd3f69fc5f2cf27cf0df0a983eeeb60a230cc03e9ab4b2; path docs/reviews/00-workspace/D00-T04-s37.md; run 20260925-D00-T04-S37-astra

- `python scripts/review_prompt.py --self-test`: `review-prompt self-test: 460 cases, 0 failed`

Provenance: candidate efbcd7c6ebdfe5bb0142956b75394e7aec71b73a; command python scripts/review_prompt.py --self-test; exit 0; tool CPython 3.14.4; digest 4cbcffd47027e3a9572cc8441b51f6b01a354373a31dc7842aa32b963edbd8d5; path docs/reviews/00-workspace/D00-T04-s37.md; run 20260925-D00-T04-S37-astra

- `python scripts/campaign_guard.py --self-test`: `campaign_guard self-test: 113 cases, 0 failed`

Provenance: candidate efbcd7c6ebdfe5bb0142956b75394e7aec71b73a; command python scripts/campaign_guard.py --self-test; exit 0; tool CPython 3.14.4; digest 8a2005fcd1c2c4b080b2c01eea8e1231981496046176e0b760fa2096ff597762; path docs/reviews/00-workspace/D00-T04-s37.md; run 20260925-D00-T04-S37-astra

- `python scripts/todo-runs.py --check`: `49 runs, 145 rounds: all resolve, all covered, counts agree`

Provenance: candidate efbcd7c6ebdfe5bb0142956b75394e7aec71b73a; command python scripts/todo-runs.py --check; exit 0; tool CPython 3.14.4; digest 3e7e57c6ce45a2121731aed97bcaa8025c39a45457654b672c026bf1227bdd62; path docs/reviews/00-workspace/D00-T04-s37.md; run 20260925-D00-T04-S37-astra

- `python scripts/todo-graph.py validate`: `30 todos, 233 sections -- 0 fatal, 0 warning(s), 98 adjacency advisory`

Provenance: candidate efbcd7c6ebdfe5bb0142956b75394e7aec71b73a; command python scripts/todo-graph.py validate; exit 0; tool CPython 3.14.4; digest 4df98b2e2402fcb3db0431fb766f661394b1a97d901305be1efe6cb7b8077206; path docs/reviews/00-workspace/D00-T04-s37.md; run 20260925-D00-T04-S37-astra

- `python scripts/todo-claims.py`: `todo-claims: 113 claim(s) -- 113 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`

Provenance: candidate efbcd7c6ebdfe5bb0142956b75394e7aec71b73a; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest 437c10b6b4bfd50cb4740f8a77a327657864c1215bd12bfdb03f92239b7a147f; path docs/reviews/00-workspace/D00-T04-s37.md; run 20260925-D00-T04-S37-astra

- `python scripts/todo-graph.py plan --check`: `implementation plan is current -- 49/230 sections complete (21%)`

Provenance: candidate efbcd7c6ebdfe5bb0142956b75394e7aec71b73a; command python scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.4; digest 32489529f13dce7d405a274b951f3c919bcec3ee2b2f97dcda60d9a21735ae09; path docs/reviews/00-workspace/D00-T04-s37.md; run 20260925-D00-T04-S37-astra

- `python scripts/todo-findings.py --check`: `todo-findings: ledger current, 400 finding(s), transitions complete`

Provenance: candidate efbcd7c6ebdfe5bb0142956b75394e7aec71b73a; command python scripts/todo-findings.py --check; exit 0; tool CPython 3.14.4; digest 7d431d432407397aea9413f207f0aca95bee8c6d2eb1da1a27ca2c571d615f36; path docs/reviews/00-workspace/D00-T04-s37.md; run 20260925-D00-T04-S37-astra
