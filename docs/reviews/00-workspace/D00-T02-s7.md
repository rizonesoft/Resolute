# Review -- D00 T02 §7, Driven UI Completion Tests

**Reviewed:** 2026-09-26
**Stance:** post-implementation
**Rounds:** independent pass (2 findings: fixed); GPT 1 bulk (2 fixed); GPT 2 bulk (approve); GPT 3 sign-off (approve).
**Tier:** Full panel. The diff changes the shared UI library's animation manager and all five animated controls (every tool consumes them), and adds the driven test file.
**Attestation:** `docs/reviews/00-workspace/D00-T02-s7.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `c8a31bd3` | implementation: `tests/ui_driven_test.cpp` (the theme crossfade, content view, sidebar collapse, list view input, and teardown cases); animation ownership (`AnimateFor`, `CancelOwner`, a destructor per animated control) and a tick over a moved-out copy; the `[driven]` tag in `tests/README.md` |
| `af27a44f` | answers the independent pass: `Animation::Step`, a cancellation check between update and completion, and kept animations filtered against the frame's final cancellations |
| `18111005` | answers panel round 1: nested ticks skipped, and the teardown leg pumped past the animation through the host's `WM_SIZE` count |

Record commits (filings only, no reviewed content): `3b55ab53` and `3aeeadac` (the plan-review filings: two claims in D00 T02 §7, two items in D00 T02 §10).

Range `c8a31bd3^..18111005` (base `27b52f4d`), contiguous, no declaration owed. Each round fenced the range from `27b52f4d` to its own head. The rundir is session scratch outside the tree (/tmp/review-cH5WU11G, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Makes sense: each item maps to a driven case, and the third item to the library change it needed (the controls had no destructors, so every animation callback outlived its control). Logical: every endpoint is asserted through a read-back (`GetWindowLongPtrW` for the overlay's click-through style, `ScaledWidth()`, selection and sort state after real mouse messages, an independently read target palette), and every drill is recorded: a stuck crossfade failed by name, and each regression leg failed against the code it guards. Broke nothing: `check-all: 20 gate(s) ok`, both presets `100% tests passed out of 74`, `tidy` at 166 under its baseline of 169, and the `[ui]` suite passed under five random orders (`320 assertions in 47 test cases` each).

`source-defect`: not owed (no Win32 contract read beyond documented timer and window behavior). `design`: not owed (the windows are never shown; no rendered surface is judged).

## Independent pass

`python scripts/panel_slots.py exec independent --commit c8a31bd3...`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 118s, clock 2026-09-26T12:29:12Z, cost unresolved (the review runner printed no token figure). Output and stderr stay in the build scratch outside the tree (build/rev7/independent.out, independent.err); the output is transcribed below.

### F1 -- a kept animation survives a later cancellation of its owner -- adversarial -- FIXED in `af27a44f` (independent) [major]

The kept list is filtered against the frame's final cancellations; the leg failed `77 == 1` against the SHIP library.

### F2 -- a cancel from an animation's own final update still runs its completion -- adversarial -- FIXED in `af27a44f` (independent) [major]

`Animation::Step` advances the clock alone and the manager checks between update and completion; the leg failed `1 == 0` against the SHIP library.

```text
The cancellation implementation misses two callback-ordering cases that undermine the teardown safety this patch introduces. Both were reproduced with targeted timer-driven probes against the changed animation implementation.

Full review comments:

- [P1] Recheck retained animations before clearing cancellations — R:\GitHub\Resolute\shared\resolute-ui\src\animation.cpp:60-64
  If animation A ticks before animation B cancels A's owner, A is already in `kept` and is restored without another cancellation check. Clearing the cancellation records then allows A's callbacks to run on subsequent frames, potentially against a destroyed control. A timer-driven probe reproduced callbacks after cancellation with one animation still retained. Filter the retained animations against the final cancellation records before merging newly added animations; cover cancellation of an earlier animation as well as a later one.

- [P1] Honor cancellation between update and completion callbacks — R:\GitHub\Resolute\shared\resolute-ui\src\animation.cpp:55-56
  When an animation's final `onUpdate` cancels its owner, `Animation::Tick` still invokes `onComplete` before returning to this cancellation check. Consequently, teardown triggered synchronously by an update can leave the completion callback accessing the destroyed control despite `CancelOwner`. A timer-driven probe calling `CancelOwner` from the final update still executed the completion callback. Check cancellation between these two callbacks, and add a regression case for cancellation from the animation's own update.
```

## GPT panel Round 1

Round 1 over `c8a31bd3^..af27a44f` with the independent pass answered. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 28s, clock 2026-09-26T12:51:15Z, cost 28025tokens. Output and stderr stay in the session scratch rundir outside the tree (r1.out, r1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` needs-attention

### F3 -- a nested tick can clear the frame's cancellations -- adversarial -- FIXED in `18111005` (independent) [major]

`OnTick` skips a tick dispatched from inside a callback's nested message loop; the leg that pins it failed `77 == 1` without the guard.

### F4 -- the teardown leg never pumps past the animation -- record -- FIXED in `18111005` (independent) [minor]

It creates a real sidebar child, counts the `WM_SIZE` its collapse sends the host, and pumps 400 ms after teardown.

```text
RECEIPT sha=e2af9a9b22e2bd2fc3af2b49eca2ec7b70680ac32cf6726f96b5c8f7c8ded556 end=PANEL-b1a94e880908e3a5 nonce=d4c4fb6f7aa6e5e9
**adversarial: needs-attention** (1)
1. shared/resolute-ui/src/animation.cpp:48-52: `OnTick` lacks a reentrancy guard. If an animation callback cancels an owner and pumps a nested message loop, another timer tick clears the outer frame’s cancellation records and resets `m_ticking`. The outer frame can then invoke the cancelled owner’s completion or retain its animations, permitting callbacks against destroyed controls. Reject nested ticks before modifying frame state and add a regression test.
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)
1. tests/ui_driven_test.cpp:240-245 and todo/00-workspace/TODO-02-test-backbone.md:518: The teardown case does not pump the manager timer past the destroyed control’s animation endpoint as required. After successful cancellation, `Settled()` is already true, so `PumpUntil(Settled, 500)` immediately returns without dispatching any messages; 500 is a timeout, not a pumping duration. Pump through an independent elapsed-time condition beyond the original animation duration before asserting that no callback remains.
```

## GPT panel Round 2

Round 2 over `c8a31bd3^..18111005` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 27s, clock 2026-09-26T13:03:54Z, cost 29448tokens. Output and stderr stay in the session scratch rundir outside the tree (r2.out, r2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` approve
`record` approve

```text
RECEIPT sha=0be62c4ef874baa34081ecddf89c49d2bd9058bfe2794e5c645c8383fb73a0fa end=PANEL-0cc7b3b7724f0131 nonce=f79c63e57f7d946b
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## GPT panel Round 3

Round 3 over `c8a31bd3^..18111005` the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 29s, clock 2026-09-26T13:04:29Z, cost 42795tokens. Output and stderr stay in the session scratch rundir outside the tree (r3.out, r3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` approve
`record` approve

```text
RECEIPT sha=46cd7be43181415d33068fe0852bc790a66bcd70b45efc87aa9dc6b364abea06 end=PANEL-91ecdcb2a2aa5970 nonce=bac9863b82e0bc9b
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Plan review

Manifest: sections [D00 T02 §7, D00 T02 §5, D00 T02 §10]; dependents [D00 T02 §10, D00 T02 §11, D00 T02 §12]; bytes 23994; run 20260926-D00-T02-S7-astra

`bytes 23994` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section, its dependency D00 T02 §5, and its direct dependent D00 T02 §10, with D00 T02 §11 and D00 T02 §12 one hop past. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 40s, clock 2026-09-26T13:05:11Z, cost 17445tokens. Check: `PASS 13 findings, one per line`.

Triage follows the operator direction of 2026-09-26 (the plan must not grow unconditionally): ten findings fold into two items of the open D00 T02 §10, one becomes two claims in this section, and two are rejected with a reason.

Ledger:
- [D00-T02-S7-PR1] [minor] the fence treats hidden driven cases as headful -> filed D00 T02 §10
- [D00-T02-S7-PR2] [minor] popups are named in the cluster but not covered -> filed D00 T02 §10
- [D00-T02-S7-PR3] [minor] toolbar overflow and filtering have no disposition -> filed D00 T02 §10
- [D00-T02-S7-PR4] [minor] teardown is exercised for the sidebar only -> filed D00 T02 §10
- [D00-T02-S7-PR5] [minor] nothing keeps a future control from an ownerless animation -> filed D00 T02 §7
- [D00-T02-S7-PR6] [minor] rapid reversal while animating is untested -> filed D00 T02 §10
- [D00-T02-S7-PR7] [minor] a callback that never returns hangs the run -> filed D00 T02 §10
- [D00-T02-S7-PR8] [minor] the checkpoint omits the manager repair and quotes an early count -> rejected the stamp quotes the final candidate's counts and the repair, and the Done notes give both the SHIP and final counts
- [D00-T02-S7-PR9] [minor] no restoration obligation or randomized combined run -> rejected ctest runs each case in its own process, and the combined [ui] suite passed under five random orders (seeds 1, 42, 20260926, 777, 31337: 320 assertions in 47 test cases each)
- [D00-T02-S7-PR10] [minor] hit targets and widths are checked at 96 DPI only -> filed D00 T02 §10
- [D00-T02-S7-PR11] [minor] the fence's no-foreground promise has no continuous observation -> filed D00 T02 §10
- [D00-T02-S7-PR12] [minor] the fence could certify itself through skips -> filed D00 T02 §10
- [D00-T02-S7-PR13] [minor] resumed input during a modal or drag has no cancellation contract -> filed D00 T02 §10
End of ledger

13 findings: 11 filed (PR5 as two claims in D00 T02 §7; the other ten as two items in D00 T02 §10, `3b55ab53` and `3aeeadac`), 2 rejected.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=a2e60c4a821d95b650f9d5f2e30e9be627936ffa5f774a6a4d064dd654d71f50 end=PLAN-59e1e7c8df6b794d nonce=9559ca1800da71fc
- §7 proves its cases with hidden windows, but §10 classifies driven completion as inherently headful; §10 should classify each case by actual visibility requirements so background-safe coverage runs during every default gate.
- §7 introduces its host for a cluster including popups, but its checklist covers only list clicking, scrolling, and column dragging; §7 needs popup selection, dismissal, and teardown coverage or an explicit successor owner so the stated cluster is fully accounted for.
- §5 leaves toolbar overflow and filtering uncovered, and §7 provides no explicit disposition for them; §7 should assign observable driven tests or link their owning sections because inaccessible private state does not remove the user-facing behavior from the test obligation.
- §7 changes cancellation across five control classes but demonstrates destruction only for Sidebar; §7 should exercise pending-animation teardown for ContentView, ListView, StatusBar, and Toolbar, including child-window destruction, because each control has distinct callback and lifetime paths.
- §7 adds an owner-based animation API without specifying whether ownerless control callbacks remain permitted; the shared animation contract owned by §7 needs an enforceable rule and call-site audit so future controls cannot silently reintroduce dangling callbacks.
- §7 proves isolated transition endpoints but leaves rapid reversal and replacement unspecified; §7 should test repeated theme toggles, collapse reversals, and replacement errors while animations run because fast interaction can expose stale completions that isolated runs miss.
- §7's bounded PumpUntil cannot recover when a dispatched callback or modal loop never returns; §7 needs a process-level watchdog and a deliberate blocked-dispatch drill so every stuck-transition failure produces a bounded, attributable failure.
- §7's checkpoint omits its substantial animation-manager repair, and its quoted five-case assertion total predates later regression legs; §7 should require final-candidate evidence for teardown, cancellation ordering, nested ticks, and completion-created animations so the checkpoint covers the actual shipped scope.
- §5 explicitly guards process-global state and proves randomized execution, while §7 does not state equivalent restoration obligations for its real timer and theme transitions; §7 should require failure-safe timer, window, palette, and animation cleanup with randomized combined-suite runs to prevent order-dependent failures.
- §7 verifies list coordinates and sidebar widths only at 96 DPI; §7 should own parameterized interaction and settling checks at supported scales, with §10 owning placement, because correct rendering alone does not prove accurate hit targets or resize boundaries.
- §10 alternately promises no foreground changes, zero foreground holds, and zero visible windows without defining continuous observation; §10 should capture transient activation and visibility events and include a deliberate brief activation failure so a polling census cannot certify an interrupting run.
- §10 requires both a visible headful pass and permission to stamp with all headful work deferred; §10 should distinguish initial fence validation from later sections' deferred evidence so an unproven exclusion or placement gate cannot certify itself through skips.
- §10 promises instant abort on user input without defining interruption during a modal interaction or synthetic drag; §10 should own cancellation and cleanup requirements, with §11 supplying the interruption signal, so resumed operator activity cannot inherit capture, held input, or surviving test windows.
```
