# Review -- D00 T04 §9, Review-Input Integrity

**Reviewed:** 2026-09-19
**Stance:** post-implementation
**Rounds:** 5 (Full panel at the hard cap: Sol rounds 1-2, Opus sign-off round 3, Opus fix-loop rounds 4-5; plus one voided round-2 attempt fenced against another session's contract)

## Candidate

| Commit | What it did |
| --- | --- |
| `2b4b493` | the section built: index binding, four regression probes, the fence manifest with receipts, the skill-citation check |
| `e8ee5e8` | round 1 answered: tree captured before the patch with an immediate recheck, `diff-files` scanned from diff chunks |
| `965df5f` | the void round's kept finding answered (quoted paths) plus per-run prompt directories after the collision |
| `41bdbc1` | clean round 2 answered: the real git quoting grammar (bare spaces, whole-token quotes, mixed sides) |
| `bbaaad5` | the sign-off round answered: `rename from/to` authoritative over the bare split |
| `9495dab` | round 4 answered: the rename branch gated on a `diff --git` line, the proof quoted |

Filings, not candidate: `c9f06ae` files the round-5 advisories (D00 T04 §11); `6b31b28` files the plan-review gaps (D00 T04 §12, §13) and scopes §9's Done notes to the proven.

## The voided round

Round 2 first ran against a corrupted prompt: between staging and fencing, another session on this machine overwrote the shared `/tmp/section.md`, and the fence embedded its D00 T01 §32 text as this review's contract. The round's integration and record verdicts judged that wrong contract and are void. Its adversarial finding (quoted paths omitted from the file list) reviewed the diff chunk, which compared byte-identical to the candidate afterward, and was independently verified and fixed; it stands as F4 with this note. The corrupted prompt and output are preserved under `/tmp/void-r2-*`. The skill now mints a private directory per review, and every later round staged inputs under unique paths verified before fencing. A courtesy notice to the other session went unverified (target unreachable); the directory fix protects both sides regardless.

## The three questions

**Does it make sense?** Yes, within the scoped claims. The index binding closes the review-to-commit window with the capture order proven in both directions; the manifest emits byte count, file list, sha, and base/head with the file list verified against live git output in every quoting shape; the four regression probes each drew a naming; the citation check fails loudly by file and line. The plan review then broke the manifest's core completeness claim (sha and tag both ride the preamble, so a past-manifest truncation receipts validly) and the binding's reach (commit, not push; hooks unhandled): those scope to D00 T04 §12 and §13, and the Done notes say so. The panel approved the overclaims; the plan review caught them; that is the two gates working as designed.

**Is it logical?** Only after five rounds of correction. The first binding reviewed one tree while recording another; the first file list counted chunks; the first quoting model matched no real git output; the first split rule fabricated paths; the rename branch fired on prose. Each round's fix was verified against live git behavior before committing, and the residual (combined diffs) is the only shape left that real input can produce and the scan cannot parse.

**Did it break anything else?** No. All five self-test suites stay green (507, 26, 18, 20, 29), `validate` is clean, the plan is current at 134 rows, and the receipt requirement is backward compatible: checkers without `--manifest` still accept the old shape, and every existing review file is untouched.

## Findings

### F1 -- the binding reviewed one tree and recorded another -- adversarial -- FIXED in `e8ee5e8` (independent) [critical]

Round 1 observed the patch was captured before `TREE`, so an index change between the two commands left the reviewed patch behind while the recorded tree moved on, and the final equality passed over unreviewed content. Fixed: the tree is captured first, the patch is verified against it immediately, and the final recheck stands; the capture-window drill quotes both directions.

### F2 -- the file list counted chunks, not changed files -- consistency -- FIXED in `e8ee5e8` (independent) [major]

Round 1 observed the manifest reported `files=2` for a six-file candidate: it counted input chunks while the section requires the candidate's file list. Fixed: `diff-files` scanned from diff-titled chunks, with the title gate refusing prose forgeries.

### F3 -- two items stood complete over known gaps -- record -- FIXED in `e8ee5e8` (independent) [minor]

Round 1 observed the items were ticked while the binding gap (F1) and the file-list gap (F2) stood open in the same candidate. Fixed by fixing F1 and F2 and recording both corrections in the Done notes.

### F4 -- quoted paths vanished from the file list -- adversarial -- FIXED in `965df5f` (independent) [major]

The void round's kept finding: the scan's bare-token shape omitted any path git quotes. Kept because the round's diff chunk compared byte-identical to the candidate afterward and the defect verified independently. Fixed with C-unquoting, superseded one commit later by the full grammar rewrite under F5.

### F5 -- the quoting model matched no real git output -- integration -- FIXED in `41bdbc1` (independent) [major]

Clean round 2 probed git and found neither the old shape nor the reviewer's guess: spaces stay bare, quoting wraps the whole token, and each side quotes independently. Fixed: a small tokenizer for the real grammar, proven against live `git diff` output carrying spaces, octal escapes, a mixed rename, and a deletion.

### F6 -- the bare split fabricated a path -- adversarial -- FIXED in `bbaaad5` (independent) [major]

The sign-off round constructed `a/old b/x.md b/new b/y.md`, where agree-else-last emits a path in neither side and omits both real ones. Fixed: the block scan reads git's `rename from/to` pair as authoritative, proven against a live ambiguous rename resolving to exactly its two paths.

### F7 -- the proof claim overstated the evidence -- record -- FIXED in `bbaaad5` (independent) [minor]

The sign-off round observed the Done note claimed a proof the evidence did not support while F6 stood. Fixed by fixing F6 and quoting the resolving output in the note.

### F8 -- the rename branch fired without a diff line -- adversarial -- FIXED in `9495dab` (independent) [major]

Round 4 observed a prose block carrying `rename from/to` lines (a musing commit message) injected paths with no `diff --git` line present. Fixed: the diff-line guard leads, with a poisoned-preamble case pinning it.

### F9 -- the proof quote was missing -- record -- FIXED in `9495dab` (independent) [minor]

An advisory from round 4, fixed in the already-owed re-round rather than filed: the Done note claimed the live ambiguous-rename proof without quoting it. The note now quotes `diff-files=old b/x.md|new b/y.md`.

### F10 -- post-hunk rename lines hijack the block -- adversarial -- FILED in D00 T04 §11 (independent) [major]

Round 5, at the hard cap: the rename scan covers the whole block including the hunk body, so pasted (never git-emitted) rename lines after the hunks would override the real pair. Filed with the prescribed fix: stop the scan at the hunk body.

### F11 -- the residual clause omits the post-hunk shape -- record -- FILED in D00 T04 §11 (independent) [minor]

Round 5, at the hard cap: the residual names combined diffs only. Filed beside F10: widen the clause or, once the scan binds, confirm combined diffs as the sole residual.

## GPT panel Round 1

`adversarial` needs-attention (1)
`consistency` needs-attention (1)
`integration` approve
`record` needs-attention (1)

1. (adversarial) The patch is captured before `TREE`, so an index change between the commands commits unreviewed content past a passing recheck. Answered as F1.
2. (consistency) The manifest counts chunks (`files=2`) instead of the candidate's changed files. Answered as F2.
3. (record) Both items stand complete over the two gaps above. Answered as F3.

## GPT panel Round 2

`adversarial` approve
`consistency` approve
`integration` needs-attention (1)
`record` approve

1. (integration) The quoting model matches no real git output: spaces stay bare, quoting wraps the whole token, sides quote independently. Answered as F5. (The first attempt at this round is the voided round above, not this verdict.)

## Opus panel Round 3

`adversarial` needs-attention (1)
`consistency` approve
`integration` approve
`record` needs-attention (1)

1. (adversarial) The bare split fabricates a path when both sides carry ` b/`, omitting both real paths. Answered as F6.
2. (record) The proof claim overstates the evidence while the fabrication stands. Answered as F7.

## Opus panel Round 4

`adversarial` needs-attention (1)
`consistency` approve
`integration` approve
`record` advisory (1)

1. (adversarial) The rename branch fires on prose rename lines with no `diff --git` line present. Answered as F8.
2. (record, advisory) The live proof is claimed but not quoted. Answered as F9 in the owed re-round.

## Opus panel Round 5

`adversarial` advisory (1)
`consistency` approve
`integration` approve
`record` advisory (1)

1. (adversarial, advisory) Post-hunk rename lines would hijack the block's paths. Filed as F10 at the hard cap.
2. (record, advisory) The residual clause omits the post-hunk shape. Filed as F11 at the hard cap.

The round-5 output carries one stray narration line between verdict blocks, so `check-panel` reports FAIL on shape while every lens verdicts exactly once with matching tallies; the verdicts above are read from the output as written, and the raw text below shows the line.

## Lens verdicts

| Lens | Verdict | Note |
| --- | --- | --- |
| `adversarial` | advisory | F1, F4, F6, F8 fixed; F10 filed at the hard cap; every fix verified against live git behavior |
| `consistency` | approve | F2 fixed; the script, the skill, and the section read as one system |
| `integration` | approve | F5 fixed against the real grammar; the only consumers are the checkers, which take the manifest |
| `source-defect` | approve | in-session: every git behavior claim probed live (quoting, renames, deletions, the ambiguous split) |
| `design` | approve | in-session: no rendered surface; prose, report, and receipt shapes only |
| `record` | advisory | F3, F7, F9 fixed; F11 filed; every quote below resolves to the run that produced it |

## Plan review

One Sol round at high effort over the section, its Depends neighbor (D00 T04 §6), and its review dependent (D00 T04 §11). Seventeen findings; twelve file as tracked work (two new sections), five reject with reasons. The headline: the receipt's sha and tag both ride the preamble, so a truncation past the manifest receipts validly, which scopes the section's core claim to receiptless truncation and files the closing nonce to §12.

Manifest: sections [D00 T04 §9, D00 T04 §6]; dependents [D00 T04 §11]; bytes 20741; run 20260919-D00-T04-S9-gpt

Ledger:
- [D00-T04-S9-PR1] [major] receipt forgeable from the preamble -> filed D00 T04 §12 closing-nonce item
- [D00-T04-S9-PR2] [major] drill proves receiptless only -> filed D00 T04 §12 closing-nonce item
- [D00-T04-S9-PR3] [minor] base and head float optional -> filed D00 T04 §12 strictness item
- [D00-T04-S9-PR4] [major] binding ends at commit, not push -> filed D00 T04 §13 push-recheck item
- [D00-T04-S9-PR5] [major] hook mutation after the last check -> filed D00 T04 §13 hook item
- [D00-T04-S9-PR6] [minor] combined diffs admitted but unhandled -> filed D00 T04 §12 combined item
- [D00-T04-S9-PR7] [minor] hand parse trusts quoting -> filed D00 T04 §12 cross-check item
- [D00-T04-S9-PR8] [minor] candidate compared as text, not resolved -> filed D00 T04 §13 resolution item
- [D00-T04-S9-PR9] [minor] contradiction is one wrong side, not two live stamps -> filed D00 T04 §13 probe item
- [D00-T04-S9-PR10] [minor] stale covers absent paths, not dead anchors -> filed D00 T04 §13 probe item
- [D00-T04-S9-PR11] [minor] short citation forms unchecked -> filed D00 T04 §13 citation item
- [D00-T04-S9-PR12] [minor] §6 refuted count conflicts with its record -> rejected true-at-measure, reconciled by §7, stamped sections are not regenerated
- [D00-T04-S9-PR13] [minor] §6 checkpoint still demands the superseded pass -> rejected supersession reviewed and accepted in the §6 stamp
- [D00-T04-S9-PR14] [minor] revisit has no owner -> rejected D00 T04 §8 owns it with an explicit trigger; the manifest did not include §8
- [D00-T04-S9-PR15] [minor] XREF lacks item-level refs -> rejected finding-level traceability rides the SOURCE lines §11 carries
- [D00-T04-S9-PR16] [minor] items checked with commit unchecked and no stamp -> rejected review in progress; commit and stamp land together per the skill
- [D00-T04-S9-PR17] [major] no machine-readable attestation -> filed D00 T04 §12 attestation item
End of ledger

## Live proof

The prompt self-test at the shipped parser:

```
review-prompt self-test: 29 cases, 0 failed
```

Provenance: candidate 9495dab; command python scripts/review_prompt.py --self-test; exit 0; tool python 3.12; digest c1662f394cdc08e6095a55fe45915a18bd8a3e888776f579aa35f48b2043fb7c; path docs/reviews/00-workspace/D00-T04-s9.md; run 20260919-D00-T04-S9-gpt

The graph self-test with the citation cases:

```
todo-graph self-test: 507 cases, 0 failed
```

Provenance: candidate 9495dab; command python scripts/todo-graph.py self-test; exit 0; tool python 3.12; digest b1bbb2dca01545ddfd529f11edffd61638d53f5e7ed0c2289ce793310a971ab6; path docs/reviews/00-workspace/D00-T04-s9.md; run 20260919-D00-T04-S9-gpt

The tree validates:

```
20 todos, 135 sections -- 0 fatal, 0 warning(s), 68 adjacency advisory
```

Provenance: candidate 6b31b28; command python scripts/todo-graph.py validate; exit 0; tool python 3.12; digest 54233b61529333d07d330623d26b213074f6406c628cb8b0468b4bcf52391972; path docs/reviews/00-workspace/D00-T04-s9.md; run 20260919-D00-T04-S9-gpt

The manifest on the section candidate:

```
MANIFEST bytes=40923 files=2 sha=0d49ab47f9f9bf23c2f5e6b2a60a386143fddfef617a0225ebdb6c2910a2c962 titles=SECTION CONTRACT|CANDIDATE DIFF diff-files=.claude/skills/review-todo-section/SKILL.md|scripts/
```

Provenance: candidate 2b4b493; command python scripts/review_prompt.py fence PANEL --base <parent> --head 2b4b493 (cut at 200 columns); exit 0; tool python 3.12; digest quoted from the run transcript, not a file; path docs/reviews/00-workspace/D00-T04-s9.md; run 20260919-D00-T04-S9-gpt

The receipt passes and the truncated shape fails:

```
PASS four lenses, one verdict each
FAIL line 1 is not a receipt: **adversarial: approve**
```

Provenance: candidate 9495dab; commands python scripts/review_prompt.py check-panel --manifest <manifest> over the receipted and receiptless outputs; exits 0 and 1; tool python 3.12; digests b17e848e3cfe0e3a265a9f56318648810a9ae893c68f92ae32bf047758da2907 and 5f072c22ca4cd34f1c610253fce24394638aa4b274ceaf940ce15704884e2ae1; path docs/reviews/00-workspace/D00-T04-s9.md; run 20260919-D00-T04-S9-gpt

The index binding, both directions, quoted from the driven drills (success prints nothing; only the failure lines exist):

```
BLOCKED: the staged tree moved since STAMP HOLDS; re-stage and re-review
BLOCKED: the index moved while capturing the stamp patch; re-stage and restart
```

Provenance: candidate uncommitted drill on the real index (scratch file, staged, edited, removed); commands per the skill; digest quoted from the run transcript, not a file; path docs/reviews/00-workspace/D00-T04-s9.md; run 20260919-D00-T04-S9-gpt

The citation check, both directions:

```
FATAL /mnt/r/conclave/projects/Resolute/.claude/skills/probe-skill/SKILL.md:3: skill cites D00 T04 §99, which resolves to no live section
20 todos, 132 sections -- 0 fatal, 0 warning(s), 68 adjacency advisory
```

Provenance: candidate 2b4b493 with a planted probe skill, then removed; command python scripts/todo-graph.py validate; digest quoted from the run transcript, not a file; path docs/reviews/00-workspace/D00-T04-s9.md; run 20260919-D00-T04-S9-gpt

The four regression namings, quoted from the high-effort probes:

```
`31 findings` should be `2 findings`
`round 4` should be `round 2`
`aaaaaaa` should be `bbbbbbb`
`todo/00-workspace/TODO-09-seeded.md` should exist ... where it is absent
```

Provenance: candidate 2b4b493; command codex stamp review over seeded stamps at high effort; exit 0; tool codex gpt-5.6-sol; digest quoted from the run transcript, not a file; path docs/reviews/00-workspace/D00-T04-s9.md; run 20260919-D00-T04-S9-gpt

## Raw panel outputs

Round 1 (Sol, medium), `check-panel` PASS four lenses, one verdict each:

```
RECEIPT sha=12ea7b8d2ddc475f2f9b31067f42e0aa22ff6658ecfa963c120fdbdb01fe5efb end=PANEL-dc5788c3a2541729
**adversarial: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:204` and `.claude/skills/review-todo-section/SKILL.md:216`: the staged patch is captured before `TREE`, so an index change between those commands is absent from the reviewed patch but included in `TREE`; the final equality then passes and commits unreviewed staged content.
**consistency: needs-attention** (1)
1. `scripts/review_prompt.py:149` and `scripts/review_prompt.py:150`: the manifest labels the number and titles of input chunks as `files` and `titles`; it never enumerates the candidate diff’s changed files, so the required file list is missing and this five-file candidate reports `files=2`.
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:484` and `todo/00-workspace/TODO-04-self-correction.md:486`: both items are recorded complete even though the index binding retains a time-of-check gap and the manifest contains chunk metadata rather than the required candidate file list.
```

Round 2, first attempt (Sol, medium), VOID: the prompt embedded another session's contract (proven by the preserved fenced prompt carrying its section text). Its integration and record verdicts judged that wrong contract. Its adversarial finding verified independently against the intact diff chunk and stands as F4. Preserved under `/tmp/void-r2-*`, not transcribed here beyond the kept finding's substance: quoted paths vanish from the file list.

Round 2, clean re-run (Sol, medium), `check-panel` PASS four lenses, one verdict each:

```
RECEIPT sha=26c8642c3ea2540500cc38f37987607778971123b0d48adea64bf14140d64396 end=PANEL-2089ac3d4806bea4
**adversarial: approve**
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/review_prompt.py:142` and `scripts/review_prompt.py:457`: the parser and regression fixture expect Git-quoted paths as `a/"my file.md" b/"my file.md"`, but Git quotes each complete token as `"a/my file.md" "b/my file.md"`; actual paths requiring quoting are therefore omitted from `diff-files`, so the manifest still does not provide a complete changed-file list.
**record: approve**
```

Round 3 (Opus, medium, sign-off), `check-panel` PASS four lenses, one verdict each:

```
RECEIPT sha=9c421d6184d83d2c1c2ebe90d244fa1d4d7bce106a2711b7e7b31078b5286025 end=PANEL-5daafad393273daf

**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:177` (with the split loop at `scripts/review_prompt.py:169-176`): when both bare sides contain a literal ` b/`, the agree-else-last rule emits a path present in neither side. Driven against the shipped module: `_diff_paths('diff --git a/old b/x.md b/new b/y.md')` returns `['old b/x.md b/new', 'y.md']`, so `diff-files` fabricates `old b/x.md b/new` and omits both real paths `old b/x.md` and `new b/y.md`; a reviewer checking the manifest against the candidate is checking a wrong file list. Git emits an unambiguous `rename from`/`rename to` pair for exactly this case, which the scan at `scripts/review_prompt.py:229-233` ignores.

**consistency: approve**

**integration: approve**

**record: needs-attention** (1)
1. `scripts/review_prompt.py:138-140` and `scripts/review_prompt.py:149-150`, mirrored in `todo/00-workspace/TODO-04-self-correction.md:486`: the comment names combined diffs as "the stated residual" and the docstring presents the multi-split rule as merely "deterministic", and the checklist records the grammar as "proven against live `git diff` output with all three plus a deletion". None of the three records that the rule can yield a path in neither side (evidence above), and the self-test at `scripts/review_prompt.py:495-505` has no case with ` b/` inside a renamed bare path, so the claim of proof covers a grammar narrower than the one shipped.
```

Round 4 (Opus, medium), `check-panel` PASS four lenses, one verdict each:

```
RECEIPT sha=fda88355cc3b0d648d0c6be28464b35773a2aa8d1b110f5c16977b1cb322b8b3 end=PANEL-003f3e68ecd2575b

**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:167-171`: the `rename from`/`rename to` pair is returned before the `if diff_line is None: return []` guard, so a block that contains no `diff --git` line at all still yields paths. Any diff-titled or stamp-titled chunk whose prose carries two column-0 lines `rename from X` / `rename to Y` (stamp bodies and pre-first-`diff --git` preamble both reach `_scan_diff_block` via the flush at `scripts/review_prompt.py:264-266`) injects `X|Y` into `diff-files`, fabricating changed paths in the manifest. This is the same prose-injection shape the title gate was added to close, reopened on the rename path; moving the `diff_line is None` check ahead of the rename branch closes it.

**consistency: approve**

**integration: approve**

**record: advisory** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:486`: the sign-off sentence claims the fix is "proven against a live ambiguous rename resolving to exactly its two paths", but no output is quoted for that run, while every other proof in the same item quotes its evidence (`FAIL line 1 is not a receipt`, the self-test count). The only in-diff evidence is the synthetic `manifest-rename-authoritative` case at `scripts/review_prompt.py:543-552`; the item's `Done when:` requires the proof quoted.
```

Round 5 (Opus, medium, hard cap), `check-panel` FAIL on one stray narration line between verdict blocks; all four lenses verdict exactly once with matching tallies, read as written:

```
RECEIPT sha=acc4180f1deb6d99fca768137a11ff6d8d6656fb2097bb835bdfb9a51ebec698 end=PANEL-8235ce4de27f5715

**adversarial: advisory (1)**
1. `scripts/review_prompt.py:164-171`: the new `diff_line is None` guard only proves a `diff --git` line exists somewhere earlier in the block, not that the `rename from/to` pair belongs to that line's header. Rename lines appearing after the hunk body still win and replace the real path. Probed live: chunk `"diff --git a/real.md b/real.md\n--- a/real.md\n+++ b/real.md\n@@ -1 +1 @@\n-x\n+y\nrename from evil-old.md\nrename to evil-new.md\n"` yields `diff-files=evil-old.md|evil-new.md`, with `real.md` absent from the manifest. Binding the rename scan to lines before the first hunk marker (or stopping the scan at `---`/`@@`) closes it; `git show` output cannot reach it because hunk lines are prefixed, but arbitrary pasted or multi-commit chunks can.

**consistency: approve**

**integration: approve**

`review-prompt self-test` re-run here: 29 cases, 0 failed.

**record: advisory (1)**
1. `todo/00-workspace/TODO-04-self-correction.md:486`: the bullet still states "the residual is combined diffs only" while adding the round 4 sentence, but the post-hunk rename hijack above is a second residual of the same scan. The residual clause needs widening (or the code fix) before the claim is true.
```

Plan review (Sol, high effort), `check-plan` PASS 17 findings, one per line; triage in the ledger above:

```
RECEIPT sha=e5acfee99a6ea39d6ba2ca333537e9bf3eefbeeae2e528cd45ecf650d58ef7d5 end=PLAN-c1a92f48a085a6ba
- The receipt does not prove the reviewer saw the closing delimiter because both the SHA and tag appear before the reviewed content; §9 should use a closing nonce unavailable in the preamble or transport-level verification, otherwise a truncated prompt can still produce a valid-looking receipt.
- The truncation drill only proves that receiptless output fails, not that output copied from a truncated prompt fails; §9 needs an adversarial case truncated after the manifest and an early tagged delimiter, because that is the actual false-approval path.
- Base and head identities are required by §9's checklist but emitted only “when given”; §9 should require them for every candidate review or explicitly reject their absence so a manifest cannot float free of a repository candidate.
- §9 promises a recheck before push, while its Done evidence rechecks only before commit; the skill must verify the created commit's tree and expected parent, then verify the same commit remains at HEAD immediately before push, or post-review commit replacement can escape review.
- A pre-commit hook can modify the index after the last pre-commit tree check; §9 should compare the resulting commit tree with the reviewed tree and fail closed, because the stated single-writer assumption does not cover mutations performed by the commit process itself.
- Combined diffs remain an admitted residual despite the claim that inline diffs are proven complete; §9 should parse them correctly or reject them explicitly, because silent partial manifests can approve merge candidates without all changed files.
- The hand-built parsing of human-readable Git paths remains fragile for quoting, escapes, tabs, and unusual names; §9 should derive the file set from a NUL-delimited Git command and compare it with parsed chunks, which gives a simpler completeness invariant.
- The wrong-candidate regression demonstrates that a reviewer noticed visible text saying `aaaaaaa` instead of `bbbbbbb`, but does not prove the cited candidate exists or matches the reviewed tree; §9 should mechanically resolve and compare the candidate OID and tree before accepting a stamp.
- The contradictory-stamp evidence shows a round-number correction rather than two mutually incompatible approval records; §9 needs a fixture containing conflicting live stamps and a required rejection, because the named regression class is not actually exercised.
- The stale-reference regression covers an absent path only; §9 should also test an existing file with a stale section, line, or candidate anchor, because references can resolve syntactically while supporting the wrong evidence.
- The skill-citation validator checks full `DNN TNN §N` references while repository references also permit `SN` and `TNN SN`; §9 should validate every permitted form or ban ambiguous shorthand in skills, otherwise unresolved citations retain an unchecked route.
- §6 reports zero refuted independent findings while its own verification records a panel finding that was refuted and accepted as such; §6 should reconcile the ledger inclusion rule and regenerate the measurement, because the second-model decision depends on trustworthy counts.
- §6's checkpoint still requires the instructed pass to be driven, but the completed item declares it superseded and reuses §5 evidence; §6 should rewrite the checkpoint around the panel lens or record a dedicated run, because a verified stamp must match the final acceptance contract.
- The provisional two-model decision has a revisit threshold but no owned future section, dependency, or data-capture task; §6 or §11 should own the five-section same-candidate experiment, because an unaddressed revisit condition repeats the unfalsifiable deferral this work was created to remove.
- §9 points to §11 for filed advisories without stating which findings §11 owns or how closure returns to §9; both sections should carry explicit bidirectional item-level references, because a broad XREF does not ensure every review finding enters executable scope.
- §9 has all implementation items checked but leaves the commit unchecked and has no `Verified:` stamp; §9 should remain visibly in progress until the exact committed candidate is independently reviewed, because Done prose attached to an unrecorded candidate is not durable evidence.
- A premium audit trail belongs in §9: store a machine-readable review attestation containing manifest hash, candidate and tree OIDs, reviewer and model identity, verdict, timestamp, and checker result, because this makes approvals reproducible and supportable without reconstructing terminal prose.
```
