# Review -- D00 T04 §32, Campaign Guard: Stop Hook, Heartbeat, Breaker

**Reviewed:** 2026-09-24
**Stance:** post-implementation
**Rounds:** independent pass (1 finding: fixed); GPT 1 bulk (2 fixed); GPT 2 bulk (2 fixed); GPT 3 sign-off (2 fixed, one blocking); GPT 4 depth (1 fixed, blocking); GPT 5 depth, the hard cap (clean).
**Tier:** Full panel. The diff touches `.claude/settings.json` and a new hook (the harness wiring every session loads), two skill files (the shared run contract), and `scripts/check-all.ps1` (the gate set).
**Attestation:** `docs/reviews/00-workspace/D00-T04-s32.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `023d7aae` | implementation: the Stop hook, its wiring, the driven self-test, and the run-guard rewrite |
| `909413f0` | answers the independent pass: a hook command every shell runs |
| `3be78995` | answers panel round 1: session id recorded, heartbeat adopted or re-pointed only for this run |
| `fc45f4ef` | answers panel round 2: every run end deletes guard, state, and job; no adoption without a guard |
| `c8eda099` | answers panel round 3: a phase closeout never ends a plan run; counts |
| `db30f722` | answers panel round 4: each heartbeat branch owns its own cleanup |

Record commits (filings only, no reviewed content): `813e08a1` (plan-review findings filed in D00 T04 §34 and D00 T04 §33).

Range `023d7aae^..db30f722`, contiguous, no declaration owed. Each round fenced the range from `023d7aae^` to its own head (7 files, cross-check `7 file(s) agree`); the fenced contract was checked to open with this section's heading. The rundir is session scratch outside the tree (/tmp/review-dW7gsBax, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation. The first self-test run found a real defect in the ported hook: Windows PowerShell 5.1 decodes native output and writes stdout in the console codepage, so the section mark in a plan row became invalid bytes and the block payload stopped being JSON. ScratchPad's copy carries the same latent defect. The hook now forces UTF-8 and stays ASCII. A mutation raising the breaker limit to 99 fails the trip leg. The architecture gate is not triggered (review tooling and harness wiring, no framework state).

## Independent pass

`python scripts/panel_slots.py exec independent --commit 023d7aaefb1df4d3a549286196ee1b116df5fb1b`: `panel_slots: slot independent model gpt-6-sol effort high timeout 900s`. Exit 0, latency 200s, cost unresolved (the review runner printed no token figure). Output and stderr stay in the session scratch rundir outside the tree (ind32.out, ind32.err); the output is transcribed below.

### F1 -- the hook command does not launch under PowerShell -- correctness -- FIXED in `909413f0` (independent) [major]

`$CLAUDE_PROJECT_DIR` in the hook command is not expanded by PowerShell, Claude Code's hook shell on a Windows host without Git Bash, so the hook would never launch; the self-test missed it by calling the script directly. The command now reads the variable through .NET with no `$` in it, and the self-test runs the configured command through PowerShell and Git Bash; the old command fails the PowerShell leg.

```text
The configured Stop hook fails to launch on a supported Windows setup without Git Bash, leaving the campaign without its intended end-of-turn guard.

Review comment:

- [P1] Make the hook path work under PowerShell — R:\conclave\projects\Resolute\.claude\settings.json:8-8
  On Windows hosts without Git Bash, Claude Code uses PowerShell to run command hooks. PowerShell does not expand `$CLAUDE_PROJECT_DIR` as an environment variable, so this path becomes `\.claude\hooks\campaign-stop.ps1` and the Stop hook never launches. The self-test misses this because it invokes the script directly. Use a shell-independent path argument or PowerShell-compatible expansion; Windows is the project's target environment ([AGENTS.md:28](AGENTS.md#L28)).
```

## GPT panel Round 1

Round 1 over `023d7aae^..909413f0` with the independent pass appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-sol effort medium timeout 600s`. Exit 0, latency 53s, clock 2026-09-24T00:12:54Z, cost 25368tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-1.out, panel-1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` needs-attention
`integration` needs-attention
`record` approve

### F2 -- the run file does not record the session id -- consistency -- FIXED in `3be78995` (independent) [minor]

Round-1 consistency: the start order recorded the job id but not the session id the contract asks for. It records both now.

### F3 -- a heartbeat can be adopted or kept for the wrong run -- integration -- FIXED in `3be78995` (independent) [major]

Round-1 integration: an existing heartbeat could be adopted for another run file or session, and a phase change recreated the job without rewriting the guard. Adoption now requires this run file and session; a phase change deletes and recreates the job and rewrites the guard file and state.

```text
RECEIPT sha=36bdd121837220db2d819530ebcb8d05d5c7af589484ffe75c37472986e62bf0 end=PANEL-aa969cece6821032 nonce=4d1b0f7e3e29ccb1

**adversarial: approve**
**consistency: needs-attention (1)**
1. `.claude/skills/process-plan/SKILL.md:65` directs the runner to record the job id and guard write, but does not direct it to record the session id in the run file, as the section contract requires.
**integration: needs-attention (1)**
1. `.claude/skills/process-plan/SKILL.md:63,69,80-82,103` adopts an existing session-bound heartbeat in a new session and repoints the guard to a new phase without replacing the heartbeat prompt. The job can remain bound to the old session or see the previous phase’s closeout and delete itself while the new run is open.
**record: approve**
```

## GPT panel Round 2

Round 2 over `023d7aae^..3be78995` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-sol effort medium timeout 600s`. Exit 0, latency 85s, clock 2026-09-24T00:15:13Z, cost 38938tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-2.out, panel-2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` needs-attention
`integration` needs-attention
`record` approve

### F4 -- run ends leave the guard or the job behind -- consistency -- FIXED in `fc45f4ef` (independent) [major]

Round-2 consistency: the heartbeat's run-over path left the guard file, and the plan closeout deleted the guard without the job. Every run end now deletes the guard file, the state file, and the job.

### F5 -- a heartbeat can be adopted with no guard -- integration -- FIXED in `fc45f4ef` (independent) [minor]

Round-2 integration: adoption was allowed when the guard file was missing. It now requires an existing guard naming this session.

```text
RECEIPT sha=f24e625f8028a4d07d7c184a0bc96c6a3dffdebbc159498607c4ecf2764c6487 end=PANEL-5b6d874e235198d0 nonce=703d06b7e2b10fe0
**adversarial: approve**
**consistency: needs-attention (1)**
1. `.claude/skills/process-plan/SKILL.md:81,96,106` requires both the guard file and heartbeat job to be deleted when a run ends, but the heartbeat closeout path leaves the guard file, and the plan closeout path deletes the guard without deleting the job.
**integration: needs-attention (1)**
1. `.claude/skills/process-plan/SKILL.md:62` permits adopting a session-bound heartbeat when the guard file is missing. A job from an earlier session can therefore be adopted for the same run file instead of being replaced for the current session.
**record: approve**
```

## GPT panel Round 3

Round 3 over `023d7aae^..fc45f4ef`, the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-sol effort high timeout 600s`. Exit 0, latency 127s, clock 2026-09-24T00:18:15Z, cost 30045tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-3.out, panel-3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F6 -- a phase closeout ends the whole plan run -- correctness -- FIXED in `c8eda099` (independent) [major]

Round-3 integration: the heartbeat deleted the guard and the job whenever the phase run file closed or parked, which would end the campaign between phases with ready work left. Treated as blocking (it defeats the guard's purpose); the heartbeat now ends the run only when `query ready` prints `0 runnable now`.

### F7 -- the self-test counts in the record are stale -- record -- FIXED in `c8eda099` (independent) [minor]

Round-3 record: the Done notes quoted 12 cases after the configured-command legs made it 15 with Git Bash and 14 without.

```text
RECEIPT sha=0a7dc98d9cba0df51c45a1db5f60ef45832915d2c890701e199e3e3d960c4e3b end=PANEL-0e561b689af4388c nonce=b75d80f81789d9a8
**adversarial: approve**
**consistency: approve**
**integration: needs-attention (1)**
1. `.claude/skills/process-plan/SKILL.md:84` tells the heartbeat to delete the guard and job whenever a phase run file has `## Closeout` or `PARKED`. If the session becomes idle before the next phase is assigned to the guard, the heartbeat ends the campaign despite remaining ready work.
**record: needs-attention (1)**
1. `todo/00-workspace/TODO-04-self-correction.md:1319-1320` records a 12-case self-test, but `scripts/campaign_guard.py` now executes 14 cases without Git Bash or 15 with it. The stated test counts are stale.
```

## GPT panel Round 4

Round 4 over `023d7aae^..c8eda099` with prior verdicts appended. Slot `depth`: `panel_slots: slot depth model gpt-6-sol effort high timeout 600s`. Exit 0, latency 59s, clock 2026-09-24T00:20:24Z, cost 26270tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-4.out, panel-4.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` needs-attention
`record` approve

### F8 -- the run-over cleanup still follows the resume branch -- correctness -- FIXED in `db30f722` (independent) [major]

Round-4 integration: the round-3 answer left the cleanup sentence after the resume branch, so a resumed campaign would delete its own guard. The prompt now has four steps, and the cleanup lives only in the missing-guard step and the `0 runnable now` branch.

```text
RECEIPT sha=fbdb18ec6984c3f76114a1e346d58ea51f71a044ddaa421077c9002a05f7fba1 end=PANEL-5b7a024004b8685f nonce=58721cbad3e29e43
**adversarial: approve**
**consistency: approve**
**integration: needs-attention (1)**
1. `.claude/skills/process-plan/SKILL.md:84` tells the heartbeat to resume the next phase when ready rows remain, then unconditionally delete the job and guard and report `RUN FINISHED`. That cleanup undoes the resumed campaign. Limit it to the `0 runnable now` branch.
**record: approve**
```

## GPT panel Round 5

Round 5 over `023d7aae^..db30f722`, the hard cap, with prior verdicts appended. Slot `depth`: `panel_slots: slot depth model gpt-6-sol effort high timeout 600s`. Exit 0, latency 34s, clock 2026-09-24T00:21:57Z, cost 25927tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-5.out, panel-5.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` approve
`record` approve

```text
RECEIPT sha=f973a10f81a6e140830281213bfe6a2ac41769d6a8984c66083624143f552db6 end=PANEL-fb016c978d159065 nonce=f789f13b5d8099bc
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §32, D00 T04 §31]; dependents [none]; bytes 13895; run 20260924-D00-T04-S32-sol

`bytes 13895` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it (re-measured 2026-09-25: `tr -d "" < plan-review-prompt.md | wc -c` prints `13895`); the fence's own `MANIFEST bytes=12772` counts the fenced body alone.

Round over the section plus its sibling D00 T04 §31 (the repair loop the guard must not block); no review dependents. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-sol effort high timeout 900s`. Exit 0, latency 53s, clock 2026-09-24T00:23:06Z, cost 10660tokens. Check: `PASS 9 findings, one per line`.

Ledger:
- [D00-T04-S32-PR1] [major] an escalation report collides with the Stop hook -> filed D00 T04 §34
- [D00-T04-S32-PR2] [minor] run-end cleanup is stated but not proven -> filed D00 T04 §34
- [D00-T04-S32-PR3] [minor] 0 runnable now is treated as the end -> rejected `0 runnable now` means every remaining row is blocked, which process-plan defines as the one genuine halt; the runner still owes its PARKED record
- [D00-T04-S32-PR4] [minor] the fingerprint misses untracked file contents -> filed D00 T04 §34
- [D00-T04-S32-PR5] [minor] bookkeeping in the run file counts as progress -> filed D00 T04 §34
- [D00-T04-S32-PR6] [major] guard ownership is not exclusive across sessions -> filed D00 T04 §34
- [D00-T04-S32-PR7] [major] the heartbeat lifecycle is unexercised -> filed D00 T04 §34
- [D00-T04-S32-PR8] [minor] the repair bound resets per red -> filed D00 T04 §33
- [D00-T04-S32-PR9] [major] a failing hook goes silent -> filed D00 T04 §34
End of ledger

9 findings: 8 filed (D00 T04 §34 PR1, PR2, PR4, PR5, PR6, PR7, PR9; D00 T04 §33 PR8), 1 rejected with its reason. Filings landed in `813e08a1`.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=c3b1420ee4d5ee7ac8d77fff1f672d24edf5a751955c823b21b43698111dfc2a end=PLAN-e967397f4a8866b9 nonce=4eb24df506e1ff37
- CI escalation has no required guard transition; §31 should require a column-0 `PARKED` record or guard removal before reporting, or §32 may block the turn that reports an exhausted repair bound.
- Normal completion does not explicitly remove the guard, state file, and heartbeat job; §32 should require and test that cleanup so an expired run cannot resume or block a later session.
- `0 runnable now` is treated as permission to end even when an open run has blocked or unfinished work; §32 should distinguish completion from a temporarily empty ready queue so the campaign does not stop early.
- The stall fingerprint tracks the untracked *set* but not untracked file contents; §32 should hash relevant untracked content so edits to a new file count as progress.
- Run-file updates count as progress even when they only record another heartbeat or retry; §32 should exclude bookkeeping from the stall fingerprint so repeated ineffective turns reach the breaker.
- Guard creation and heartbeat adoption lack an exclusive ownership check; §32 should define atomic acquisition and stale-job recovery so two sessions cannot overwrite one guard or schedule duplicate reminders.
- Hook fixtures do not prove that `CronCreate` fires, resumes the named session, survives expiry replacement, and is deleted on stop; §32 needs a driven lifecycle check because its main recovery path is otherwise unverified.
- The three-repair limit is stated “per red,” but each repair push can produce a new red run; §31 should count attempts across the same unresolved failure in the run file so repeated pushes cannot reset the bound.
- A thrown hook error fails open without a required diagnostic or heartbeat recovery check; §32 should record the failure and make the next heartbeat surface it so a broken guard cannot silently abandon a run.
```

## Live proof

The full gate set ran at `67550bde` (the implementation `023d7aae`, every review answer through `db30f722`, the plan-review filings, the 2026-09-25 re-pin `0c8ffa5a`, and the Phase 0 re-scope). It first failed 4 of 19 gates at CMake configure on ignored caches written under the checkout's old path (`r:/conclave/projects/Resolute`); the stale caches were cleared and the set re-ran green, recorded in the run file's Critical events (`4a957e92`). The section and record gates re-ran at `4a957e92`, which adds only that run-file line, with this stamp's own records staged, so their totals count this section. The candidates name commits because a staged stamp tree cannot name itself.

- `pwsh -NoProfile -File scripts/check-all.ps1`: `check-all: 19 gate(s) ok`

Provenance: candidate 67550bdeecfea549f2e6392586e1179314ace45f; command pwsh -NoProfile -File scripts/check-all.ps1; exit 0; tool PowerShell 7.6.6; digest eb8ab8ab4414799260f491ba3e465a09309afee4e6370e6602b9fad4c4c24f72; path docs/reviews/00-workspace/D00-T04-s32.md; run 20260924-D00-T04-S32-sol

- `python scripts/campaign_guard.py --self-test`: `campaign_guard self-test: 15 cases, 0 failed`

Provenance: candidate 4a957e92d71f16c895ee2e851c4b3cc7e1387e06; command python scripts/campaign_guard.py --self-test; exit 0; tool CPython 3.14.4; digest c8f84f184d546fd80f0c06c923cd9321c9eadd1edcda6240980d65df4d06f6a4; path docs/reviews/00-workspace/D00-T04-s32.md; run 20260924-D00-T04-S32-sol

- `python scripts/panel_slots.py --self-test`: `panel_slots self-test: 37 cases, 0 failed`

Provenance: candidate 4a957e92d71f16c895ee2e851c4b3cc7e1387e06; command python scripts/panel_slots.py --self-test; exit 0; tool CPython 3.14.4; digest 49036cade8aac3da3f68d772627290fdb2505c69e41dc99d5a50b39fae454b26; path docs/reviews/00-workspace/D00-T04-s32.md; run 20260924-D00-T04-S32-sol

- `python scripts/todo-runs.py --check`: `44 runs, 123 rounds: all resolve, all covered, counts agree`

Provenance: candidate 4a957e92d71f16c895ee2e851c4b3cc7e1387e06; command python scripts/todo-runs.py --check; exit 0; tool CPython 3.14.4; digest 4373faac69c35cc6e3064cdcbe460cbc7c51c74b0a5da96d3db94a9b0d56bdde; path docs/reviews/00-workspace/D00-T04-s32.md; run 20260924-D00-T04-S32-sol

- `python scripts/todo-graph.py validate`: `30 todos, 228 sections -- 0 fatal, 0 warning(s), 98 adjacency advisory`

Provenance: candidate 4a957e92d71f16c895ee2e851c4b3cc7e1387e06; command python scripts/todo-graph.py validate; exit 0; tool CPython 3.14.4; digest a0791433deb72dea58899295db14d13fdc27408e9ad8e999c6317c42526914d8; path docs/reviews/00-workspace/D00-T04-s32.md; run 20260924-D00-T04-S32-sol

- `python scripts/todo-claims.py`: `todo-claims: 113 claim(s) -- 113 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`

Provenance: candidate 4a957e92d71f16c895ee2e851c4b3cc7e1387e06; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest 1afa7919ee2235a863c6e0b689ab57968edb0ea0b033b409f5689dbc45284364; path docs/reviews/00-workspace/D00-T04-s32.md; run 20260924-D00-T04-S32-sol

- `python scripts/todo-graph.py plan --check`: `implementation plan is current -- 44/225 sections complete (20%)`

Provenance: candidate 4a957e92d71f16c895ee2e851c4b3cc7e1387e06; command python scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.4; digest 7a9444d140466bd632e680b3beedba83248a8bff129f20d3d134751fd8a6582a; path docs/reviews/00-workspace/D00-T04-s32.md; run 20260924-D00-T04-S32-sol
