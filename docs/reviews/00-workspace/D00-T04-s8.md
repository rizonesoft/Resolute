# Review -- D00 T04 §8, Revisit the Two-Model Decision

**Reviewed:** 2026-09-19
**Stance:** post-implementation
**Rounds:** 4 attempts, 3 usable (Full panel: Sol rounds 1-2 usable, Opus sign-off round 3 usable; one voided round-2 attempt)
**Tier:** Full panel. The diff touches `scripts/` and `.claude/skills/`, shared contract surface every review consumes.

## Candidate

| Commit | What it did |
| --- | --- |
| `d72afe0` | the section built: blinded comparison, cut threshold, diversity verdict, export-based keep decision, severity rollout across 25 files |
| `2dde74b` | first-review round 1 answered: severity error wording, D07-T01-S1-F3 re-rated minor, ledger regen |
| `a27e296` | first-review round 2 answered: zero leg counts full-scope sections only |
| `296a838` | re-review round 1 answered: impact severity rule, F3 re-rated major, worked KEEP rewritten, F1 filed to §16 |
| `de3f66a` | re-review round 2 answered: D00-T04-S8 run block, candidate table completed, counts rescoped to rollout |

Resumed 2026-09-19 after session death: the first review's two rounds left fix commits but no findings file, so the rounds below re-run over the fixed candidate. Recorded adaptation: the candidate diff is the §8 commits' own patches concatenated (`git show` per commit), because the skill's `git show <candidate>` covers one commit, this candidate is four, and the trunk range between them carries unrelated work; round 1 fenced `d72afe0^..a27e296` (29 files, +424/-350) while still contiguous, and later rounds fence the 4-commit patch set (31 files).

## The three questions

**Does it make sense?** Yes. The blinded comparison ran on frozen `f118e30` with Jaccard 0.22 over 9 union finds; the cut rule names window, overlap, weights, and cost with worked keep and cut; the diversity verdict (model over prompt, 2 of 7 recovered) rests on four driven Sol passes; the keep decision cites the asserted export; all 160 findings carry severity with the gate enforcing it. No scope beyond the contract: the blinded and diversity filings went to §15, §16, and §17 with SOURCE keys.

**Is it logical?** Yes, after the round-1 fixes. The severity scale, the threshold rule, and the decision cohere: weights 9/3/1 price the ledger the gate enforces, and the keep margin (value 79 vs 31, Jaccard 0.22) does not depend on the cost leg that reads unresolved. The two record defects below (an exceptionless rating rule contradicting its own scale, a worked example citing excluded evidence) are fixed in the candidate.

**Did it break anything else?** No. `check-all` 13 gates ok on the candidate tree; the ledger parses 170 with 0 unreadable; the runs checker agrees 26 runs over 48 rounds; claims hold 112 of 112; the skill's heading contract updated in the same commit as the parser that enforces it, so no consumer reads the old shape.

**source-defect:** not owed (no Win32 contract, registry layout, or external behavior at stake). **design:** not a surface. **Research pass:** skipped (pure infrastructure).

## GPT panel Round 1

`adversarial` advisory (1)
`consistency` needs-attention (1)
`integration` approve
`record` needs-attention (1)

1. (adversarial, advisory) The parser accepts a never-defect carrying critical severity. Answered as F1.
2. (consistency) Plan-stage corrections rated minor without exception contradicts the scale's own major bar. Answered as F2.
3. (record) The worked KEEP reads fix-loop finds as zero-leg evidence the amended rule excludes. Answered as F3.

### F1 -- the parser accepts a never-defect carrying critical -- adversarial -- FILED to `D00 T04 §16` (independent) [major]

Confirmed by reading `parse_file`: severity is checked for closed-set membership only, with no cross-check against the disposition, so `REFUTED (self) [critical]` parses and enters the severity ledger against the stated never-defects-rate-minor rule. Advisory at round 1, so noted here and filed rather than fixed in the candidate: new item on D00 T04 §16 (checker defects in a shipped checker is that section's subject), SOURCE `panel-D00-T04-s8-2026-09-19`.

### F2 -- plan-stage corrections rated minor without exception contradicts the scale -- consistency -- FIXED (independent) [major]

Confirmed on the face of the record: the scale rates wrong behavior in a plan as major, while the rule rated plan corrections minor without exception, and the F3 re-rate fitted the datum to the rule. Fixed by replacing the exceptionless rule with the impact rule (minor when ordinary execution would have caught the defect, major when it would have survived it) and re-rating D07-T01-S1-F3 back to major: a checkpoint that cannot fail stamps without verifying, which is wrong verification behavior. Swept all 35 CORRECTED rows for the survives-execution bar; only F3 moves. Ledger totals moved to critical 6, major 77, minor 77 over 160 at the re-rate; this review's ten findings bring the regenerated ledger to 170: critical 6, major 85, minor 79. The rung comparison is untouched (F3 is self, not a rung find).

### F3 -- the worked KEEP reads fix-loop finds as zero-leg evidence -- record -- FIXED (independent) [major]

Confirmed against the run records: all 11 Opus rounds read `opportunity: delta-plus-regressions`, so the per-section 7/1/2/6/1 are fix-loop finds the amended zero leg excludes, and the first text's zero-leg reading of them is invalid. Fixed by rewriting the worked KEEP: Sol clears the zero leg at full scope (5/2/5/3/4, never zero, counted from the round lines), Opus has no full-scope observation in the window so the leg is inconclusive for it, and the arbitrating blinded comparison (7 Opus finds on shared input) voids any zeros; the overlap leg reads Jaccard 0.22 against 0.5. The decision numbers stand, with the count basis now stated: 11 panel rounds per rung, excluding the one stamp-review round (reproduced the join both ways: panel-only gives exactly 31/79 for Sol and 17/31 for Opus).

```
RECEIPT sha=33cf3debcb57b9402341f8230dd309c5df7817f484d59e52e98b988066c559b1 end=PANEL-4cb40d803e26669d
**adversarial: advisory** (1)
1. `.claude/skills/review-todo-section/SKILL.md:200` requires refuted, withdrawn, and duplicate findings to carry minor severity, but `scripts/todo-findings.py:215-245` validates only that severity belongs to the closed set; `REFUTED (self) [critical]` therefore parses and enters the severity ledger contrary to the declared weighting rule.
**consistency: needs-attention** (1)
1. `scripts/todo-findings.py:127-140` defines major as wrong behavior and minor as polish or no surviving defect, while `todo/00-workspace/TODO-04-self-correction.md:476` applies minor to plan-stage corrections without exception; severity consequently depends on discovery phase instead of impact, leaving the published 6/76/78 totals and weighted comparison inconsistent with their stated scale.
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:473` claims the worked KEEP evaluates the full-scope zero trigger using Opus findings of 7/1/2/6/1, but every cited Opus round is marked `opportunity: delta-plus-regressions` in `docs/reviews/run-records.md:99-101`, `docs/reviews/run-records.md:186-187`, `docs/reviews/run-records.md:198-199`, `docs/reviews/run-records.md:211-213`, and `docs/reviews/run-records.md:224`; under the same rule’s exclusion of fix-loop opportunities, those findings cannot demonstrate that Opus avoided zero across eligible full-scope sections.
```

Output check: `PASS four lenses, one verdict each`, exit 0.

## The voided round

Round 2 first ran against a contaminated candidate: `git diff d72afe0^..296a838` is a 30-commit range carrying 66 files, while the §8 candidate is 4 commits and 31 files. A review of that range proves nothing about the section, so its verdicts are void and the round re-ran below over the 4 §8 commits' own patches. Its three findings were all verified in-session against the tree rather than trusted from the void round, and all three were real and are fixed outside the candidate (none touches §8's files). The run block records the attempt as error with its kept findings; panel sections number the usable rounds.

### F4 -- the frozen-effect parity wording excludes PixRepair and BiosCodes -- consistency -- FIXED in `2362737` (independent) [major]

The skill demanded byte-for-byte proof on registry, ACLs, or drive contents for every frozen tool, but PixRepair freezes displayed sequences and timing (it writes nothing) and BiosCodes freezes lookup answers, so the sentence made two frozen tools' parity proofs unplannable. Fixed in the skill with the per-surface enumeration. Found by the void round, confirmed by reading the skill and the PixRepair Current state.

### F5 -- phase closeout invokes a skill that refuses partial files -- integration -- FIXED in `2362737` (independent) [major]

`process-phase` step 4 invokes `process-todo-file` for every touched file including ones with rows in later phases, while `process-todo-file` permits only exhausted files and refuses the rest. Fixed by adding the sweep-only mode the phase step already assumed (steps 1-4 for shipped rows, step 5 skipped, status untouched). Found by the void round, confirmed by reading both skills.

### F6 -- the run record's resolve counts disagree with its enumerations -- record -- FIXED in `4305bee` (independent) [minor]

The Phase repairs line read 14 runnable and 17 leftovers over enumerations of 15 and 16 (31 open rows). Recounted from the resolve output: 15 exit-0, 16 exit-4. The enumerations were right and both counts wrong; no decision rested on them. Found by the void round, fixed in the run file.

## GPT panel Round 2

Re-run of the voided round-2 attempt over the exact candidate (the 4 §8 commits' own patches, 31 files), with round-1 verdicts appended. (The first attempt at this round is the voided round above, not these verdicts.)

`adversarial` approve
`consistency` approve
`integration` needs-attention (1)
`record` needs-attention (1)

1. (integration) The review file's independent findings have no run block claiming them. Answered as F7.
2. (record) The candidate table omits the fix commit and the count lines lag the ledger. Answered as F8.

### F7 -- the review file has findings but no run block -- integration -- FIXED (independent) [major]

Confirmed live: `todo-runs.py --check` failed naming F5 and F6 unclaimed and the file runless. A review that breaks the runs gate is wrong behavior in the record either way the counts go. Fixed by adding the D00-T04-S8 run block (rounds 1-3 with the void noted, all eight findings claimed), after which `--check` passes.

### F8 -- the candidate table and count lines lag the review -- record -- FIXED (independent) [major]

Confirmed: the table omitted `296a838`, and the section plus this file retained the 160-finding inventory against a 163-finding ledger. Fixed by recording the fix commit, rescoping the section's counts to rollout time with the live query owning current figures, and restating this file's lines at the regenerated totals.

```
RECEIPT sha=465b58266e9685f2e21a0de97c6cc7f3a3a6d36f12152c3eb7b7a2da67254c33 end=PANEL-db5eef63d85e60f8
**adversarial: approve**
**consistency: approve**
**integration: needs-attention** (1)
1. `docs/reviews/00-workspace/D00-T04-s8.md:39`, `docs/reviews/00-workspace/D00-T04-s8.md:43`, and `docs/reviews/00-workspace/D00-T04-s8.md:47` add three independent findings without the D00-T04-S8 run block required by `docs/reviews/run-records.md:7`; consequently `todo-runs.py --check` reports all three refs unclaimed and the review file uncovered.
**record: needs-attention** (1)
1. `docs/reviews/00-workspace/D00-T04-s8.md:12-16` still identifies a three-commit candidate ending at `a27e296`, omitting candidate commit `296a838`, while `docs/reviews/00-workspace/D00-T04-s8.md:20`, `docs/reviews/00-workspace/D00-T04-s8.md:24`, `docs/reviews/00-workspace/D00-T04-s8.md:45`, and `todo/00-workspace/TODO-04-self-correction.md:476` retain the pre-review inventory of 160 findings and severity totals 6/77/77; the final generated ledger records 163 findings and totals 6/80/77 at `docs/reviews/findings.md:7` and `docs/reviews/findings.md:28-30`.
```

Output check: `PASS four lenses, one verdict each`, exit 0.

## Opus panel Round 3

Sign-off over the 5-commit candidate with rounds 1-2 verdicts appended.

`adversarial` approve
`consistency` approve
`integration` needs-attention (1)
`record` advisory (1)

1. (integration) Sweep-only mode still demands the whole Verification block. Answered as F9.
2. (record, advisory) The severity self-attestation confound goes unnamed. Answered as F10.

### F9 -- sweep-only mode still demands the whole Verification block -- integration -- FIXED (independent) [major]

Confirmed against the file the fix touched: step 1 scopes steps 1-4 to shipped rows and step 5 is skipped, but step 2 still says every Verification item runs with none trimmed, and Verification blocks are file-wide (the cited 35-pack loader and restore-record items sit in the framework file's block, verified present). A partial sweep would have to trim silently or fail falsely. Fixed in the skill: sweep-only records un-runnable Verification items as not-yet-runnable with their owner. Below the sign-off bar (process wording, no safety, data, or stamp impact), fixed directly in the skill like F4/F5 rather than re-rounded.

### F10 -- the severity self-attestation confound goes unnamed -- record -- FILED to `D00 T04 §18` (independent) [minor]

Confirmed: the weights' subjectivity is disclosed as judgment, but the bulk self-rating after outcomes were known is not, and the §7 precedent (source self-attestation stated as a limitation) shows the project names this class of confound. The decision stands on the blinded corroboration, so this is a missing limitation, not a wrong outcome. Filed as D00 T04 §18 (second revisit with independent rating), which records the limitation in its context and owns the next trigger; the sign-off candidate is untouched.

```
RECEIPT sha=b3734901065b6f7bc7c84e406ee4b1e33688e25ed4d5eb5c4871bf1d1386f6cd end=PANEL-de02be1b931a6718
**adversarial: approve**
**consistency: approve**
**integration: needs-attention** (1)
1. `.claude/skills/process-todo-file/SKILL.md:15` directs sweep-only mode (phase closeout with rows still open) to "run steps 1-4 for the shipped rows only", and `.claude/skills/process-todo-file/SKILL.md:27` scopes step 1 and skips step 5, but step 2 at `.claude/skills/process-todo-file/SKILL.md:29-40` still demands the whole file-level Verification block "each executed, none trimmed"; that block is file-wide, not row-scoped, so on `todo/01-framework/TODO-01-framework-core.md:` the Phase 0 sweep is told to execute "All 35 Firemin language packs load through the framework loader" and "A tool faulted mid-repair still leaves a restore record" for sections that have not shipped, which can only be silently trimmed or reported as a false failure. The fix recorded at `docs/reviews/00-workspace/D00-T04-s8.md:75` therefore closes F5's contradiction at steps 1 and 5 and leaves it open at step 2.
**record: advisory** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:475` closes with "the decision rests on value with cost recording as the named gap", and `todo/00-workspace/TODO-04-self-correction.md:476` records the rating rules, but neither records that the severities the value figures are computed from (`critical 9 / major 3 / minor 1` over 31 Sol and 17 Opus accepted finds) were assigned in bulk by the implementing session in `d72afe0`, after every outcome was known, and then used by that same session to decide keep-or-cut. `todo/00-workspace/TODO-04-self-correction.md:441` states the equivalent confound for the source split ("the source itself is self-attested by the session being measured") as a limitation §7 was built to remove; the weighted-value leg carries the same confound unnamed, so a reader cannot tell whether the 79-vs-31 margin is a measurement or a rating choice. The keep conclusion is independently corroborated by the blinded 0.22 Jaccard, so this is a missing limitation rather than a wrong decision.
```

Output check: `PASS four lenses, one verdict each`, exit 0.
