# Review -- D00 T04 §30, CI Read-Back and Reachable Provenance

**Reviewed:** 2026-09-24
**Stance:** post-implementation
**Rounds:** independent pass (1 finding: fixed); GPT 1 bulk (2 findings: 2 fixed); GPT 2 bulk (1 finding: fixed); GPT 3 sign-off (clean).
**Tier:** Full panel. The diff touches `scripts/review_prompt.py` (shared checker) and three skill files (the shared review and run contract).
**Attestation:** `docs/reviews/00-workspace/D00-T04-s30.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `da5ca346` | implementation: `provenance-tags`, `check-reachable`, `ci-wait`, and the skill wiring |
| `d9e6ca4e` | answers the independent pass: the read-back honours the workflow's path filter and the pushed range |
| `6cd781e0` | answers panel round 1: the committed workflow filter, the SHIP read-back recorded |
| `6981ac4b` | answers panel round 2: the atomic stamp-and-tags push |

Record commits (filings only, no reviewed content): `05c79f18` (plan-review findings filed in D00 T04 §26).

Range `da5ca346^..6981ac4b`, contiguous, no declaration owed. Each round fenced the range from `da5ca346^` to its own head (5 files, cross-check `5 file(s) agree`). The rundir is session scratch outside the tree (/tmp/review-QT43Blft, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation commit. A mutation that disables the tree-reach check fails six legs by name (the first cut crashed the suite at a fixture `git push` instead, now non-fatal so a mutation reads as named failures). Real drives on this repository: `check-reachable --remote origin` passes the D00 T04 §22, D00 T04 §24, D00 T04 §27, and D00 T04 §29 findings; the D00 T04 §24 findings against `--refs HEAD` alone exit 1 naming both untagged trees; `ci-wait` reads the real red run (`06e570b5`) as exit 1 and the real green run (`657a1d88`) as exit 0. `gh run list --commit` matches only a full sha, probed before the code was written. The architecture gate is not triggered (review tooling).

## Independent pass

`python scripts/panel_slots.py exec independent --commit da5ca346d90a24e904f116a76046d9d0d5b99a49`: `panel_slots: slot independent model gpt-6-sol effort high timeout 900s`. Exit 0, latency 219s, cost unresolved (the review runner printed no token figure). Output and stderr stay in the session scratch rundir outside the tree (ind30.out, ind30.err); the output is transcribed below.

### F1 -- the CI read-back waits out code-only pushes that start no run -- correctness -- FIXED in `d9e6ca4e` (independent) [major]

`plan-gates` runs only when the pushed range touches its path filter, so a SHIP push under `src/`, `shared/`, or `extensions/` would have waited 900 seconds and stopped the phase on every C++ section. `ci-wait` now reads the filter over `--since <remote head before the push>`..head and reports `not triggered`, exit 0, when nothing matches; GitHub also runs only the pushed head, which the skills now say. Real drive: `ci-wait bac6bde9` (a docs-only push with no run) reads `not triggered`.

```text
The mandatory CI read-back blocks valid code-only SHIP pushes for which the configured workflow creates no run.

Review comment:

- [P1] Wait for CI only when the push triggers plan-gates — R:\conclave\projects\Resolute\.claude\skills\process-phase\SKILL.md:65-65
  When a SHIP push changes only `src/`, `shared/`, or `extensions/`, `.github/workflows/plan.yml` does not trigger `plan-gates` for that commit. The new after-every-push wait then times out after 900 seconds and stops the phase despite a successful push. Restrict the wait to pushes covered by the workflow, or expand its path filters. This also conflicts with the completion-first rule in `AGENTS.md:141`.
```

## GPT panel Round 1

Round 1 over `da5ca346^..d9e6ca4e`. Slot `bulk`: `panel_slots: slot bulk model gpt-6-sol effort medium timeout 600s`. Exit 0, latency 149s, clock 2026-09-23T22:30:13Z, cost 44392tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-1.out, panel-1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`. **Contract slip, recorded:** the round script fenced the D00 T04 §29 section text as the contract instead of D00 T04 §30's (a pattern that did not rewrite when the script was copied); the reviewer judged the correct diff against the wrong contract, so this round's findings are diff findings and its verdicts are not a D00 T04 §30 contract verdict. Rounds 2 and 3 fenced the D00 T04 §30 text, confirmed from the fenced section's first line.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` needs-attention

### F2 -- the read-back trusts the working tree's workflow filter -- correctness -- FIXED in `6cd781e0` (independent) [major]

Round-1 adversarial: `ci-wait` read `.github/workflows/plan.yml` from the working tree, so a local edit could turn a red run into `not triggered`. It now parses the pushed commit's copy (`git show <head>:...`), with a leg where the working-tree copy would trigger and the committed one does not.

### F3 -- the SHIP read-back line has no record -- record -- FIXED in `6cd781e0` (independent) [minor]

Round-1 record: `process-todo-section` read CI back after the SHIP push without recording the line. It now records it in the run file.

```text
RECEIPT sha=a7f26320d54e154299e6f019f3b24509d30b08f8e54e3da4e6bd4fcc26a40d8f end=PANEL-485a76e38e066702 nonce=1e58803f871a134e

**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:1101` reads the workflow filter from the working tree rather than the pushed commit. A local edit to that file can make `ci-wait` report `not triggered` for a push that actually started `plan-gates`, skipping a red result.

**consistency: approve**

**integration: approve**

**record: needs-attention** (1)
1. `.claude/skills/process-todo-section/SKILL.md:204` requires CI read-back after the SHIP push but never requires its conclusion to be written to the run file, leaving the contract’s after-every-push record incomplete.
```

## GPT panel Round 2

Round 2 over `da5ca346^..6cd781e0` with the round-1 verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-sol effort medium timeout 600s`. Exit 0, latency 52s, clock 2026-09-23T22:33:28Z, cost 26789tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-2.out, panel-2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` needs-attention
`record` approve

### F4 -- the stamp can land without its provenance tags -- correctness -- FIXED in `6981ac4b` (independent) [major]

Round-2 integration: the stamp and its tags were pushed without `--atomic`, so a rejected tag could leave the stamp landed with its cited tree unreachable. The push is now `git push --atomic` with the tags, and the skill pin follows.

```text
RECEIPT sha=f6beda27e0d27e8812d248db5e863948dad3520924652c0bc88136823fbff739 end=PANEL-6476a79046bca36c nonce=637f81d169cabb3e
**adversarial: approve**
**consistency: approve**
**integration: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:281` pushes the stamp and provenance tags without `--atomic`. If the remote accepts the branch update but rejects a tag, the stamp lands while its cited tree remains unreachable in a fresh clone; the later check detects the failure only after the bad push.
**record: approve**
```

## GPT panel Round 3

Round 3, the Full sign-off, over `da5ca346^..6981ac4b` with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-sol effort high timeout 600s`. Exit 0, latency 131s, clock 2026-09-23T22:37:04Z, cost 43792tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-3.out, panel-3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` approve
`record` approve

```text
RECEIPT sha=399c37186d9ed4d3c3ab179eb219298344fdee5790ed6d59931a507ebaa22046 end=PANEL-ce01f50029bb4027 nonce=109ef4ff79a6608f
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §30, D00 T04 §24, D00 T04 §26]; dependents [D00 T04 §26]; bytes 67979; run 20260923-D00-T04-S30-sol

Round over the section plus its Depends neighbor (D00 T04 §24) and its XREF-only consumer (D00 T04 §26, the section its items were split from). Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-sol effort high timeout 900s`. Exit 0, latency 56s, clock 2026-09-23T22:38:19Z, cost 30252tokens. Check: `PASS 6 findings, one per line`.

Ledger:
- [D00-T04-S30-PR1] [minor] provenance tags must reach the remote with the stamp -> rejected already satisfied by the round-2 answer `6981ac4b`, which pushes the stamp and its tags with `--atomic`
- [D00-T04-S30-PR2] [major] a red CI after the stamp leaves a Verified stamp on a failing commit -> filed D00 T04 §26
- [D00-T04-S30-PR3] [minor] the read-back does not match branch, event, or run attempt -> filed D00 T04 §26
- [D00-T04-S30-PR4] [major] a wrong not-triggered verdict would hide a missing CI run -> filed D00 T04 §26
- [D00-T04-S30-PR5] [minor] the bundle does not carry the provenance tags -> filed D00 T04 §26
- [D00-T04-S30-PR6] [minor] no integrated push and read-back drive proves the whole flow -> accepted to be proven by this section's own stamp push, which runs `provenance-tags`, `check-reachable` before and after, the atomic push, and `ci-wait` on the stamp commit; the run file records the lines after the push
End of ledger

6 findings: 4 filed (D00 T04 §26 PR2, PR3, PR4, PR5), 1 accepted (PR6, pending the stamp push and its run-file record), 1 rejected as already satisfied. Filings landed in `05c79f18`.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=bd11acab16b1bff86af1c11d5e324c96fa4664b2daa4464c0c4c1d5c854c613a end=PLAN-32e716af07c7d615 nonce=0402071e5f3ca62d
- §30 should require provenance tags to reach the remote before the stamp commit can trigger CI, or push them atomically; separate pushes can recreate the fresh-clone failure.
- §30 should define what happens to a stamped section when CI returns red or unverifiable; stopping the next section still leaves a `Verified:` stamp on a failing commit.
- §30 should match `plan-gates` to the exact pushed head, branch, workflow, and run attempt; a stale or unrelated green run must not authorize the campaign to continue.
- §30 should fail closed when its local path-filter calculation cannot prove that GitHub skipped the workflow; an incorrect `not triggered` result would hide a missing CI run.
- §30 and §26’s bundle item should carry provenance tag names and remote targets in the portable receipt; the current bundle can verify while its cited staged-tree candidates are unavailable to a fresh clone.
- §30’s checkpoint should include one integrated push and read-back drive for the same stamp commit and provenance tags; the listed historical CI probes and isolated fixtures do not prove the complete flow.
```

## Live proof

Section gates driven on the working tree at `05c79f18` (the implementation plus every review answer and filing, all of which the stamp commit carries as ancestors). The code gates read `05c79f18` exactly; the record gates (the runs check, the plan check, validate, and the ledger check) read it with this stamp's own records added (its run-records block, stamp, row flip, and findings file), which is why their totals count this section: `42 runs, 113 rounds`, `42/221 sections`, and `327 finding(s)` include D00 T04 §30 where `05c79f18` alone reads 41, 41/221, and 323. The candidate names `05c79f18` because a staged stamp tree cannot name itself: the findings file it would cite is inside it. Each quote below is the command's last output line; the digest covers the command's whole stdout.

- `python scripts/review_prompt.py --self-test`: `review-prompt self-test: 345 cases, 0 failed`

Provenance: candidate 05c79f1817052e487faea1e3e359846d81dbe7f9; command python scripts/review_prompt.py --self-test; exit 0; tool CPython 3.14.4; digest e138b67a40add8c9838c487c6c57409a250153b93592bf9786553291cc2f1294; path docs/reviews/00-workspace/D00-T04-s30.md; run 20260923-D00-T04-S30-sol

- `python scripts/todo-graph.py self-test`: `todo-graph self-test: 598 cases, 0 failed`

Provenance: candidate 05c79f1817052e487faea1e3e359846d81dbe7f9; command python scripts/todo-graph.py self-test; exit 0; tool CPython 3.14.4; digest fab9f27430ec3e895774beecb6628eaa8a5fdd40af2d8fb9ca4069c976223b23; path docs/reviews/00-workspace/D00-T04-s30.md; run 20260923-D00-T04-S30-sol

- `python scripts/todo-runs.py --check`: `42 runs, 113 rounds: all resolve, all covered, counts agree`

Provenance: candidate 05c79f1817052e487faea1e3e359846d81dbe7f9; command python scripts/todo-runs.py --check; exit 0; tool CPython 3.14.4; digest ffcfc1c787e5e4693c7104f56566f1b32ad512cf4b23cce990d4707bce90ad48; path docs/reviews/00-workspace/D00-T04-s30.md; run 20260923-D00-T04-S30-sol

- `python scripts/review_prompt.py check-reachable --findings docs/reviews/00-workspace/D00-T04-s24.md --remote origin`: `check-reachable: 2 candidate(s) reachable from remote origin`

Provenance: candidate 05c79f1817052e487faea1e3e359846d81dbe7f9; command python scripts/review_prompt.py check-reachable --findings docs/reviews/00-workspace/D00-T04-s24.md --remote origin; exit 0; tool CPython 3.14.4; digest 4bf75fd06c78195f7222ad35d1556eae257f55723d0adf7d1f2134d32893d371; path docs/reviews/00-workspace/D00-T04-s30.md; run 20260923-D00-T04-S30-sol

- `python scripts/review_prompt.py ci-wait 657a1d88 --timeout 60`: `ci-wait: 657a1d88a3a5 plan-gates success https://github.com/rizonesoft/Resolute/actions/runs/35924459201`

Provenance: candidate 05c79f1817052e487faea1e3e359846d81dbe7f9; command python scripts/review_prompt.py ci-wait 657a1d88 --timeout 60; exit 0; tool CPython 3.14.4; digest 45b197dca0189b2292dcc31b782453b668c9304bf56ba75ce998cd8d195e97b3; path docs/reviews/00-workspace/D00-T04-s30.md; run 20260923-D00-T04-S30-sol

- `python scripts/review_prompt.py ci-wait bac6bde9 --timeout 5`: `ci-wait: bac6bde942a7 plan-gates not triggered (1 changed path(s), none in the workflow's path filter)`

Provenance: candidate 05c79f1817052e487faea1e3e359846d81dbe7f9; command python scripts/review_prompt.py ci-wait bac6bde9 --timeout 5; exit 0; tool CPython 3.14.4; digest cd76b0655a2cc1ca7f6025aa159098fb5e533a12203097dfe2cdaa44fc4cb5ca; path docs/reviews/00-workspace/D00-T04-s30.md; run 20260923-D00-T04-S30-sol

- `python scripts/todo-graph.py validate`: `30 todos, 224 sections -- 0 fatal, 0 warning(s), 98 adjacency advisory`

Provenance: candidate 05c79f1817052e487faea1e3e359846d81dbe7f9; command python scripts/todo-graph.py validate; exit 0; tool CPython 3.14.4; digest c0ec1d453fe109c87c0e43c6565c5256370a26e41fafa8b28dc8b1cbc2e1ddd5; path docs/reviews/00-workspace/D00-T04-s30.md; run 20260923-D00-T04-S30-sol

- `python scripts/todo-claims.py`: `todo-claims: 113 claim(s) -- 113 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`

Provenance: candidate 05c79f1817052e487faea1e3e359846d81dbe7f9; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest ea0573d8fb53202a7220a1983ef09eea9d62da12e4a59142f16826bb196df4a4; path docs/reviews/00-workspace/D00-T04-s30.md; run 20260923-D00-T04-S30-sol

- `python scripts/todo-graph.py plan --check`: `implementation plan is current -- 42/221 sections complete (19%)`

Provenance: candidate 05c79f1817052e487faea1e3e359846d81dbe7f9; command python scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.4; digest 0ac4e5eacc1d86bd04ab38847127fa8a092e7eb63348edce1a10e64b7a6c7b02; path docs/reviews/00-workspace/D00-T04-s30.md; run 20260923-D00-T04-S30-sol

- `python scripts/todo-findings.py --check`: `todo-findings: ledger current, 327 finding(s), transitions complete`

Provenance: candidate 05c79f1817052e487faea1e3e359846d81dbe7f9; command python scripts/todo-findings.py --check; exit 0; tool CPython 3.14.4; digest e2180f86c6abe45287223387b82c622280510dcb2ccc3d97f04e7e913df6c143; path docs/reviews/00-workspace/D00-T04-s30.md; run 20260923-D00-T04-S30-sol
