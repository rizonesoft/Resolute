# Review -- D00 T02 §8, Icon Manifest Audit

**Reviewed:** 2026-09-26
**Stance:** post-implementation
**Rounds:** independent pass (clean); GPT 1 bulk (1 fixed); GPT 2 bulk (1 fixed); GPT 3 sign-off (1 below the blocking bar, filed in D00 T02 §9).
**Tier:** Full panel. The diff adds seven icons to the shared icon set every tool embeds, and the audit that gates it.
**Attestation:** `docs/reviews/00-workspace/D00-T02-s8.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `a6aaa693` | implementation: `tests/icon_manifest.txt` (26 names with referrers), `tests/icon_manifest_test.cpp`, and the seven referenced icons the set never carried, from Lucide 1.48.0 with `shared/lucide/icons/SOURCES.md` |
| `f4479e69` | answers panel round 1: a source-to-manifest scan, and file-level referrers |
| `b1738177` | answers panel round 2: the scan matches any single-token narrow literal |

Record commits (filings only, no reviewed content): `de14ca93` and `d25946c5` (the sign-off leftover and plan-review findings folded into D00 T02 §9).

Range `a6aaa693^..b1738177` (base `23734a03`), contiguous, no declaration owed. Each round fenced the range from `23734a03` to its own head. The rundir is session scratch outside the tree (/tmp/review-hi9qNsoO, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Makes sense: the audit found what it exists to find: seven names the controls reference (the content view's empty and error states, the status bar's notification icons, the toolbar's overflow) that the embedded set never carried, each drawing nothing. Logical: the added SVGs come from a pinned upstream release under the names the code uses, with provenance, rather than drawn by hand, and every drill is recorded (the seven moved aside, a misspelled entry, stray and malformed literals). Broke nothing: `check-all: 20 gate(s) ok`, both presets `100% tests passed out of 76`, `tidy` 166 under its baseline. Self-review also replaced the first manifest's line-number referrers with file referrers, since an unrelated edit would otherwise break the audit.

`source-defect`: owed and done: Lucide 1.48.0's own files are the source for the seven glyphs, named in `SOURCES.md`. `design`: not owed here (no surface is judged; D00 T02 §9 now owes the rendered check of the seven glyphs).

## Independent pass

`python scripts/panel_slots.py exec independent --commit a6aaa693...`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 70s, clock 2026-09-26T13:42:10Z, cost unresolved (the review runner printed no token figure). No findings; the output is transcribed below.

```text
No actionable defects were identified. The manifest covers the current icon references, and the added SVGs match their consumers. The icon-manifest test passed all 137 assertions using both existing Debug and Release binaries.
```

## GPT panel Round 1

Round 1 over `a6aaa693^..a6aaa693`. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 21s, clock 2026-09-26T13:43:28Z, cost 17371tokens. Output and stderr stay in the session scratch rundir outside the tree (r1.out, r1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` approve

### F1 -- the audit never checks sources against the manifest -- adversarial -- FIXED in `f4479e69` (independent) [minor]

A second case scans the sources and requires every icon-shaped literal listed for its file; referrers became files so a moved line never breaks it.

```text
RECEIPT sha=d4328f870316863daf9c0ec9da687ff050e21054962a60ab049907789d93d5d2 end=PANEL-2ef9446e661419ac nonce=795cff9a3c32ba07
**adversarial: needs-attention** (1)
1. `tests/icon_manifest_test.cpp:73-75,91-94` checks only references already listed in the manifest; it never checks source references against the manifest. Adding an unlisted misspelled icon, or deleting an entry while retaining at least 20 entries, passes despite a missing glyph. Add source-to-manifest coverage validation so omitted references fail by name.
**consistency: approve**
**integration: approve**
**record: approve**
```

## GPT panel Round 2

Round 2 over `a6aaa693^..f4479e69` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 22s, clock 2026-09-26T13:56:14Z, cost 18855tokens. Output and stderr stay in the session scratch rundir outside the tree (r2.out, r2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` approve

### F2 -- names with capitals or underscores escape the scan -- adversarial -- FIXED in `b1738177` (independent) [minor]

The scan matches any single-token narrow literal, with seven more declared non-icons.

```text
RECEIPT sha=2df3df4d729047b79f39106a793b79173740db1de1834cbf8e23ec3d29679b8f end=PANEL-869abe291a329a85 nonce=2dead938f4519673
**adversarial: needs-attention** (1)
1. `tests/icon_manifest_test.cpp:116,126-131` ignores literals containing uppercase letters or underscores. An unlisted icon reference such as `"chevron-Up"` or `"chevron_up"` therefore passes the audit while resolving to null. Validate icon-use arguments independently of the valid-name pattern so malformed names also fail by name.
**consistency: approve**
**integration: approve**
**record: approve**
```

## GPT panel Round 3

Round 3 over `a6aaa693^..b1738177` the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 25s, clock 2026-09-26T14:06:39Z, cost 19818tokens. Output and stderr stay in the session scratch rundir outside the tree (r3.out, r3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` approve

### F3 -- names with doubled or trailing separators or spaces escape the scan -- adversarial -- FILED to `D00 T02 §9` (independent) [minor]

The third variant of one root cause, spelling-based detection. Below the blocking bar at the Full sign-off, so it files rather than re-rounds, and the fix is rethought rather than patched: validate names at their call sites, in D00 T02 §9's item with a visible fallback.

```text
RECEIPT sha=76c50051385bd0fc32264de4360e84c8cb66eecc7c444ba1ad21ad7a564a41bc end=PANEL-9c55a3a27558db5d nonce=11d60731031a5e94
**adversarial: needs-attention** (1)
1. `tests/icon_manifest_test.cpp:121,131-136` still filters references by name syntax: `"chevron--up"`, `"chevron-up-"`, and `"chevron up"` are skipped entirely. Using any as an unlisted icon name therefore passes the audit while resolving to null. Validate icon-use arguments independently of valid-name syntax so malformed references fail by name.
**consistency: approve**
**integration: approve**
**record: approve**
```

## Plan review

Manifest: sections [D00 T02 §8, D00 T02 §5]; dependents [none]; bytes 15132; run 20260926-D00-T02-S8-astra

`bytes 15132` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency D00 T02 §5; no section depends on D00 T02 §8. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 31s, clock 2026-09-26T14:07:24Z, cost 15104tokens. Check: `PASS 8 findings, one per line`.

Triage follows the operator direction of 2026-09-26 (the plan must not grow unconditionally): four findings join the sign-off leftover in one item of the open D00 T02 §9, and four are rejected with a reason.

Ledger:
- [D00-T02-S8-PR1] [minor] an unknown icon draws nothing and says nothing -> filed D00 T02 §9
- [D00-T02-S8-PR2] [minor] the permitted icon-reference forms are undefined -> filed D00 T02 §9
- [D00-T02-S8-PR3] [minor] new source files may escape the scan -> rejected the scan walks its directories recursively, so a new file joins it automatically
- [D00-T02-S8-PR4] [minor] a file-level exemption can hide an icon use of the same string -> filed D00 T02 §9
- [D00-T02-S8-PR5] [minor] the checkpoint names no gate and CI may not select the tests -> rejected check-all runs the ui label under both presets, which includes both cases (76 tests each), and CI runs no C++ by design
- [D00-T02-S8-PR6] [minor] resolving is not proof the seven glyphs look right -> filed D00 T02 §9
- [D00-T02-S8-PR7] [minor] D00 T02 §5's uncovered gaps have no successors -> rejected D00 T02 §7 and the two items it filed in D00 T02 §10 own them
- [D00-T02-S8-PR8] [minor] ordinary control teardown is unproven -> rejected D00 T02 §7 shipped the teardown case, each control cancelling its animations
End of ledger

8 findings: 4 filed with the sign-off leftover F3 as one item in D00 T02 §9 (`de14ca93`, `d25946c5`), 4 rejected.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=fc2b53330f324477b1dfb2fbaf5808e4ec273bead2d8725c80af71ef252cbf54 end=PLAN-87c29b864433d31b nonce=e201e04b6b330f9e
- Unknown-icon behavior conflicts: §5 promises failure by name but verifies null, while §8 catches only audited references; assign runtime diagnostics and a visible fallback to the shared icon resolver so missed references cannot silently erase controls.
- Literal scanning does not establish complete reference coverage: §8 explicitly skips wide/UTF literals and does not address computed names or names containing spaces or punctuation; define permitted icon-reference forms and reject unsupported forms so the gate’s completeness claim is enforceable.
- Scan scope lacks an explicit discovery guarantee: §8 should require automatic inclusion of new launcher, shared-control, and tool-descriptor files, with a new-file failure drill, so future tools cannot bypass the audit.
- File-level non-icon exemptions can mask an actual icon use of the same string in that file; §8 should make exemptions specific to their use and reject stale exemptions so the allowlist cannot conceal broken references.
- The checkpoint does not name a reproducible command or prove CI selects the manifest tests; §8 should specify the gate and demonstrate that an unlisted reference fails through it, avoiding the test-selection problem already documented in §5.
- SVG and bitmap creation do not prove that an icon is visible or conveys the intended action; add an owned visual check for the seven repaired glyphs at their actual sizes and appearances, especially toolbar overflow and status indicators, to verify the user-facing repair.
- §5 records animation delivery, filtering, overflow, and high-contrast behavior as uncovered without explicit follow-up owners in the supplied sections; give each gap an executable successor section and acceptance gate so test limitations do not become permanent product limitations.
- §5’s teardown guard drains animations before controls die, leaving ordinary control destruction unproven; assign a shared-control lifecycle test that destroys an animated control before subsequent ticks, because test cleanup alone does not establish production lifetime safety.
```
