# Review -- D00 T04 §24, Review-Tooling Follow-Ups

**Reviewed:** 2026-09-21
**Stance:** post-implementation
**Rounds:** Sol 1 (5 findings: 3 fixed, 2 refuted); Sol 2 (7 findings: 3 fixed, 2 refuted, 2 duplicate); Opus 3 sign-off (2 findings: 2 fixed, below bar); Opus 4 verification (4 findings: 1 fixed, 1 refuted, 1 filed, 1 advisory noted); Opus 5 hard cap (5 findings: 4 fixed, 1 duplicate, answers land without re-round)
**Tier:** Full panel. The diff touches `scripts/review_prompt.py`, `scripts/todo-graph.py`, `scripts/todo-validate.py`, `scripts/todo-runs.py`, `scripts/todo-findings.py` (shared checkers), both skill files (shared review contract), and `todo/README.md` (author-facing spec): shared contract surface.
**Attestation:** `docs/reviews/00-workspace/D00-T04-s24.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `1eb1d979` | implementation: items 1-6, 9-23 (items 7-8 implemented, tick at ship/review) |
| `9900286d` | self-review fixes 3-10: forged-attestation re-resolution, bundle caps, single-PASS transcripts, history soft/hard, blockquote probe, residuals, CANONICAL_URL guard, shell matrix |
| `f3511316` | answers panel round 1: pair coherence, stdin probe plus item-8 tick, Commit tick |
| `b4eae82b` | answers panel round 2: cite cutoff plus unattested oids, suffix case plus recorded duplicates, permutation bound |
| `2ddbc7d2` | answers panel round 3: single-fault dead spans, history-silence record |
| `78a0e002` | answers panel round 4: holding transcripts |
| `e0d43fc9` | answers panel round 5: attempt cap, newest, delta sweep, witnessed shapes |

Record commits (findings/record only, no reviewed content): `53989b63`, `d5fddb85`, `8466f178`, `7177badf`. The Candidate table lists content commits only, so a record commit never needs a row in the table that ships inside it (D00 T04 §21 round-4 R1 convention). Later record commits join this list as they land, terminally at stamp (the findings file rides the stamp set, so the last list update needs no extra commit).

Range `efdc54e0..e0d43fc9`. Fenced ranges: round 1 assembled `efdc54e0..9900286d` declared [`1eb1d979`, `9900286d`] (record `53989b63` excluded per the D00 T04 §21 F1 span-exclusion rule; 13 files; cross-check `13 file(s) agree, 2 commit(s) covered`); round 2 assembled `efdc54e0..f3511316` declared [`1eb1d979`, `9900286d`, `f3511316`] (records `53989b63`, `d5fddb85` excluded; 13 files; cross-check `13 file(s) agree, 3 commit(s) covered`); round 3 assembled `efdc54e0..b4eae82b` declared [`1eb1d979`, `9900286d`, `f3511316`, `b4eae82b`] (records `53989b63`, `d5fddb85`, `8466f178` excluded; 13 files; cross-check `13 file(s) agree, 4 commit(s) covered`); round 4 assembled `efdc54e0..2ddbc7d2` declared [`1eb1d979`, `9900286d`, `f3511316`, `b4eae82b`, `2ddbc7d2`] (records `53989b63`, `d5fddb85`, `8466f178` excluded; 13 files; cross-check `13 file(s) agree, 5 commit(s) covered`); round 5 assembled `efdc54e0..78a0e002` declared [`1eb1d979`, `9900286d`, `f3511316`, `b4eae82b`, `2ddbc7d2`, `78a0e002`] (records `53989b63`, `d5fddb85`, `8466f178`, `7177badf` excluded; 13 files; cross-check `13 file(s) agree, 6 commit(s) covered`).

## Self-review (in-session, costs no round)

Worked the three questions across the candidate diff and its blast radius.
Fixes, committed in `9900286d` before the panel ran (fixes 1-2 are prose-only, in the same commit):

- Item-7 discipline enforced on the section itself: items 12-23 Dones converted to delta form, totals quoted once in the implementation body (fix 1).
- Bundle verify hardened against hostile bytes: role str-type checks, 64MB file / 16MB member caps, duplicate-name refusal, 4 new legs (fix 2).
- Ticked-Commit unreadable-history silence narrowed to soft cases (no git, no `.git`); a broken-.git repo warns `commit-history-unreadable` (fix 3, integration lens).
- Membership re-resolves the attested pair, tree, and declaration before judging oids; forged attestations fail naming themselves; skill read-back sentence corrected (fix 4, adversarial lens).
- Bundle verifier bounds member total (`BUNDLE_MAX_TOTAL`) and count (`BUNDLE_MAX_COUNT`) beside the per-member bounds (fix 5, adversarial lens).
- Carried transcripts must be exactly one PASS-led line; injected-PASS pinned at emit and verify (fix 6, adversarial lens).
- `anchors-quote-clean` probe pins no `>` leak into tick spans and the wrapped-continuation recall limit (fix 7, integration lens).
- Wholesale-rewrite residual recorded in the skill and the item-20 Done (fix 8, record lens).
- `CANONICAL_URL` exported in every pushing shell; fence `:?` guard refuses instructively when unset, driven both ways under git-bash (fix 9).
- Per-attempt redirect proven under bash/sh/pwsh (stdout byte-identical, check-stamp green over pwsh/sh bytes); Windows PowerShell 5.1 excluded with cause (fix 10, integration lens).

Attribution note: the informal multi-lens critique was first labeled round-1 in code comments, but the session log shows zero executed runner invocations, so no panel round ran; relabeled to self-review before the formal panel, whose finding ids stay uncollided.

**Does it do what the section says?** Yes. All 23 work items plus Commit ticked with Done evidence: README deferral contract, bank freshness, trigger legend, ticked-Commit binding, OID binding with the D00 T04 §9 migration, item-citing Resolved proofs, count discipline (demonstrated by the implementation body), stdin plan-review (113323-byte probe, receipt quoted), internal checker witness, ordered content-exact coverage, seven stale cites re-pointed or marked, declaration-pair binding, model provenance, run clock, central replace-guard, anchor-candidate membership, cite-hash line binding, round correspondence with the README mirror, pinned push URL with the fork-clone drill, portable bundle with second-machine replay, per-attempt stamp outputs, single-dash refusal, format carve-outs.
**Is anything in the blast radius broken?** No. `review_prompt` 289 green (280 plus self-review and round-1 legs, every pre-existing leg intact); graph 579, runs 115, findings 47 green; `validate` 0 fatal with the ticked Commit binding `1eb1d979` exactly; skill pins flipped with intent preserved (checker absence, failover redirect); CLI changes additive (new flags, narrowed gates with legs); the D00 T04 §§16/18 membership reds are pre-existing sealed-history true positives, diagnostic-only.
**Did it break anything else?** No. Live tree validates 0 fatal; plan current 38/216; claims 112 hold; no consumer outside the review flow reads the touched entry points.

Research pass skipped: pure infrastructure (review scripts, skills, plan records), no tool plan and no tool surface.

`source-defect` approves in-session: every foreign-tool claim in the candidate rides a drive, not an assertion (push-URL rewrite shapes on git 2.55, bash/sh/pwsh/5.1 redirect bytes, `:?` guard both ways, `ls-files` spelling, `merge-base` ancestry). `design` not owed: no surface.

## GPT panel Round 1

Round 1 over assembled `efdc54e0..9900286d` declared [`1eb1d979`, `9900286d`] (13 files, cross-check `13 file(s) agree, 2 commit(s) covered`). Sol medium, exit 0, latency 126s, clock 2026-09-21T04:00:05Z, cost 145582tokens. Rundir `/tmp/review-OM6wm5FZ` (`round-1.out`, stderr `round-1.err`), manifest `PANEL-098f17159235870b` sha `58ec9bb8...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 fixed, A2 refuted)
`consistency` needs-attention (C1 refuted)
`integration` needs-attention (I1 fixed)
`record` needs-attention (R1 fixed)

### F1 -- attested pair resolves without proving a chain -- adversarial -- FIXED in `f3511316` (independent) [major]

Round-1 A1: membership re-resolved the attested endpoints but never proved the base ancestral to the head, so a forged pair of arbitrary real commits (plus the head's real tree) authorized unrelated Review-line oids. Membership now fails an incoherent pair naming it (`pair incoherent: base ... not ancestral to head ...`, untestable fails closed), with the swapped-endpoints shape pinned. The finding's manifest-binding half is refuted in place: the session supplies every input including the manifest, so binding the attested pair to a session-supplied manifest is vacuous, and the no-`Attestation:` skip means a forgery never needs a forged attestation at all.

### F2 -- bundle manifest unsigned against consistent rewrite -- adversarial -- REFUTED (independent) [minor]

Round-1 A2: the unsigned `manifest.json` lets a consistent rewrite (member plus its hash together) pass default offline verify, so the artifact is not the independently tamper-verifiable signed bundle the title names. Refuted structurally: this is self-review fix 8's recorded residual, and offline detection of a consistent rewrite without an external anchor is information-theoretically impossible. The design's anchors are the run-record digest quote and `--recheck-remote` against the live remote; the item-20 Done-when's verifiable criteria (one-command emit, second-machine replay, schema plus tampered legs) all pass; `gpg --detach-sign` over the deterministic bytes stays a one-liner. Nothing buildable within the contract answers the finding.

### F3 -- OID binding re-resolves instead of pinning persistently -- consistency -- REFUTED (independent) [minor]

Round-1 C1: the binding resolves the quoted subject afresh every run and returns the OID transiently, so history rewriting can silently rebind the claim and no immutable pin persists. Refuted structurally: `resolved-and-pinned` is the representation the item-5 Done-when permits, implemented as specified (subject resolves, OID verifies by HEAD-ancestry plus section touch, reported in the EXEMPT line). A dropped subject fails `commit-unbound`, a duplicated one fails `commit-ambiguous`, a rebased-away commit fails the ancestry gate: every non-equivalent rewrite fails loud. The remaining silent case is a deliberate equivalent recommit under single-writer linear history with branch protection, which should rebind; a persistent pin is line-carried, the rejected alternative.

### F4 -- item-8 drive missing from the candidate -- integration -- FIXED in `f3511316` (independent) [major]

Round-1 I1: the stdin transport shipped with item 8 unticked and no quoted >48KB run, leaving the Done-when unmet. Fixed by the probe (receipt-only instruction, not a review round): a 113323-byte fenced plan prompt (D00 T04 §24 section plus D00 T04 §§20-23 neighbors, sha `b5c4f3f4...`) runs the skill's plan-review command shape over stdin exit 0 with its receipt quoted (`end=PLAN-159d0995e6cd2da4`), latency 15s, cost 33736tokens. The run-file no-throwaway plan (the plan review carries the drive) is corrected in the item-8 Done: the panel rightly demanded the drive now.

### F5 -- Commit item unchecked across two same-section commits -- record -- FIXED in `f3511316` (independent) [minor]

Round-1 R1: the Commit item stayed unchecked while history held the implementation plus a fix commit. Fixed by ticking it in `f3511316`, binding `1eb1d979`'s subject exactly (tick slipped one commit past the D00 T04 §20 implementation-commit convention, which the implementation commit missed; noted on the line). The finding's split half is refuted in place: fixes go forward in new commits per the skill, and D00 T04 §22 shipped five content commits the same way, so `one section = one commit` forbids multi-section commits, not review fix commits.

Round-1 transcript (fenced, strips from every scan):

```text
RECEIPT sha=58ec9bb882e8d44ee9a05fbd420c7f9c1fb3ec45d34705c1a7d8d3cf651ea72d end=PANEL-098f17159235870b nonce=a87f34103d38b06f
**adversarial: needs-attention** (2)
1. `scripts/review_prompt.py:1126` only proves the attestation's base and head resolve, not that the base is an ancestor of the head or that the pair belongs to the reviewed manifest; a forged attestation using arbitrary real commits and the head's real tree can still authorize unrelated Review-line OIDs.
2. `scripts/review_prompt.py:2270` trusts hashes stored in the unsigned `manifest.json`, while `scripts/review_prompt.py:5082` generates that manifest without an external signature or immutable digest binding; an attacker can rewrite a member and its manifest hash together and pass default offline verification, so the artifact is not the independently tamper-verifiable signed bundle required by the contract.
**consistency: needs-attention** (1)
1. `scripts/todo-validate.py:342` resolves the quoted subject afresh on every run and `scripts/todo-validate.py:353` only returns the selected OID transiently; no checklist line or persistent mapping records that OID, so history rewriting can silently rebind the claim and the promised immutable OID pin is absent.
**integration: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:183` changes the transport to stdin, but `todo/00-workspace/TODO-04-self-correction.md:1032` remains unchecked and the candidate adds no executable regression or quoted run proving a fenced prompt larger than 48KB succeeds, leaving that Done-when unmet.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:1048` leaves the required Commit item unchecked even though the candidate already contains the exact-subject commit followed by a second same-section fix commit; the canonical checklist disagrees with history and the section is split across two commits despite the one-section-one-commit contract.
```

## GPT panel Round 2

Round 2 over assembled `efdc54e0..f3511316` declared [`1eb1d979`, `9900286d`, `f3511316`] (13 files, cross-check `13 file(s) agree, 3 commit(s) covered`). Sol medium, exit 0, latency 130s, clock 2026-09-21T04:14:00Z, cost 149389tokens. Rundir `/tmp/review-OM6wm5FZ` (`round-2.out`, stderr `round-2.err`), manifest `PANEL-84352c6071ec0ec3` sha `ec6822a4...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 fixed, A2 refuted)
`consistency` needs-attention (C1 fixed)
`integration` needs-attention (I1 fixed, I2 refuted)
`record` needs-attention (R1 refuted, R2 refuted)

### F6 -- legacy skips bypass binding for new stamps -- adversarial -- FIXED in `b4eae82b` (independent) [major]

Round-2 A1: hashless path:line cites skip content verification by the legacy rule, so a newly authored stamp could dodge binding with the hashless shape. Fixed by the cutoff: stamps dated after `CITEHASH_CUTOFF = "2026-09-21"` fail hashless cites as content-unbound, mirroring the short-form cutoff (the cutoff date itself escapes by hygiene). The sibling skip fell in the same fix: resolving Review-line oids without an `Attestation:` line now fail as unattested past the cutoff, while undated and legacy stamps still skip. Five legs pin fail, escape, hashed-pass, unattested-fail, and legacy-skip; the full-tree sweep re-proves with the same 2 membership reds and no new fires.

### F7 -- carried transcripts not replayed against their inputs -- adversarial -- REFUTED (independent) [minor]

Round-2 A2: bundled checker transcripts verify as arbitrary single PASS lines, so a fabricated `PASS forged` verifies without replay. Refuted structurally: replay needs carried plan/stamp inputs the item-20 contract never specifies, and replay would not stop forgery anyway (forged inputs replay clean beside forged transcripts), so full replay sits in the same trust class as the round-1 F2 unsigned-bundle residual, closed by the digest quote plus `--recheck-remote`. As mistake-catching, replay duplicates the emitter's deterministic transcript re-runs, which already fail the emit when a transcript no longer passes. Nothing buildable within the contract answers the finding.

### F8 -- round-suffix case split plus ambiguous recorded duplicates -- consistency -- FIXED in `b4eae82b` (independent) [major]

Round-2 C1: panel headings parse case-insensitively while the `Round N` suffix parses case-sensitively, so `## OPUS PANEL ROUND 2` claimed its position; and duplicated recorded round numbers matched tags ambiguously. Fixed both: the suffix parses case-insensitively like its heading, and two headings claiming one round fail as a recorded duplicate with correspondence skipping (uniqueness now enforced both sides under the existing class, no mirror change). Four legs pin the all-caps claim (round-1 tag misses, round-2 tag matches, inverted pre-fix) plus duplicate-fail and skip; live `validate` stays 0 fatal.

### F9 -- unbounded permutation search hangs the content leg -- integration -- FIXED in `b4eae82b` (independent) [major]

Round-2 I1: the order leg tries distinct permutations of the declared sequences with no count bound, so a long declaration hangs the gate factorially. Fixed by refusing declarations past `_SEQ_MAX_COMMITS = 6` before matching (720 orders max; assemblies are hand-picks with three the largest observed; bigger shapes fence as ranges), with the sole permutation site in the tree swept. One leg pins the refusal on seven fake oids, proving the bound fires before resolution.

### F10 -- OID binding re-resolves instead of pinning persistently -- integration -- DUPLICATE of F3 (independent) [minor]

Round-2 I2: the same no-persistent-pin claim as round-1 C1 (F3), citing the same resolver lines and the same item-5 Done. Answered at F3; no new claim.

### F11 -- bundle delivered unsigned against a signed title -- record -- DUPLICATE of F2 (independent) [minor]

Round-2 R1: the same unsigned-manifest claim as round-1 A2 (F2), citing the same skill sentence and the same item-20 Done. Answered at F2; no new claim.

### F12 -- sweep contradiction across the item-11 and item-17 records -- record -- REFUTED (independent) [minor]

Round-2 R2: item 11 records a clean 38-section sweep while item 17 records 2 reds in the same sweep, so the checkpoint's clean rerun reads unsatisfied. Refuted structurally, in three parts. No contradiction: the item-11 sweep ran before the item-16 membership gate existed, and the item-17 sweep applied the new gate; dated evidence from different gates, and the checkpoint's clean rerun was satisfied by the item-11 run under its gate (commit-bound evidence per the item-7 discipline). The reds are legacy-shaped, not lapses: in both, the attested head is ancestral to the cited oid (quoted `merge-base --is-ancestor` exits), i.e. below-bar-direct post-sign-off fixes on sealed D00 T04 §16 and D00 T04 §18 stamps, a pattern the every-loop re-emission rule supersedes; the fixes were reviewed as round candidates and verified by drive or gate plus the stamp review. And the reds live only in diagnostic sweeps: no gate runs membership over sealed stamps (`check-anchors` runs pre-commit on the stamping section; `validate` never invokes it), so demanding a clean diagnostic mistakes it for a gate. Reopening sealed sections plus parking the downstream closure for record-coverage on reviewed changes is disproportionate; sealed reds grandfather by design like every new gate.

Round-2 transcript (fenced, strips from every scan):

```text
RECEIPT sha=ec6822a4938dc3858d085c556bb1cc8ecd0efd2f95efefadca0c7f718ecdae78 end=PANEL-84352c6071ec0ec3 nonce=4a70d748fe3a3066
**adversarial: needs-attention (2)**
1. `scripts/review_prompt.py:1290` accepts a path-and-line citation when `#hash12` is absent, so a newly authored stamp can bypass the content-binding requirement simply by using the legacy hashless shape.
2. `scripts/review_prompt.py:2184` and `scripts/review_prompt.py:5033` validate every bundled checker transcript only as an arbitrary single `PASS ` line; fabricated plan or stamp transcripts such as `PASS forged` are not replayed against their manifests and still verify.

**consistency: needs-attention (1)**
1. `scripts/todo-validate.py:15` parses panel headings case-insensitively but parses the `Round N` suffix case-sensitively, while `scripts/todo-validate.py:2245` tests only membership in the resulting list and never rejects duplicate recorded round numbers; `## OPUS PANEL ROUND 2` can therefore be interpreted as its positional round, and two persisted `Round 1` headings leave a tag ambiguously “matched.”

**integration: needs-attention (2)**
1. `scripts/review_prompt.py:1428` tries every permutation of declared commit sequences without a commit-count bound, making coverage checking factorial in attacker-controlled manifest size and capable of hanging before producing a verdict.
2. `scripts/todo-validate.py:210` and `scripts/todo-validate.py:326` resolve subjects to OIDs afresh on every validation but persist no resolved OID; `todo/00-workspace/TODO-04-self-correction.md:1029` therefore marks the immutable-OID contract complete without implementing the promised resolved-and-pinned representation.

**record: needs-attention (2)**
1. `todo/00-workspace/TODO-04-self-correction.md:1043` records the “signed portable review bundle” item complete, while `.claude/skills/review-todo-section/SKILL.md:303` explicitly states that no key signs the bundle; the delivered artifact is hash-linked but unsigned.
2. `todo/00-workspace/TODO-04-self-correction.md:1035` records the required 38-section anchor sweep as clean with zero failures, while `todo/00-workspace/TODO-04-self-correction.md:1041` later records two failures in that same full-tree sweep; the section checkpoint requiring a clean rerun is not satisfied at the reviewed head.
```

## Opus panel Round 3

Round 3 over assembled `efdc54e0..b4eae82b` declared [`1eb1d979`, `9900286d`, `f3511316`, `b4eae82b`] (13 files, cross-check `13 file(s) agree, 4 commit(s) covered`). Opus medium, exit 0, latency 109s, clock 2026-09-21T04:30:39Z, cost 2945391tokens. Rundir `/tmp/review-OM6wm5FZ` (`round-3.out` JSON envelope, stderr `round-3.err`), manifest `PANEL-69fc443cf61b52cd` sha `cd89ba21...`. check-panel: PASS four lenses, one verdict each.

`adversarial` approve
`consistency` needs-attention (C1 fixed)
`integration` approve
`record` needs-attention (R1 fixed)

Both findings sit below the blocking bar (no safety, data-integrity, or stamp-invalidating content) and shipped in-loop rather than filing (completion-first: both shippable now). **Corrected:** the first telling claimed no round 4, but fixed findings re-run their lenses under the fix-loop rule (filing is the no-re-round shape, fixing is not), so round 4 verifies below; the round-3 fix commit message carries the stale telling, corrected here.

### F13 -- dead post-cutoff cite fails twice for one defect -- consistency -- FIXED in `2ddbc7d2` (independent) [minor]

Round-3 C1: a dead hashless post-cutoff cite failed dead-lines plus content-unbound (no `continue` separated the branches), against the candidate's own one-defect-one-fault discipline. Fixed by short-circuiting dead spans before the content verdicts, covering the malformed sibling the same shape produced (both doubles observed red pre-fix: `297 cases, 2 failed`). Two legs pin dead-skips-unbound and dead-skips-malformed.

### F14 -- history-silence record names impossible and false consequences -- record -- FIXED in `2ddbc7d2` (independent) [minor]

Round-3 R1: the ticked-branch comment justified soft-case silence with `must not FATAL the suite` while the class registers WARN (verified in `SEVERITY_MAP`: `commit-history-unreadable` is `warn`, so no FATAL is possible on that path), and the item-4 Done claimed the ticked case `warns like an open claim` while soft cases stay silent for ticked and warn for open. Fixed as record: the comment now states the true reason (a shipped claim in a gitless tree is unverifiable rather than unverified, so warning would train skipping; the open claim's unverified state stays visible), and the Done carries the corrected asymmetry. No behavior change: the asymmetry itself stands.

Round-3 transcript (the envelope `result` text verbatim; fenced, strips from every scan):

```text
RECEIPT sha=cd89ba21713508ce735abf3b93f684ed00c7570fa2e443a7ec818c63a642c44a end=PANEL-69fc443cf61b52cd nonce=21487ff116fb5e38

**adversarial: approve**

**consistency: needs-attention (1)**
1. `scripts/review_prompt.py:1311-1331` — a post-cutoff `path:line` cite whose span is dead reports twice for one defect: the dead-lines branch appends at 1313, then `want` is `None` so the `elif bound:` branch appends `cites content-unbound` at 1330 (no `continue` separates them). This candidate elsewhere enforces the opposite discipline for the same class of ambiguity (`scripts/todo-validate.py:2264-2266` skips correspondence on duplicated tags, and 2303-2312 skips it on duplicated recorded rounds, both stating "one defect owns one fault"), so a dead post-cutoff cite yields two failures naming one line while a dead pre-cutoff cite yields one.

**integration: approve**

**record: needs-attention (1)**
1. `scripts/todo-validate.py:613-617` — the ticked-branch comment justifies the `_history_soft()` silence with "hermetic fixture trees must not FATAL the suite that proves this rule", but the class it would raise is `commit-history-unreadable`, registered WARN at `scripts/todo-graph.py:849`, so no FATAL is possible on that path; the recorded reason names a consequence the code cannot produce. Compounding it, the unchecked branch at `scripts/todo-validate.py:656-661` flags the same class with no `_history_soft()` guard, so `todo/00-workspace/TODO-04-self-correction.md:1028`'s claim that the ticked case "now warns `commit-history-unreadable` like an open claim" is false in exactly the soft cases the guard carves out (no git binary, no `.git`), where an open claim warns and a ticked claim stays silent.
```

## Opus panel Round 4

Round 4 over assembled `efdc54e0..2ddbc7d2` declared [`1eb1d979`, `9900286d`, `f3511316`, `b4eae82b`, `2ddbc7d2`] (13 files, cross-check `13 file(s) agree, 5 commit(s) covered`). Opus medium, exit 0, latency 73s, clock 2026-09-21T04:47:14Z, cost 277549tokens. Rundir `/tmp/review-OM6wm5FZ` (`round-4.out` JSON envelope, stderr `round-4.err`), manifest `PANEL-311fcbc9c3aba051` sha `f5c14659...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 fixed)
`consistency` needs-attention (C1 refuted)
`integration` advisory (I1 filed)
`record` advisory (R1 noted, no F-row per the D00 T04 §21 advisory precedent)

### F15 -- blocking stamp transcript verifies as a passing record -- adversarial -- FIXED in `78a0e002` (independent) [major]

Round-4 A1: single-PASS-led admitted the checker's own blocking output (`PASS N naming(s) to answer`, exit 0 by the skill's branch-on-reason design), so a blocking stamp transcript emitted exit 0 and verified `transcripts pass 3/3`. Fixed by holding shapes: a transcript must match one checker's holding output (panel exact, plan alternation, stamp holding-only; the three disjoint, so no role plumbing), with blocking and forged PASS lines refusing at emit and failing at verify, and the parameterized plan shape accepting. Five legs; the item-20 drive re-passes green. Round-2 F7 stands uncontradicted: no replay was built (forged inputs replay clean), only the holding vocabulary narrowed.

### F16 -- history-unreadable class allegedly unregistered and unmirrored -- consistency -- REFUTED (independent) [minor]

Round-4 C1: the class allegedly ships with no `SEVERITY_MAP` entry and no README row. Refuted by inspection: the entry exists at `scripts/todo-graph.py:849` (`commit-history-unreadable: warn`, with the D00 T04 §20 bare-tree comment) and the mirror row at `todo/README.md:443`; the class predates this candidate (D00 T04 §20), which only narrowed its emission. The panel cited a wrong line and a false absence, twice.

### F17 -- oversized assemblies with voided members have no legal path -- integration -- FILED to D00 T04 §26 (independent) [minor]

Round-4 I1 (advisory): past the 6-commit bound the refusal advises fencing the range, but a range re-admits the voided commits declarations exist to exclude (D00 T04 §21 F1), so a 7-plus assembly with a voided span member is unfenceable. Filed: the corner needs a designed path (chunked declarations, smarter matching, or a reasoned wont-fix), not a fix-loop matcher redesign. Lands with the plan-ledger filing.

R1 advisory noted without action: the item-17 Done's `new stamps cannot bypass` overstates beside its own disclosure (`the cutoff date itself escapes by hygiene`). The disclosure stands adjacent (one sentence), rewording a stamping section's contract post-panel costs more than the overclaim, and the D00 T04 §24 stamp demonstrates the rule by example (its path:line cites carry hashes).

Round-4 transcript (the envelope `result` text verbatim; fenced, strips from every scan):

```text
RECEIPT sha=f5c146590df13581d5284ecc8f83c593a2bdd5edbb79e0709395a624a093b55c end=PANEL-311fcbc9c3aba051 nonce=f1f23a57d6420804

**adversarial: needs-attention (1)**
1. `scripts/review_prompt.py:2027` (`_bundle_transcript_ok`), used at `scripts/review_prompt.py:2172` (verify) and `scripts/review_prompt.py:5021` (emit): the tightened predicate accepts any single `PASS `-led line, but `check-stamp` emits `PASS N naming(s) to answer` for the BLOCKING state the skill defines at `.claude/skills/review-todo-section/SKILL.md:257`. A bundle carrying the stamp transcript `PASS 2 naming(s) to answer` emits exit 0 and `bundle-verify` prints `transcripts pass 3/3`, so a blocking stamp result verifies as a passing one; self-review fix 6 closes the injected-PASS shape only, not the checker's own non-holding PASS.

**consistency: needs-attention (1)**
1. `scripts/todo-validate.py:624` registers a new diagnostic class `commit-history-unreadable` with no entry in `SEVERITY_MAP` (`scripts/todo-graph.py:829`, where this same candidate registers its sibling `review-citation-role-mismatch`) and no row in the severity table at `todo/README.md:437` (which documents WARN classes such as `filter-overclaim-stamped`, not only FATAL ones). Every other class this candidate adds is registered and mirrored; this one ships on the implicit default.

**integration: advisory (1)**
1. `scripts/review_prompt.py:1452` (`_SEQ_MAX_COMMITS = 6`) and the refusal at `scripts/review_prompt.py:1515` return `fence the range instead of declaring` for any declaration past six commits, before the multiset legs run. That advice conflicts with the reason declarations exist (D00 T04 §21 F1, restated at `.claude/skills/review-todo-section/SKILL.md:235`: a span may hold voided commits the chunk deliberately excludes). A seven-commit assembly whose span contains a voided commit has no legal path: declaring refuses at the bound, fencing the range re-admits the voided commit.

**record: advisory (1)**
1. `todo/00-workspace/TODO-04-self-correction.md:1041` (item 17 round-2 fix) states "new stamps cannot bypass binding via the legacy shapes" and then "the cutoff date itself escapes by hygiene". The implemented test is `verified_day > CITEHASH_CUTOFF` (`scripts/review_prompt.py:971`, cutoff `2026-09-21`), so every stamp dated today, including the D00 T04 §24 stamp, is exempt from the rule the item ships; the first clause overstates what the second clause discloses.
```

## Opus panel Round 5

Round 5 over assembled `efdc54e0..78a0e002` declared [`1eb1d979`, `9900286d`, `f3511316`, `b4eae82b`, `2ddbc7d2`, `78a0e002`] (13 files, cross-check `13 file(s) agree, 6 commit(s) covered`). Opus medium, exit 0, latency 70s, clock 2026-09-21T04:59:17Z, cost 279584tokens. Rundir `/tmp/review-OM6wm5FZ` (`round-5.out` JSON envelope, stderr `round-5.err`), manifest `PANEL-fc061ca3324ad988` sha `6f296b6c...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 fixed, A2 fixed)
`consistency` needs-attention (C1 fixed)
`integration` needs-attention (I1 fixed)
`record` needs-attention (R1 duplicate)

At the hard cap, all answers land without re-round (D00 T04 §21 precedent): four fixed in `e0d43fc9` (legs plus gates plus the stamp review verify), one duplicate.

### F18 -- count cap sits at the live assembly size -- adversarial -- FIXED in `e0d43fc9` (independent) [major]

Round-5 A1: the F9 count cap refused at seven while this review fences six, so one more fix commit would break the flow, and its range advice fails voided spans. Fixed by bounding work instead of count: declaration order tries first, then distinct permutations up to `_SEQ_MAX_ORDERS = 721` per file (a full 6! sweep), and past the budget the file reports exhausted rather than rearranged, since an untried order may tile it. Seven in declaration order verifies on try one whatever N is; six in any order still verifies. The count-cap leg is replaced by the attempt trio (two-line parts: single-line parts tile under every order, so only intra-commit disorder exhausts). The F17 filing narrows accordingly (residual: non-declaration-ordered mega-assemblies).

### F19 -- bank newest reads last-listed, not newest -- adversarial -- FIXED in `e0d43fc9` (independent) [major]

Round-5 A2: `newest = comps[-1]["dir"]` let a hand-appended out-of-order bank line pose as newest, so `since` under-counted and the leg read fresh on overdue calibration, the exact silent delay the item exists to end. Fixed by newest-by-directory (`max`); the undated branch stays conservative. One leg pins an out-of-order bank (newer leads, single section since); the existing single-entry legs never distinguished.

### F20 -- in-flight suite total survives in item 12 against item 7 -- consistency -- FIXED in `e0d43fc9` (independent) [minor]

Round-5 C1: item 12's Done carried `was 225`, an absolute in-flight total the item-7 discipline (stated two lines above) forbids, stale at 302. Fixed by stripping it to the delta; the family sweep across D00 T04 §24's Dones finds no other absolute totals (the `+N` compositions are deltas, which the discipline keeps).

### F21 -- holding patterns unwitnessed against checker wording -- integration -- FIXED in `e0d43fc9` (independent) [minor]

Round-5 I1: the plan and stamp holding patterns duplicated checker reason strings with hand-written legs restating the regex, so a reason drift would break the emit while every suite stayed green. Fixed by witnessing: real `check-plan` and `check-stamp` stdout pipe into the transcripts (a drift moves the transcript and fails the pattern), plus the first stamp accepting leg. The hand-written plan leg stays for parameterized-count generality.

### F22 -- sizing observation stale at the live assembly size -- record -- DUPLICATE of F18 (independent) [minor]

Round-5 R1: item 12's `three observed largest` rationale postdates the 6-commit candidate. Answered at F18, whose fix replaces the count cap and its rationale; the F9 sentence stands as dated history (true at the 3-commit round 2), corrected by the F18 note beside it.

Round-5 transcript (the envelope `result` text verbatim; fenced, strips from every scan):

```text
RECEIPT sha=6f296b6c38b05c5ef5d70d02a35ca67d712102519695f108075ffc3388da5a04 end=PANEL-fc061ca3324ad988 nonce=d6446027a40c0c59

**adversarial: needs-attention (2)**
1. scripts/review_prompt.py:1455 — `_SEQ_MAX_COMMITS = 6` refuses the whole content leg (`check_commits_covered` returns the bound failure before any coverage is computed) for any declaration of 7+ commits, and the bound sits at the live assembly size rather than above it: this very review's manifest declares 6 commits (`1eb1d979|9900286d|f3511316|b4eae82b|2ddbc7d2|78a0e002`). One further round-fix commit makes the next assembled cross-check and `attest` refuse with `fence the range instead of declaring`, which is not an available remedy for this flow, since the declaration exists precisely because the base..head span may carry voided commits (D00 T04 §21 F1). The DoS the bound answers is bounded by 6!·n, not by the declaration count alone.
2. scripts/todo-runs.py:723 — `newest = comps[-1]["dir"]` takes the last-listed bank entry, not the newest, while the emitted line reads `newest banked <dir>` and `since` counts only panel runs with `run.date > newest_date`. A bank whose final line records an older comparison (hand-appended out of order, or a re-banked earlier crossover) reports a later date than truly banked, so `since` under-counts and the leg reads `fresh` while the calibration is overdue: the exact silent delay the item exists to end. All four legs (`legs-bank-fresh`/`stale`/`empty`/`undated`) use single-entry or empty banks, so no leg distinguishes newest from last-listed.

**consistency: needs-attention (1)**
1. todo/00-workspace/TODO-04-self-correction.md:1036 — item 12's Done carries `(+9 cases; was 225; ...)`, an absolute in-flight suite total, while item 7's Done two lines above claims `items 1-6 and 9-23 Dones converted to delta form (in-flight suite totals stripped, +N deltas kept)` and item 7's own contract text forbids suite totals in Dones. The figure is also stale (the suite reads 302 at head), which is the failure mode item 7 was filed against; the section therefore states its discipline and violates it in the same checklist.

**integration: needs-attention (1)**
1. scripts/review_prompt.py:2101 — `_BUNDLE_HOLDING_RES` hardcodes the plan checker's two reason strings and the stamp checker's holding line as literals duplicated from `check_plan_output`/`check_stamp_output`, and no leg binds them to those functions: `bundle-plan-transcript-accepts` (scripts/review_prompt.py:4581) feeds a hand-written `PASS 24 findings, one per line` that is the regex restated, and the stamp shape has no accepting leg at all. Only the panel shape is witnessed (`bundle-fixture-panel-passes` pipes real `check-panel` stdout into `transcript-1.txt`). If either checker's reason wording drifts by a word (`finding(s)`, a trailing count clause), the skill's post-push bundle fence refuses at emit with `is not a holding PASS line` on a genuinely holding transcript, and every suite stays green.

**record: needs-attention (1)**
1. todo/00-workspace/TODO-04-self-correction.md:1036 — item 12's round-2 rationale states `assemblies are hand-picks (three observed largest)`, offered as the justification for the 6-commit bound. The candidate this Done ships in is itself a 6-commit assembly, so the recorded observation is false at the moment it is written, and the sizing argument it supports rests on it.
```

## Plan review

Manifest: sections [D00 T04 §24, D00 T04 §19, D00 T04 §20, D00 T04 §21, D00 T04 §22]; dependents [none]; bytes 121659; run 20260921-D00-T04-S24-gpt

Round over the section plus its Depends neighbors (D00 T04 §§20-22) and XREF neighbor (D00 T04 §19); no review dependents (no section Depends On or XREFs to D00 T04 §24; the D00 T04 §25 opened-beside wording is intro prose, not an edge). Sol high, exit 0, latency 111s, clock 2026-09-21T04:40:05Z, cost 47025tokens. Rundir `/tmp/review-OM6wm5FZ` (`plan.out`, stderr `plan.err`), manifest `PLAN-725a89c78bf942aa` sha `7816cf18...`. check-plan: PASS 24 findings, one per line. The 121659-byte prompt doubles as further stdin-transport evidence (2.5x the item-8 bar), observed after the item-8 probe ticked.

Ledger:

- [D00-T04-S24-PR1] [major] span exclusions lack an independent omission check -> filed D00 T04 §26 (exclusion-ledger item)
- [D00-T04-S24-PR2] [minor] runner identity inferred from output shape -> rejected no binding point exists (ephemeral bytes); the field is descriptive and the witness hash covers the bytes
- [D00-T04-S24-PR3] [minor] run clock caller-authored through --run-clock -> rejected all inputs are session-supplied including latency; the envelope clock is the recorded direction when a runner reports one
- [D00-T04-S24-PR4] [major] trusted pins unenforced on approval artifacts -> filed D00 T04 §26 (sign-off provenance item)
- [D00-T04-S24-PR5] [major] recorded rounds lack candidate OIDs -> filed D00 T04 §26 (round-ledger item)
- [D00-T04-S24-PR6] [minor] membership should allow range plus recorded superseded list -> duplicate D00-T04-S24-PR5 (same work; the ledger's non-tip candidates are the recorded superseded set)
- [D00-T04-S24-PR7] [minor] first-wins attestation masks conflicts -> filed D00 T04 §26 (single-attestation item)
- [D00-T04-S24-PR8] [minor] undated stamps escape hash and attestation binding -> filed D00 T04 §26 (closes-or-proves item)
- [D00-T04-S24-PR9] [minor] 12-hex digest provides 48-bit collision resistance -> rejected second-preimage needs 2^48 work (infeasible); birthday collisions do not transfer to chosen cited lines
- [D00-T04-S24-PR10] [minor] bundle title overclaims signing -> filed D00 T04 §26 (rename item in live prose; the signing half duplicates D00-T04-S24-F2)
- [D00-T04-S24-PR11] [minor] internal hashes do not stop wholesale rewriting -> duplicate D00-T04-S24-F2 (answered there; recorded closers are the digest quote plus recheck-remote)
- [D00-T04-S24-PR12] [minor] second-machine proof shares its host -> filed D00 T04 §26 (separate-machine item, runs when independent hardware or CI exists)
- [D00-T04-S24-PR13] [major] bundle carries no approval chain -> filed D00 T04 §26 (chain item, premium win)
- [D00-T04-S24-PR14] [minor] drives live in %TEMP%, uncommitted -> rejected committed legs are the durable proof; drives are deterministic one-shots and project rules place throwaway in TEMP
- [D00-T04-S24-PR15] [minor] section contradicts its own sizing rationale -> rejected untimely against a stamping section; the split decision was made at filing with D00 T04 §25 created
- [D00-T04-S24-PR16] [minor] fix-commit exception reads nowhere -> filed D00 T04 §26 (exception-prose item)
- [D00-T04-S24-PR17] [minor] item-7 totals stale after review fixes -> rejected no plan gap; deltas compose, commit bodies re-quote, and the stamp quotes final totals
- [D00-T04-S24-PR18] [minor] trigger-watch filing side effect unproven -> rejected filing is session procedure via add-todo, not a tool call; no executable side effect exists to fixture
- [D00-T04-S24-PR19] [minor] D00 T04 §20 checkpoint wording overstates fenced blocks -> rejected sealed text; audit stance forbids editing stamped checklists for wording and the Done records the correction
- [D00-T04-S24-PR20] [minor] retry-owed lacks an executable owner -> rejected tracked in plan-health with owner, due, and overdue escalation; a TODO section per retry is section-spam
- [D00-T04-S24-PR21] [minor] usage errors stay text under --format json -> filed D00 T04 §25 (format-governed usage item)
- [D00-T04-S24-PR22] [minor] interleaved report is not machine-readable -> filed D00 T04 §25 (report-document item)
- [D00-T04-S24-PR23] [minor] ticked-Commit silence needs a disposition and a sign-off block -> rejected the [UNVERIFIED Commit] disposition exists and the stamp flow independently requires git via anchors
- [D00-T04-S24-PR24] [minor] no aggregate gate covers all items -> rejected check-all.ps1 is the committed aggregate; drives are one-shot color with outputs quoted in Dones

End of ledger.

24 findings: 11 filed (D00 T04 §26 PR1, PR4, PR5, PR7, PR8, PR10, PR12, PR13, PR16; D00 T04 §25 PR21, PR22), 2 duplicate (PR6 of PR5, PR11 of F2), 11 rejected with reasons above. No corrections pre-stamp (the candidate was frozen through panel rounds; plan corrections would have reopened it). Filing commit `b38deef9`.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=7816cf18c4c9ea44cea14058c4f7fa392311f03cd6ed9c211d37636cacc9525b end=PLAN-725a89c78bf942aa nonce=94ffbef09489c49e
- Assembly completeness remains unowned in D00 T04 §24 item 12: proving every declared commit lies inside the pair cannot detect a caller omitting an intended in-span commit, so exclusions need an independently derived list or explicit, reviewed exclusion ledger.
- Runner identity remains unowned in D00 T04 §24 item 14: the Done explicitly leaves reviewer identity inferred from output shape even though the item promises identity beyond mutable provenance, allowing another producer to mimic the accepted shape.
- The run clock in D00 T04 §24 item 14 is still caller-authored through `--run-clock`; bind signed runner start/end values or cross-check wall time with monotonic latency, because an arbitrary timestamp can currently be attested.
- Model provenance in D00 T04 §24 item 13 permits caller-controlled `trusted` pins in attestations and bundles; reserve trusted pins for non-sign-off runs and require derived identity for approval artifacts so labeling does not become authorization.
- Round-role correspondence is incomplete in D00 T04 §24 item 18: checking that round N exists and is unique does not prove the tagged OID is the candidate recorded for round N, so the persisted round ledger must bind each round number to its exact candidate OID.
- Candidate-anchor membership is overbroad in D00 T04 §24 item 16: ancestor-or-self of head admits commits before the attested base, so allow only the reviewed range plus explicitly recorded superseded fix-loop candidates.
- Attestation selection is ambiguous in D00 T04 §24 item 16 because “first wins” permits multiple `Attestation:` references; require exactly one authoritative attestation per stamp to prevent a stale first reference masking a conflicting later one.
- The post-cutoff citation rules in D00 T04 §24 item 17 allow undated stamps to skip hash and attestation binding; require a valid stamp date or treat missing dates as post-cutoff, otherwise new evidence can deliberately use the legacy escape.
- The 12-hex content digest in D00 T04 §24 item 17 provides only 48 bits of collision resistance; store the full SHA-256, or at least a substantially longer digest, because premium audit evidence should not trade integrity for a few characters.
- D00 T04 §24 item 20 calls the artifact “signed” while its Done explicitly says no key signs it; rename it an unsigned integrity bundle or add a real signature and signer verification so the product does not overclaim authenticity.
- Internal hashes in the D00 T04 §24 bundle do not make it tamper-evident against wholesale rewriting; bind the bundle digest to a trusted external receipt, signature, transparency log, or remote release record because a self-consistent forged bundle currently verifies.
- The “second-machine” proof in D00 T04 §24 item 20 only copies files into WSL on the same host; run verification in an independent clean VM or CI worker to prove portability across a genuinely separate environment.
- The bundle in D00 T04 §24 item 20 carries only the sign-off round’s artifacts; include every review attempt, candidate transition, fix disposition, and superseded attestation so one portable artifact can reconstruct the full approval chain.
- Numerous D00 T04 §24 proofs rely on `%TEMP%/s24-*-drive.py` scripts described as re-runnable but not committed; move the drives and bounded outputs into tracked tests or review evidence so later auditors can reproduce the claimed behavior.
- D00 T04 §24 contradicts its own section-sizing rationale: it contains twenty-three hardening items while claiming D00 T04 §25 was opened to avoid exceeding the cap; split the section by attestation, provider, transport, and bundle concerns so failures and ownership stay reviewable.
- D00 T04 §24’s commit item admits the implementation tick slipped and later fixes landed in additional commits, conflicting with the repository’s one-section-one-commit contract; either enforce one candidate commit before review or formally define review-fix commits as an allowed, mechanically bounded exception.
- D00 T04 §24 item 7 records suite totals at `1eb1d979`, but later review fixes add cases; require fresh totals on the final stamped candidate or report only stable per-item deltas, because implementation-commit counts are not final-section evidence.
- D00 T04 §20’s trigger-watch checkpoint requires a fired leg to file an early revisit, but its Done only tests printed FIRED/ARMED text without exercising creation of the owner task; add a fixture proving the filing side effect and reciprocal references.
- D00 T04 §20 still says “all four skill fence blocks” pass while its corrected implementation acknowledges only three fenced invocations plus one unfenced assembled prompt; correct the checkpoint and verification wording so the claimed gate matches the actual surfaces.
- D00 T04 §22 leaves its partial plan-review retry as prose with a person and due date but no executable TODO owner; file a dependency-visible retry section or automated due trigger so the required higher rung cannot silently expire.
- D00 T04 §22 keeps usage errors as text even when `--format json` is requested; make valid format selection govern subsequent argument refusals so CI and IDE consumers never fall back to parsing prose for RUN-001 or FIND-001.
- D00 T04 §§22 and 24 document JSON embedded between prose lines in findings report mode; provide one stable JSON document containing findings and summary metadata, because interleaved output is not reliably machine-readable despite being described as structured.
- Ticked `Commit:` verification in D00 T04 §24 item 4 becomes silent when Git or `.git` is unavailable; expose an explicit unverifiable disposition and prevent sign-off in that state so portable validation cannot report green without checking shipped commit claims.
- D00 T04 §24’s checkpoint lists expected phrases but no single committed command sequence or durable transcript covering all twenty-three items; add a bounded aggregate gate and archive its output so the final stamp proves the complete section rather than a collection of ephemeral demonstrations.
```
