# Review -- D00 T04 §33, CI Repair-Loop Follow-Ups

**Reviewed:** 2026-09-25
**Stance:** post-implementation
**Rounds:** independent pass (3 findings: fixed); GPT 1 bulk (3 fixed); GPT 2 bulk (1 fixed); GPT 3 sign-off (1 filed below the blocking bar, D00 T04 §35).
**Tier:** Full panel. The diff touches `scripts/review_prompt.py` (the review tooling every stamp runs) and two skill files (`review-todo-section`, `process-phase`: the shared run contract).
**Attestation:** `docs/reviews/00-workspace/D00-T04-s33.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `bbb8869c` | implementation: the slow-run ceiling, the cause classifier, the no-log fallback, the workflow-edit wait, the skill rules, the D00 T04 §30 note, and the drill record |
| `78f2ae26` | answers the independent pass: classify the whole log, keep unmapped evidence, override only the selected workflow |
| `d70deba9` | answers panel round 1: an empty log falls through, block commands print as written, drill messages quoted |
| `75dad62c` | answers panel round 2: a run block ends at its key's column |

Record commits (filings only, no reviewed content): `c3c7d42e` (the sign-off finding filed in D00 T04 §35), `bc4150f0` (the plan-review findings filed in D00 T04 §35).

Range `bbb8869c^..75dad62c` (base `dfa79d3a`), contiguous, no declaration owed. Each round fenced the range from `dfa79d3a` to its own head (5 files, cross-check `5 file(s) agree`); the fenced contract was checked to open with this section's heading. The rundir is session scratch outside the tree (/tmp/review-Casv6aAL, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation. The end-to-end drill found a real gap before any reviewer did: a red that started no job (the drill workflow's YAML error) read `cause: unknown`, and now reads repairable, pinned by a leg. Mutations: dropping the workflow-edit rule fails 2 legs by name, disabling the platform classifier fails 1, and each independent-review fix fails its own leg when removed. The architecture gate is not triggered (review tooling and skill text, no framework state). The `source-defect` lens approves in-session: the `gh` behaviors the code relies on were probed live (`gh run list --workflow` resolves names only on the default branch; `gh run view --json jobs` returns `"jobs":[]` for a run whose workflow never loaded). `design` is not owed.

## Independent pass

`python scripts/panel_slots.py exec independent --commit bbb8869cf3de0d87ae5f4f094a771b49a71936c1`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 140s, cost unresolved (the review runner printed no token figure). Output and stderr stay in the session scratch rundir outside the tree (ind33.out, ind33.err); the output is transcribed below.

### F1 -- the cause is judged on the bounded excerpt -- correctness -- FIXED in `78f2ae26` (independent) [major]

The excerpt keeps only signal lines, so a shutdown signal followed by cancellation errors read repairable. `classify_red` now reads every normalized log line.

### F2 -- the full-log step filter can drop every line -- correctness -- FIXED in `78f2ae26` (independent) [minor]

gh labels segments it cannot map `UNKNOWN STEP`; a filter that keeps nothing now keeps the whole log, and an empty full log falls through to the commands.

### F3 -- any workflow edit overrides the selected workflow's filter -- correctness -- FIXED in `78f2ae26` (independent) [major]

An edit to another workflow would wait for a `plan-gates` run that never starts and escalate. The override now fires only for the selected workflow's own file.

```text
The 371 self-tests pass, but the new diagnostics can discard essential failure evidence and bypass their command fallback. The workflow-change override also escalates pushes that correctly trigger no selected workflow.

Full review comments:

- [P2] Classify the complete log before reducing it to an excerpt — R:\GitHub\Resolute\scripts\review_prompt.py:1334-1337
  `summarize_failed_log()` discards non-signal lines whenever an error line exists and retains only the last 20 selected lines. Consequently, a log containing `The runner has received a shutdown signal.` followed by `##[error]The operation was canceled.` is classified as repository-repairable because the shutdown evidence disappears. Classify the complete normalized log independently of the bounded display excerpt, including in the full-log fallback, so infrastructure failures escalate instead of initiating unnecessary repairs.

- [P2] Continue the fallback when filtering removes all log evidence — R:\GitHub\Resolute\scripts\review_prompt.py:1342-1346
  When the failed-step log is unavailable, `gh --log` can label log segments `UNKNOWN STEP` because it cannot associate them with step metadata. Filtering those segments against the names from `failed_job_steps()` removes them all, but this branch still returns successfully without reaching the workflow-command fallback. Preserve unmapped evidence or continue to command extraction when the filtered log is empty; otherwise this supported GitHub CLI output leaves the repair runner without either diagnostics or the promised local command.

- [P2] Limit the workflow-edit override to the selected workflow — R:\GitHub\Resolute\scripts\review_prompt.py:6501-6504
  A push that only adds or edits another workflow, such as `.github/workflows/release.yml`, now forces a wait for `plan-gates`. However, the existing `plan.yml` path filter includes only its own workflow file, not `.github/workflows/**`, so that push legitimately starts no `plan-gates` run. The command consequently waits through both timeouts and escalates instead of reporting `not triggered`. Restrict this override to `wf_path`; unrelated workflow changes should still use the selected workflow's path filter.
```

## GPT panel Round 1

Round 1 over `bbb8869c^..78f2ae26` with the independent pass appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 51s, clock 2026-09-25T17:01:13Z, cost 29843tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-1.out, panel-1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F4 -- an empty failed-step log skips both fallbacks -- adversarial -- FIXED in `d70deba9` (independent) [minor]

A `--log-failed` call that succeeds with no lines returned an unknown cause at once; it now falls through like a missing log.

### F5 -- block commands lose indentation and join with && -- integration -- FIXED in `d70deba9` (independent) [minor]

The re-run text stripped every block line and joined them with `&&`, breaking Python blocks, conditionals, and heredocs. Blocks now print as written, dedented by their own indentation.

### F6 -- the drill record does not quote its repair commit messages -- record -- FIXED in `d70deba9` (independent) [minor]

The owner checkpoint asks for the repair commit's message quoted; the run file now quotes all three drill messages.

```text
RECEIPT sha=5ffa8bd5ec71b1bfed0acf06c685f495d3a848ec2827bd31a27c2b78b8e1a0a1 end=PANEL-cf332f770eceea8a nonce=b1af8212d56286b8
**adversarial: needs-attention** (1)
1. scripts/review_prompt.py:1349: A successful `--log-failed` call with empty output returns immediately with an unknown cause, bypassing both the full-log and workflow-command fallbacks. Require usable evidence before returning, as the full-log branch already does.
**consistency: approve**
**integration: needs-attention** (1)
1. scripts/review_prompt.py:1262: Workflow command extraction strips every block line's indentation, destroying Python blocks and other indentation-sensitive commands; the subsequent newline-to-`&&` rendering also breaks shell conditionals and heredocs. Preserve the script's relative indentation and newlines so the advertised local rerun reproduces the failing command.
**record: needs-attention** (1)
1. docs/phase-runs/2026-09-25-phase-0.md:46: The record asserts that repair commit messages name D00 T04 §33, but neither repair entry at lines 49 and 50 quotes its commit message. The owner checkpoint explicitly requires that quotation; hashes and descriptions of the repairs do not supply it.
```

## GPT panel Round 2

Round 2 over `bbb8869c^..d70deba9` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 32s, clock 2026-09-25T17:05:27Z, cost 30352tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-2.out, panel-2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` needs-attention
`record` approve

### F7 -- a dash-led run block swallows its sibling keys -- integration -- FIXED in `75dad62c` (independent) [minor]

For `- run: |` the block's indentation was measured before the dash, so `shell:` or `env:` became script. The block now ends at its own key's column.

```text
RECEIPT sha=4e3c6d73f8d72cc307e36c29a7102873e5977550988ad1ef413499bf4177afe3 end=PANEL-fccb9688277893d3 nonce=75b5e65163649e4f
**adversarial: approve**
**consistency: approve**
**integration: needs-attention** (1)
1. scripts/review_prompt.py:1260: For a step beginning `- run: |`, block extraction measures indentation before the list dash, so subsequent sibling properties such as `shell: bash` or `env:` are consumed as script content. Their indentation also changes the dedent calculation. Stop at the mapping key’s indentation and add a fixture with `- run: |` followed by `shell:`; otherwise the advertised local rerun includes commands GitHub never executed.
**record: approve**
```

## GPT panel Round 3

Round 3 over `bbb8869c^..75dad62c`, the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 48s, clock 2026-09-25T17:07:58Z, cost 31399tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-3.out, panel-3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` needs-attention
`record` approve

### F8 -- the command fallback keeps YAML scalar quoting and folding -- integration -- FILED to `D00 T04 §35` (independent) [minor]

Quoted inline `run:` commands keep their quotes and folded `>` blocks keep their newlines, so the printed re-run command can differ from what GitHub runs. Below the blocking bar (a diagnostic's display, no safety, data, or stamp effect), and `workflow_step_commands` was patched in rounds 1, 2, and 3 running, so the three-round rule stops the patching: filed in `c3c7d42e` for a rethink, and D00 T04 §33 carries a dated limitation note. No round 4 runs.

```text
RECEIPT sha=ae25cf5527cd397b6d51b1c3b0ddab975bfb4f7764494cd9c9c6a694d1863261 end=PANEL-647290ef5d76ca08 nonce=ad7180fd9e3e7699
**adversarial: approve**
**consistency: approve**
**integration: needs-attention** (1)
1. scripts/review_prompt.py:1254,1255,1274: Command extraction does not preserve YAML scalar semantics. Quoted inline commands retain their enclosing quotes, so Bash treats `"python3 scripts/check.py"` as one executable name; folded `>` blocks retain newlines instead of folding them, splitting a multiline command into separate commands. Decode these supported scalar forms and add fixtures so the no-log fallback reproduces the command GitHub executes.
**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §33, D00 T04 §31, D00 T04 §35]; dependents [D00 T04 §35]; bytes 21522; run 20260925-D00-T04-S33-astra

`bytes 21522` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency D00 T04 §31 and its review dependent D00 T04 §35. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 34s, clock 2026-09-25T17:11:36Z, cost 13637tokens. Check: `PASS 12 findings, one per line`.

Ledger:
- [D00-T04-S33-PR1] [minor] D00 T04 §31 still states the per-red bound and step-name escalation -> filed D00 T04 §35
- [D00-T04-S33-PR2] [minor] a run pending past the ceiling has no outcome of its own -> filed D00 T04 §35
- [D00-T04-S33-PR3] [minor] a zero-job red reads repairable without stated evidence rules -> filed D00 T04 §35
- [D00-T04-S33-PR4] [minor] mixed repository and platform evidence has no stated precedence -> filed D00 T04 §35
- [D00-T04-S33-PR5] [minor] a decoded command lacks its run context -> filed D00 T04 §35
- [D00-T04-S33-PR6] [minor] the decoder is proven only against self-authored expectations -> filed D00 T04 §35
- [D00-T04-S33-PR7] [minor] the supported YAML subset is unstated -> filed D00 T04 §35
- [D00-T04-S33-PR8] [minor] an older stamped owner is not re-stamped -> rejected both skills already state that a reopened section is repaired, re-reviewed, and re-stamped before anything continues, for every reopened owner
- [D00-T04-S33-PR9] [minor] the episode count does not survive a restart -> filed D00 T04 §35
- [D00-T04-S33-PR10] [minor] the drill left bound exhaustion and a stamped-owner repair unexercised -> filed D00 T04 §35
- [D00-T04-S33-PR11] [minor] an authorized workflow retirement always escalates -> filed D00 T04 §35
- [D00-T04-S33-PR12] [minor] unsupported command forms print a guessed command until D00 T04 §35 ships -> filed D00 T04 §35
End of ledger

12 findings: 11 filed in D00 T04 §35 (PR1-PR7, PR9-PR12), 1 rejected with its reason. Filings landed in `bc4150f0`.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=014004fb35a76cffbdf2e723c49306e322472236a1aaf6f4fe24424792325c65 end=PLAN-ece5df7fdd17c9e3 nonce=a1a20d43984e07ed
- §31 still specifies a per-red bound and escalation for setup failures and pending runs, contradicting §33; add dated supersession notes beside those rules in §31 while preserving its stamp, so runners encounter one current contract.
- §33 gives pending runs a 3600-second ceiling but no outcome when that ceiling expires; own a distinct pending-timeout outcome and fixture in §33 so a healthy slow run is neither mislabeled unreachable nor waited on indefinitely.
- §33 treats a run with no jobs as a repairable workflow defect without requiring diagnostic evidence; add an unknown-cause outcome and fixtures for missing job metadata and ambiguous failures, preventing speculative repository repairs.
- §33 classifies platform faults from all log lines without specifying precedence when repository errors and platform signals coexist; define evidence precedence and mixed-cause fixtures there so incidental outage text cannot suppress a repairable failure.
- §35 decodes command text but does not account for shell, working directory, environment, matrix values, expressions, or prerequisite steps; own reproduction context and an explicit cannot-reproduce outcome there because a correctly decoded command can still execute differently or unsafely locally.
- §35 requires decoding “the way GitHub does” but validates only against locally authored fixture expectations; add authoritative expected outputs or a disposable workflow comparison there to avoid proving the parser against its own assumptions.
- §35 ambiguously applies `-`/`+` chomping to every scalar form and leaves indentation indicators unspecified; define the supported YAML subset and refusal boundary there so the checkpoint is executable without another series of special-case patches.
- §33 requires every repair to have an owner but does not explicitly require audit, re-review, and re-stamping when that owner is an older stamped section; extend §31’s just-stamped-section lifecycle to every reopened owner so existing verification cannot survive changed code.
- §33’s episode cap lacks a durable episode identifier and restart/resume rules, while §31 requires re-entry through the push block; define persisted attempt accounting across commits, owners, and nested invocations so restarting the runner cannot reset the three-attempt limit.
- §33’s drill proves two repairs followed by green but deliberately avoids reopening a stamped section; add evidence for bound exhaustion and a stamped-owner repair with candidate replacement and re-stamping, covering the loop’s most consequential control paths.
- §33 forces a run whenever the selected workflow changes, including intentional removal or trigger changes that prevent that run; own an explicit workflow retirement or replacement verification path so an authorized configuration change does not inevitably escalate.
- §33 marks command fallback complete while §35 records known cases that produce incorrect rerun instructions; add a dated limitation to §33 and require unsupported forms to fail closed until §35 ships, preventing deferred correctness work from presenting guessed commands as verified guidance.
```

## Live proof

Section gates driven on the working tree at `356b6ab2` (the implementation `bbb8869c`, every review answer through `75dad62c`, the D00 T04 §35 filings, and the recorded findings and run records) with this stamp's own records added, so the record totals count this section. The candidate names `356b6ab2` because a staged stamp tree cannot name itself. The drill's live `ci-wait` lines ride the run file, quoted in the Verified line.

- `pwsh -NoProfile -File scripts/check-all.ps1`: `check-all: 19 gate(s) ok`

Provenance: candidate 356b6ab2eea90883fc2120d0af8ee9a855f4f666; command pwsh -NoProfile -File scripts/check-all.ps1; exit 0; tool PowerShell 7.6.6; digest 578bacc36aaa7c307ce1cf6e0a2db5eb8ac4af760728f5197eea32342ff85908; path docs/reviews/00-workspace/D00-T04-s33.md; run 20260925-D00-T04-S33-astra

- `python scripts/review_prompt.py --self-test`: `review-prompt self-test: 376 cases, 0 failed`

Provenance: candidate 356b6ab2eea90883fc2120d0af8ee9a855f4f666; command python scripts/review_prompt.py --self-test; exit 0; tool CPython 3.14.4; digest c1630939ce25c39d5050c339212078d7946447284fd55a107b45de564476764c; path docs/reviews/00-workspace/D00-T04-s33.md; run 20260925-D00-T04-S33-astra

- `python scripts/panel_slots.py --self-test`: `panel_slots self-test: 37 cases, 0 failed`

Provenance: candidate 356b6ab2eea90883fc2120d0af8ee9a855f4f666; command python scripts/panel_slots.py --self-test; exit 0; tool CPython 3.14.4; digest 49036cade8aac3da3f68d772627290fdb2505c69e41dc99d5a50b39fae454b26; path docs/reviews/00-workspace/D00-T04-s33.md; run 20260925-D00-T04-S33-astra

- `python scripts/todo-runs.py --check`: `45 runs, 127 rounds: all resolve, all covered, counts agree`

Provenance: candidate 356b6ab2eea90883fc2120d0af8ee9a855f4f666; command python scripts/todo-runs.py --check; exit 0; tool CPython 3.14.4; digest 2ff84ee9a15e50da195535e781bec82ce965538538eb15d27b1a8e88e37121fd; path docs/reviews/00-workspace/D00-T04-s33.md; run 20260925-D00-T04-S33-astra

- `python scripts/todo-graph.py validate`: `30 todos, 229 sections -- 0 fatal, 0 warning(s), 98 adjacency advisory`

Provenance: candidate 356b6ab2eea90883fc2120d0af8ee9a855f4f666; command python scripts/todo-graph.py validate; exit 0; tool CPython 3.14.4; digest 9214d7a36b0427107ff9552ff8c3358910140ab9ac39fc02a654d8a7a04fa717; path docs/reviews/00-workspace/D00-T04-s33.md; run 20260925-D00-T04-S33-astra

- `python scripts/todo-claims.py`: `todo-claims: 113 claim(s) -- 113 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`

Provenance: candidate 356b6ab2eea90883fc2120d0af8ee9a855f4f666; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest 7c7c67a93758d619e89c0bc7a4f32ed2e20c6033ad8b2e99ae06806ac625b798; path docs/reviews/00-workspace/D00-T04-s33.md; run 20260925-D00-T04-S33-astra

- `python scripts/todo-graph.py plan --check`: `implementation plan is current -- 45/226 sections complete (20%)`

Provenance: candidate 356b6ab2eea90883fc2120d0af8ee9a855f4f666; command python scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.4; digest 28cf97a5d8e67593b3cd46de4275d649a1651a8ec693373c70444c90c8fe2f6d; path docs/reviews/00-workspace/D00-T04-s33.md; run 20260925-D00-T04-S33-astra

- `python scripts/todo-findings.py --check`: `todo-findings: ledger current, 346 finding(s), transitions complete`

Provenance: candidate 356b6ab2eea90883fc2120d0af8ee9a855f4f666; command python scripts/todo-findings.py --check; exit 0; tool CPython 3.14.4; digest e2b355167bceb2aff5f2739bcf62720cc52753ee391a662437f893e43a5dd510; path docs/reviews/00-workspace/D00-T04-s33.md; run 20260925-D00-T04-S33-astra
