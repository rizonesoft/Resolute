# Review -- D00 T04 §36, Campaign Guard Lifecycle Follow-Ups

**Reviewed:** 2026-09-25
**Stance:** post-implementation
**Rounds:** independent pass (2 findings: fixed); GPT 1 bulk (5 fixed); GPT 2 bulk (2 fixed, 1 hardened); GPT 3 sign-off (1 filed below the blocking bar, D00 T04 §38; 1 cleared by this stamp's Live proof).
**Tier:** Full panel. The diff touches the Stop hook every campaign session loads, `scripts/campaign_guard.py` (a gate in `scripts/check-all.ps1`), and two skills (`process-plan`, `process-phase`: the run contract).
**Attestation:** `docs/reviews/00-workspace/D00-T04-s36.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `c9525e42` | implementation: the locked hook state, generation, pending cancellation, reconcile, expiry, health, the acknowledged hook error, the bookkeeping marker, the run binding, the coverage policy, and the drives |
| `17fabe5a` | answers the independent pass: markers carry the run id; `hook-error` reads the fallback log |
| `cae84b78` | answers panel round 1: the hook re-checks the guard under the lock; one file per fallback error; pending cancellations kept as a list; real interleaving and size-bound legs |
| `305e3bd9` | answers panel round 2: the error path re-checks the guard; the heartbeat confirms before clearing a cancellation; the fixture's payload rides a file |

Record commits (filings only, no reviewed content): `400268f7` (the sign-off and plan-review findings filed in D00 T04 §38).

Range `c9525e42^..305e3bd9` (base `15145ef2`), contiguous, no declaration owed. Each round fenced the range from `15145ef2` to its own head; the fenced contract was checked to open with this section's heading. The rundir is session scratch outside the tree (/tmp/review-NfF3f3Ql, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation. The cross-language lock was proven live, not assumed: a Python process holding `msvcrt.locking` makes the PowerShell hook's `FileStream.Lock` wait, and a held lock past the bound fails the hook open into its error file. The drives ran against real `CronCreate` jobs (expiry replacement and a new-session handover, a hook error carried through a malformed guard into a run file), and the live heartbeat was migrated to the new prompt after the section-boundary check read its creation time as unknown. The interleaving legs were re-run three times running to check they are not timing-flaky. The architecture gate is not triggered (review tooling and harness wiring, no framework state). `source-defect` approves in-session: `FileStream.Lock` and `msvcrt.locking` take the same Win32 byte-range lock, proven by the waiting and timeout legs. `design` is not owed.

## Independent pass

`python scripts/panel_slots.py exec independent --commit c9525e4208c90b67cf6bf0aeafbc171487b01847`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 175s, cost unresolved (the review runner printed no token figure). Output and stderr stay in the session scratch rundir outside the tree (ind36.out, ind36.err); the output is transcribed below.

### F1 -- a byte offset breaks the run binding -- correctness -- FIXED in `17fabe5a` (independent) [major]

A park record inserted mid-file sat before the offset, and text inserted above an old marker pushed it past; markers now carry the guard's run id wherever they sit.

### F2 -- fallback-log errors never reach the heartbeat -- correctness -- FIXED in `17fabe5a` (independent) [minor]

`hook-error` read only the state file; it now reads the fallback record too.

```text
The 92-case self-test passes, but targeted fixtures reproduce rejected valid park records and fallback errors invisible to the heartbeat. These leave gaps in the new lifecycle and failure-reporting behavior.

Full review comments:

- [P2] Bind terminal markers to run identity rather than byte position — R:\GitHub\Resolute\scripts\campaign_guard.py:318-321
  The run file is edited within existing sections, not exclusively appended: `process-phase` places the park record under `## Gap audit`. A new `PARKED` line inserted there can precede `run_offset`, causing both the hook and `end --reason park` to ignore it. Reproducing this with the documented template leaves the hook blocking and `end` rejecting the valid park record. Conversely, inserting text before an old marker can move it beyond the offset and incorrectly authorize termination. Use a stable run identity for markers, or enforce an append-only marker protocol across all callers.

- [P2] Consume fallback hook errors in the heartbeat reporting path — R:\GitHub\Resolute\.claude\hooks\campaign-stop.ps1:244-246
  When both lock attempts time out, the hook writes its failure only to this fallback log. However, `hook-error` reads only `claude-campaign-state.json`, and the canonical heartbeat never reads the fallback log. Consequently, the heartbeat receives an empty result and resumes without reporting the failed-open hook. Extend the reporting and acknowledgement path to consume these log entries so lock-contention failures are surfaced rather than silently accumulating.
```

## GPT panel Round 1

Round 1 over `c9525e42^..17fabe5a` with the independent pass appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 41s, clock 2026-09-25T20:42:24Z, cost 45266tokens (latency and clock measured from the rundir file times, the round's prompt copy to its output, because the printed timing line was cut from the session's view). Output and stderr stay in the session scratch rundir outside the tree (panel-1.out, panel-1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` needs-attention
`integration` needs-attention
`record` needs-attention

### F3 -- the hook writes state without re-checking the guard under the lock -- adversarial -- FIXED in `cae84b78` (independent) [major]

A hook that read the old guard and then waited could overwrite a handed-over run's state or recreate an ended run's; it re-reads the guard under the lock now.

### F4 -- fallback-log appends race the drain -- adversarial -- FIXED in `cae84b78` (independent) [minor]

The fallback record is one file per error now, so a drain never races a writer.

### F5 -- the phase transition still deletes state directly -- consistency -- FIXED in `cae84b78` (independent) [minor]

`acquire` resets it under the lock instead.

### F6 -- a later end overwrites an unconfirmed cancellation -- integration -- FIXED in `cae84b78` (independent) [minor]

Pending cancellations are a list now, drained one at a time and surfaced by `reconcile`.

### F7 -- the lock legs never interleave a change and nothing exercises the size bound -- record -- FIXED in `cae84b78` (independent) [minor]

Real interleavings (a handover and an end landing while the hook waits) and a size-bound leg were added.


```text
RECEIPT sha=bbb44bb578b40df1dd12ed0bf6c88de90dff37a79f8a389f3b2a939aeb5b8316 end=PANEL-84f751234273eb34 nonce=7670128bb6c72c51
**adversarial: needs-attention** (2)
1. `.claude/hooks/campaign-stop.ps1:170-171,227-243`: Neither locked state-write path revalidates the guard's ownership or run identity after acquiring the lock. A hook that read the old guard can wait behind a handover or `end`, then overwrite the new run's state or recreate the ended run's state. Locking the write alone does not prevent the contracted lifecycle race.
2. `scripts/campaign_guard.py:479-497` and `.claude/hooks/campaign-stop.ps1:246-247`: Fallback acknowledgement reads and replaces the log while fallback writers append without that lock. An error appended after the acknowledgement's read but before its replacement is silently discarded, violating durable error reporting.
**consistency: needs-attention** (1)
1. `.claude/skills/process-plan/SKILL.md:107`: Phase transition still explicitly deletes `build/claude-campaign-state.json` directly after acquisition. This bypasses the new locked reset protocol and can delete state belonging to an intervening acquisition or handover, despite the section claiming every direct deletion was replaced.
**integration: needs-attention** (1)
1. `scripts/campaign_guard.py:296-302`: Every `end` unconditionally replaces the single pending-cancellation record. If cancellation of job A fails, a subsequent campaign can acquire and end with job B before a heartbeat drains A; B overwrites A's only recovery record. Acquisition/startup must reconcile outstanding cancellation, or pending cancellations must retain multiple jobs.
**record: needs-attention** (1)
1. `scripts/campaign_guard.py:917-947,1194-1220` and `todo/00-workspace/TODO-04-self-correction.md:1462,1468`: Completed checklist items overstate their fixtures. The lock tests hold and release a lock without interleaving a handover or `end`, and coverage tests change only file-count bounds, never exercising `CAMPAIGN_HASH_BYTES`. The contract explicitly requires both lifecycle interleavings and the size boundary; neither is demonstrated by the added tests.
```

## GPT panel Round 2

Round 2 over `c9525e42^..cae84b78` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 44s, clock 2026-09-25T20:50:00Z, cost 43183tokens (latency and clock from the round's printed line). Output and stderr stay in the session scratch rundir outside the tree (panel-2.out, panel-2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F8 -- the hook's error path skips the guard re-check -- adversarial -- FIXED in `305e3bd9` (independent) [minor]

The error path re-checks the guard under the lock and, when it changed, writes the error to a file of its own.

### F9 -- the heartbeat clears a cancellation before CronList confirms it -- integration -- FIXED in `305e3bd9` (independent) [minor]

Both branches confirm with `CronList` first now.

### F10 -- the interleaving fixture calls communicate on a closed stdin -- record -- FIXED in `305e3bd9` (independent) [minor]

The round named a `ValueError`; three consecutive runs had passed on this interpreter, so the finding did not reproduce, but the payload now rides a file and the fragility is gone.


```text
RECEIPT sha=877475c199eb1394d02b1c8baca366cda41275ec57125dda5483df295537051a end=PANEL-4aa603d7a48fff74 nonce=e97c378c62f88e9d
**adversarial: needs-attention** (1)
1. `.claude/hooks/campaign-stop.ps1:244-264`: The exception handler acquires `lock2` but never revalidates guard ownership or run identity before writing state. A handover or `end` during the lock wait can still cause an obsolete hook to overwrite the successor's state or recreate ended state. The normal-path revalidation fixes only one of the two write paths.
**consistency: approve**
**integration: needs-attention** (1)
1. `.claude/skills/process-plan/SKILL.md:92-93`: Canonical heartbeat steps 5 and 6 call `cancel-confirmed` immediately after `CronDelete`, omitting the required `CronList` confirmation. Both branches can erase the pending cancellation without proving the job disappeared, defeating recovery after a failed deletion.
**record: needs-attention** (1)
1. `scripts/campaign_guard.py:1230-1273`: `_interleave` closes `hook.stdin` and subsequently calls `hook.communicate(timeout=60)` without clearing the stream reference. Python attempts to flush that closed stream and raises `ValueError`, aborting the self-test before the interleaving assertions. These fixtures cannot substantiate the claimed lifecycle proof as written.
```

## GPT panel Round 3

Round 3 over `c9525e42^..305e3bd9`, the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 78s, clock 2026-09-25T20:54:28Z, cost 50731tokens (latency and clock measured from the rundir file times, the round's prompt copy to its output, because the printed timing line was cut from the session's view). Output and stderr stay in the session scratch rundir outside the tree (panel-3.out, panel-3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` needs-attention

### F11 -- the error path writes state when the owner was never captured -- adversarial -- FILED to `D00 T04 §38` (independent) [minor]

A hook that failed before capturing ownership skips the re-check. Below the blocking bar (breaker state only: no user data, safety, or stamp effect), and the hook's lock path was patched three rounds running, so the three-round rule files it (`400268f7`). No round 4 runs.

### F12 -- the checkpoint's suite count is not quoted yet -- record -- CLEARED by this stamp's Live proof (independent) [minor]

The candidate is reviewed before its stamp; the stamp's `Live proof` quotes `campaign_guard.py --self-test` with its count, which is what the checkpoint asks.


```text
RECEIPT sha=feffc9c4900546f67446e7a453d99794173daea0995f5d37802b4b61dda0d25b end=PANEL-b1239e0fc6ea6db1 nonce=bc4ba0221ecf398c
**adversarial: needs-attention** (1)
1. `.claude/hooks/campaign-stop.ps1:252-263`: Error-path revalidation is skipped when `$owner` is unset, but `$lock2` still authorizes writing state. A hook that encounters malformed guard JSON before capturing ownership can wait while another process repairs/acquires the guard, then overwrite the successor's state. An unavailable original identity must route the error to its separate fallback file.
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:1474` and `docs/phase-runs/2026-09-25-phase-0.md:120-145`: The checkpoint requires a quoted self-test execution with its suite count, but the candidate records only named test legs and lifecycle drives. No execution result or updated suite count substantiates the final fixtures after the panel fixes.
```

## Plan review

Manifest: sections [D00 T04 §36, D00 T04 §34]; dependents [none]; bytes 30392; run 20260925-D00-T04-S36-astra

`bytes 30392` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency D00 T04 §34; no review dependents. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 46s, clock 2026-09-25T20:55:36Z, cost 19909tokens. Check: `PASS 14 findings, one per line`.

Ledger:
- [D00-T04-S36-PR1] [minor] generation fencing stops at the first whoami -> filed D00 T04 §38
- [D00-T04-S36-PR2] [minor] duplicate-generation jobs are indistinguishable -> filed D00 T04 §38
- [D00-T04-S36-PR3] [minor] a NO GUARD exit can strand a pending cancellation -> filed D00 T04 §38
- [D00-T04-S36-PR4] [minor] cancellation records name no scheduler session -> filed D00 T04 §38
- [D00-T04-S36-PR5] [minor] reconcile trusts the job ids it is handed -> filed D00 T04 §38
- [D00-T04-S36-PR6] [minor] recovery fixtures prove classification, not recovery -> filed D00 T04 §38
- [D00-T04-S36-PR7] [minor] multi-file updates lack a crash-consistency contract -> filed D00 T04 §38
- [D00-T04-S36-PR8] [minor] legacy guards accept any marker -> filed D00 T04 §38
- [D00-T04-S36-PR9] [minor] recorded errors carry no event id or run -> filed D00 T04 §38
- [D00-T04-S36-PR10] [minor] a handover's state preservation is unspecified -> filed D00 T04 §38
- [D00-T04-S36-PR11] [minor] health runs only at stamps -> filed D00 T04 §38
- [D00-T04-S36-PR12] [minor] no driven health repair -> filed D00 T04 §38
- [D00-T04-S36-PR13] [minor] degraded coverage has no runtime signal -> filed D00 T04 §38
- [D00-T04-S36-PR14] [minor] superseded Done notes need a final acceptance contract -> filed D00 T04 §38
End of ledger

14 findings, all filed in D00 T04 §38 (`400268f7`).

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=9a1a932ce54e5ee8ec04f9e41bfa01ddd6120dbc51ee16dfe26b41ac5fdf9c08 end=PLAN-7a2853b4f9a6885d nonce=72bf77dcd215d2e8
- Generation fencing stops at the initial `whoami`; extend §36’s generation item so every heartbeat mutation checks the expected generation and run id under the lock, because replacement between ownership checking and `end`, reset, or acknowledgement could let an obsolete heartbeat alter its successor.
- The generation contract requires matching generation or job identity, but the described `whoami` receives no job id; resolve this in §36 with an explicit identity invariant and duplicate-job fixture, because two jobs carrying the same generation would otherwise be indistinguishable.
- Pending cancellation recovery is not ordered against the heartbeat’s early `NO GUARD` exit; §36 should specify who drains cancellations after `end` removes the guard, because a failed deletion followed by self-exit can leave the pending record without an active recovery owner.
- Cancellation records name jobs without specifying their owning scheduler session; extend §36’s cancellation and handover items with session provenance and cross-session recovery evidence, because a new session must not assume it can list or cancel its predecessor’s jobs.
- Startup reconciliation does not define how supplied heartbeat ids are attributed to this workspace and run; add that scope to §36’s recovery contract, because deleting an apparent orphan must not cancel another campaign’s legitimate job.
- Startup recovery fixtures prove diagnostic classifications rather than completed recovery; strengthen §36 with interruption drives between job creation, acquisition, and run-file recording, because reporting an orphan does not prove recovery leaves exactly one current heartbeat and a consistent run record.
- Guard, state, and pending-cancellation updates span multiple files without an explicit crash-consistency contract; assign atomic publication and interruption fixtures to §36’s lifecycle item, because holding a lock prevents concurrent writes but does not prevent partial state after process termination.
- Legacy guards explicitly accept any terminal marker, weakening the new run-binding guarantee; give §36 a migration requirement that establishes a run id before accepting termination, because an old marker in a reused run file can still release a migrated campaign.
- The fallback error queue lacks explicit run attribution and acknowledgement identity beyond the printed line; extend §36’s error-reporting item with unique event ids and originating run metadata, because identical errors or delayed delivery across handover can acknowledge or report the wrong event.
- Acquisition resets state when handing over or moving a run, but preservation of unacknowledged errors and accumulated stall trips is unspecified; define this in §36’s state lifecycle, because recovery must neither erase failures nor repeatedly grant a stalled campaign a fresh breaker allowance.
- Heartbeat health checks occur only after stamps; extend §36’s health item with a bounded check during long sections and recovery waits, because a missing or expired heartbeat can remain undetected precisely when progress stops producing boundaries.
- The health checkpoint requires a driven repair, while the recorded evidence only shows a healthy job; complete §36 with a deliberately missing heartbeat, replacement, and verified resume, because successful inspection does not demonstrate successful repair.
- Fingerprint degradation is documented but has no required runtime signal; add a visible coverage summary to §36’s fingerprint item, because operators need to distinguish genuine inactivity from edits the configured bounds cannot observe.
- Several §36 `Done when` clauses still describe superseded offset, single-record cancellation, and deletion semantics; add a concise final acceptance contract while preserving §34’s historical evidence, because future reviewers need one unambiguous statement of the behavior they must verify.
```
