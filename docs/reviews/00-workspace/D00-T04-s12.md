# Review -- D00 T04 §12, Prove the Manifest, Not Just Emit It

**Reviewed:** 2026-09-20
**Stance:** post-implementation
**Rounds:** Sol 1-2, Opus sign-off 3, Opus blocking re-round 4; Sol plan review (one re-emit)
**Tier:** Full panel. The diff touches `scripts/review_prompt.py`, shared contract surface every review consumes.

## Candidate

| Commit | What it did |
| --- | --- |
| `20c41c1` | the section built: closing nonce, mandatory base/head, combined-diff refusal, NUL cross-check, attestation with read-back, per-terminator smuggle pins, binary-body bound |
| `b7d8692` | independent-review fixes: `--no-renames` cross-check, tree from candidate, attest identity pinned to the manifest pair |
| `7809a48` (one hunk) | attest file written with `newline="\n"` so the bytes are LF on every platform |
| `1832370` | checkpoint re-drive fix: stdout/stderr UTF-8 at CLI entry (F5) |
| `e9f3156` | self-review fix: attestation schema gates on exact int (F4) |
| `2f0c835` | round 1 answered: cross-check identity bound (F6), attest field semantics (F7) |
| `7971e20` | round 2 answered: case-insensitive title match (F8), stamp-time read-back (F9), checker/timestamp strictness (F10) |
| `42b9742` | round 3 answered: staged-blob read-back (F13) |

Recorded adaptation: the candidate diff is the §12 commits' own patches concatenated (`git show` per commit, `7809a48` filtered to `scripts/review_prompt.py`), because the trunk range between them carries unrelated work (`7809a48`'s other 8 files, `91383c4`, `d1c4f1c`, `633e32b`) and the §8 lesson voids contaminated ranges. The fence carries the span pair (`3ee9e5f..42b9742`); the span cross-check diverges listing only the interleaved unrelated files (11 only-in-git, zero only-in-manifest), and a union proof over the candidate commits' own ranges quotes exactly the manifest's 3 files. The attestation binds the span pair plus the manifest sha (the reviewed bytes); the commit table above plus wording-only `a209c4a` (one clause, outside the span, same 3 files) is the exact set.

## The three questions

**Does it make sense?** Yes. Each item closes the §9/§11 gap it names: the receipt quotes tail-only evidence, the manifest refuses to float free or guess a merge, the file list is proven against git's own NUL list, and the attestation survives the terminal beside the findings file. No widening: OID resolution files to §13, short-form citations stay §13's, and the combined-diff-in-commit-message residual is refused loudly with its workaround stated.

**Is it logical?** Yes. The nonce is minted with the tag, collision-checked against the payload, carried on the END line only, and verified against the session-side TAG line that never ships; the checkers take `--manifest` everywhere the skill fences; the rename scan's terminator set (`--- `, `+++ `, `@@`, binary bodies) matches git's emission order (pair above, hunks below). The emission fix aligns printed bytes with the canonical UTF-8 bytes the manifest hashes, so a re-hash of the received prompt verifies on Windows too.

**Did it break anything else?** No. `review_prompt --self-test` runs 74 green (66 at ship plus 8 fix-loop pins) including every pre-existing rename, grammar, prose-trap, and preamble case; the manifest on git-emitted input is unchanged except the added fields the checkers require; `validate` 0 fatal, `todo-claims` 112 hold, `todo-findings --check` current.

**source-defect:** not owed (no Win32 contract, registry layout, or external behavior at stake). **design:** not a surface. **Research pass:** skipped (pure infrastructure).

## Independent review

`codex review --commit 20c41c1 -c model="gpt-5.6-sol" -c model_reasoning_effort="high"` returned **four findings, all correct, all fixed**. The prior session ran the same command (no record survives; its outcome lives in the item notes and in `b7d8692`); this re-run reproduces its three fixes and independently confirms the schema defect self-review had already fixed.

### F1 -- cross-check rejects valid rename candidates -- correctness -- FIXED in `b7d8692` (independent) [major]

Confirmed: the manifest records both rename sides while default `git diff --name-only` reports only the destination, so every rename candidate diverged. Fixed with `--no-renames`, driven on `68c7ea9a` (38 default vs 68 listed, `cross-check: 68 file(s) agree`).

### F2 -- attested tree comes from the checkout, not the candidate -- record -- FIXED in `b7d8692` (independent) [major]

Confirmed: `--tree $(git rev-parse 'HEAD^{tree}')` records the checkout's tree, self-contradictory once HEAD advances past the candidate. Fixed by resolving `<candidate>^{tree}` in the skill and the drill.

### F3 -- attestation identity floats free of the manifest -- record -- FIXED in `b7d8692` (independent) [major]

Confirmed: `--base`/`--head` were written without comparison, so a read-back could claim a candidate the manifest never covered. Fixed by `check_attest_identity`, pinned by `attest-identity-match/mismatch`, with a mismatched head failing naming both pairs and writing no file.

### F4 -- read-back admits non-integer schemas -- adversarial -- FIXED in `e9f3156` (self) [minor]

Self-review found it first (fixed and committed before the re-run returned); the independent re-run confirmed the same defect on the pre-fix commit. `True == 1` and `1.0 == 1` admitted `{"schema": true}` and `{"schema": 1.0}`, the §14 class in this reader. The schema gates on exact int now, pinned by `attest-schema-not-bool` and `attest-schema-not-float`, driven exit 0 before and 1 after. Hand-crafted attestations only; the writer never emits them.

### F5 -- fence emission crashes on non-cp1252 diffs -- adversarial -- FIXED in `1832370` (self) [major]

The checkpoint re-drive fenced `68c7ea9a` (box-drawing tree diagram) and drew `UnicodeEncodeError: 'charmap' codec can't encode characters in position 7125-7126`, exit 1: stdout follows the console code page while the manifest hashes canonical UTF-8. stdout/stderr are UTF-8 at CLI entry now, pinned by `fence-emits-utf8-under-cp1252` (subprocess with `PYTHONIOENCODING=cp1252`), with the rename fence exit 1 before and 0 after. Any panel, plan, or stamp prompt over such a candidate failed setup before this fix; nothing approved falsely, it only crashed.

## GPT panel Round 1

`adversarial` needs-attention (1)
`consistency` approve
`integration` approve
`record` needs-attention (1)

1. (adversarial) cross-check runs git against caller-supplied base/head without checking the manifest identity. Answered as F6.
2. (record) attest text fields validate as non-blank only, so `banana`/`FAIL`/`t` survive. Answered as F7.

### F6 -- cross-check range floats free of the manifest pair -- adversarial -- FIXED in `2f0c835` (independent) [major]

Confirmed: unlike attest (F3), cross-check never compared its range against the manifest's pair, so a swapped or foreign range with the same file set passed silently (swapped base/head lists identical files). Fixed by sharing `check_manifest_identity` (the F3 helper, generalized with a command name), pinned by `crosscheck-identity-match/mismatch`; a swapped pair fails naming both pairs, the correct pair still agrees. Same defect class as F3, one round later.

### F7 -- attestation text fields carry no semantics -- record -- FIXED in `2f0c835` (independent) [minor]

Confirmed in part, fixed in full for the stable half: verdict gates on approve/needs-attention, checker on a PASS-led line, timestamp on Zulu shape, pinned by `attest-bad-verdict/checker/timestamp` and driven all three refusals plus a valid round-trip at the CLI. Reviewer/model stay non-blank deliberately (open vocabularies: a new runner or model must attest without a code change), stated in the code comment. Session-typo class; the skill's producer path always emits valid values.

```
RECEIPT sha=3ffa31ea308486f0c1758b4505801a88b989ca045eb4dac5761fc693746f6387 end=PANEL-d1c56780e3e4f1d9 nonce=195098eb7d44e899
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:993` runs `git diff` against caller-supplied base/head without checking them against the manifest identity, so a different range with the same file set can pass the cross-check.
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)
1. `scripts/review_prompt.py:576`-`scripts/review_prompt.py:579` validates reviewer, model, verdict, checker, and timestamp only as nonblank strings, so semantically invalid values such as verdict `banana`, checker `FAIL`, or timestamp `t` survive attestation read-back.
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost: 45,897 tokens (codex "tokens used"); latency unresolved.

## GPT panel Round 2

`adversarial` needs-attention (1)
`consistency` needs-attention (1)
`integration` approve
`record` needs-attention (1)

1. (adversarial) The title regex is case-sensitive, so a mistitled diff bypasses identity enforcement. Answered as F8.
2. (consistency) The skill reads the attestation back only at emit, not at stamp time as the section claims. Answered as F9.
3. (record) `startswith("PASS")` admits `PASSIVE` and the timestamp regex admits impossible dates. Answered as F10.

### F8 -- mistitled diffs bypass identity enforcement -- adversarial -- FIXED in `7971e20` (independent) [minor]

Confirmed for the typo class, refuted for novel labels. The title match is case-insensitive now, so `candidate diff` still demands base/head (pinned by `identity-title-case-insensitive`, driven both legs). `CANDIDATE`/`CHANGE` stay outside the contract by design: titles are the skill/tool contract (fixed strings), and content-sniffing would re-admit the prose forgery §9's title gate exists to refuse. Session-typo class, fail-closed in the fixed direction.

### F9 -- stamp-time read-back claimed but not wired -- consistency -- FIXED in `7971e20` (independent) [minor]

Confirmed: item 5 claimed "reads back at stamp time" while the skill read back only at emit. The stamp-review block now reads the attestation back first thing after staging, so an altered file fails before the stamp review runs. An alteration after read-back cannot enter the commit: the committed blob equals the verified staged content, and any re-stage moves the index and re-runs the block. Proven by this stamp running the new command (see Live proof).

### F10 -- checker prefix and timestamp shape admit nonsense -- record -- FIXED in `7971e20` (independent) [minor]

Confirmed: both are round-1-fix looseness. Checker requires the `PASS ` prefix (exact-equality would void old attestations when the message evolves; the prefix captures pass-vs-fail robustly) and timestamp requires regex shape plus a real calendar date via strptime, pinned by `attest-bad-checker-passive` and `attest-bad-timestamp-impossible`, driven both refusals at the CLI. Same session-typo class as F7.

```
RECEIPT sha=84360d17c3ec7ab407272376c201c1fa847688bb4f5e8b6163168a02f40adb54 end=PANEL-8833630662b5fce4 nonce=fdc416226119c964
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:160` classifies candidate content solely through an unanchored, case-sensitive title regex; a real diff fenced under `CANDIDATE`, `CHANGE`, or lowercase `candidate diff` bypasses both base/head enforcement and file extraction, producing the floating candidate manifest this section requires the CLI to reject.
**consistency: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:160` reads the attestation back only immediately after creation, while `todo/00-workspace/TODO-04-self-correction.md:601` claims the skill reads it back at stamp time; a subsequently altered attestation can therefore be staged without the claimed mechanical read-back.
**integration: approve**
**record: needs-attention** (1)
1. `scripts/review_prompt.py:588` accepts non-checker text such as `PASSIVE` because it only tests `startswith("PASS")`, and `scripts/review_prompt.py:591` accepts impossible timestamps such as `2026-99-99T99:99:99Z`; both malformed values survive attestation read-back as valid review evidence.
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost: 49,533 tokens (codex "tokens used"); latency unresolved.

## Opus panel Round 3

Sign-off over the 7-commit candidate.

`adversarial` needs-attention (1)
`consistency` needs-attention (1)
`integration` needs-attention (1)
`record` needs-attention (1)

1. (adversarial) Cross-check verifies file sets, not content coverage; a chunk dropping commits with the same files agrees. Filed as F11.
2. (consistency) The stamp rung demands a receipt nothing verifies. Filed as F12.
3. (integration) The F9 read-back reads the disk file, not the staged blob. Answered as F13.
4. (record) The §20 back-XREF in the contract comes from `633e32b`, outside the fenced candidate. Refuted as F14.

### F11 -- cross-check blind to dropped commits -- adversarial -- FILED to D00 T04 §21 (independent) [minor]

Confirmed as new scope, below the re-round bar: item 4 contracts file SETS, and the designed contiguous flow is construction-safe (the chunk is git's own output). Only hand-assembled non-contiguous candidates can drop commits silently, demonstrated by `633e32b` inside the span yet absent from the chunk with sets agreeing. §21 owns content coverage (comparison, per-commit manifests, or a tool-driven union proof) with this demonstration as its regression case.

### F12 -- stamp receipt demanded but never verified -- consistency -- FILED to D00 T04 §21 (independent) [minor]

Confirmed as new tooling, below the re-round bar: no stamp-output checker ever existed (§9's stamp rung was eyeballed too), and the skill states that honestly. §21 owns a mechanical stamp-output check (receipt plus `STAMP HOLDS`-vs-namings shape). The procedural control (session verifies by eye) stands until then.

### F13 -- stamp read-back verifies the disk file, not the staged blob -- integration -- FIXED in `42b9742` (independent) [major]

Confirmed: the F9 placement reads the working-tree file, so a valid-but-different blob on disk passes read-back while the committed staged blob goes unverified. The stamp flow now extracts `git show :<stem>` past the write-tree fix-point and reads that back; any later re-stage moves the index and restarts the block. Driven both directions through blob bytes (valid exit 0, `banana` exit 1) with the index untouched. This stamp runs the new commands for real (see Live proof). Blocking: the candidate's own binding claim was false as placed.

### F14 -- §20 back-XREF outside the fenced candidate -- record -- REFUTED (independent) [minor]

The candidate is correct to exclude `633e32b`: it is plan churn (the §20 filing), not §12 implementation, and the panel reviews implementation, not the ref block. Bidirectionality is proven mechanically, not from panel evidence: `633e32b` added both directions atomically (`-> XREF: D00 T04 §12` in §20 and `-> XREF: D00 T04 §20` in §12), and `validate` (XREF reciprocity, FATAL on one-sided) reports 0 fatal. The non-contiguous assembly friction is real process friction, already recorded in the Candidate adaptation above.

```
RECEIPT sha=430924ee9c5e45cb341914fb8cdeff365e0b9b7dc9a34ebd12e47d051c9cbfc2 end=PANEL-d45e268a95e67a8a nonce=72c4f78e86d07498

**adversarial: needs-attention** (1)
1. scripts/review_prompt.py:1028-1060 (`cross-check` CLI) and :523-539 (`cross_check_files`) -- the cross-check compares only the *set of path names* between the manifest and `git diff --name-only -z --no-renames <base> <head>`, never the content or the commit coverage of the fenced chunk. A candidate whose fenced body drops entire commits inside `base..head` still agrees, so the check cannot detect the failure it is advertised to catch ("if it persists the chunk is not the candidate it claims", .claude/skills/review-todo-section/SKILL.md:124). This candidate demonstrates it: the fenced CANDIDATE DIFF for base=3ee9e5f..head=7971e20 lists seven commits and omits commit 633e32b ("todo: file review-tooling operability follow-ups in D00 T04 §20"), which is an ancestor of 7971e20 and modifies `todo/00-workspace/TODO-04-self-correction.md`, one of the three manifest files; the file set still agrees, so nothing fails.

**consistency: needs-attention** (1)
1. .claude/skills/review-todo-section/SKILL.md:237 -- the stamp rung's prompt is updated to demand `RECEIPT sha=<sha> end=<tag> nonce=<nonce>`, and SKILL.md:234-235 now saves `$RUNDIR/stamp-manifest.md`, but no checker ever validates the stamp reviewer's output against that manifest (the panel rung runs `check-panel --manifest` at SKILL.md:121 and the plan rung runs `check-plan --manifest` at SKILL.md:198; the stamp rung ends at SKILL.md:238 with the runner invocation and nothing after). The truncation defence this section exists to install is therefore absent on the one rung whose prompt demands it: a truncated or receiptless stamp review is accepted by inspection.

**integration: needs-attention** (1)
1. .claude/skills/review-todo-section/SKILL.md:231 -- `attest --read-back <findings stem>.attest.json` reads the *working-tree* file and runs *before* `TREE=$(git write-tree)` at SKILL.md:232, so it verifies neither the staged blob nor the index identity that the rest of the stamp gate is bound to. An attestation staged with `git add` and then altered on disk, or altered in the index after a good on-disk copy, is not caught; the inline comment's claim ("the staged attestation reads back now ... an altered file fails here, never ships") does not hold. Reading the staged content (e.g. `git show :<path>`) after the `git write-tree` fix-point is what the claim requires.

**record: needs-attention** (1)
1. todo/00-workspace/TODO-04-self-correction.md:608-618 -- the §12 reference block in the section contract carries `-> XREF: D00 T04 §20 -- operability follow-ups from this section's review`, but no hunk in the candidate introduces that line (the candidate adds only `-> XREF: D00 T04 §13` at TODO-04-self-correction.md:617 in commit b7d8692). The line was recorded by commit 633e32b, which lies inside base..head yet is absent from the fenced candidate, so the candidate diff does not account for the section's recorded state and the §20 back-reference cannot be checked for bidirectionality from this evidence.
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost and latency unresolved (no runner telemetry).

## Opus panel Round 4

Blocking re-round over the 8-commit candidate (F13 verification; the fix drew no further criticism).

`adversarial` needs-attention (1)
`consistency` approve
`integration` advisory (1)
`record` advisory (1)

1. (adversarial) The identity parse searches unanchored, so ` base=` inside a path poisons the pair. Filed as F15.
2. (integration) The stamp receipt is eyeballed, not checked. Duplicate of F12, recorded as F16.
3. (record) No attestation exists beside the findings file yet. Closed by emission, recorded as F17.

### F15 -- unanchored identity parse poisoned by paths -- adversarial -- FILED to D00 T04 §21 (independent) [minor]

Confirmed, below the re-round bar: a path or title containing ` base=`/` head=` makes the parse lift the pair from the file list instead of the trailing fields. The failure is LOUD (mismatch, exit 1, never silent acceptance: argv carries the true pair), so this is an availability wart on adversarial filenames, fail-closed. §21 owns the anchored parse with the poisoned-manifest regression case. (Self-review noted the same shape and accepted fail-closed; the panel is right that refusal is still a bug.)

### F16 -- stamp receipt eyeballed, not checked -- integration -- DUPLICATE of D00-T04-S12-F12 (independent) [minor]

Same gap as F12 (filed to §21 item 2), one round later. No new action.

### F17 -- attestation missing beside the findings file -- record -- FIXED by emission (independent) [minor]

Confirmed and closed by the flow's next step: the attestation emits after the final panel round, and `docs/reviews/00-workspace/D00-T04-s12.attest.json` now sits beside this file, read back exit 0 (`schema 1, verdict needs-attention, manifest d6b75a8d9af6..., candidate 3ee9e5f76564...42b9742182d0..., tree ff7477e48ce4..., reviewer claude-panel, model opus`). Item 5's Done-when holds literally: one attestation per review beside the findings file, read back at emit and again at stamp time.

```
RECEIPT sha=d6b75a8d9af674f467a4df0605afbeaa172b43242071fa4d1767c701943851da end=PANEL-f79fba776c4120fc nonce=9b20adf7d2313b24

**adversarial: needs-attention (1)**
1. `scripts/review_prompt.py:617-622` -- `parse_manifest_identity` finds `base=`/`head=` with an unanchored `re.search` over the whole `rest` of the MANIFEST line, which also carries `titles=...` and `diff-files=...`. The sibling parser `parse_manifest_diff_files` (lines 521-535) documents and accepts this hazard only because a truncated file list "fails loudly"; the identity parse has no such property. A changed path (or fenced chunk title) containing the literal text ` base=` or ` head=` makes `check_manifest_identity` compare argv against a value lifted out of the file list instead of the trailing identity fields, so `cross-check` and `attest` can refuse a correct pair, and a crafted path can make the check compare a value the fence never emitted. The fields are always the last two on the line and should be anchored (e.g. `\sbase=(\S+)\s+head=(\S+)\s*$`) rather than searched from the left.

**consistency: approve**

**integration: advisory (1)**
1. `.claude/skills/review-todo-section/SKILL.md:240,244` -- the stamp round's prompt now demands the three-field `RECEIPT sha=... end=... nonce=...`, and line 238 saves `$RUNDIR/stamp-manifest.md`, but no checker consumes that manifest for the receipt: line 244 leaves it as "the session verifies it by eye against the manifest, since no output checker constrains this round". §12's first micro-step states the receipt is "verified against a session-side value"; on the stamp rung that verification does not fail closed, unlike the panel (line 121) and plan (line 199) rungs, which pipe their manifest into `check-panel`/`check-plan`. The saved `stamp-manifest.md` is already the input a mechanical check would need.

**record: advisory (1)**
1. `todo/00-workspace/TODO-04-self-correction.md:601` -- the attestation item's contract is `Done when: one attestation per review exists beside the findings file and the skill reads it back`, but the quoted evidence is a scratch drill (`attest: wrote .../drill.attest.json`), and no `*.attest.json` exists anywhere under `docs/reviews/` in the candidate tree. What is proven is the subcommand's round-trip, not the "exists beside the findings file" half of the Done-when; the checkpoint line ("one attestation reads back") inherits the same gap.
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost and latency unresolved (no runner telemetry).

## Plan review

gpt run over §12 plus §11 and provisional §13: 5 findings, 2 filed (both to §21), 2 rejected, 1 accepted.

First attempt failed its output check (`FAIL line 2 is not a '- ' finding: VERDICT: needs-attention`, exit 1: VERDICT plus numbered list instead of `- ` rows); one re-emit with restated grammar passed (`PASS 5 findings, one per line`, exit 0, 18,249 tokens). Only the checked output is triaged; the unchecked attempt's four findings are superseded, not cherry-picked.

Manifest: sections [D00 T04 §12, D00 T04 §11, D00 T04 §13]; dependents [D00 T04 §20, D00 T04 §21]; bytes 17892; run 20260920-D00-T04-S12-gpt

Ledger:
- [D00-T04-S12-PR1] [major] PATCH-titled chunk evades base/head -> rejected (the regex is `DIFF|PATCH|STAMP`, so PATCH always matched and always demanded base/head; quoted from the tree; the novel-label remainder was answered in F8)
- [D00-T04-S12-PR2] [major] one attestation retains only the closing round -> rejected (the contract promises one attestation covering the stamped bytes; earlier rounds' evidence is their quoted manifests and checks in this file; per-round attestations would bind superseded bytes)
- [D00-T04-S12-PR3] [major] checker string unbound to any run -> filed D00 T04 §21 (attestation provenance binding)
- [D00-T04-S12-PR4] [major] findings file unbound to the attestation -> filed D00 T04 §21 (findings hash in the attestation)
- [D00-T04-S12-PR5] [minor] nonce wording claims the prompt never carries it -> accepted (wording corrected in a209c4a pre-stamp: the END line carries the prompt's only copy, the preamble never does; a filing could never touch the frozen checklist, and no behavior, contract, or checkpoint changed, so no re-round)
End of ledger

```
RECEIPT sha=4e933b0f4115aaa5f87f6f31393995c6ea3a2b3770e7419c5fe17a8badcbec2f end=PLAN-c1870528c01b0608 nonce=36b7c2b355dfd130
- [major] candidate identity is inferred from a case-insensitive `diff` substring in the chunk title, so a candidate chunk titled `PATCH` can be misclassified as a plan manifest and evade the stated requirement that every candidate carry base and head
- [major] the contract requires one attestation per review but explicitly retains only the closing round's attestation; receipts for earlier rounds do not contain the required reviewer, model, verdict, timestamp, candidate tree, or checker fields
- [major] the attestation accepts an explicitly supplied PASS-prefixed checker string and validates only its shape, so it does not prove that the checker actually ran successfully against the attested manifest
- [major] the attestation does not bind the findings artifact by path or hash, allowing the findings file beside it to be replaced without invalidating read-back
- [minor] the closing nonce is described as a session-side value that the prompt never carries, but the nonce is necessarily present in the prompt's END line; the actual property proved is absence from the preamble, not absence from the prompt
```

Plan manifest: 4e933b0f4115 PLAN-c1870528c01b0608. Cost: 18,249 tokens (re-emit; first attempt 21,070 tokens); latency unresolved.

## Live proof

Checkpoint legs, each driven in this review (rundirs under `/tmp/s12drill-*`, `/tmp/s12r1-*`, `/tmp/s12r2-*`, `/tmp/s12f13-*`):

- Old two-field receipt: `FAIL line 1 is not a receipt`, exit 1. Full receipt: `PASS four lenses, one verdict each`, exit 0. Wrong nonce: `FAIL line 1 receipts nonce deadbeefdead..., manifest wants 6c5ecd3d0935...`, exit 1.
- Baseless candidate: `fence: refusing candidate manifest without base and head (diff in: CANDIDATE DIFF; pass --base/--head)`, exit 1. Combined diff: `fence: refusing combined diff (diff --cc) in CANDIDATE DIFF: merge candidates list no files; fence a non-merge range instead`, exit 1.
- Cross-check on `3ee9e5f`: `cross-check: 3 file(s) agree`, exit 0. Tampered manifest: `only in manifest: todo/FORGED-plan.md` plus `only in git: todo/implementation-plan.md`, exit 1. Rename commit `68c7ea9a`: default flags list 38, `--no-renames` lists 68, `cross-check: 68 file(s) agree`, exit 0. Swapped pair: `cross-check: --base/--head (...) do not match the manifest (...)`, exit 1.
- Attestation: `attest: wrote .../drill.attest.json`, exit 0; read-back `attest: schema 1, verdict approve, manifest ..., candidate ..., tree ..., reviewer codex-panel, model gpt-5.6-sol, checker: PASS four lenses, one verdict each`, exit 0. Mismatched head fails naming both pairs with no file written, exit 1. `banana` verdict, `FAIL`-led and `PASSIVE` checkers, `t` and `2026-99-99T99:99:99Z` timestamps, and `{"schema": true}` all fail naming their field, exit 1.
- Emission: the `68c7ea9a` fence crashed pre-fix (`UnicodeEncodeError: 'charmap' codec can't encode characters in position 7125-7126`, exit 1) and fences exit 0 post-fix with the closing `--- END [...] nonce=... ---` intact.
- Self-test: `review-prompt self-test: 74 cases, 0 failed`, covering every item pin (`truncated-receipt-fails`, `wrong-nonce-fails`, `manifest-preamble-lacks-nonce`, `identity-*`, `manifest-combined-refused-*`, `manifest-smuggle-stops-at-*`, `manifest-binary-*`, `attest-*`, `crosscheck-*`, `fence-emits-utf8-under-cp1252`).
- Gates: `validate` 0 fatal; `todo-claims` 112 hold; `todo-findings --check` ledger current, 192 findings, transitions complete. `check-all` final sweep: 12/13 green (build Debug 6.6s and Release 9.2s, tidy 169/169 over 22 TUs, tests, validate, self-tests, plan, claims, profile, toolchain); the findings gate failed only on the pre-`--write` ledger, resolved by `--write` plus the F16 duplicate transition, with `--check` exit 0 after. `validate` and `plan --check` re-verified green after every filing commit.
