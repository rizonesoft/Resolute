# Review -- D00 T04 §11, Bind the Rename Scan to the Diff Header

**Reviewed:** 2026-09-19
**Stance:** post-implementation
**Rounds:** running (Full panel: Sol rounds 1-2, Opus sign-off round 3)
**Tier:** Full panel. The diff touches `scripts/review_prompt.py`, shared contract surface every review consumes.

## Candidate

| Commit | What it did |
| --- | --- |
| `e5da9ae` | the section built: hunk-body stop in `_scan_diff_block`, residual comment and docstring rewritten, `manifest-rename-stops-at-hunk` self-test case |

## The three questions

**Does it make sense?** Yes. Item 1 implements exactly the fix F10 prescribed (stop at the hunk body); item 2 rewrites the two prose surfaces the residual clause lives in. No widening: combined-diff handling stays §12's owned work, and no other rename behavior is touched.

**Is it logical?** Yes. The break sits after the `diff --git` capture (a block opens with its diff line, so it is always found first) and before the rename match. A git header never opens a line with `--- `, `+++ `, or `@@`, and git emits the rename pair above the hunk body, so git-emitted manifests are byte-identical and only pasted post-hunk pairs change behavior. Pure-rename blocks without hunks never hit the break (the pre-existing authoritative-rename case pins that).

**Did it break anything else?** No. The only consumer is `build_manifest`, whose output on git-emitted input is unchanged by construction; the self-test suite passes 30 of 30 including the pre-existing rename, grammar, prose-trap, and preamble cases.

**source-defect:** not owed (no Win32 contract, registry layout, or external behavior at stake). **design:** not a surface. **Research pass:** skipped (pure infrastructure).

## GPT panel Round 1

`adversarial` approve
`consistency` approve
`integration` approve
`record` needs-attention (1)

1. (record) The Done notes claim a quoted probe but record no literal probe output. Answered as F1.

### F1 -- the proof quote records the verdict fragment, not the probe output -- record -- FIXED (independent) [minor]

Half-confirmed: the section did quote `diff-files=real.md` verbatim (the §9 precedent quotes the same way), but the full manifest line is stronger and costs a sentence. Fixed by recording the driven `fence` manifest verbatim with the per-run tag and sha named as such (the tag is random per invocation, so the sha cannot reproduce; bytes, files, titles, and diff-files can).

```
RECEIPT sha=4e636500ee0ab84c08c0ae41815641eff2c5cb22a9fc7f6042ca08c650d7d0cd end=PANEL-349e0ba127d5c6d1
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:573`, `todo/00-workspace/TODO-04-self-correction.md:574`, and `todo/00-workspace/TODO-04-self-correction.md:577` mark the quoted driven fence probe complete but record no literal probe output, so the section lacks its required proof quote.
```

Output check: `PASS four lenses, one verdict each`, exit 0.

## GPT panel Round 2

`adversarial` approve
`consistency` needs-attention (1)
`integration` approve
`record` needs-attention (1)

1. (consistency) The docstring does not name combined diffs as the sole residual, unlike the module comment. Answered as F2.
2. (record) The Done note claims three-way agreement the docstring and quote do not show. Answered as F3.

### F2 -- the docstring omits the sole-residual sentence -- consistency -- FIXED (independent) [minor]

Confirmed: the residual sentence was comment-only. Fixed by adding it to the `_scan_diff_block` docstring verbatim, so comment and docstring state the same residual.

### F3 -- the Done note overclaims agreement -- record -- FIXED (independent) [minor]

Confirmed: item 2's Done claimed comment, docstring, and probe quote agree while the docstring lacked the clause (the probe quote demonstrates the binding, which is its half of the argument). Fixed by the F2 docstring sentence; the Done note now trails the fix.

```
RECEIPT sha=e5a637f704874a6fd9685799b5c0e322bfb781f51f826b64c36a1f1629bde16e end=PANEL-c6b8e4acf75d8c84
**adversarial: approve**
**consistency: needs-attention** (1)
1. `scripts/review_prompt.py:155` does not name combined diffs as the sole residual, unlike the module comment at `scripts/review_prompt.py:138`, so the required comment/docstring agreement is absent.
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:574` claims the module comment, docstring, and driven proof quote agree that combined diffs are the sole residual, but the docstring at `scripts/review_prompt.py:155` and the recorded manifest quote at `todo/00-workspace/TODO-04-self-correction.md:573` contain no such residual clause.
```

Output check: `PASS four lenses, one verdict each`, exit 0.
