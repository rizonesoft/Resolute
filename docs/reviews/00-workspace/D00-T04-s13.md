# Review -- D00 T04 §13, Bind the Stamp to the Push

**Reviewed:** 2026-09-20
**Stance:** post-implementation
**Rounds:** Sol 1-2, Opus sign-off 3, Opus blocking re-rounds 4-5 (hard cap); Sol plan review (genesis, first attempt passed)
**Tier:** Full panel. The diff touches `scripts/review_prompt.py`, `scripts/todo-graph.py`, `scripts/todo-validate.py`, and three review skills: shared contract surface every review consumes.
**Attestation:** `docs/reviews/00-workspace/D00-T04-s13.attest.json` (round-5 manifest and candidate, emitted after the panel closed, read back exit 0)

## Candidate

| Commit | What it did |
| --- | --- |
| `ff90910` | the section built: push-time rechecks, hook-tree comparison, OID resolution, probe record, short-form ban with span guard (self-review), 3 citation expansions |
| `740bbac` | round 1 answered: attest commit-type gates (F1), hermetic fixture hardening (F2), quoted OIDs (F3) |
| `e7a4756` | round 2 answered: `--no-replace-objects` on identity calls (F4), whole hook quote (F5) |
| `397bc2f` | round 3 answered: flag all seventeen skill `rev-parse` calls (F6, F7) |
| `2d0d406` | round 4 answered: flag `git show` x2 and cross-check `git diff` (F8), correct the `show` rationale (F9) |

No range adaptation: the five candidates are contiguous on trunk, each round fencing its own commit (base `HEAD^`) with the prior rounds' verdicts appended as a third chunk (receipt lines stripped, so no stale receipt is copyable). Recorded adaptation: fence and cross-check path arguments ride cygpath-translated Windows paths (MSYS2 converts bare path arguments but not paths inside `TITLE=path`, so Windows python resolves Git Bash `/tmp` to the wrong place); fenced bytes are unaffected (titles stay the labels), and D00 T04 §20 owns the durable fix.

## The three questions

**Does it make sense?** Yes. Each item carries the binding one step further: the push rechecks the commit's parent, tree, and HEAD position; the hook comparison fails closed on tree rewrites; the candidate resolves mechanically before any stamp is accepted; both probe shapes drew namings at high effort; short citation forms are FATAL with the span guard covering what fixed-width lookbehinds cannot. No widening: the fence stays text (resolution at acceptance, not assembly), read-back stays git-free so history reads offline, and cross-check stays existence-only with the staged-tree reason recorded at the call site.

**Is it logical?** Yes. Attest resolves base/head/tree after the shape checks and before the write, so every failure path writes no file; the commit-type gate closes the tree-as-head hole the first round proved; replacement-blindness covers the attest/cross-check reads, with the stamp patch and `:44` self-review reads filed to §21 (F10/F11); the panel ran the skill's pinned models, efforts, and output checks with cross-check agreeing on every round's manifest.

**Did it break anything else?** No. `review-prompt self-test` runs 84 green (78 at implementation plus 6 fix-loop pins) with every pre-existing case intact; `todo-graph self-test` runs 511 green (510 plus the span case); `validate` 0 fatal, `todo-claims` 112 hold, `todo-findings --check` current. `check-all` reports 12/13: the `graph self-test` gate fails on a pre-existing stderr hint (the suite itself exits 0), bisected to reproduce identically at `ef362d6` in its own workdir and filed to D00 T04 §20; every other gate is green (see Live proof).

**source-defect:** not owed (no Win32 contract, registry layout, or external behavior at stake). **design:** not a surface. **Research pass:** skipped (pure infrastructure).

## Self-review

Two fixes before the panel, both in `ff90910`, costing no round. The short-form lookbehinds only excluded single-space full refs, so a tab- or double-space-separated full D-ref (legal per `SKILL_CITE_RE`'s `\s+`) drew a false FATAL: proven failing first (`got True, want False`), fixed with a span guard that skips short matches inside a full ref's span, pinned by `wide-spaced-full-ref-draws-no-FATAL`. The hermetic-fixture comment claimed the cases pass "on any machine" while the fixture shells git: corrected to state the git requirement the CLI paths impose throughout. Checked and cleared: attest/cross-check resolution order (failures write nothing), short-OID semantics (loud mismatch, never silent), commit/hook failure paths (fail closed by the parent check), probe-fixture alternation order, and the function-local import style (the module's own convention).

## GPT panel Round 1

Round 1 over `ff90910` (8 files, cross-check agrees).

`adversarial` needs-attention (1)
`consistency` advisory (1)
`integration` approve
`record` needs-attention (1)

1. (adversarial) Identity checks prove object existence, not commit type, so a tree-as-head passes. Answered as F1.
2. (consistency) The hermetic fixture inherits global templates and hooks. Answered as F2.
3. (record) Hook and OID diagnostics abbreviate with literal `...` instead of quoting. Answered as F3.

### F1 -- tree-as-head passes existence checks -- adversarial -- FIXED in `740bbac` (independent) [major]

Confirmed, premise driven: `git rev-parse <tree>^{tree}` exits 0 echoing the tree, so existence alone admits a tree as `--head` with its own tree. Attest now gates base and head on commit type through `check_attest_resolution` (a tree-as-head refuses as `attest: --head ... is not a commit`), pinned by the `resolve-attest-*` triple against the hermetic repo; cross-check stays existence-only because the stamp flow passes a staged tree as head, with the reason recorded at the call site. (Case-writing bug caught by the suite in the same commit: the new cases first used `(got, want)` against a `(name, cond, detail)` checker, failing the valid triple; rewritten to the module's shape, 81 green.)

### F2 -- hermetic fixture inherits templates and hooks -- consistency -- FIXED in `740bbac` (independent) [minor]

Confirmed: `git init` and `git commit` honor configured `init.templateDir` and `core.hooksPath`. The fixture now inits from an empty template dir and commits with an empty hooks dir. Advisory fixed in the same candidate at zero marginal round cost.

### F3 -- abbreviated OIDs quoted as run evidence -- record -- FIXED in `740bbac` (independent) [minor]

Confirmed for items 2-4: `2ab9a60a...`-style truncations are not quotes. Item 3 now quotes the failure lines whole, driven live against this repo (`attest: --base deadbeef...full... resolves to nothing`, the foreign-tree line with full OIDs); item 4 quotes the full seeded OID verbatim from the probe output. Item 2 was first fixed by marking its prefixes as prefixes, which round 2 sent back (F5) for a whole re-drive. (Panel imprecision, twice: the citation names `:636`, item 1, which carries no OIDs; the family spans items 2-4.)

```
RECEIPT sha=942b835d10f67b05d4936fc8116421bc67df433a9fdc1964315fce5727051212 end=PANEL-5d991ffe1a76dbf8 nonce=9d2e1e5784bfb4dc
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:650`, `scripts/review_prompt.py:660`, `scripts/review_prompt.py:1117`, and `scripts/review_prompt.py:1201`: candidate identity validation proves only that base and head are Git objects, not commits; `git rev-parse <tree-oid>^{tree}` accepts a tree object as `--head`, so a forged tree-as-head manifest passes the claimed mechanical commit-resolution boundary.
**consistency: advisory** (1)
1. `scripts/review_prompt.py:997`, `scripts/review_prompt.py:1004`, and `scripts/review_prompt.py:1010`: the self-test calls its scratch repository hermetic and claims portability to any Git installation, but `git init` and `git commit` still inherit global/system templates and hooks; configured `init.templateDir` or `core.hooksPath` can mutate or fail the fixture.
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:636` and `todo/00-workspace/TODO-04-self-correction.md:637`: the checkpoint claims driven output was quoted, but the recorded hook and OID diagnostics replace emitted full object IDs with literal `...`; these are reconstructed summaries rather than the required quoted run evidence.
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost: 24,849 tokens (codex "tokens used"); latency unresolved.

## GPT panel Round 2

Round 2 over `740bbac` (2 files, cross-check agrees), round-1 verdicts appended.

`adversarial` needs-attention (1)
`consistency` approve
`integration` approve
`record` needs-attention (1)

1. (adversarial) Replacement refs spoof the new commit-type gate. Answered as F4.
2. (record) The hook diagnostic still abbreviates; prefix-marking is not quoting. Answered as F5.

### F4 -- replacement refs spoof the type gate -- adversarial -- FIXED in `e7a4756` (independent) [major]

Confirmed: `refs/replace/<tree>` pointing at a commit makes unflagged `cat-file -t` read `commit`. Every identity call passes `--no-replace-objects` now, driven unflagged `commit` against flagged `tree` on a planted replacement, pinned by `resolve-ignores-replace-refs`.

### F5 -- hook prefixes marked, not quoted -- record -- FIXED in `e7a4756` (independent) [minor]

Confirmed: asserting prefixes were quoted does not quote them. The drill was re-driven in a scratch clone (rewriting pre-commit hook, clone removed) and the note quotes the whole `BLOCKED` line with both full tree OIDs. (Same `:636` citation imprecision as F3.)

```
RECEIPT sha=dc8cc84efdfc1ba7ae2c4d1ae39f9189d7f755d983b19536680634a22428f377 end=PANEL-025e5f048be2a285 nonce=e06a8fe198d116b0
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:668`, `scripts/review_prompt.py:682`, and `scripts/review_prompt.py:1251`: `git cat-file` and `git rev-parse` honor replacement refs, so a repository-controlled `refs/replace/<tree-oid>` can make a physical tree object appear commit-typed and yield the replacement commit's tree; the tree-as-head forgery remains accepted unless identity checks disable replacement-object resolution.
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:636`: the hook diagnostic still replaces the emitted full object IDs with abbreviated values ending in literal `...`; asserting that these prefixes were quoted does not supply the contract's required quoted driven-run output.
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost: 20,415 tokens (codex "tokens used"); latency unresolved.

## Opus panel Round 3

Sign-off over `e7a4756` (2 files, cross-check agrees), rounds 1-2 verdicts appended.

`adversarial` needs-attention (1)
`consistency` approve
`integration` approve
`record` needs-attention (1)

1. (adversarial) The skill's shell `rev-parse` calls honor replacement refs. Answered as F6.
2. (record) "Every identity call" overstates the Python-only fix. Answered as F7.

### F6 -- shell identity calls honor replacement refs -- adversarial -- FIXED in `397bc2f` (independent) [major]

Confirmed, including the finding's sweep: the push-binding comparisons plus the fence-assembly and attest-tree calls (seventeen `rev-parse` calls across twelve lines) all resolved through replacement, which also enables the consistent-shift shape (spoofed assembly text re-resolves cleanly downstream). All seventeen carry the flag now with the rationale recorded in the skill. Siblings swept: neither `process-plan` nor `process-todo-section` shells git, so the family ends here.

### F7 -- recorded scope overstates the fix -- record -- FIXED in `397bc2f` (independent) [minor]

Confirmed and closed by F6: "every identity call" holds once the shell calls are flagged (`git show` and `git diff` are content calls, not identity calls, so they sit outside the sentence as written).

```
RECEIPT sha=72b04b5f8c52314800b591ca012aa1f922173016f65b2c62fada25aa04ec829a end=PANEL-fb0cc62fe608692a nonce=bb7572818cf29171

**adversarial: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:252`, `:255`, `:256`, `:257`, `:259`, `:260`, `:261` (also `:107`, `:110`, `:236`, `:239`, `:161`): the push-time recheck this section owns resolves identity with bare `git rev-parse HEAD`, `HEAD^`, and `HEAD^{tree}`, which honor replacement refs; the same repo-controlled `refs/replace/<oid>` the round-2 fix disabled inside `scripts/review_prompt.py:650`, `:661`, `:675` can substitute the parent and tree that PARENT_OK/TREE_OK/HEAD_OK compare, so the hook-mutation and HEAD-moved gates can be satisfied by a replaced object. The shell sites need `git --no-replace-objects` (or `GIT_NO_REPLACE_OBJECTS=1`) as well.

**consistency: approve**

**integration: approve**

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:637`: the round-2 answer records "every identity call passes `--no-replace-objects`", but only the three Python helpers were flagged; the push-time identity calls in `.claude/skills/review-todo-section/SKILL.md:252-261`, which this section's first checklist item owns, remain unflagged, so the recorded scope overstates the fix.
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost and latency unresolved (no runner telemetry).

## Opus panel Round 4

Blocking re-round over `397bc2f` (2 files, cross-check agrees), rounds 1-3 verdicts appended.

`adversarial` needs-attention (1)
`consistency` approve
`integration` approve
`record` needs-attention (1)

1. (adversarial) `git show` and cross-check's `git diff` are unflagged; the cross-check rationale is false. Answered as F8.
2. (record) The `show` rationale asserts a gate with no run behind it. Answered as F9.

### F8 -- unflagged show and diff break the cross-check rationale -- adversarial -- FIXED in `2d0d406` (independent) [major]

Confirmed on both legs, and the session's round-3 analysis was wrong: cross-check's own `git diff` resolves through replacement as well, and `cross_check_files` compares file-name sets only, so no loud failure existed. Both `git show` calls (panel diff, staged blob) and the diff (extracted as `git_diff_names`) carry the flag now, pinned by `resolve-diff-ignores-replace-refs` with `resolve-diff-replace-swaps-unflagged` as the non-vacuity control. (Fixture bug caught by the suite in the same commit: a planted tree-to-commit replacement poisons later commits with `bad tree object HEAD`, so both fixture commits now precede all plantings.)

### F9 -- false show rationale recorded as fact -- record -- FIXED in `2d0d406` (independent) [minor]

Confirmed: the note asserted cross-check protection that did not exist, with no driven run cited. Corrected in place, Corrected-style, with the original claim preserved beside its refutation.

```
RECEIPT sha=ec1c603b27323727de86bd17db4218cb857e1f140a8e7790cefc20c9e858229b end=PANEL-cb91b02984ef21b8 nonce=640911770c143879

**adversarial: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:105` (and the same family at `:234`) plus `scripts/review_prompt.py:1180`: the candidate's own rationale asserts that leaving `git show` unflagged is safe because a spoofed diff "fail[s] loudly at cross-check against the unspoofed range", but the range is not unspoofed and the comparison is not of content. `cross-check`'s `git diff --name-only -z --no-renames <base> <head>` at `scripts/review_prompt.py:1180` resolves through replacement too, so the same `refs/replace/<oid>` that rewrites `git show <candidate>` at `SKILL.md:105` rewrites the range git diffs, and `cross_check_files` compares only file-name sets, never hunks. A replacement that alters file contents while touching the same paths passes `cross-check: N file(s) agree` under the flagged, genuine base/head OIDs, so the panel reviews and attests a diff that is not the candidate's. The unflagged reads need `git --no-replace-objects show` at `SKILL.md:105`/`:234` and `--no-replace-objects` on the `git diff` subprocess at `scripts/review_prompt.py:1180`.

**consistency: approve**

**integration: approve**

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:637`: the new Round 3 F1 note records "`git show` stays unflagged, a spoofed shown diff failing loudly at cross-check against the unspoofed range" as the reason the remaining unflagged call is safe. No driven run is cited for that claim, and it is false as written: `cross-check` resolves its range with an unflagged `git diff` (`scripts/review_prompt.py:1180`) and compares file-name sets only (`cross_check_files`), so it neither sees an unspoofed range nor detects a content-level substitution. The recorded evidence asserts a gate that does not hold.
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost and latency unresolved (no runner telemetry).

## Opus panel Round 5

Hard-cap round over `2d0d406` (3 files, cross-check agrees), rounds 1-4 verdicts appended. Per the cap rule the two leftover findings file as tracked work and the section stamps with the filings named.

`adversarial` needs-attention (1)
`consistency` advisory (1)
`integration` approve
`record` approve

1. (adversarial) `git diff --cached` still produces the stamp patch unflagged. Filed as F10.
2. (consistency) The page-coverage sentence overstates again. Filed as F11.

### F10 -- stamp patch reads through replacement -- adversarial -- FILED to D00 T04 §21 (independent) [major]

Confirmed: `git diff --cached` resolves HEAD through replacement, the fence and cross-check consume the spoofed patch, and the name-set comparison cannot see a content-only substitution on the same paths. This refutes the session's round-4 residual analysis the same way F8 refuted the round-3 one (asserted loud failure, no run, false). Below the cap bar for a sixth round, which the hard cap forbids: filed to §21's replacement-blindness item with the driven-replacement proof required, not just the flag.

### F11 -- page-coverage sentence overstates again -- consistency -- FILED to D00 T04 §21 (independent) [minor]

Confirmed: the sentence claims complete coverage while `:232` and the `:44` instructed reads stay unflagged. Filed beside F10; the §21 item corrects the sentence when the flags land.

```
RECEIPT sha=2fd58d74cbbc8ec356b4203c902a8b3ca91f27898401d0766656928d55aa9f91 end=PANEL-947a21391eba5f7c nonce=0a1210e36ffcb84b

**adversarial: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:232` (same family at `:44`): the round-4 fix flagged both `git show` calls and cross-check's `git diff`, but the stamp patch the STAMP panel actually reads is still produced by an unflagged `git diff --cached > $RUNDIR/stamp.patch`. `git diff --cached` resolves HEAD through the object-replacement layer, so a repo-controlled `refs/replace/<HEAD-oid>` makes the patch shown to the stamp reviewer be computed against the replacement commit's tree rather than the real HEAD. The downstream guards do not catch it: the fence at `:236` and cross-check at `:239` are flagged, and `cross_check_files` compares file-name sets only (`scripts/review_prompt.py:1216-1230`), so a replacement that alters content on the same paths yields agreeing name sets and the stamp panel reviews a patch that is not the staged change. Needs `git --no-replace-objects diff --cached` at `:232`; `:44`'s instructed `git diff`/`git show` self-review reads are the same unflagged family.

**consistency: advisory** (1)
1. `.claude/skills/review-todo-section/SKILL.md:248`: "Every `rev-parse` and `show` on this page passes `--no-replace-objects` (cross-check's `git diff` likewise): a replacement ref would otherwise substitute the compared parent, tree, fenced range, and shown content" states the page-wide binding as complete, but the page's own `git diff --cached` at `:232` and the `git diff`/`git show` reads instructed at `:44` are unflagged, so the asserted coverage of "shown content" does not hold for the stamp patch.

**integration: approve**

**record: approve**
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost and latency unresolved (no runner telemetry).

## Plan review

gpt run over §13 plus §9, §12, and §19: 20 findings, 13 accepted into 9 implementation items (7 in §21, 2 in §19), 5 rejected, 2 duplicate. Merges stated: PR1+PR2+PR3+PR13 share the explicit-push item; PR16+PR17 share the representation item.

Manifest: sections [D00 T04 §13, D00 T04 §9, D00 T04 §12, D00 T04 §19]; dependents [none]; bytes 31020; run 20260920-D00-T04-S13-gpt

Ledger:
- [D00-T04-S13-PR1] [major] push never pins remote and destination ref -> filed D00 T04 §21 (explicit OID push item, with PR2/PR3/PR13)
- [D00-T04-S13-PR2] [major] no remote readback after push -> filed D00 T04 §21 (same item as PR1)
- [D00-T04-S13-PR3] [major] pre-push hook race; push OID not branch -> filed D00 T04 §21 (same item as PR1)
- [D00-T04-S13-PR4] [major] merge passes first-parent check -> filed D00 T04 §21 (merge-parent item)
- [D00-T04-S13-PR5] [minor] file-sets-not-bytes cross-check -> rejected (already tracked: D00 T04 §21 item 1 owns content coverage, filed from panel finding D00-T04-S12-F11, which the duplicate shape cannot name since it accepts PR-shaped canonicals only; no new item)
- [D00-T04-S13-PR6] [minor] forgeable checker string -> duplicate D00 T04 §21 item 4 (D00-T04-S12-PR3)
- [D00-T04-S13-PR7] [minor] caller-controlled reviewer/model/timestamp -> filed D00 T04 §21 (provenance completion item)
- [D00-T04-S13-PR8] [minor] unsigned attestation regenerable -> rejected (local-trust model: single-writer signatures prove nothing; no identity-evidence claim made)
- [D00-T04-S13-PR9] [minor] closing-round-only attestation -> duplicate D00 T04 §21 item 5 (D00-T04-S12-PR4)
- [D00-T04-S13-PR10] [minor] judgment-only anchor detection -> filed D00 T04 §21 (mechanical anchors item)
- [D00-T04-S13-PR11] [minor] citation ban skills-only -> filed D00 T04 §21 (citation forms item)
- [D00-T04-S13-PR12] [minor] existence-only vs candidate terminology -> rejected (decided in-review at R1F1: dual-flow semantics documented; split adds surface, no new safety)
- [D00-T04-S13-PR13] [major] no driven end-to-end push receipt -> filed D00 T04 §21 (same item as PR1)
- [D00-T04-S13-PR14] [minor] merge-range advice under-specified -> filed D00 T04 §21 (merge-range item)
- [D00-T04-S13-PR15] [minor] diff-files textual serialization -> filed D00 T04 §21 (length-safe encoding item)
- [D00-T04-S13-PR16] [minor] Commit representation triple-meaning -> filed D00 T04 §19 (representation item, with PR17)
- [D00-T04-S13-PR17] [minor] exemption contradicts every-micro-step rule -> filed D00 T04 §19 (same item as PR16)
- [D00-T04-S13-PR18] [minor] deferral owners unvalidated -> filed D00 T04 §19 (owner validation item)
- [D00-T04-S13-PR19] [minor] malformed mappings vs §19 scan -> rejected (row/section correspondence already validated; no silent bypass)
- [D00-T04-S13-PR20] [minor] §13 stamps while §21 open -> rejected (pipeline completes at plan 100%; SOURCEs order the work; a section edge would serialize independent items)
End of ledger

```
RECEIPT sha=9739233b5f90dea4ea9a05bea8c70c526944ab0f1f1e3f09b1e5bc967b50754e end=PLAN-139480790fe87fd1 nonce=63d0b5570b5f0f40
- The push binding in D00 T04 §13 never pins the remote and destination ref; require an explicit `<commit>:<ref>` push because `push.default`, upstream configuration, or multiple push refspecs can send something other than the checked HEAD.
- D00 T04 §13 verifies local state before push but never verifies the remote ref after success; add a bare-remote driven test and exact remote-OID readback because the premium audit claim should prove that the reviewed commit actually landed.
- A pre-push hook can move HEAD after the final recheck; D00 T04 §13 should push the captured full commit OID rather than a symbolic branch and drill this timing because checking HEAD immediately before `git push` does not close the last race.
- D00 T04 §13 checks an expected parent but does not require exactly one parent; reject merge commits or define review against every parent because a merge can pass a first-parent check while introducing unreviewed resolution content.
- D00 T04 §12 cross-checks only the changed-file set, not the patch bytes; §21 should bind a canonical `git diff --no-replace-objects` hash or byte stream to the manifest because omitted or altered hunks with the same filenames still attest successfully.
- D00 T04 §12 records a caller-supplied checker result without proving that the checker ran on the attested manifest; make the planned §21 checker-run binding a prerequisite for stamp acceptance because a valid-looking `PASS` string is currently forgeable.
- D00 T04 §12 leaves reviewer, model, and timestamp caller-controlled, with reviewer/model deliberately open vocabulary; emit these from the review runner and constrain timestamp to the run because the attestation currently records claims rather than provenance.
- The JSON attestation in D00 T04 §12 is unsigned and can be regenerated by any local writer; add a tamper-evident signature or clearly downgrade it from identity evidence because a premium audit trail should distinguish reviewer output from reconstructed metadata.
- D00 T04 §12 says only the closing round receives an attestation while earlier rounds survive solely through prose receipts; §21 should hash the complete findings and round chain because fixes and rejected findings cannot otherwise be independently reconstructed.
- D00 T04 §13 probes contradictory stamps and stale anchors through reviewer judgment but adds no deterministic rejection; add mechanical validation for conflicting live stamps, dead lines, dead sections, and missing candidate OIDs because nondeterministic prompt detection is not a fail-closed control.
- D00 T04 §13 validates citation forms only in skills; extend ownership to stamps, attestations, and review records because those are the evidence surfaces where an ambiguous or stale citation can falsely support completion.
- D00 T04 §13 gives `cross-check` weaker existence-only OID semantics because one flow passes a tree as `head`, conflicting with §12's candidate base/head terminology; define typed identities per flow or split commands because a blob, tree, and commit should not share an ambiguous field contract.
- D00 T04 §13 lacks a driven successful end-to-end push receipt; add a hermetic bare remote proving review, commit, explicit push, and remote readback because local pre-push checks alone do not test the behavior named by the section.
- D00 T04 §12 refuses combined diffs but merely advises fencing a non-merge range; specify the exact legal range or forbid merge candidates because an arbitrary parent range can conceal merge-resolution behavior.
- D00 T04 §12 serializes parsed paths into a textual `diff-files` field after obtaining a NUL-safe Git list; use a structured length-safe representation or explicitly reject unrepresentable names because the robust source is weakened at the manifest boundary.
- D00 T04 §9 is Verified while its `Commit:` item remains unchecked, D00 T04 §13 checks the same item, and D00 T04 §19 proposes exempting it; normalize `Commit:` as non-checkbox metadata because three incompatible representations make completion state misleading.
- D00 T04 §19's `Commit:` exemption contradicts the repository rule that every micro-step is checked at completion; redesign the lifecycle or syntax instead of suppressing the contradiction because the validator would formally bless partial-looking sections.
- D00 T04 §19 accepts a struck item carrying a `Deferred` marker without requiring a resolvable owner; validate the debt target and reciprocal reference because an unowned deferral can become a permanent silent scope drop.
- D00 T04 §19 scans only shipped rows for open items but does not state how duplicate rows, missing rows, or section-to-row mismatches interact with the rule; compose it explicitly with the graph validator because a malformed mapping could bypass the new FATAL.
- D00 T04 §13 accepts stamps while §12 explicitly files content coverage, checker binding, and findings hashing to §21 without making §21 a dependency; add an ordering edge before the review pipeline is considered complete because known integrity holes remain operationally reachable.
```

Plan manifest: 9739233b5f90 PLAN-139480790fe87fd1. Cost: 22,321 tokens (codex "tokens used"); latency unresolved.

## Live proof

Section gates, each re-run after the last fix (candidate `2d0d406`, rundir `/tmp/review-WFzILKeD`). Digests are sha256 over the quoted bytes as shown (no trailing newline). All six quotes bind the review's marker run (the plan-review run; panel rounds mint no run IDs).

- Suite: `review-prompt self-test: 84 cases, 0 failed`, covering every item pin (`resolve-*`, `resolve-attest-*`, `resolve-ignores-replace-refs`, `resolve-diff-*`, `wide-spaced-full-ref-draws-no-FATAL`).

Provenance: candidate 2d0d4069793655f44ab412febd0ddfa168d288de; command python scripts/review_prompt.py --self-test; exit 0; tool CPython 3.14.4; digest 000a962d6db147e36d149ed85c8c59b120f341d382ba6ff3e32faa9dc37dd6ff; path docs/reviews/00-workspace/D00-T04-s13.md; run 20260920-D00-T04-S13-gpt

- Graph suite: `todo-graph self-test: 511 cases, 0 failed`.

Provenance: candidate 2d0d4069793655f44ab412febd0ddfa168d288de; command python scripts/todo-graph.py self-test; exit 0; tool CPython 3.14.4; digest adb430d6ff222a964ec7005d14045e3fffd00d831523e9b4747f23828af5177e; path docs/reviews/00-workspace/D00-T04-s13.md; run 20260920-D00-T04-S13-gpt

- Validate: `30 todos, 213 sections -- 0 fatal, 0 warning(s), 100 adjacency advisory`.

Provenance: candidate 2d0d4069793655f44ab412febd0ddfa168d288de; command python scripts/todo-graph.py validate; exit 0; tool CPython 3.14.4; digest 9d0ccc36f8242b0f3d3049c5434d20f54daf65a9953aa9f80153f96cf08d5201; path docs/reviews/00-workspace/D00-T04-s13.md; run 20260920-D00-T04-S13-gpt

- Claims: `todo-claims: 112 claim(s) -- 112 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`.

Provenance: candidate 2d0d4069793655f44ab412febd0ddfa168d288de; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest 95616e39a4200e824612521c83820e14d00a5fdbbc56d3d7625c0c90c32a1366; path docs/reviews/00-workspace/D00-T04-s13.md; run 20260920-D00-T04-S13-gpt

- Findings ledger: `todo-findings: ledger current, 192 finding(s), transitions complete` (pre-`--write`; regenerated with this review before the stamp).

Provenance: candidate 2d0d4069793655f44ab412febd0ddfa168d288de; command python scripts/todo-findings.py --check; exit 0; tool CPython 3.14.4; digest 013624eb0e62846f8a19f60d4a748fa7185208878980a722ac5d3b7983174936; path docs/reviews/00-workspace/D00-T04-s13.md; run 20260920-D00-T04-S13-gpt

- Sweep: `check-all: 1 of 13 gate(s) FAILED`. Twelve green (build Debug 6.1s and Release 10.9s, tidy 169/169 over 22 TUs, tests, validate, plan, claims, findings, conformance profile, toolchain); the `graph self-test` gate fails on the pre-existing stderr hint while the suite itself passes exit 0, bisected to `ef362d6` and filed to D00 T04 §20.

Provenance: candidate 2d0d4069793655f44ab412febd0ddfa168d288de; command powershell scripts/check-all.ps1; exit 1; tool check-all.ps1; digest 65164f3b69025369d987f87c97bb792bcaae2d524f94dba129a55f43f133d22d; path docs/reviews/00-workspace/D00-T04-s13.md; run 20260920-D00-T04-S13-gpt
