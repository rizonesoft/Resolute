# Review -- D00 T04 §21, Review-Input Integrity Hardening

**Reviewed:** 2026-09-20
**Stance:** post-implementation
**Rounds:** independent (7 findings: 6 fixed, 1 filed); Sol 1 (4 findings: 3 fixed, 1 duplicate); Sol 2 (4 findings: 2 fixed, 1 filed, 1 duplicate); Opus 3 (7 findings: 3 fixed, 2 record-fixed, 1 refuted, 1 duplicate); Opus 4 (7 findings: 6 fixed, 1 mixed fix-plus-refutation); Opus 5 (6 findings: 5 fixed, 1 refuted-with-clarification)
**Tier:** Full panel. The diff touches `scripts/review_prompt.py` (content leg, stamp check, attest schema 2, anchors, merge refusal, quoter), `scripts/todo-graph.py` and `scripts/todo-validate.py` (typed identity, evidence citations), and the review skill: shared contract surface.
**Attestation:** `docs/reviews/00-workspace/D00-T04-s21.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `734e5c0` | the D00 T04 §21 implementation: content coverage, stamp check, anchored identity, schema-2 attestation, replacement-blind reads, explicit push, merge refusal, run-emitted fields, anchor check, citation ban, merge-range ban, manifest quoter, round roles, typed identity, hostile paths |
| `81c4fd78` | answers the independent pass: assembled wiring plus union file leg, attest-after-plan, anchors gate, metadata markers, disk-equals-staged, `\s` quoter; F4 filed to D00 T04 §24 |
| `3005d15f` | hardens the attest-order pin against renamed headers (self-review catch) |
| `ef2f7470` | answers panel round 1: diminishing consumption, surplus rejection, CR stripping, stamp-set staged-plus-clean block, item-4 residual note |
| `edef994` | answers panel round 2: codex stderr model, gated spaced anchors plus sweep debt, ordered-coverage filing |
| `093d206` | answers panel round 3: failover redirect, whole-tree clean, line-count re-quote |
| `7958dc2` | answers panel round 4: tracked cites, bare HOLDS, range-strip, attest-last, D00 T04 §24 relation |
| `624a83b` | answers panel round 5: cite shipshape, findings-refs rule, loop re-attestation, R1 re-quote, R2 cites-over-paths, R3 suite counts |

Record commits (findings/ledger/transitions only, no reviewed content): `68e77a1`, `4be7d03`, `26e0612e`, `bb2a95d6`. The Candidate table lists content commits only, so a record commit never needs a row in the table that ships inside it (round-4 R1 convention); as-of anchors ride the transition blocks themselves.

Range `dbe5d0e..624a83bd`. Fenced ranges: round 1 `dbe5d0e..3005d15f` (8 files), round 2 `dbe5d0e..ef2f7470` (8 files), round 3 `dbe5d0e..edef994` (11 files), round 4 `dbe5d0e..bb2a95d6` (11 files), round 5 `dbe5d0e..638f67e0` (11 files); cross-check agrees on all five.

## The three questions

**Does it make sense?** Yes. All 16 items map to committed changes: the content leg (`check_commits_covered`, `--commits`, `--body`, union file leg, diminishing consumption, surplus rejection, CR stripping, block markers), the stamp check (`check_stamp_output`, CLI, skill wiring), the anchored identity parse, schema-2 attestation (checker/runner/findings bindings, run-emitted fields), replacement-blind flags, the explicit push plus readback, the merge and merge-range refusals, the anchor check plus skill gate, the evidence citation ban, the `\s` quoter, round-role citations, typed identity kinds, and hostile-path handling. No widening beyond the filed items plus review answers.

**Is it logical?** Yes, with the independent pass plus round 1 correcting seven wirings (see below). The union replaces the range oracle only when commits are declared; markers exclude `diff --git`/`index` lines so contiguous ranges never false-fail; diminishing is order-free on complete chunks (counts are the commits' sum); the stamp set is proved staged before drift is checked because `diff --quiet` ignores untracked paths.

**Did it break anything else?** No. `todo-graph self-test` 565 green; `review-prompt self-test` 210 green with every pre-existing case intact (two pins updated to the new `\s` emission, two extended for surplus twins, respelt cites silent inside the untracked leg); `validate` 0 fatal; plan 36/215 current; check-all 16/16 green.

**source-defect:** approves in-session. **design:** not owed (no surface). **Research pass:** skipped (pure infrastructure: review tooling plus tests plus records).

## Self-review

Re-read the candidate diff file by file rather than from memory. The quoter escape order (backslash first), the signal exclusions (`diff --git`/`index` re-emitted per net block), the union leg's irrelevance of span merges, and the CRLF asymmetry (fence text mode vs raw `git show` bytes) all verified in the code. One self-catch: the attest-order pin used bare `.index()`, which traceback-crashes the suite on a renamed header instead of failing the pin; fixed in `3005d15f`. Gates green before round 1.

## Independent pass

`codex review --commit 734e5c0` at high effort before panel round 1 (the candidate's first commit only). Exit 0. Latency unresolved (untimed background run); cost unresolved (no token figure on stderr). Seven findings, six fixed in `81c4fd78`, one filed:

### F1 -- content leg unwired into the skill flow -- integration -- FIXED in `81c4fd78` (independent) [major]

The `commits=` content check never ran because the skill passed neither `--commits` nor `--body`. Fixed with the assembled-candidate wiring (skill paragraph, `--commits` declaration, `--body` verification, assembly list in the round record) plus the union file leg, without which file-divergent excluded commits would false-fail the range oracle.

### F2 -- attestation hashed findings before later edits -- record -- FIXED in `81c4fd78` (independent) [major]

The skill attested right after sign-off, but plan review and round recording edit the findings file after. The Attestation block moved after plan review (position-pinned), with the sign-off rundir persistence rule and the rundir path plus output filename in the round record.

### F3 -- check-anchors never invoked -- integration -- FIXED in `81c4fd78` (independent) [major]

The anchor checker shipped without a skill call site. The stamp block runs `check-anchors` after STAMP HOLDS and before the commit, with the fix, re-stage, re-fence, re-run loop (needle-pinned).

### F4 -- checker evidence unbound to runner output -- adversarial -- FILED to D00 T04 §24 (independent) [major]

Any one-line `PASS` file hashes into a valid-looking attestation without proving the checker ran. Beyond item 4's bound-transcript contract: attest must run its checker internally (or bind input hashes). Owned by D00 T04 §24 with its Done and checkpoint clause.

### F5 -- non-textual changes vacuously covered -- adversarial -- FIXED in `81c4fd78` (independent) [major]

Pure renames, mode flips, and binary patches carry no `+`/`-` lines, so the content leg covered them vacuously. Fixed with per-file block-marker multisets (`_diff_signal_lines`, subset plus diminishing in `check_commits_covered`), excluding `diff --git`/`index` lines so contiguous ranges never false-fail.

### F6 -- staged-vs-disk findings at read-back -- adversarial -- FIXED in `81c4fd78` (independent) [major]

Read-back hashed the disk findings file while the commit ships the staged blob. Fixed with the disk-equals-staged proof before read-back (later subsumed by the round-1 stamp-set block, F11).

### F7 -- manifest lookalike filenames -- adversarial -- FIXED in `81c4fd78` (independent) [major]

Literal spaces let `foo commits=deadbeef` parse as manifest metadata. Spaces (plus tab and CR) now escape (`\s`), so writer-emitted names carry no literal whitespace; no manifest is ever committed, so no legacy corpus carries backslash-s.

```text
Independent output (codex review --commit 734e5c0), quoted verbatim:
Several new integrity checks are either not wired into the actual review workflow or do not bind the artifacts they claim to verify. The normal attestation ordering also hashes a findings file before required later edits, making the documented flow fail read-back.

Full review comments:

- [P1] Run content coverage in the documented review flow — R:\conclave\projects\Resolute\scripts\review_prompt.py:2395-2399
  The content check is conditional on a `commits=` manifest claim, but the documented panel commands in `.claude/skills/review-todo-section/SKILL.md` pass neither `fence --commits` nor `cross-check --body`. Consequently, the normal multi-commit review flow still takes this false branch and can approve a chunk that omits an inner commit while preserving the file set. Make the workflow derive the authoritative commit list and always supply the fenced body when reviewing assembled candidates.

- [P1] Delay attestation until the findings file is final — R:\conclave\projects\Resolute\.claude\skills\review-todo-section\SKILL.md:161-164
  This hashes the findings file immediately after panel sign-off, but the workflow subsequently records the round and runs the plan review, both of which add required content to that same findings file. Following the skill in order therefore makes the later schema-2 read-back reject the normally finalized findings as a replacement. Emit or regenerate the attestation only after all findings and plan-review records are complete.

- [P1] Invoke the anchor checker before accepting a stamp — R:\conclave\projects\Resolute\.claude\skills\review-todo-section\SKILL.md:247-250
  The commit adds `check-anchors`, but the stamp workflow never calls it; after `check-stamp` passes, execution proceeds directly toward committing. Thus a stamp with a dead OID, file line, or section reference still ships whenever the model misses it, defeating the new deterministic gate. Invoke `check-anchors <todo-path> <section>` before `STAMP HOLDS` can authorize the commit.

- [P2] Bind checker evidence to the reviewed runner output — R:\conclave\projects\Resolute\scripts\review_prompt.py:2568-2573
  Any file containing a single line beginning with `PASS ` is accepted as checker evidence, even if it was manually created or belongs to another runner output or manifest. Hashing that file only proves those bytes existed, not that the checker ran against the attested `runner_sha256`, so a stale or fabricated PASS still produces a valid-looking attestation. Run the checker internally or bind and verify its input hashes.

- [P2] Account for non-textual commit changes — R:\conclave\projects\Resolute\scripts\review_prompt.py:816-821
  Coverage records only unified `+` and `-` content lines. A pure rename, mode-only change, or binary patch produces no such entries, so the declared commit contributes an empty counter and is considered covered without any corresponding rename metadata, mode line, or binary payload in the fenced chunk. This can miss a dropped metadata-only commit when other included commits preserve the same aggregate file set.

- [P2] Verify the staged findings blob during stamp review — R:\conclave\projects\Resolute\.claude\skills\review-todo-section\SKILL.md:241-242
  The attestation is extracted from the index, but its findings hash is checked against the working-tree path. If the index contains findings version B while the working tree contains the attested version A, read-back passes even though the commit will ship B; the captured tree remains stable throughout, so the later tree checks do not catch it. Extract the staged findings blob and pass that temporary file to read-back, just as is already done for the attestation.

- [P2] Escape manifest metadata lookalikes in filenames — R:\conclave\projects\Resolute\scripts\review_prompt.py:622-628
  The filename encoding leaves spaces and `=` literal, so a legal path ending in text such as `foo commits=deadbeef` is interpreted as the manifest's `commits` field. `parse_manifest_diff_files` then returns `foo`, while `parse_manifest_commits` returns `deadbeef`, causing valid candidates to fail or invoke the wrong content-check behavior. Encode spaces or length-prefix fields so filenames cannot be confused with manifest metadata.
```

## GPT panel Round 1

Round 1 over `dbe5d0e..3005d15f` (8 files, cross-check agrees). Sol medium, exit 0, latency 190s, cost 87839tokens. Rundir `/tmp/review-nrRfqSgB`, manifest `PANEL-3a29578194065922` sha `5bf49f6e...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 fixed, A2 duplicate of F4)
`consistency` approve
`integration` needs-attention (I1 fixed)
`record` needs-attention (R1 fixed)

### F8 -- attest-order pin crashes on renamed headers -- correctness -- FIXED in `3005d15f` (self) [minor]

Self-review catch before round 1: the position pin used bare `.index()`, and the self-test harness has no exception guard, so a renamed skill header traceback-crashes the suite instead of failing the pin. Guarded with try/except ValueError.

### F9 -- content consumption never diminishes, surplus rides free -- adversarial -- FIXED in `ef2f7470` (independent) [major]

Round-1 A1: every declared commit compared against the same undiminished chunk counters (one occurrence covered identical lines in two commits), and surplus chunk lines failed nothing (unlike the exact file leg). Consumption now diminishes per commit, unclaimed lines and markers fail naming the file, and both parsers strip CR so CRLF blobs compare logically. Legs pin diminishing, surplus, CRLF, and the two surplus twins (rename-dropped, unresolvable).

### F10 -- checker evidence unbound (round-1 restatement) -- adversarial -- DUPLICATE of F4 (independent) [minor]

Round-1 A2 restates F4 (filed to D00 T04 §24 before round 1): a pasted-but-unrun PASS still attests. No new defect; the D00 T04 §24 item owns it.

### F11 -- untracked findings pass the staged proof, anchors read the worktree -- integration -- FIXED in `ef2f7470` (independent) [major]

Round-1 I1: `git diff --quiet -- <path>` ignores untracked paths (exit 0 on a never-staged findings file), and `check-anchors` resolves against worktree bytes. The stamp block now proves every stamp file staged (`ls-files --error-unmatch` over all five) and the stamp set drift-free (one `diff --quiet` over all five) before read-back and anchors (both lines needle-pinned), subsuming the F6 findings-only check.

### F12 -- completion record contradicts the filed residual -- record -- FIXED in `ef2f7470` (independent) [minor]

Round-1 R1: item 4's Done read complete while the D00 T04 §24 item admits the forged-PASS residual. The Done now names the residual and its owner explicitly.

```text
Round-1 output (Sol medium), quoted verbatim:
RECEIPT sha=5bf49f6ecc76b73e0b3cc0010e72663bf3510876f9c5375ca90653866248ffd8 end=PANEL-3a29578194065922 nonce=e01c1fce187d163d
**adversarial: needs-attention** (2)
1. `scripts/review_prompt.py:1072-1128` compares every declared commit against the same undiminished chunk counters and never rejects surplus same-file changes, so one occurrence can cover identical changes from multiple commits and injected lines can ride unbound.
2. `scripts/review_prompt.py:2810-2820` accepts and hashes any supplied one-line `PASS` file without running the checker or binding that result to the runner input and manifest, so a pasted-but-unrun PASS still attests successfully.
**consistency: approve**
**integration: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:243` uses `git diff --quiet`, which ignores an untracked findings file, while `scripts/review_prompt.py:1028-1045` resolves cited files and line counts from the worktree rather than the staged tree; untracked or unstaged evidence can therefore pass both gates yet be absent or different in the committed tree.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:927` marks checker provenance complete and `docs/phase-runs/2026-09-20-phase-0.md:102` records the section as implemented, while `todo/00-workspace/TODO-04-self-correction.md:1016` explicitly admits that a fabricated PASS still produces a valid attestation; the completion record contradicts the unresolved contract defect.
```

## GPT panel Round 2

Round 2 over `dbe5d0e..ef2f7470` (8 files, cross-check agrees). Sol medium, exit 0, latency 195s, cost 89305tokens. Rundir `/tmp/review-nrRfqSgB`, manifest `PANEL-ff2513e2bad693f2` sha `895f54db...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A filed to D00 T04 §24)
`consistency` needs-attention (C fixed)
`integration` needs-attention (I fixed)
`record` needs-attention (R duplicate of F4)

### F13 -- multiset coverage ignores order and position -- adversarial -- FILED to D00 T04 §24 (independent) [major]

Round-2 A: the content leg compares multisets, so a rearranged patch with identical multisets passes while the reviewed content differs. Reordering is sabotage, not the assembly-accident shape the leg exists to catch (drops), and the fix (diminishing subsequence match) is a strengthening beyond item 1's dropped-commit contract. Owned by D00 T04 §24 with its Done and checkpoint clause.

### F14 -- codex model claim unfalsifiable -- consistency -- FIXED in `edef994` (independent) [major]

Round-2 C: codex stdout names no model, so `--model` was trusted and no codex mismatch could fire. The codex stderr banner carries `model:` (probed on v0.155.1), so attest takes `--runner-stderr`: the banner derives the codex model, a hand-supplied mismatch fails, and missing/unparseable stderr fails closed. The skill saves codex stderr beside the output and passes the flag on codex sign-offs; without stderr the pin stays trusted (stated residual). Legs pin banner, echo trap, rule-less, derive, mismatch, no-banner, and unreadable; the round-2 drive derives `gpt-5.6-sol` with no `--model`.

### F15 -- spaced cites skip silently; sweep finds stale D00 T01 cites -- integration -- FIXED in `edef994` (independent) [major]

Round-2 I: the anchor path patterns excluded spaces, so spaced cites skipped. Spaced pathlines now check-or-fire; slashed spans root-gate (repo roots and relative prefixes check, commands skip); bare spaced spans verify-if-resolvable with the rest a documented recall limit; `=|*?[]` spans skip as kv/pipe/glob prose; extensions must bear a letter (`v1.2` skips, `f.c` checks). The seeded pin grows to dead-seven with the sweep's command/list/glob/kv/version spans silent. The live sweep over all 36 stamped sections fires only the true-dead cites in D00 T01 §1, D00 T01 §2, and D00 T01 §3 (six then; the round-4 tracked leg adds a seventh, see F24), zero false positives; they file as record debt to D00 T04 §24.

### F16 -- PASS binding restatement (third report) -- record -- DUPLICATE of F4 (independent) [minor]

Round-2 R restates F4 a third time (independent F4, round-1 A2): a forged PASS still attests while item 4's Done reads complete. The Done names the residual and its owner since R1; the item's letter (transcript hash, an explicitly listed equivalent) is met and run-witnessing is the filed strengthening. No new defect; the D00 T04 §24 item owns it.

```text
Round-2 output (Sol medium), quoted verbatim:
RECEIPT sha=895f54db6c205bf9a27f092877c8ffe51e2ec5db6ef24506d895197cb2e45e8f end=PANEL-ff2513e2bad693f2 nonce=53aa9727b7e10397
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:796-838` and `scripts/review_prompt.py:1064-1145` reduce patches to multisets of signed lines and selected markers while discarding context, hunk positions, and \ No newline at end of file; a modified patch with the same multisets passes check_commits_covered, so the declared commits are not content-exactly covered.
**consistency: needs-attention** (1)
1. `scripts/review_prompt.py:1434-1456` and `scripts/review_prompt.py:2867-2875` cannot derive a Codex model from runner output and instead accept the caller's --model value, so an incorrect Codex model claim cannot produce the required hand-supplied mismatch failure.
**integration: needs-attention** (1)
1. `scripts/review_prompt.py:940-941` and `scripts/review_prompt.py:1027-1050` recognize cited paths only through `[\w./-]+`; backticked paths containing spaces or other legal filename characters are silently skipped, so `check-anchors` does not resolve every cited file or line as contracted.
**record: needs-attention** (1)
1. `scripts/review_prompt.py:2896-2901` accepts any supplied one-line `PASS` file without binding it to the runner output or manifest; `todo/00-workspace/TODO-04-self-correction.md:927` nevertheless marks the actual-run binding complete while `todo/00-workspace/TODO-04-self-correction.md:1016` explicitly records that a fabricated PASS still attests.
```

## Opus panel Round 3

Round 3 over `dbe5d0e..edef994` (11 files, cross-check agrees). Opus medium, exit 0, latency 15s, cost 137706tokens. Rundir `/tmp/review-nrRfqSgB`, manifest `PANEL-5459335a17a3c2e3` sha `b2951d0e...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 refuted, A2 duplicate of F4)
`consistency` approve
`integration` needs-attention (I1 fixed, I2 fixed)
`record` needs-attention (R1 fixed, R2/R3 record-fixed)

### F17 -- title-carried commits claim diverges the parsers -- adversarial -- REFUTED (independent) [minor]

Round-3 A1: a hostile title carrying ` commits=<oid>` would make `parse_manifest_commits` (last match) disagree with the diff-files strip (one trailing). Refuted three ways: honest flows cannot produce it (fence titles are skill-fixed keys; paths never enter `titles=`); a forger who can inject titles can forge `commits=` directly, gaining no privilege; garbage claims fail closed downstream (unresolvable or uncovered, exit 1). No exploitable defect.

### F18 -- checker binding restatement (fourth report) -- adversarial -- DUPLICATE of F4 (independent) [minor]

Round-3 A2 restates F4 a fourth time (independent F4, round-1 A2, round-2 R): a foreign or hand-written PASS file hashes into a valid attestation. The panel acknowledges the D00 T04 §24 filing and asks for more; the D00 T04 §24 item owns run-witnessing and is the highest-value follow-up by acclamation. No new defect.

### F19 -- stamp failover checks stale bytes -- integration -- FIXED in `093d206` (independent) [major]

Round-3 I1: the Opus stamp-failover command had no redirect into `$RUNDIR/stamp.out`, so `check-stamp` read the stale failed codex bytes while the Opus sentence survived only on the terminal. The failover now overwrites `stamp.out` (needle-pinned); the panel and plan-review failovers save fresh session-named outputs and never shared the flaw (verified by reading).

### F20 -- anchors read non-stamp files unverified -- integration -- FIXED in `093d206` (independent) [major]

Round-3 I2: the stamp-set proof covered only the five stamp files, but anchors resolve cited paths in any file from worktree bytes. A bare `git diff --quiet` now proves the whole tree clean before read-back and anchors (needle-pinned); unrelated unstaged edits block the stamp until stashed or committed, which is the discipline the byte-equality claim needs.

### F21 -- stale line-count quote -- record -- FIXED in `093d206` (independent) [minor]

Round-3 R1: item 10's Done quoted `(file has 2682)` while the file measures 3213 at the candidate. Re-driven live and re-quoted (`cites dead lines scripts/review_prompt.py:999999 (file has 3213)` exit 1); the suite pins the message substring, never the count, so only the Done drifted.

### F22 -- candidate table omits edef994 -- record -- FIXED in `4be7d03` (independent) [minor]

Round-3 R2: the committed findings record's Candidate table ended at `ef2f7470` while the reviewed range ran to `edef994`. The record commit carries the full table; the findings file completes at the stamp by design.

### F23 -- round-2 record missing -- record -- FIXED in `4be7d03` (independent) [minor]

Round-3 R3: the committed findings file carried no round-2 section while the range's Dones cited round-2 fixes. The record commit carries the round-2 record with F13-F16; same living-record flow as F22.

```text
Round-3 output (Opus medium envelope result), quoted verbatim:
RECEIPT sha=b2951d0ea0c86379a49fb41c2d5db179bbeeb083caeada10058f08b9e6d87c0e end=PANEL-5459335a17a3c2e3 nonce=57c1fa23736c8969

**adversarial: needs-attention** (2)
1. scripts/review_prompt.py:731-740 (`parse_manifest_commits`) takes the LAST `\scommits=(\S+)` match on the MANIFEST line while `parse_manifest_diff_files` strips only ONE trailing `\s+commits=\S+\s*$`; a hostile title (titles are not quoted: `build_manifest` joins raw titles, and `_DIFF_TITLE_RE`-free titles pass unescaped) can carry a literal ` commits=<oid>` so the parsed commit claim and the parsed file list disagree, and in the no-real-`commits=` case the cross-check silently switches to the declared-union path against an attacker-chosen oid list.
2. scripts/review_prompt.py:3060-3070 the checker binding accepts any single line beginning `PASS ` from `--checker-output` without requiring that line to be `check-panel`'s output over the same `--runner-output`/manifest pair, so `check.out` from a different round (or a hand-written `PASS x`) hashes into a valid schema-2 attestation; item 4's Done claims "a pasted PASS string has no file and refuses", but a one-line file is trivially producible and the residual is only filed to D00 T04 §24.

**consistency: approve**

**integration: needs-attention** (2)
1. .claude/skills/review-todo-section/SKILL.md:245 wires `check-stamp --manifest $RUNDIR/stamp-manifest.md < $RUNDIR/stamp.out`, but the stamp runner line writes stdout to `$RUNDIR/stamp.out` while the Opus failover variant documented in the following paragraph (`timeout 600 claude -p ... < $RUNDIR/stamp-prompt.md`) has no redirect into `stamp.out`, so the documented failover path leaves `check-stamp` reading the stale codex output rather than the Opus round it claims to check.
2. .claude/skills/review-todo-section/SKILL.md:246 runs `check-anchors <todo-path> <section>` after the staged-set proof, but `check_stamp_anchors` (scripts/review_prompt.py:900-917) opens `todo_path` from the worktree, and the preceding `diff --quiet` covers only the five named stamp files, so a stamp citing a path/line in any other file resolves against worktree bytes that need not match the staged tree the review approved; the item-10 Done and the F11 fix both assert anchors verify "the shipped bytes".

**record: needs-attention** (3)
1. todo/00-workspace/TODO-04-self-correction.md:939 (item 10 Done) quotes `cites dead lines scripts/review_prompt.py:999999 (file has 2682)` while the candidate's `scripts/review_prompt.py` is 3224 lines after this diff, so the quoted evidence figure cannot be reproduced at the candidate.
2. docs/reviews/00-workspace/D00-T04-s21.md:16 lists the candidate range as `dbe5d0e..ef2f7470` with four commits, but the MANIFEST for this round declares `base=dbe5d0e1652d6b0ecb880df8a749ff334c88211e head=edef994fb92ccd7a0caafd2f94ca468dcc1969e7`; commit `edef994f` (round-2 answers) is inside the reviewed range and absent from the Candidate table, so the record understates the reviewed candidate.
3. docs/reviews/00-workspace/D00-T04-s21.md:5 records `Rounds: independent (7 findings: 6 fixed, 1 filed); Sol 1 (4 findings: 3 fixed, 1 duplicate)` and the file carries no round-2 section, while todo/00-workspace/TODO-04-self-correction.md:932 and :939 cite "Round-2 fix (C)" and "Round-2 fix (I)" and docs/reviews/transitions.md:91 stamps `as-of: 68e77a1`; the findings file omits the round-2 record its own fixes depend on.
```

## Opus panel Round 4

Round 4 over `dbe5d0e..bb2a95d6` (11 files, cross-check agrees). Opus medium, exit 0, latency 113s, cost 164290tokens. Rundir `/tmp/review-nrRfqSgB`, manifest `PANEL-ad10f39f252c49d8` sha `4dd2ced7...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 fixed, A2 fixed)
`consistency` needs-attention (C1 fixed)
`integration` needs-attention (I1 fixed, I2 fixed)
`record` needs-attention (R1 fixed, R2 mixed)

### F24 -- untracked cites pass anchors -- adversarial -- FIXED in `7958dc2` (independent) [major]

Round-4 A1: a stamp citing a file present on disk but never added passed anchors (worktree `exists`) and shipped without it; the `ls-files` loop covered only the five stamp files, not anchor targets. Every resolving cite must now also be tracked (one `ls-files` over the cited set; untracked fires naming the file), and outside-repo paths fail as non-repo evidence (realpath containment, which catches Windows drive-relative `/tmp/...` that `isabs` misses). Legs pin the untracked and absolute fires; the sweep re-runs at exactly-7 (the new leg catches the machine-local `build/todo-operator.json` cite, added to the debt item).

### F25 -- bare HOLDS loops with no figure -- adversarial -- FIXED in `7958dc2` (independent) [major]

Round-4 A2: `STAMP HOLDS` without the period read as one blocking naming with no figure to fix, looping fix-re-stage-rerun with no escape. The checker accepts the hold with or without the period, the prompt states the period is part of the verdict, and a round naming no figure (refusal, off-topic) re-runs instead of fix-looping. Legs pin the bare hold plus both skill needles.

### F26 -- range-stamp coverage trips the citation ban -- consistency -- FIXED in `7958dc2` (independent) [major]

Round-4 C1: the Verified coverage strip handled only single-section coverage, so a range stamp the graph accepts (pair and dash twins) tripped `evidence-citation-short-form` FATAL on legal grammar (latent: no live multi-stamp exists). The strip mirrors the graph's own grammar (`STAMP_BODY_RE` plus per-element `COVER_ITEM_RE`), so the strip and the parser can never disagree on what is coverage. Legs pin the pair and dash twins silent (observed red before the fix).

### F27 -- Live proof postdates the attestation -- integration -- FIXED in `7958dc2` (independent) [major]

Round-4 I1: `Live proof` and the stamp-round record are written after the F2 attestation position, so read-back fails on the finalized file and no step re-emits. The attestation moves after step 9 (stamp, `Live proof`, ledger written; position-pinned between step 9 and the stamp review); stamp rounds (records, raw outputs, cost) ride the run file, never the findings file, so no post-attestation findings edit exists (bounded regress: the run file carries the last mile, matching D00 T04 §20's record which carries no stamp rounds).

### F28 -- D00 T04 §24 items lack their D00 T04 §21 relation -- integration -- FIXED in `7958dc2` (independent) [minor]

Round-4 I2: three D00 T04 §24 items state "needs D00 T04 §21 shipped" but the graph carried no relation. The D00 T04 §24 table row now depends on `D00 T04 §20, D00 T04 §21`, with a one-way filing XREF (`-> XREF: D00 T04 §21`) matching the D00 T04 §12-to-D00 T04 §20 precedent (filing pointers are one-way claims; the target stays untouched).

### F29 -- candidate table understates again -- record -- FIXED in `fb9c380` (independent) [minor]

Round-4 R1: the table omitted the record commits inside the reviewed range, repeating F22 by construction (a record commit cannot carry a row for itself). Convention fix: the Candidate table lists content commits only; record commits ride one line (no reviewed content); the Range line states the fenced ranges per round.

### F30 -- SOURCE without finding ID; F15 misread -- record -- FIXED in `7958dc2` (independent) [minor]

Round-4 R2, first half fixed: the F4 SOURCE line now carries `D00-T04-S21-F4` like its siblings, so the filing resolves back. Second half refuted in place: the panel reads the D00 T04 §24 debt item's `D00-T04-S21-F15` SOURCE key as claiming F15 was filed, but SOURCE keys are provenance (where the item originated: F15's sweep), not disposition claims; F15 heads FIXED, and every D00 T04 §20 SOURCE keys fixed-or-filed findings alike with the tree green to prove the check does not exist.

```text
Round-4 output (Opus medium envelope result), quoted verbatim:
RECEIPT sha=4dd2ced786829444f6ec96e7758e43f895d97088325b490fc942a9eaa3f5f21f end=PANEL-ad10f39f252c49d8 nonce=2b098269da1e23b9

**adversarial: needs-attention** (2)
1. `.claude/skills/review-todo-section/SKILL.md:245` and `todo/00-workspace/TODO-04-self-correction.md:925` (item 5 Round-3 fix) claim a bare `git --no-replace-objects diff --quiet` "proves the whole tree clean", but `git diff` reports only tracked-file worktree-vs-index drift and silently ignores untracked paths; `check_stamp_anchors` (`scripts/review_prompt.py:876-905`) resolves cited paths with `os.path.exists` against the worktree, so a stamp citing a file that exists on disk but was never `git add`ed passes both the F20 gate and `check-anchors`, then ships in a commit that does not contain the cited file. The `ls-files --error-unmatch` loop at `SKILL.md:243` covers only the five named stamp files, not anchor targets.
2. `scripts/review_prompt.py:589` pins `_STAMP_HOLDS_RE = re.compile(r"^STAMP HOLDS\.\s*$")` while the stamp prompt at `.claude/skills/review-todo-section/SKILL.md:253` instructs `say exactly: STAMP HOLDS.` with the period ambiguous as sentence punctuation; a reviewer returning `STAMP HOLDS` (no period), or any refusal/off-topic line, returns `True, "1 naming(s) to answer"`, which `SKILL.md:257` defines as BLOCKING, so a clean stamp loops through fix-restage-rerun with no figure to fix and no escape branch.

**consistency: needs-attention** (1)
1. `scripts/todo-validate.py:2009` strips the Verified coverage segment with `re.sub(r"^\d{4}-\d{2}-\d{2} \| §\d+ \| ?", "", body)`, which matches exactly one single-section form, but `parse_todo` assigns one Verified body to every target in `stamp_targets` (`scripts/todo-graph.py:551-554`), so a post-cutoff multi-section stamp whose coverage reads `2026-09-21 | §12, §13 | evidence` never strips and both coverage marks are flagged by the new FATAL `evidence-citation-short-form` as short-form citations, failing `validate` on grammar the stamp format itself defines.

**integration: needs-attention** (2)
1. `.claude/skills/review-todo-section/SKILL.md:208` emits the attestation "with the findings file final ... no finding edit follows", but `SKILL.md:160` requires that "Every round line the review writes (panel rounds, **stamp rounds**, independent passes)" carry cost and latency, and `SKILL.md:216`/`SKILL.md:247` re-hash the findings file at the stamp block; the stamp round and the step-9 `Live proof` section are written after the attestation hashed the file, so `attest --read-back --findings` fails with `findings file ... hashes X, attestation binds Y` on the first re-stage, and no step instructs re-emitting the attestation.
2. `todo/00-workspace/TODO-04-self-correction.md:1016` and `:1017` state "needs §21 shipped" as a hard prerequisite for two new §24 items, but the diff adds no `-> XREF: D00 T04 §21` beside the existing `-> XREF: D00 T04 §20 -- follow-ups this review filed` (`:1020`) and no Depends On edge, so the graph carries no relation between §21 and the items that require it, unlike the analogous §20 filing recorded one line above.

**record: needs-attention** (2)
1. `docs/reviews/00-workspace/D00-T04-s21.md:22` records the candidate as "Range `dbe5d0e..093d206` (8 commits on trunk at round 4)" and the table at `:13-20` lists those eight, but this round's MANIFEST declares `head=bb2a95d6eae158d2ab0fa935f80826b62f954a8e`; commits `26e0612e` and `bb2a95d6` are inside the reviewed range and absent from the table, and their content (the F17/F18 rows in `docs/reviews/transitions.md:85-115`) is in the reviewed diff. This is the same understatement F22 recorded at `:16` and claims to have fixed.
2. `todo/00-workspace/TODO-04-self-correction.md:1033` files the §24 item nine under `-> SOURCE: independent-D00-T04-s21-2026-09-20` with no finding ID, while its two siblings at `:1034` and `:1035` carry `D00-T04-S21-F13` and `D00-T04-S21-F15`; the ledger rule requires the target's SOURCE key to name the finding ID, so F4's filing cannot be resolved back from §24. Separately, `docs/reviews/findings.md:225` records F15 as `fixed`, not `filed`, while `:1035` keys it as a filing into §24.
```

## Opus panel Round 5

Round 5 over `dbe5d0e..638f67e0` (11 files, cross-check agrees). Opus medium, exit 0, latency 85s, cost 170149tokens. Rundir `/tmp/review-nrRfqSgB`, manifest `PANEL-18dca774dbf93400` sha `b2e16716...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 fixed)
`consistency` needs-attention (C1 fixed)
`integration` needs-attention (I1 fixed)
`record` needs-attention (R1 fixed, R2 refuted-with-clarification, R3 fixed)

### F31 -- alternatively-spelled cites false-fire untracked -- adversarial -- FIXED in `624a83b` (independent) [major]

Round-5 A1: the tracked leg compared raw cited spellings against the forward-slash `ls-files` set, so `./x` and backslash-spelled cites of tracked files false-fire `cites untracked file` with no fix available past respelling. Cited paths now normalize to shipshape before the comparison (`_anchor_shipshape`: separators to forward slashes, `.` segments collapsed, leading `./` stripped); case stays exact (a wrong-case cite fires untracked, fixed by correcting the case). Legs pin the normalizer plus respelt seed cites silent inside the exactly-2 untracked leg.

### F32 -- findings-file citation rule undocumented in the skill -- consistency -- FIXED in `624a83b` (independent) [major]

Round-5 C1: the skill stated the citation rule as stamp-only while the validator scans stamp bodies, the named findings file unfenced, and the sibling attestation; the D00 T04 §21 findings prose itself carried bare short shapes proving the miss. The skill now states the findings file is scanned too (unfenced lines; fences strip so transcripts stay safe), needle-pinned, and the findings prose takes full refs. D00 T04 §21's own stamp lands on `EVIDENCE_CITE_CUTOFF` itself and escapes the gate (`stamped_on <= cutoff`), so the prose fix is hygiene plus the example the next review copies.

### F33 -- stamp-review fix loop never re-emits the attestation -- integration -- FIXED in `624a83b` (independent) [major]

Round-5 I1: F27 moved the attestation after step 9, but the stamp-review loop (fix the figures, re-stage, re-run) routinely edits the findings file (`Live proof` lives there; the D00 T04 §20 stamp attempts 1-3 moved findings figures), so the next attempt's read-back fails with `hashes X, binds Y` and no step re-emits. Every fix loop now re-emits the attestation after the fix and before re-staging (needle-pinned); unchanged inputs reproduce identical bytes because the timestamp rides the runner-output mtime, never the wall clock.

### F34 -- stale line-count quote again -- record -- FIXED in `624a83b` (independent) [minor]

Round-5 R1: item 10's Done quoted `(file has 3213)` while the file measures 3329 at the candidate (round-4 A1 legs plus round-5 shipshape). Re-driven live and re-quoted (`cites dead lines scripts/review_prompt.py:999999 (file has 3329)` exit 1); the suite pins the message substring, never the count, so only the Done drifted. Second occurrence of the F21 shape.

### F35 -- seven cites over six paths, not one short -- record -- REFUTED with an in-place clarification in `624a83b` (independent) [minor]

Round-5 R2 reads the D00 T04 §24 debt item's "seven stale evidence cites" against its six enumerated paths and calls the enumeration one short. Re-driven: the sweep fires 7 failure lines over 6 paths (`cpp-env.ps1` cited twice, D00 T01 §1 lines 152 and 155), so the count was always cites, not paths, and the enumeration is complete. The count stands; the item now reads "seven stale evidence cites over six paths" with the double cite named, so the next reader cannot repeat the misread.

### F36 -- candidate-tip suite counts stale -- record -- FIXED in `624a83b` (independent) [minor]

Round-5 R3: the run file's "graph 563 at the implementation commit" and the findings file's "563 green at the candidate" cannot both hold after round-4 C1 added two graph legs. Split verdict: the implementation figure stands (563 re-measured in a worktree at `734e5c0`), the candidate-tip figures were stale and now read graph 565 plus review-prompt 210, both measured at the round-5 answer commit.

```text
Round-5 output (Opus medium envelope result), quoted verbatim:
RECEIPT sha=b2e1671608aefa855638eaf0a82ab879aeedbdbddd2477faafeb8285ad68fe01 end=PANEL-18dca774dbf93400 nonce=a65eac86b300f2d3

**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:1046` (the `if cited:` tracked leg in `check_stamp_anchors`) compares cited path strings against `parse_nul_file_list(git ls-files -z)`, whose output is always forward-slash, repo-relative and prefix-free, while the cite acceptors deliberately admit other spellings of the same tracked file: `_anchor_root_ok` (`scripts/review_prompt.py:940-948`) root-gates on `re.split(r"[\\/]")` and `_ANCHOR_REL_PREFIX_RE` (`scripts/review_prompt.py:935`) accepts `./`, `.\`, `../`, `..\`. On this Windows-only tree a stamp citing `` `reskit\Build-Resolute.ps1` `` or `` `./scripts/todo-graph.py` `` passes `os.path.exists`, enters `cited` under that spelling, and can never appear in the `ls-files` set, so `set(cited) - tracked` fires `cites untracked file ...` on a file that is tracked and ships; `check-anchors` is wired BLOCKING between STAMP HOLDS and the commit (`.claude/skills/review-todo-section/SKILL.md:246`), so the false fire stalls the stamp with no fix available.

**consistency: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:207` states the new citation rule as "Stamp prose cites full `DNN TNN §N` refs", but `scripts/todo-validate.py:2012-2060` scans three surfaces: stamp bodies, the named findings file (unfenced lines) and the sibling `.attest.json`, and `todo/README.md:427` states all three. The skill is the authoring surface for findings files and never tells its author the findings file is scanned, and the findings file this candidate adds demonstrates the miss: `docs/reviews/00-workspace/D00-T04-s21.md:118` reads "No new defect; the §24 item owns it" in unfenced prose, with the same bare-`§` shape in the F16, F18, F28 and F30 bodies (`§24`, `§21`, `§20`, `§12`). §21's own stamp escapes only because it lands on `EVIDENCE_CITE_CUTOFF` itself (`scripts/todo-graph.py:1062`); the next review authored from this skill stamps post-cutoff and fails `validate` on its own findings file.

**integration: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:223` moves the attestation after step 9 so the findings file is final, but the stamp-review loop at `.claude/skills/review-todo-section/SKILL.md:258` ("fix the figures, re-stage, and re-run until the review says STAMP HOLDS") routinely edits the findings file: the `Live proof` section and its provenance lines live there (`.claude/skills/review-todo-section/SKILL.md:207`), and the recorded precedent is exactly that (`docs/phase-runs/2026-09-20-phase-0.md:99`: §20 stamp attempts 1-3 named "manifest-file cite, TBD, 552, 35/214, :870", "removal count, transcript note, :600 cite", all figures inside the findings file). Any such fix changes `findings_sha256`, so the next attempt's staged read-back (`.claude/skills/review-todo-section/SKILL.md:216`, `attest --read-back $RUNDIR/staged.attest.json --findings <findings path>`) fails with `findings file ... hashes X, attestation binds Y`, and no step in the loop instructs re-emitting the attestation. F27 closed the step-9 ordering but not the stamp-review fix cycle it feeds.

**record: needs-attention** (3)
1. `todo/00-workspace/TODO-04-self-correction.md:933` (item 10 Done) quotes `cites dead lines scripts/review_prompt.py:999999 (file has 3213)`, a figure re-driven at `093d206` per the round-3 R1 fix, but the round-4 commit `7958dc2` added further lines to that file (`_anchor_inside_repo`, the `if cited:` ls-files leg, the `anchors-untracked-fires` legs); the candidate diff's own last hunk header for that file is `@@ -1455,7 +3289,7 @@`, putting the file near 3300 lines, so the quoted evidence cannot be reproduced at the candidate. This is the same drift F21 recorded and claims to have fixed.
2. `todo/00-workspace/TODO-04-self-correction.md:1018` files "Re-point the seven stale evidence cites in sealed D00 T01 §1, D00 T01 §2, D00 T01 §3, and D00 T01 §5 stamps", but the item enumerates six paths: `cpp-env.ps1`, `bootstrap.ps1`, `reskit/Build-Resolute.ps1`, `reskit/Build-Extension.ps1`, `compile_commands.json`, `build/todo-operator.json`. Item 10's Done (`todo/00-workspace/TODO-04-self-correction.md:933`) states six dead cites in D00 T01 §1/§2/§3 plus the round-4 `build/todo-operator.json` catch, so the enumeration is one cite short of the count it files, and the "sweep re-runs clean" checkpoint names a set the item does not list.
3. `docs/phase-runs/2026-09-20-phase-0.md:105` records "Suites at the implementation commit: graph 563" and `docs/reviews/00-workspace/D00-T04-s21.md:31` records "`todo-graph self-test` 563 green" at the candidate, but the round-4 C1 fix (`7958dc2`) added two graph self-test legs, `evidence-cite: range coverage pair silent` and `evidence-cite: dash range coverage silent`, plus their §11-§14 fixture sections (`scripts/todo-graph.py:7385-7389`); the implementation-commit count and the candidate-tip count therefore cannot both be 563, and one of the two quoted suite figures is stale.
```
