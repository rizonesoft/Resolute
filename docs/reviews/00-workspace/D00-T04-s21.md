# Review -- D00 T04 §21, Review-Input Integrity Hardening

**Reviewed:** 2026-09-20
**Stance:** post-implementation
**Rounds:** independent (7 findings: 6 fixed, 1 filed); Sol 1 (4 findings: 3 fixed, 1 duplicate); Sol 2 (4 findings: 2 fixed, 1 filed, 1 duplicate)
**Tier:** Full panel. The diff touches `scripts/review_prompt.py` (content leg, stamp check, attest schema 2, anchors, merge refusal, quoter), `scripts/todo-graph.py` and `scripts/todo-validate.py` (typed identity, evidence citations), and the review skill: shared contract surface.
**Attestation:** `docs/reviews/00-workspace/D00-T04-s21.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `734e5c0` | the §21 implementation: content coverage, stamp check, anchored identity, schema-2 attestation, replacement-blind reads, explicit push, merge refusal, run-emitted fields, anchor check, citation ban, merge-range ban, manifest quoter, round roles, typed identity, hostile paths |
| `81c4fd78` | answers the independent pass: assembled wiring plus union file leg, attest-after-plan, anchors gate, metadata markers, disk-equals-staged, `\s` quoter; F4 filed to D00 T04 §24 |
| `3005d15f` | hardens the attest-order pin against renamed headers (self-review catch) |
| `ef2f7470` | answers panel round 1: diminishing consumption, surplus rejection, CR stripping, stamp-set staged-plus-clean block, item-4 residual note |
| `68e77a1` | findings record through round 1 (as-of for the F10 duplicate transition) |
| `edef994` | answers panel round 2: codex stderr model, gated spaced anchors plus sweep debt, ordered-coverage filing |

Range `dbe5d0e..edef994` (6 commits on trunk at round 3). Round 1 fenced `dbe5d0e..3005d15f` (8 files, cross-check agrees); round 2 fenced `dbe5d0e..ef2f7470` (8 files, cross-check agrees).

## The three questions

**Does it make sense?** Yes. All 16 items map to committed changes: the content leg (`check_commits_covered`, `--commits`, `--body`, union file leg, diminishing consumption, surplus rejection, CR stripping, block markers), the stamp check (`check_stamp_output`, CLI, skill wiring), the anchored identity parse, schema-2 attestation (checker/runner/findings bindings, run-emitted fields), replacement-blind flags, the explicit push plus readback, the merge and merge-range refusals, the anchor check plus skill gate, the evidence citation ban, the `\s` quoter, round-role citations, typed identity kinds, and hostile-path handling. No widening beyond the filed items plus review answers.

**Is it logical?** Yes, with the independent pass plus round 1 correcting seven wirings (see below). The union replaces the range oracle only when commits are declared; markers exclude `diff --git`/`index` lines so contiguous ranges never false-fail; diminishing is order-free on complete chunks (counts are the commits' sum); the stamp set is proved staged before drift is checked because `diff --quiet` ignores untracked paths.

**Did it break anything else?** No. `todo-graph self-test` 563 green; `review-prompt self-test` 191 green with every pre-existing case intact (two pins updated to the new `\s` emission, two extended for surplus twins); `validate` 0 fatal; plan 36/215 current; check-all 16/16 green.

**source-defect:** approves in-session. **design:** not owed (no surface). **Research pass:** skipped (pure infrastructure: review tooling plus tests plus records).

## Self-review

Re-read the candidate diff file by file rather than from memory. The quoter escape order (backslash first), the signal exclusions (`diff --git`/`index` re-emitted per net block), the union leg's irrelevance of span merges, and the CRLF asymmetry (fence text mode vs raw `git show` bytes) all verified in the code. One self-catch: the attest-order pin used bare `.index()`, which traceback-crashes the suite on a renamed header instead of failing the pin; fixed in `3005d15f`. Gates green before round 1.

## Independent pass

`codex review --commit 734e5c0` at high effort before panel round 1 (the candidate's first commit only). Exit 0. Latency unresolved (untimed background run); cost unresolved (no token figure on stderr). Seven findings, six fixed in `81c4fd78`, one filed:

### F1 -- content leg unwired into the skill flow -- integration -- FIXED in `81c4fd78` (independent) [major]

The `commits=` content check never ran because the skill passed neither `--commits` nor `--body`. Fixed with the assembled-candidate wiring (skill paragraph, `--commits` declaration, `--body` verification, assembly list in the round record) plus the union file leg, without which file-divergent excluded commits would false-fail the range oracle.

### F2 -- attestation hashed findings before later edits -- record -- FIXED in `81c4fd78` (independent) [major]

The skill attested right after sign-off, but plan review and round recording edit the findings file after. The Attestation block moved after plan review (position-pinned), with the sign-off rundir persistence rule and the rundir path plus output filename in the round record.

### F3 -- check-anchors never invoked -- integration -- FIXED in `81c4fd78` (independent) [major]

The anchor checker shipped without a skill call site. The stamp block runs `check-anchors` after STAMP HOLDS and before the commit, with the fix, re-stage, re-fence, re-run loop (needle-pinned).

### F4 -- checker evidence unbound to runner output -- adversarial -- FILED to D00 T04 §24 (independent) [major]

Any one-line `PASS` file hashes into a valid-looking attestation without proving the checker ran. Beyond item 4's bound-transcript contract: attest must run its checker internally (or bind input hashes). Owned by D00 T04 §24 with its Done and checkpoint clause.

### F5 -- non-textual changes vacuously covered -- adversarial -- FIXED in `81c4fd78` (independent) [major]

Pure renames, mode flips, and binary patches carry no `+`/`-` lines, so the content leg covered them vacuously. Fixed with per-file block-marker multisets (`_diff_signal_lines`, subset plus diminishing in `check_commits_covered`), excluding `diff --git`/`index` lines so contiguous ranges never false-fail.

### F6 -- staged-vs-disk findings at read-back -- adversarial -- FIXED in `81c4fd78` (independent) [major]

Read-back hashed the disk findings file while the commit ships the staged blob. Fixed with the disk-equals-staged proof before read-back (later subsumed by the round-1 stamp-set block, F11).

### F7 -- manifest lookalike filenames -- adversarial -- FIXED in `81c4fd78` (independent) [major]

Literal spaces let `foo commits=deadbeef` parse as manifest metadata. Spaces (plus tab and CR) now escape (`\s`), so writer-emitted names carry no literal whitespace; no manifest is ever committed, so no legacy corpus carries backslash-s.

```text
Independent output (codex review --commit 734e5c0), quoted verbatim:
Several new integrity checks are either not wired into the actual review workflow or do not bind the artifacts they claim to verify. The normal attestation ordering also hashes a findings file before required later edits, making the documented flow fail read-back.
Full review comments:
- [P1] Run content coverage in the documented review flow -- scripts/review_prompt.py:2395-2399 -- conditional on commits=, but the panel commands pass neither fence --commits nor cross-check --body.
- [P1] Delay attestation until the findings file is final -- SKILL.md:161-164 -- hashes findings before plan review and round recording edit them.
- [P1] Invoke the anchor checker before accepting a stamp -- SKILL.md:247-250 -- check-anchors never called; a dead anchor ships whenever the model misses it.
- [P2] Bind checker evidence to the reviewed runner output -- scripts/review_prompt.py:2568-2573 -- any PASS-prefixed file attests without proving the checker ran.
- [P2] Account for non-textual commit changes -- scripts/review_prompt.py:816-821 -- rename/mode/binary commits contribute empty counters, covered vacuously.
- [P2] Verify the staged findings blob during stamp review -- SKILL.md:241-242 -- read-back hashes disk while the commit ships the staged blob.
- [P2] Escape manifest metadata lookalikes in filenames -- scripts/review_prompt.py:622-628 -- `foo commits=deadbeef` parses as the commits field.
```

## GPT panel Round 1

Round 1 over `dbe5d0e..3005d15f` (8 files, cross-check agrees). Sol medium, exit 0, latency 190s, cost 87839tokens. Rundir `/tmp/review-nrRfqSgB`, manifest `PANEL-3a29578194065922` sha `5bf49f6e...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 fixed, A2 duplicate of F4)
`consistency` approve
`integration` needs-attention (I1 fixed)
`record` needs-attention (R1 fixed)

### F8 -- attest-order pin crashes on renamed headers -- correctness -- FIXED in `3005d15f` (self) [minor]

Self-review catch before round 1: the position pin used bare `.index()`, and the self-test harness has no exception guard, so a renamed skill header traceback-crashes the suite instead of failing the pin. Guarded with try/except ValueError.

### F9 -- content consumption never diminishes, surplus rides free -- adversarial -- FIXED in `ef2f7470` (independent) [major]

Round-1 A1: every declared commit compared against the same undiminished chunk counters (one occurrence covered identical lines in two commits), and surplus chunk lines failed nothing (unlike the exact file leg). Consumption now diminishes per commit, unclaimed lines and markers fail naming the file, and both parsers strip CR so CRLF blobs compare logically. Legs pin diminishing, surplus, CRLF, and the two surplus twins (rename-dropped, unresolvable).

### F10 -- checker evidence unbound (round-1 restatement) -- adversarial -- DUPLICATE of F4 (independent) [minor]

Round-1 A2 restates F4 (filed to D00 T04 §24 before round 1): a pasted-but-unrun PASS still attests. No new defect; the §24 item owns it.

### F11 -- untracked findings pass the staged proof, anchors read the worktree -- integration -- FIXED in `ef2f7470` (independent) [major]

Round-1 I1: `git diff --quiet -- <path>` ignores untracked paths (exit 0 on a never-staged findings file), and `check-anchors` resolves against worktree bytes. The stamp block now proves every stamp file staged (`ls-files --error-unmatch` over all five) and the stamp set drift-free (one `diff --quiet` over all five) before read-back and anchors (both lines needle-pinned), subsuming the F6 findings-only check.

### F12 -- completion record contradicts the filed residual -- record -- FIXED in `ef2f7470` (independent) [minor]

Round-1 R1: item 4's Done read complete while the §24 item admits the forged-PASS residual. The Done now names the residual and its owner explicitly.

```text
Round-1 output (Sol medium), quoted verbatim:
RECEIPT sha=5bf49f6ecc76b73e0b3cc0010e72663bf3510876f9c5375ca90653866248ffd8 end=PANEL-3a29578194065922 nonce=e01c1fce187d163d
**adversarial: needs-attention** (2)
1. scripts/review_prompt.py:1072-1128 compares every declared commit against the same undiminished chunk counters and never rejects surplus same-file changes, so one occurrence can cover identical changes from multiple commits and injected lines can ride unbound.
2. scripts/review_prompt.py:2810-2820 accepts and hashes any supplied one-line PASS file without running the checker or binding that result to the runner input and manifest, so a pasted-but-unrun PASS still attests successfully.
**consistency: approve**
**integration: needs-attention** (1)
1. .claude/skills/review-todo-section/SKILL.md:243 uses git diff --quiet, which ignores an untracked findings file, while scripts/review_prompt.py:1028-1045 resolves cited files and line counts from the worktree rather than the staged tree; untracked or unstaged evidence can therefore pass both gates yet be absent or different in the committed tree.
**record: needs-attention** (1)
1. todo/00-workspace/TODO-04-self-correction.md:927 marks checker provenance complete and docs/phase-runs/2026-09-20-phase-0.md:102 records the section as implemented, while todo/00-workspace/TODO-04-self-correction.md:1016 explicitly admits that a fabricated PASS still produces a valid attestation; the completion record contradicts the unresolved contract defect.
```

## GPT panel Round 2

Round 2 over `dbe5d0e..ef2f7470` (8 files, cross-check agrees). Sol medium, exit 0, latency 195s, cost 89305tokens. Rundir `/tmp/review-nrRfqSgB`, manifest `PANEL-ff2513e2bad693f2` sha `895f54db...`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A filed to D00 T04 §24)
`consistency` needs-attention (C fixed)
`integration` needs-attention (I fixed)
`record` needs-attention (R duplicate of F4)

### F13 -- multiset coverage ignores order and position -- adversarial -- FILED to D00 T04 §24 (independent) [major]

Round-2 A: the content leg compares multisets, so a rearranged patch with identical multisets passes while the reviewed content differs. Reordering is sabotage, not the assembly-accident shape the leg exists to catch (drops), and the fix (diminishing subsequence match) is a strengthening beyond item 1's dropped-commit contract. Owned by D00 T04 §24 with its Done and checkpoint clause.

### F14 -- codex model claim unfalsifiable -- consistency -- FIXED in `edef994` (independent) [major]

Round-2 C: codex stdout names no model, so `--model` was trusted and no codex mismatch could fire. The codex stderr banner carries `model:` (probed on v0.155.1), so attest takes `--runner-stderr`: the banner derives the codex model, a hand-supplied mismatch fails, and missing/unparseable stderr fails closed. The skill saves codex stderr beside the output and passes the flag on codex sign-offs; without stderr the pin stays trusted (stated residual). Legs pin banner, echo trap, rule-less, derive, mismatch, no-banner, and unreadable; the round-2 drive derives `gpt-5.6-sol` with no `--model`.

### F15 -- spaced cites skip silently; sweep finds stale D00 T01 cites -- integration -- FIXED in `edef994` (independent) [major]

Round-2 I: the anchor path patterns excluded spaces, so spaced cites skipped. Spaced pathlines now check-or-fire; slashed spans root-gate (repo roots and relative prefixes check, commands skip); bare spaced spans verify-if-resolvable with the rest a documented recall limit; `=|*?[]` spans skip as kv/pipe/glob prose; extensions must bear a letter (`v1.2` skips, `f.c` checks). The seeded pin grows to dead-seven with the sweep's command/list/glob/kv/version spans silent. The live sweep over all 36 stamped sections fires only the 6 true-dead cites in D00 T01 §1, D00 T01 §2, and D00 T01 §3, zero false positives; the six file as record debt to D00 T04 §24.

### F16 -- PASS binding restatement (third report) -- record -- DUPLICATE of F4 (independent) [minor]

Round-2 R restates F4 a third time (independent F4, round-1 A2): a forged PASS still attests while item 4's Done reads complete. The Done names the residual and its owner since R1; the item's letter (transcript hash, an explicitly listed equivalent) is met and run-witnessing is the filed strengthening. No new defect; the D00 T04 §24 item owns it.

```text
Round-2 output (Sol medium), quoted verbatim:
RECEIPT sha=895f54db6c205bf9a27f092877c8ffe51e2ec5db6ef24506d895197cb2e45e8f end=PANEL-ff2513e2bad693f2 nonce=53aa9727b7e10397
**adversarial: needs-attention** (1)
1. scripts/review_prompt.py:796-838 and scripts/review_prompt.py:1064-1145 reduce patches to multisets of signed lines and selected markers while discarding context, hunk positions, and \ No newline at end of file; a modified patch with the same multisets passes check_commits_covered, so the declared commits are not content-exactly covered.
**consistency: needs-attention** (1)
1. scripts/review_prompt.py:1434-1456 and scripts/review_prompt.py:2867-2875 cannot derive a Codex model from runner output and instead accept the caller's --model value, so an incorrect Codex model claim cannot produce the required hand-supplied mismatch failure.
**integration: needs-attention** (1)
1. scripts/review_prompt.py:940-941 and scripts/review_prompt.py:1027-1050 recognize cited paths only through [\w./-]+; backticked paths containing spaces or other legal filename characters are silently skipped, so check-anchors does not resolve every cited file or line as contracted.
**record: needs-attention** (1)
1. scripts/review_prompt.py:2896-2901 accepts any supplied one-line PASS file without binding it to the runner output or manifest; todo/00-workspace/TODO-04-self-correction.md:927 nevertheless marks the actual-run binding complete while todo/00-workspace/TODO-04-self-correction.md:1016 explicitly records that a fabricated PASS still attests.
```
