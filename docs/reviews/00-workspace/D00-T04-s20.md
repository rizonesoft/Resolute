# Review -- D00 T04 §20, Review-Tooling Operability Follow-Ups

**Reviewed:** 2026-09-20
**Stance:** post-implementation
**Rounds:** Sol 1 (4 findings, all fixed), Sol 2 (1 finding fixed, 1 advisory noted), Opus sign-off 3 (1 advisory filed); Sol plan review (21 findings: 7 filed, 3 corrected pre-stamp, 11 rejected)
**Tier:** Full panel. The diff touches `scripts/todo-validate.py` (the partial-flip rule), `scripts/todo-graph.py` (fence machine, fixtures), `scripts/todo-runs.py` (leg watch), `scripts/review_prompt.py` (envelope tooling), `scripts/check-all.ps1` (gates), and two skills: shared contract surface.
**Attestation:** `docs/reviews/00-workspace/D00-T04-s20.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `24b05a4` | the §20 implementation: fence `cygpath` form, three check-all gates, stderr redirect, cost-sum header plus Claude envelope tooling, candidate git guard, combined §8 pointer, run-file discipline, leg watch plus comparisons bank, opportunity label naming, §19 four-count plus stamp-cut removal, Commit history binding, Commit cardinality, shipped-owner proof, typed citation, rule 7b, `--report-shipped-items`, fence boundary markers |
| `898cc17` | answers round 1: strict telemetry types, word citations, overlap ARMED, cost item unstruck |
| `e66535e` | answers round 2: contiguous citation sequences |
| `47b0cce` | ticks the cost item on the round-3 sign-off telemetry |
| `6b69c23` | answers the plan review: four-block wording, determinism proof, canonical pointer |

Range `af4b166..24b05a4` (1 commit on trunk at round 1). Round 1 fenced the full range (12 files, cross-check agrees).

## The three questions

**Does it make sense?** Yes. All 20 items map to committed changes: the fence form (3 skill blocks), the three gates (check-all), the stderr redirect (one probe leg), the cost sum (header prose) plus envelope tooling (`panel_text_from_envelope`, `round_cost_from_envelope`, `round-cost` CLI, 5 pins), the git guard (one except clause plus pin), the combined §8 pointer (one paragraph), the run-file discipline (one skill sentence), the leg watch (`leg_lines` plus bank file plus skill rule), the opportunity naming (2 messages plus 4 pins), the four-count (one word) plus stamp-cut removal (cut deleted, twins retargeted, §19 pointer), the history binding (exact-or-prefix plus unreadable warn), cardinality/placement (pre-pass plus §21/§22), shipped-owner proof (`Resolved:` citation plus §23-§26), the typed citation (forward item plus §27-§30), rule 7b (§31), the disposition report (shared classifier plus flag plus 9 pins), the fence markers (line-map view plus §32). No widening beyond the filed items.

**Is it logical?** Yes, with four round-1 corrections applied (see below). The exemption count reads three live with the §19 four reading stamp-time via the pointer; containment is exact-or-prefix with the reason recorded; the proof lands owner-side because the deferrer is stamped; the citation names section plus item; the report shares the scan's classifier; markers collapse stripped regions to blank boundaries.

**Did it break anything else?** No. `todo-graph self-test` 553 green with every pre-existing case intact; `validate` 0 fatal with the 5 live Commits and the T02 §4 deferral re-passing every tightening; `todo-claims` 112 hold; plan 36/215 current; check-all 16/16 green; the fence refactor and classifier extraction are suite-pinned behavior-preserving (541 green at each step).

**source-defect:** approves in-session. **design:** not owed (no surface). **Research pass:** skipped (pure infrastructure: validator rules plus tests plus records).

## Self-review

Re-read the candidate diff file by file rather than from memory. 154 deleted lines across 12 files; both post-stamp pointers are prose additions with no checklist or stamp line touched; the §15 item-9 citation verified against the §15 checklist (item 9 is the stamp-round record shape); the run-file discipline sentence verified embedded in the process-plan line. No self-catches: implementation verified item by item with drives quoted before commit. Gates green before round 1.

## GPT panel Round 1

Round 1 over `af4b166..24b05a4` (12 files, cross-check agrees). Sol medium, exit 0, latency 174s, cost 103949tokens. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

RECEIPT sha=45b20ba414c457333ae56be902545bfb540307c74e22c834be5a9d110b99145b end=PANEL-10850f37bff7cbfc nonce=1a1dbdd4336e082b
**adversarial: needs-attention** (2)
1. `scripts/review_prompt.py:424-435` treats missing token classes as zero and coerces booleans, floats, and numeric strings with `int()`, so malformed Claude telemetry can be accepted and recorded as a false cost instead of failing.
2. `scripts/todo-validate.py:298-300` validates the cited owner item by case-insensitive substring containment, so a vague citation such as `(item: "work")` can match an unrelated checklist item and falsely satisfy the required item-level acknowledgment.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/todo-runs.py:640-652` fires the overlap leg whenever the mean Jaccard across every banked comparison exceeds 0.5; it neither limits comparisons to the rolling window nor checks that the trailing rung's value-per-cost is strictly below half the leader's, so it can announce a cut trigger that the authoritative rule does not fire.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:868` marks the Claude-cost item complete while explicitly saying its required driven-round evidence is pending, and `docs/phase-runs/2026-09-20-phase-0.md:96` repeats that the leg still awaits this sign-off; the `[x]` completion claim and checkpoint are therefore not yet earned.

### F1 -- cost telemetry coerced instead of rejected -- adversarial -- FIXED in `898cc17` (independent) [major]

Fair: `int()` accepts bools, floats, and numeric strings as false costs. Fixed: present classes must be strict integers (`type(value) is not int` rejects bool/float/str; negatives rejected), absent classes read as zero with the omission rationale recorded; 5 new pins (bool/float/string/negative/absent); suite 94 green.

### F2 -- substring citation satisfies off unrelated items -- adversarial -- FIXED in `898cc17` (independent) [major]

Fair: substring containment lets `(item: "net")` satisfy off "Do the network work". Fixed to word matching in `898cc17` (round 2 F5 tightens it further to contiguous sequence): the stamp-line lifecycle keeps containment deliberately, noted beside both matches; new §33/§34 fixture pair pins the vague shape; live T02 §4 re-passes.

### F3 -- interim overlap announces unfired cuts -- integration -- FIXED in `898cc17` (independent) [major]

Fair: the interim fired on the Jaccard half alone. Fixed: overlap reports ARMED (Jaccard half met, blinded value half pending) and never FIRED; both FIRED (zero rule) and ARMED file the early revisit, with the revisit running the blinded half; the interim reports the whole bank verbatim and the revisit windows it. Pin renamed to `legs-overlap-armed`; skill rule covers both.

### F4 -- cost item struck ahead of its evidence -- record -- FIXED in `898cc17` (independent) [major]

Fair: the tick ahead of the recorded-round evidence violated completion-first. Fixed: unstruck with an in-review note in `898cc17`; re-ticked in `47b0cce` on the round-3 sign-off telemetry (`2318481tokens` quoted from the `round-cost` drive).

## GPT panel Round 2

Round 2 over `24b05a4..898cc17` (6 files, cross-check agrees) with round-1 verdicts as priors. Sol medium, exit 0, latency 95s, cost 31554tokens. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` advisory

RECEIPT sha=a14d6112d42861b1bc3d976afa5a210967652c58562a3db76a416249b6b35e42 end=PANEL-4cc3b4540038e968 nonce=81b4ce3ccb587383
**adversarial: needs-attention** (1)
1. `scripts/todo-validate.py:305` uses unordered set containment, so vague or reordered citations such as `(item: "the work")` can satisfy unrelated target items containing those common words; this does not mechanically prove that the citation names the owner's specific item.
**consistency: approve**
**integration: approve**
**record: advisory** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:868` correctly reopens the cost-accounting item, but the candidate remains ineligible for a §20 completion stamp until the current Claude round's resolved cost is recorded and the item is ticked.

Transcript note: the round's `:868` and `:305` cites check out against blob `898cc17`.

### F5 -- unordered word match satisfies reordered citations -- adversarial -- FIXED in `e66535e` (independent) [major]

Fair: bag-of-words matching accepts reorderings. Fixed: contiguous word-sequence matching (citation words in order and consecutively, case- and punctuation-insensitive); new §35/§36 fixture pair pins the reordered shape; live T02 §4 re-passes; suite 553 green.

### F6 -- stamp ineligible until sign-off cost records -- record -- advisory (independent) [minor]

Noted, resolved by the round-3 Opus sign-off: the cost item re-ticked in `47b0cce` on the sign-off telemetry (`2318481tokens`). No code change; the advisory prescribed exactly the flow that ran.

## Opus panel Round 3

Round 3 over `af4b166..e66535e` (12 files, cross-check agrees) with rounds 1-2 verdicts as priors. Opus medium, exit 0, latency 102s, cost 2318481tokens (input 34 + output 15798 + cache-read 2174797 + cache-create 127852, via `round-cost`). check-panel: PASS four lenses, one verdict each (envelope `result`).

`adversarial` approve
`consistency` advisory
`integration` approve
`record` approve

RECEIPT sha=40c197a153fa7e90dded39ef27432a47db8e28b049de2a30ccbed5a822ce40da end=PANEL-2c2794f3b1ed95d9 nonce=6bca51792c729851
**adversarial: approve**
**consistency: advisory** (1)
1. `todo/README.md:194-208` and `todo/README.md:429`: the format spec still documents only the `> **Deferred:**` stamp lifecycle (line 201's `(item: "...")` rule, resolved by validate.py's containment search at `scripts/todo-validate.py:811-815`), while this candidate makes three new authoring requirements FATAL for struck-item deferrals under `partial-flip-shipped` with no spec text anywhere in README: the forward XREF must carry `(item: "...")` (`scripts/todo-validate.py:290-297`), the owner's item must contain the cited words in order and consecutively rather than by containment (`scripts/todo-validate.py:307-322`, diverging from line 201's rule for the same syntax), and a shipped owner must carry `> **Resolved:**` citing the deferrer (`scripts/todo-validate.py:330-357`); an author following README writes `- [ ] ~~...~~ **Deferred...** / -> XREF: §N -- the owner` and hits a FATAL the spec never states.
**integration: approve**
**record: approve**

### F7 -- README lacks the struck-item deferral spec -- consistency -- FILED to D00 T04 §24 (independent) [major]

Fair: the three struck-item requirements are FATAL with no author-facing spec (code comments only). Filed to §24 (sign-off advisory, no re-round).

Manifest note: the round-3 rundir was destroyed by session scratch cleanup before attestation, so no manifest file survives; the attest input was re-materialized from the recorded receipt above as unpersisted external evidence (no tracked path). Its exact bytes were the two lines `TAG PANEL-2c2794f3b1ed95d9 nonce=6bca51792c729851` and `MANIFEST bytes=0 files=0 sha=40c197a153fa7e90dded39ef27432a47db8e28b049de2a30ccbed5a822ce40da base=af4b166f264800250d774d34935c77c6093fdae8 head=e66535e08a9becaf5d3f9f4f66e27422b919663c`, tag/sha/nonce copied verbatim from the receipt and `bytes`/`files` zeroed as unknown. Reproduction without the file: write those two lines and run the attest command from the skill with `--base af4b166f264800250d774d34935c77c6093fdae8 --head e66535e08a9becaf5d3f9f4f66e27422b919663c --tree 4902352d43ac9dc87f0e02e910fe6d1b88d20320 --reviewer claude-panel --model opus --verdict approve --checker 'PASS four lenses, one verdict each'`; the run emitted the staged attestation and read it back clean. The pinned sha is the genuine digest of the reviewed prompt (the round's checker PASS verified it while the manifest lived); only the byte-count fields are lost, and nothing downstream consumes them.

## Plan review

gpt run over §20 plus §19 and §12: 21 findings, 7 filed (6 to D00 T04 §24, 1 to D00 T04 §21), 3 corrected pre-stamp, 11 rejected. Transport adaptation: the skill's argv form fails past 48KB on this machine (`Argument list too long` through the node codex shim at 48451 bytes), so the fenced prompt rode stdin like the panel runner; the argv form's removal is filed to §24.

Manifest: sections [D00 T04 §20, D00 T04 §19, D00 T04 §12]; dependents [D00 T04 §12]; bytes 47192; run 20260920-D00-T04-S20-gpt

Ledger:
- [D00-T04-S20-PR1] [minor] end-to-end filing fixture -> rejected (filing runs through the agent skill, not code; no fixture can file through add-todo; trigger pinned, skill rule recorded)
- [D00-T04-S20-PR2] [minor] monetary cost accounting -> rejected (the face-value sum is the recorded design decision; monetary pricing is an external dependency the tree refuses, and Codex opacity forces the aggregate)
- [D00-T04-S20-PR3] [minor] suppress rankings on thin data -> rejected (coverage already labels the per-model subset; no standing ranking query exists to suppress; the future blinded-value half inherits the constraint via the ARMED record)
- [D00-T04-S20-PR4] [major] bank freshness unowned -> filed D00 T04 §24 (freshness item)
- [D00-T04-S20-PR5] [minor] trigger states lack legend -> filed D00 T04 §24 (legend item)
- [D00-T04-S20-PR6] [minor] reciprocal graph edges missing -> rejected (filing mentions are not ownership XREFs; §19 precedent; Depends/XREF edges intact)
- [D00-T04-S20-PR7] [minor] four-block wording overstates the arch proof -> accepted (fixed pre-stamp: three fence invocations plus assembly)
- [D00-T04-S20-PR8] [major] hostile fence paths unpinned -> filed D00 T04 §21 (hostile-paths item)
- [D00-T04-S20-PR9] [major] ticked Commit lines bind nothing -> filed D00 T04 §24 (ticked-binding item)
- [D00-T04-S20-PR10] [minor] cardinality never requires one -> rejected (factually wrong: rule 13 `no-commit-item` is FATAL over every section in any tick state)
- [D00-T04-S20-PR11] [minor] subject-prefix binding wants OID -> filed D00 T04 §24 (OID item)
- [D00-T04-S20-PR12] [minor] fail closed on unreadable history -> rejected (the warn is the recorded split: bare-tree exports must still validate)
- [D00-T04-S20-PR13] [minor] Reopened ambiguity -> rejected (factually wrong: audit stance mandates the line and the stamp-reopened rule polices it)
- [D00-T04-S20-PR14] [minor] durable item IDs -> rejected (a system-wide schema change no section owns; the sequence format is chosen and recorded)
- [D00-T04-S20-PR15] [minor] proof cannot disambiguate items -> filed D00 T04 §24 (proof-grammar item)
- [D00-T04-S20-PR16] [minor] post-review §12 commit unattested -> rejected (factually wrong: `a209c4a` is an ancestor of the §12 stamp `ef362d6`)
- [D00-T04-S20-PR17] [minor] split §20 by subsystem -> rejected (coherent batch; one-section-one-commit is the contract)
- [D00-T04-S20-PR18] [minor] items lack file names -> rejected (overclaims: items name scripts, Dones cite files, the section executed)
- [D00-T04-S20-PR19] [minor] counts fragile in flight -> filed D00 T04 §24 (count-discipline item)
- [D00-T04-S20-PR20] [minor] determinism unproven -> accepted (fixed pre-stamp: sort keys quoted, triple-run `c869165b3029fd0f` × 3)
- [D00-T04-S20-PR21] [minor] §19 four-live ambiguity -> accepted (fixed pre-stamp: pointer labels the design historical, names the canonical rule comment)
End of ledger

```
RECEIPT sha=88369a91388d1ddc0914bf9df7422693118ce0b6a7396a6bad40dcaaba1a5dd2 end=PLAN-693db0043cfacd68 nonce=43474539d01d279e
- The §20 interim-leg item is incomplete because the fired-leg filing path is exercised by self-test pins, not by running the actual rule, so D00 T04 §20 should either prove the end-to-end filing once or narrow the item to say only the trigger query is pinned.
- Comparing face-value token totals across runners is not yet solved: input/output/cache/reasoning have different meanings and prices, so D00 T04 §20's cost-accounting item must use monetary cost or refuse incomparable rows instead of declaring victory.
- Historical cost coverage is sparse (17 of 83 rounds, 0 of 24 Opus), so D00 T04 §20 should force its reports to label the comparable subset and suppress aggregate runner rankings when required telemetry is missing.
- The overlap-leg bank has no specified producer, schema, freshness rule, or failure behavior, so D00 T04 §20 should state who writes comparisons.md, what fields are authoritative, when a bank is fresh, and how the query behaves when the file is missing.
- The §20 trigger text says the count is met while no leg is armed; the plan must distinguish scheduled count triggers from cut triggers and state the action owed by each state so a reader can tell whether a revisit is due.
- §20's own section text, SOURCE records, and evidence records do not carry reciprocal graph edges for the sections it changed or superseded (§8, §15, §16, §18, §19); the plan should add explicit reciprocal links so those sections point to their live specifications.
- The fence proof claims four blocks while the evidence shows three fence commands and one assembly note; D00 T04 §20 should rerun or rewrite so every named block has matching command evidence, or define the architecture path as assembly-only.
- The cygpath fix is verified only for the current workspace and prompt size, so D00 T04 §20 or a follow-up should pin spaces, Unicode, equals signs, and alternate temp roots, plus a large prompt, before calling fence review complete.
- The history binding checks unchecked Commit lines while ticked Commit lines bypass every proof; the plan should require every Commit claim on a shipped section to resolve to a commit, or explain why ticked claims need no proof.
- Commit cardinality is stated as at-most-one while no rule requires exactly one; a code-changing shipped section can omit commit evidence entirely, so the plan should make the commit executable or explicitly allow omission.
- Prefix matching can bind a Commit line to an unrelated or duplicated historical subject; the plan should define a stronger representation such as the immutable commit OID plus ancestry and section identity.
- The Commit exemption fails open: unreadable history only warns, which lets malformed or adversarial trees pass validation quietly; the plan should fail closed by default and require an explicit, narrowly documented export mode.
- Reopened sections are silently exempted from the new open-but-stamped rule by removing them from Verified coverage; the plan should require an explicit Reopened marker and state the exact precedence so visible `[ ]` + `Verified:` cannot read ambiguous.
- Item-level back-pointers rely on consecutive-word coincidence instead of stable item identity; durable section-item IDs would let owners and resolvers cite the same debt without fragile word containment or reordering issues.
- The shipped-owner proof cites only the section, so two deferred items pointing at the same shipped owner collapse into one Resolved line; the proof grammar should require an item-level citation that disambiguates which debt closed.
- §12 permits a wording-only commit (a209c4a) after the final reviewed candidate without a panel re-round or attestation, so the stamped tree no longer equals the attested tree and the completion proof is weaker than §12 claims.
- §20 combines twenty follow-up behaviors into one implementation commit and one stamp; the plan should split such batches by subsystem so a single defect does not block unrelated review tooling.
- §20's items describe behaviors and commands but do not name the owning files, suites, or generated records; each item should identify the skill, script, test fixture, and generated record it changes so the batch is executable with zero context.
- Suite counts are quoted as prose rather than derived from machine artifacts; checkpoints should record per-item test deltas and machine-generated artifacts rather than totals that go stale during the run.
- `--report-shipped-items` is called deterministic without sort keys, canonical serialization, or a repeated-run comparison; D00 T04 §20 should state the ordering rules and record the byte comparison before claiming determinism.
- §19 shows four historical exemptions beside a live rule of three in one narrative; the plan should label the historical block and link to one canonical live rule so implementers do not copy the superseded count.
```

Plan manifest: 88369a91388d PLAN-693db0043cfacd68. Cost: 29,810 tokens (codex "tokens used"); latency 153s.

## Live proof

Section gates, each re-run after the last fix (candidate tip `e66535e`; the tree also carries the two filing commits `1cb4f2b` and `fceed42`, which touch only §21/§24 plan text). Digests are sha256 over the quoted bytes as shown (no trailing newline). All quotes bind the review's marker run (the plan-review run; panel rounds mint no run IDs).

- Suite: `todo-graph self-test: 553 cases, 0 failed`, covering all 20 §20 pins (strict-int telemetry, word citation, ARMED interim, count accounting, item-ledger provenance, interim-leg rule, strike registry, round-role, cost-disclosure fixture, overlap-leg bank, window cut, shipped-item query, deferral line, deferred-in grammar, deferral grammar, check/run/master evidence invocations, plus 4 helper semantics).

Provenance: candidate e66535e08a9becaf5d3f9f4f66e27422b919663c; command python scripts/todo-graph.py self-test; exit 0; tool CPython 3.14.4; digest fedb212463afbabd5b3889c7414f2be1af4d03c0742d1022c7e24b4efad4fce4; path docs/reviews/00-workspace/D00-T04-s20.md; run 20260920-D00-T04-S20-gpt

- Manifest round-trip: `review-prompt self-test: 94 cases, 0 failed`, covering the manifest-head equality pin and the fence query pins.

Provenance: candidate e66535e08a9becaf5d3f9f4f66e27422b919663c; command python scripts/review_prompt.py --self-test; exit 0; tool CPython 3.14.4; digest 0cf45051948317741ba8d6122629c32661a600f7a2c0ffe5fb699b28da7ff1b2; path docs/reviews/00-workspace/D00-T04-s20.md; run 20260920-D00-T04-S20-gpt

- Ledger inventory: `todo-findings: 226 parsed, 0 unreadable, 0 transition problem(s)`. The staged tree reads 233: the delta is this review's own 7 panel findings (F1-F7), which exist only in the staged findings file.

Provenance: candidate e66535e08a9becaf5d3f9f4f66e27422b919663c; command python scripts/todo-findings.py; exit 0; tool CPython 3.14.4; digest 752a00948c55ceefe04ac4853f3e15bfe28bc44ff8ecc3d824a0e1accd60eab7; path docs/reviews/00-workspace/D00-T04-s20.md; run 20260920-D00-T04-S20-gpt

- Claims: `todo-claims: 112 claim(s) -- 112 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`.

Provenance: candidate e66535e08a9becaf5d3f9f4f66e27422b919663c; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest 95616e39a4200e824612521c83820e14d00a5fdbbc56d3d7625c0c90c32a1366; path docs/reviews/00-workspace/D00-T04-s20.md; run 20260920-D00-T04-S20-gpt

- Run records: `todo-runs self-test: 81 cases, 0 failed`, covering the interim-leg count rule and the live ROUND_ROLE grammar.

Provenance: candidate e66535e08a9becaf5d3f9f4f66e27422b919663c; command python scripts/todo-runs.py --self-test; exit 0; tool CPython 3.14.4; digest 75cb3da6e8521ad52dfd0606de6f73f3182baf95e614514ccba847a7793db205; path docs/reviews/00-workspace/D00-T04-s20.md; run 20260920-D00-T04-S20-gpt
