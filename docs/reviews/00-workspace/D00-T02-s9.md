# Review -- D00 T02 §9, Rendered-Output Regression Tests

**Reviewed:** 2026-09-26
**Stance:** post-implementation
**Rounds:** independent pass (4 findings: fixed); GPT 1 bulk (3 fixed); GPT 2 bulk (2 fixed); GPT 3 sign-off (1 blocking and 1 advisory, fixed); GPT 4 depth (1 blocking and 1 advisory, fixed); GPT 5 depth (approve).
**Tier:** Full panel. The diff changes all five D2D controls, the popup menu, and the icon resolver in the shared UI library every tool consumes, and adds the render harness.
**Attestation:** `docs/reviews/00-workspace/D00-T02-s9.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `bba2c4ad` | implementation: `tests/ui_render_test.cpp` and 48 goldens under `tests/golden/`, `RenderTo` on the five D2D controls and `PopupMenu::Measure`/`RenderTo(HDC)`, `AnimationManager::Flush`, the icon resolver with the `square-dashed` fallback, the toolbar overflow fix, and the capture matrix convention |
| `4d81a3ec` | answers the independent pass: the overflow submenu, the stated capture appearance, the strict-mode lookup, and the sized diff |
| `d51e60ae` | answers panel round 1: `RenderTo` reports the drawing's result, the public icon APIs report unknown names, and the record correction |
| `bd0feecf` | answers panel round 2: the draw calls draw the fallback, and the re-quoted `[render]` figure |
| `714571af` | answers panel round 3: the union diff with artifacts on a size change, and the unbound-target failure case |
| `8e1788a5` | answers panel round 4: the window target survives an offscreen failure, and the crossed-size count |

Record commits (filings only, no reviewed content): `29391688` (the plan-review findings folded into D00 T02 §12).

Range `bba2c4ad^..8e1788a5` (base `adf77c1c`), contiguous, no declaration owed. Each round fenced the range from `adf77c1c` to its own head. The rundir is session scratch outside the tree (/tmp/review-il6q2iiP, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Makes sense: every shared control renders offscreen through the same paint path its window uses, into goldens a human looked at, and the comparator is proven to catch what it exists to catch (a one-pixel list view drill failed all four list view goldens by name, and a misspelled sidebar icon failed by name and by pixels). Logical: animations are flushed to their end so a golden shows the settled state, the tolerance is stated with its reason, and the goldens are pinned to their host with the restake procedure written down. Broke nothing: `check-all: 20 gate(s) ok` at every answer, `tidy` under its baseline. The goldens found a real defect (a toolbar too narrow for its first item hid every command with no overflow button), fixed here.

`source-defect`: owed and done: `ID2D1RenderTarget::EndDraw` and `ID2D1DCRenderTarget` behavior per Microsoft's documentation (an unbound DC target fails its `EndDraw`, which the drill confirmed). `design`: owed and done: every golden was looked at before committing, light and dark at 96 and 144, including `glyphs-*` (the seven restored glyphs and the fallback at their drawn size). The launcher's four window captures moved to D00 T02 §10, which owns every headful run.

## Independent pass

`python scripts/panel_slots.py exec independent --commit bba2c4ad...`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 169s, clock 2026-09-26T15:10:25Z, cost unresolved (the review runner printed no token figure). Output and stderr stay in the build scratch outside the tree (build/rev9s/independent.out, independent.err); the output is transcribed below.

### F1 -- an overflowed View dropdown offers a command nothing handles -- integration -- FIXED in `4d81a3ec` (independent) [minor]

`Toolbar::BuildOverflowMenu` gives a dropdown a submenu of its choices; `An overflowed toolbar offers every hidden command` pins the four View choices and Refresh.

### F2 -- the capture sidecar labels the system setting as the window's appearance -- record -- FIXED in `4d81a3ec` (independent) [minor]

`capture-window.ps1 -Appearance light|dark` states the appearance; without it the sidecar records the system setting, labelled as such.

### F3 -- an absent theme value throws under strict mode before the fallback -- adversarial -- FIXED in `4d81a3ec` (independent) [minor]

The lookup checks the property exists before reading it, so the `unknown` fallback runs.

### F4 -- the shift leg can read past a smaller golden -- adversarial -- FIXED in `4d81a3ec` (independent) [major]

`DiffCount` refuses mismatched sizes and the shift leg asserts sizes first; round 3 later replaced the refusal with a union comparison (F10).

```text
The render suite passes locally with 482 assertions across nine cases. However, the newly reachable overflow action is ineffective, capture metadata can be incorrect or fail entirely, and a geometry regression can trigger an out-of-bounds read.

Full review comments:

- [P2] Preserve View choices when the first toolbar item overflows — R:\GitHub\Resolute\shared\resolute-ui\src\controls\toolbar.cpp:545-546
  When `m_overflowStart == 0`, the newly enabled overflow menu exposes View as a plain command rather than a dropdown. Its selection sends `IDC_TB_VIEW`, but the launcher only handles `IDC_TB_VIEW_LARGE`, `SMALL`, `LIST`, and `DETAILS`, so selecting View does nothing. Populate a submenu from the item's choices or open its existing popup.

- [P2] Record the captured application's actual appearance — R:\GitHub\Resolute\scripts\capture-window.ps1:216-217
  When the launcher explicitly selects light or dark through `Theme::Toggle`, its appearance can differ from `AppsUseLightTheme`. This code then labels a dark capture as light, or vice versa, undermining the new capture-matrix evidence. Record the application's effective appearance, or require an explicit capture parameter instead of treating the system preference as authoritative. Explicit theme overrides are required by [DESIGN.md:63-64](R:/GitHub/Resolute/DESIGN.md#L63-L64).

- [P2] Handle an absent theme registry value before accessing its property — R:\GitHub\Resolute\scripts\capture-window.ps1:216-217
  If `AppsUseLightTheme` is absent, `Get-ItemProperty -ErrorAction SilentlyContinue` returns no object. Because this script enables strict mode, accessing `.AppsUseLightTheme` throws before the intended `unknown` fallback runs, leaving the capture without its sidecar. Check the returned object/property first or use a nullable value lookup.

- [P2] Check image dimensions before the shift test compares pixels — R:\GitHub\Resolute\tests\ui_render_test.cpp:451-453
  If the sidebar width increases without restaking its golden, this call passes differently sized images to `DiffCount`, which indexes `want.px` for every pixel in `actual`. It consequently reads beyond the golden's buffer instead of reporting a controlled regression failure. Add the dimension assertions already used in `CheckGolden`, or make `DiffCount` reject mismatched dimensions.
```

## GPT panel Round 1

Round 1 over `bba2c4ad^..4d81a3ec`. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 52s, clock 2026-09-26T15:26:10Z, cost 43817tokens. Output and stderr stay in the session scratch rundir outside the tree (r1.out, r1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F5 -- RenderTo reports success from a pointer, not the drawing's result -- adversarial -- FIXED in `d51e60ae` (independent) [minor]

Each D2D control keeps its last `EndDraw` result in `m_paintHr`, set to `E_PENDING` before an offscreen paint, and `RenderTo` returns `SUCCEEDED(m_paintHr)`.

### F6 -- a name passed straight to the public icon API bypasses the reporting -- integration -- FIXED in `d51e60ae` (independent) [minor]

`Render`, `CreateBitmap`, and `GetSvgData` record an unknown name as `Resolve` does.

### F7 -- item 3 and the checkpoint still demand the launcher's four captures -- record -- FIXED in `d51e60ae` (independent) [minor]

Both are marked **Corrected 2026-09-26** and name D00 T02 §10 as the captures' owner.

```text
RECEIPT sha=80e5a3498b996a10a1cfc20aca37a6d53dfdda85443b1b5879906c299ddc6518 end=PANEL-0825c8dc5c38fb1a nonce=d7133f7cef4840ea
**adversarial: needs-attention** (1)
1. `shared/resolute-ui/src/controls/contentview.cpp:488`, `listview.cpp:1169`, `sidebar.cpp:1120`, `statusbar.cpp:762`, and `toolbar.cpp:891`: every `RenderTo` reports success from pointer presence rather than the drawing HRESULT. `OnPaint` clears that pointer only for `D2DERR_RECREATE_TARGET`, so other drawing failures return true and satisfy the render test's success assertion. Propagate the actual drawing result.
**consistency: approve**
**integration: needs-attention** (1)
1. `shared/resolute-ui/src/icons.cpp:69,80` and `shared/resolute-ui/include/resolute/icons.h:17-19`: validation remains opt-in through `Resolve`; direct `Render`, `CreateBitmap`, and `GetSvgData` calls retain their silent unknown-name behavior. A computed or malformed name passed directly to these public APIs bypasses logging, fallback, and `UnknownNames()`, leaving the contract's explicit API-boundary coverage incomplete.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:572,576`: the capture item is checked complete and transfers the launcher matrix to §10, but its acceptance wording and the section checkpoint still require those four PNGs and sidecars in §9. Align both with the recorded transfer so the section has one unambiguous completion gate.
```

## GPT panel Round 2

Round 2 over `bba2c4ad^..d51e60ae` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 29s, clock 2026-09-26T15:42:25Z, cost 44810tokens. Output and stderr stay in the session scratch rundir outside the tree (r2.out, r2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F8 -- the direct draw calls report an unknown name but still draw nothing -- integration -- FIXED in `bd0feecf` (independent) [minor]

`Render` and `CreateBitmap` draw `Resolve(name)`, so an unknown name draws the fallback, compared pixel for pixel; `GetSvgData` stays a strict lookup, which the icon manifest relies on, and the log line no longer claims a fallback on every path. The older `[icons]` case expecting null from the draw calls is corrected with it.

### F9 -- the recorded green run predates the review's added cases -- record -- FIXED in `bd0feecf` (independent) [minor]

Item 2 re-quotes `[render]` at the answering commit, and again at each later answer.

```text
RECEIPT sha=d03018660183f2cb7e449a69e08ae6eb2c40cfb06ae915be13c24ed065b07b6e end=PANEL-c847c8feeff6afe2 nonce=4c8af43a08f664d4
**adversarial: approve**
**consistency: approve**
**integration: needs-attention** (1)
1. `shared/resolute-ui/src/icons.cpp:79-98`: direct `Render`, `CreateBitmap`, and `GetSvgData` calls now record unknown names but still return null instead of rendering or supplying the fallback. This leaves the prior API-boundary finding partially unresolved: malformed or computed names passed directly still produce missing glyphs, despite the diagnostic claiming a fallback is being drawn.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:571` and `docs/phase-runs/2026-09-25-phase-0.md:305`: the recorded green result covers nine test cases, whereas the candidate contains twelve render test cases. No updated run evidence covers the added overflow-menu, direct-icon-API, and drawing-result tests; the quoted result does not establish that the revised candidate passes.
```

## GPT panel Round 3

Round 3 over `bba2c4ad^..bd0feecf` the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 51s, clock 2026-09-26T16:05:31Z, cost 51894tokens. Output and stderr stay in the session scratch rundir outside the tree (r3.out, r3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` advisory

### F10 -- a golden of another size aborts before its artifacts and count -- adversarial -- FIXED in `714571af` (independent) [major]

Blocking at the sign-off: item 2 owes the diff image and count on failure. `DiffCount` compares over the union of both sizes and `CheckGolden` fails with its artifacts; `A golden of another size fails with a count and a diff` pins it.

### F11 -- the RenderTo failure case exercised only the null guard -- record -- FIXED in `714571af` (independent) [minor]

Advisory. The case paints into a DC render target never bound to a DC, whose `EndDraw` fails; with the sidebar's result reverted to the pointer test it failed at `ui_render_test.cpp:545` (drilled, reverted).

```text
RECEIPT sha=7a474824417dc7c13dd93cb50bb7b949cf2d9aacd3e254778ab3ba43bd4b5cbe end=PANEL-59119561ae208f18 nonce=08ece48ad9ba7dcc
**adversarial: needs-attention** (1)

1. `tests/ui_render_test.cpp:189-197`: dimension mismatches abort `CheckGolden` before artifact generation. A control-size regression therefore produces neither the actual/diff images nor the differing-pixel count required on failure. Handle unequal dimensions through an artifact-producing failure path.

**consistency: approve**
**integration: approve**
**record: advisory** (1)

1. `tests/ui_render_test.cpp:518-529`: the drawing-result test claims to exercise a failed drawing operation, but only tests successful rendering and `RenderTo(nullptr)`. The null guard never reaches `OnPaint` or `EndDraw`, so this test would pass with the original pointer-based success bug. Exercise a non-null target that returns a drawing failure and correct the comment.
```

## GPT panel Round 4

Round 4 over `bba2c4ad^..714571af` a depth round for the blocking sign-off finding, with prior verdicts appended. Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 96s, clock 2026-09-26T16:18:46Z, cost 49576tokens. Output and stderr stay in the session scratch rundir outside the tree (r4.out, r4.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` advisory
`consistency` approve
`integration` needs-attention
`record` approve

### F12 -- an offscreen RECREATE_TARGET resets the window's target -- integration -- FIXED in `8e1788a5` (independent) [major]

Blocking: resize and paint would disagree afterwards. Each of the five controls recreates only when the failing target is its own window target. No fixture makes D2D return `D2DERR_RECREATE_TARGET` on demand, so the guard is proven by source reading, stated as such.

### F13 -- crossed sizes count the corner neither image has -- adversarial -- FIXED in `8e1788a5` (independent) [minor]

Advisory. `DiffCount` skips a pixel in neither image; 2x3 against 3x2 counts 4, pinned.

```text
RECEIPT sha=92e3d756ad46a20e6e2da475489811fc6e9e055b03403b5e90984b671f35a8c6 end=PANEL-dde7e93e7a8aa0dd nonce=d7651247e0c9f5b4
**adversarial: advisory** (1)
1. `tests/ui_render_test.cpp:155-168`: `DiffCount` counts pixels outside both images as differences when their dimensions cross. Identical overlapping pixels in 2×3 and 3×2 images produce five differences instead of four because the nonexistent bottom-right pixel is counted. Skip coordinates outside both images.
**consistency: approve**
**integration: needs-attention** (1)
1. `shared/resolute-ui/src/controls/contentview.cpp:391,492`, `shared/resolute-ui/src/controls/listview.cpp:502,1173`, `shared/resolute-ui/src/controls/sidebar.cpp:661,1124`, `shared/resolute-ui/src/controls/statusbar.cpp:589,766`, and `shared/resolute-ui/src/controls/toolbar.cpp:612,895`: an offscreen `D2DERR_RECREATE_TARGET` resets the unrelated `m_hwndRt`, after which `RenderTo` restores the saved window target only into `m_rt`. Subsequent resize handlers skip resizing because `m_hwndRt` is null, while painting sees a non-null `m_rt` and skips recreation. Preserve the window target when handling an offscreen target failure.
**record: approve**
```

## GPT panel Round 5

Round 5 over `bba2c4ad^..8e1788a5` the hard-cap depth round confirming the round 4 answer, with prior verdicts appended. Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 28s, clock 2026-09-26T16:33:37Z, cost 42836tokens. Output and stderr stay in the session scratch rundir outside the tree (r5.out, r5.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` approve
`record` approve

```text
RECEIPT sha=e125d9c0eb1fc3fb642c4c59db70b12ecad79853f0d78456a10a53ce4a5e6f5e end=PANEL-c028704ab17025f0 nonce=f3847d9ce3926658
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Plan review

Manifest: sections [D00 T02 §9, D00 T02 §5, D00 T02 §12]; dependents [D00 T02 §12]; bytes 24855; run 20260926-D00-T02-S9-astra

`bytes 24855` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section, its dependency D00 T02 §5, and its dependent D00 T02 §12. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 41s, clock 2026-09-26T16:34:25Z, cost 17749tokens. Check: `PASS 13 findings, one per line`.

Triage follows the operator direction of 2026-09-26 (review leftovers must not grow the plan unconditionally): five findings about the port comparison become one item of the open D00 T02 §12, and eight are rejected with a reason. No new section.

Ledger:
- [D00-T02-S9-PR1] [minor] the port comparison has no composed launcher baseline to compare against -> filed D00 T02 §12
- [D00-T02-S9-PR2] [minor] offscreen rendering is not machine independent -> rejected the goldens are pinned to their host by design and say so in `tests/golden/README.md`; a restake on another build is a reviewed change
- [D00-T02-S9-PR3] [minor] no gate fails when a new control lacks goldens -> rejected every section shipping a surface owes the matrix and its goldens, and review's design lens refuses a UI section without them
- [D00-T02-S9-PR4] [minor] flushed endpoints cannot catch animation defects -> rejected D00 T02 §7 drives the animations and D00 T02 §10's driven-coverage item owns the rest
- [D00-T02-S9-PR5] [minor] high contrast has no rendered acceptance -> rejected D01 T02 owns high contrast winning over the palette, with its own acceptance
- [D00-T02-S9-PR6] [minor] focus, hover, disabled, and long-text states lack goldens -> rejected interaction states are driven by D00 T02 §10's coverage item; each control's goldens cover the states it has today
- [D00-T02-S9-PR7] [minor] restaking has no acceptance procedure -> rejected `tests/golden/README.md` requires looking at every changed image, and a restake commits old and new PNGs to a reviewed diff
- [D00-T02-S9-PR8] [minor] the region comparison does not define how each property is extracted -> filed D00 T02 §12
- [D00-T02-S9-PR9] [minor] the comparison would freeze intended redesigns -> filed D00 T02 §12
- [D00-T02-S9-PR10] [minor] the DPI-only and theme-only exemptions are unbounded -> filed D00 T02 §12
- [D00-T02-S9-PR11] [minor] mandatory and skippable DPI legs disagree -> filed D00 T02 §12
- [D00-T02-S9-PR12] [minor] a capture matrix proves images exist, not that a human looked -> rejected the design lens records its verdict on the captures in each section's candidate-bound findings file
- [D00-T02-S9-PR13] [minor] raw lookup and rendering disagree on unknown names -> rejected settled in this candidate (`bd0feecf`): the draw calls draw the fallback, the lookup stays strict, the header states both, and the older case is corrected
End of ledger

13 findings: 5 filed as one item in D00 T02 §12 (`29391688`), 8 rejected.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=26d7f2524f270443c090148e95b01fa6964864a715cbeff82dd4b7c8820c2802 end=PLAN-29219c0c3153e8d0 nonce=9f12dd2273ffa8e6
- §12 requires a launcher screenshot to match §9’s 150% golden, but §9 supplies individual control renders; §12 must own a matching composed-surface baseline and explicit crop, dimensions, state, and rasterization rules so the comparison is executable.
- Offscreen WIC rendering does not establish machine independence; §9 should pin fonts, rasterizer environment, locale, and rendering settings, then demonstrate repeatability across supported hosts so environmental drift does not encourage careless baseline replacement.
- “Every shared control” has no stated completeness gate; §9 should maintain a checked inventory of controls, states, appearances, and DPI variants that fails when required coverage is missing, preventing newly added controls from silently escaping regression tests.
- §9 renders selected settled states while §12 captures startup only; a connected successor to §5 should own animation ticks, intermediate frames, completion callbacks, and teardown during animation because flushing to the endpoint cannot detect flicker, stalled transitions, or lifetime defects.
- §5 records that high-contrast override does not exist, while §9 covers only light and dark; assign implementation and rendered acceptance to the shared theme/accessibility owner so unreadable controls cannot remain documented indefinitely as uncovered.
- The golden matrix omits explicit focus, disabled, selected, hover, long-text, and constrained-layout coverage; §9 should define required states per control and surface so premium interaction polish and clipping failures receive repeatable checks.
- Baseline replacement lacks an explicit acceptance procedure; §9 should require reviewable old, new, and diff images with the reason for each change so restaking goldens cannot silently approve the regression they should catch.
- §12 promises structural comparison without specifying how labels, values, control presence, and painted regions are extracted or compared; its region-diff item should define these mechanisms and separate acceptance rules because a generic tolerance cannot enforce all four.
- §12 treats renamed labels and changed controls as defects despite the permitted UI redesign; §12 should own an approved mapping of equivalent actions, content, and intentional redesigns so visual comparison preserves functionality without freezing the AutoIt interface.
- §12 allows DPI-only and theme-only differences without bounding the exemptions; its comparator should preserve requirements for visibility, clipping, contrast, and control presence under each exemption so actual rendering defects cannot pass as appearance differences.
- §12 permits a skip when a declared DPI is absent but requires the 100% pair unconditionally; its capture item and checkpoint should agree on mandatory coverage and skipped-run status so unavailable hardware cannot produce ambiguous completion.
- A capture matrix proves that images exist, not that a human inspected them; §9’s convention and §10’s capture delivery should require a candidate-bound review record identifying inspected surfaces and defects before claiming visual acceptance.
- §9 adds fallback icon behavior while §5’s recorded contract expects unknown names to resolve to null; a successor contract should distinguish raw lookup from public rendering behavior and specify diagnostics for every public entry point so callers and tests enforce consistent failure semantics.
```
