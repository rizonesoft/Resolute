# Review -- D00 T04 §22, Checker Diagnostic Codes and Structured Errors

**Reviewed:** 2026-09-20
**Stance:** post-implementation
**Rounds:** Sol 1 (4 findings: 4 fixed); Sol 2 (4 findings: 3 fixed, 1 refuted); Opus 3 (6 findings: 6 fixed); Opus 4 (3 findings: 3 filed)
**Tier:** Full panel. The diff touches `scripts/todo-findings.py`, `scripts/todo-runs.py` (both shared checkers), and adds `scripts/todo-diag.py` (shared registry): shared contract surface.
**Attestation:** `docs/reviews/00-workspace/D00-T04-s22.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `20ee3c4` | implementation: diagnostic registry, coded refusals, structured form, exit pins |
| `178cdfe` | self-review: `=`-form format flag, slash-normalized paths, plan sync |
| `89a8812` | answers panel round 1: usage strictness, registry pointers |
| `3b7ed18` | answers panel round 2: check-export flags, unreadable-export JSON, record wording |
| `232c44d` | answers panel round 3: tokenize re-think, live schema keys, exit docs, re-quote, record range |

Record commits (findings/record only, no reviewed content): `c65a3700`, `092801d1`, `9f481c80`. The Candidate table lists content commits only, so a record commit never needs a row in the table that ships inside it (§21 round-4 R1 convention). Later record commits join this list as they land, terminally at stamp (the findings file rides the stamp set, so the last list update needs no extra commit).

Range `5ac406b..9f481c80`. Fenced ranges: round 1 `5ac406b..178cdfe4` (4 files); round 2 `5ac406b..c65a3700` (6 files); round 3 `5ac406b..092801d1` (7 files); round 4 `5ac406b..9f481c80` (7 files). Cross-check agrees on rounds 1, 3, and 4; round 2 re-verified post-hoc from the transcript (prompt diff bytes equal the range diff, receipt matches the manifest, check-panel PASS re-run).

## Self-review (in-session, costs no round)

Worked the three questions across the candidate diff and its blast radius.
Fixes, committed in `178cdfe4` before the panel ran:

- Runs `--format` took only the space form while findings (argparse) accepts `--format=json` free: supported the `=` form in the runs manual parser plus a leg.
- Runs emitted native-separator paths while findings slash-normalizes: runs paths render `as_posix` in both text and JSON.
- Plan projection stale after the item ticks: re-synced (`build/` holds the derived projection, so no tracked diff).

**Does it do what the section says?** Yes. Item 1: 12 families coded (FIND-001..005, RUN-001..007) with the registry once in `scripts/todo-diag.py` and unlisted codes raising. Item 2: `--format json` on both checkers (gate modes print exactly one array, green prints `[]`, usage stays text), schema plus malformed-input legs in both suites. Item 3: every family's exit pinned by main-level legs (usage 2, problems 1, green 0) with hermetic fixtures; live greens quoted.
**Is anything in the blast radius broken?** No. `check_transitions` returns `(lineno, message)` (no external consumers; 13 leg assertions updated); `run_check` returns `(code, lineno, message)` (callers: `main` plus one leg, both updated); `main` gains defaulted `collected`/`review_sections` overrides; argparse `--help` path untouched.
**Did it break anything else?** No. `todo-findings self-test` 45 green (34 plus codes/exits/schema legs); `todo-runs self-test` 103 green (81 plus codes/exits/schema legs) with every pre-existing case intact (one tuple unpack plus exact-shape updates for the new contract); `validate` 0 fatal; plan 37/215 current; check-all 16/16 green.

## GPT panel Round 1

Round 1 over `5ac406b..178cdfe4` (4 files, cross-check agrees). Sol medium, exit 0, latency 124s, cost 45904tokens. Rundir `/tmp/review-s22` (`sol-r1.out`, stderr `sol-r1.err`), manifest `PANEL-09a0a0d08efa7850` sha `415713cf...`. check-panel: PASS four lenses, one verdict each. Setup note: the first attempt is void (a paraphrased prompt asked backticked verdict lines, which the output grammar rejects); preserved as `sol-r1-void.out`, re-ran with the skill prompt, not counted as a round.

`adversarial` needs-attention (A1 fixed)
`consistency` needs-attention (C1 fixed)
`integration` approve
`record` needs-attention (R1, R2 fixed)

### F1 -- unknown flags and surplus positionals slip past usage refusal -- adversarial -- FIXED in `89a8812` (independent) [major]

Round-1 A1: runs `main` treated unrecognized `--switches` as runs paths (RUN-002 exit 1) and silently ignored surplus positionals (success), instead of the RUN-001 exit-2 usage refusal the contract demands. `rest` now rejects `--*` options and a second positional, and `--self-test` refuses extras (matching findings, where argparse is strict by construction). Legs pin all three shapes.

### F2 -- code list restated outside the registry -- consistency -- FIXED in `89a8812` (independent) [major]

Round-1 C1: both checker docstrings restated the code-to-family mapping, so the Done's "exactly one documented place" was false as written. Both slimmed to registry pointers. The cited self-test expected literals stay: executable pins that fail loud on drift, which is enforcement rather than documentation (second half refuted in place).

### F3 -- single-place record false while docstrings duplicate -- record -- FIXED in `89a8812` (independent) [minor]

Round-1 R1: the item-1 Done claimed the registry lives once while the docstrings duplicated it. Fixed by the F2 slimming; the Done stands unedited (its quotes are dated evidence, not a competing list).

### F4 -- exit-2 record false for unknown switches -- record -- FIXED in `89a8812` (independent) [minor]

Round-1 R2: the item-3 Done claimed every usage error pinned to exit 2 while unknown switches fell through to RUN-002. Fixed by the F1 validation; the Done extends with the new shapes quoted.

```text
Round-1 output (Sol plain text), quoted verbatim:
RECEIPT sha=415713cf6fa71b434091ca11006a780d0aaad7548977af03ebc7fabeffed9c42 end=PANEL-09a0a0d08efa7850 nonce=e66c34ca742d7554
**adversarial: needs-attention** (1)
1. `scripts/todo-runs.py:1808` treats unrecognized switches and surplus positional arguments as a runs path or silently ignores them, producing RUN-002/exit 1 or success instead of the required RUN-001 usage refusal/exit 2.
**consistency: needs-attention** (1)
1. `scripts/todo-findings.py:34`, `scripts/todo-findings.py:917`, `scripts/todo-runs.py:28`, `scripts/todo-runs.py:1559`, and `todo/00-workspace/TODO-04-self-correction.md:978` duplicate the diagnostic code-to-family registry outside `scripts/todo-diag.py`, violating the contract that the code list live in exactly one documented place.
**integration: approve**
**record: needs-attention** (2)
1. `todo/00-workspace/TODO-04-self-correction.md:978` records that the registry lives once, but the same code-to-family list is restated in both checker docstrings and self-tests.
2. `todo/00-workspace/TODO-04-self-correction.md:980` records every usage error as pinned to exit 2, but `scripts/todo-runs.py:1808` neither rejects nor tests unknown switches and surplus positional arguments as usage errors.
```

## GPT panel Round 2

Round 2 over `5ac406b..c65a3700` (6 files). Sol medium, exit 0, latency 184s, cost 61706tokens. Rundir `/tmp/review-s22` (`sol-r2.out`, stderr `sol-r2.err`), manifest `PANEL-e309d287d83e5564` sha `8c77f583...` nonce `bb871be0359128c0`. check-panel: PASS four lenses, one verdict each. Recovery note: the rundir was lost to `/tmp` cleanup before recording, so the prompt plus output above are recovered from the codex session transcript (`rollout-2026-09-20T21-00-45-01a0c031-3656-7f70-bb15-e091d0bbc61d.jsonl`) and re-verified rather than trusted: the prompt's diff bytes equal the range diff exactly, the receipt copies the prompt's manifest sha plus tag plus nonce, and check-panel re-run over the recovered output says PASS. Latency and cost derive from the transcript's first-to-last timestamps and final cumulative token figure.

`adversarial` needs-attention (A1 fixed)
`consistency` approve
`integration` needs-attention (I1 fixed)
`record` needs-attention (R1 refuted, R2 fixed)

### F5 -- check-export flags slip past usage refusal -- adversarial -- FIXED in `3b7ed18` (independent) [major]

Round-2 A1: the round-1 strictness covered the runs-file path but not `--check-export`, whose sole argument (including `--bogus`) fell through to RUN-007 exit 1 instead of the RUN-001 exit-2 usage refusal. `_refuse_export_args` now rejects `--*` flags and surplus positionals with the same RUN-001 shape, pinned by a leg.

### F6 -- unreadable exports ignore the structured form -- integration -- FIXED in `3b7ed18` (independent) [major]

Round-2 I1: the unreadable-export branch always emitted text, so `--check-export --format json <missing-file>` broke the item-2 promise that gate modes print exactly one JSON array. The branch renders JSON under `--format json`, pinned by a schema leg.

### F7 -- attestation file absent mid-review -- record -- REFUTED (independent) [minor]

Round-2 R1: the record's `Attestation:` line names `D00-T04-s22.attest.json` before the file exists. That is the skill's own ordering, not a defect: the attestation emits only after step 9 completes (stamp written, `Live proof` quoted, ledger regenerated), because attesting earlier binds bytes a later step rewrites. Creating the file mid-review to satisfy the reading would fabricate evidence.

### F8 -- no-behavior-changed record contradicts the fix it follows -- record -- FIXED in `3b7ed18` (independent) [minor]

Round-2 R2: the item-3 Done said "No behavior changed" immediately after recording the round-1 behavior change (unknown flags and surplus positionals moving to RUN-001 exit 2). The paragraph now sequences pins first (no change), then the round-1 tightening, then the round-2 tightening.

```text
Round-2 output (Sol plain text), quoted verbatim:
RECEIPT sha=8c77f583d1ff674d4f52270a5ffb9268d640539906c453fe5c5e6f0eda39b999 end=PANEL-e309d287d83e5564 nonce=bb871be0359128c0
**adversarial: needs-attention** (1)
1. `scripts/todo-runs.py:1788` treats any sole argument after `--check-export`, including unknown options such as `--bogus`, as an export path, producing RUN-007 with exit 1 instead of the required RUN-001 usage refusal with exit 2.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/todo-runs.py:1798` always emits unreadable-export RUN-007 failures as text, so `--check-export --format json <missing-file>` does not return the promised structured JSON form.
**record: needs-attention** (2)
1. `docs/reviews/00-workspace/D00-T04-s22.md:7` records an attestation at `docs/reviews/00-workspace/D00-T04-s22.attest.json`, but that file does not exist.
2. `todo/00-workspace/TODO-04-self-correction.md:979` says “No behavior changed” immediately after recording that unknown flags and surplus positionals changed from RUN-002 or success to RUN-001 exit 2.
```

## Opus panel Round 3

Round 3 over `5ac406b..092801d1` (7 files, cross-check agrees). Opus medium, exit 0, latency 88s, cost 926627tokens (envelope usage: input 22, cache-create 89130, cache-read 826534, output 10941; cache-read dominated). Rundir `/tmp/review-s22` (`opus-r3.out` JSON envelope, stderr `opus-r3.err`), manifest `PANEL-b24784ce14281dce` sha `f63adb28...` nonce `0ed6af29b3000823`, mirrored to `/tmp/s22r3-*.bak` (round-2 rundir lesson). check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 fixed)
`consistency` needs-attention (C1, C2, C3 fixed)
`integration` approve
`record` needs-attention (R1, R2 fixed)

Sign-off bar: A1 sits above it (a known hole in the just-pinned usage contract falsifies the item-3 Done, which is stamp-invalidating), so the file-and-stamp shortcut does not trigger and the fix-loop continues; C1-C3 plus R1-R2 ride the same candidate as same-surface fixes. A1 is also the third consecutive round patching the runs arg validation (round-1 rest, round-2 export), so the unit stops and re-thinks per the skill rather than taking a third patch. Round 4 runs on Opus as confirmation over the restructured candidate.

### F9 -- self-test plus format slips past usage refusal -- adversarial -- FIXED in `232c44d` (independent) [major]

Round-3 A1: `--format` popped before the `--self-test` gate, so `--self-test --format json` (both forms) ran the suite instead of refusing RUN-001 exit 2, while the Done claimed `--self-test` extras tightened. The interleaved pop-then-gate stages restructure into one non-mutating `_tokenize` pass plus gates that all read the same `rest`, with the `--self-test` gate first on raw argv; both forms refuse with a leg, every pre-existing leg passes unmodified, and the 18-shape family sweep below runs 0 failed. Deliberate deltas, both unpinned and previously pathological: `--self-test` with a malformed `--format` reports the self-test usage, and a doubled `--format` resolves last-wins.

```text
Sweep (python R:\tmp\s22r3sweep.py, 18 CLI shapes over fixtures):
ok  exit=2 --self-test --check
ok  exit=2 --self-test --format json
ok  exit=2 --self-test --format=json
ok  exit=2 --self-test foo.md
ok  exit=2 --self-test --bogus
ok  exit=2 --format
ok  exit=2 --format yaml
ok  exit=2 --format --check <file>
ok  exit=2 --check-export
ok  exit=2 --check-export a.json b.json
ok  exit=2 --check-export --bogus
ok  exit=1 --check-export --format json <file>
ok  exit=2 --bogus
ok  exit=2 a.md b.md
ok  exit=2 --report --export
ok  exit=1 --check --format json <file>
ok  exit=1 --check --format=json <file>
ok  exit=1 --check --format text --format json <file>
sweep: 18 shapes, 0 failed
```

### F10 -- describe docstring names the wrong exception -- consistency -- FIXED in `232c44d` (independent) [minor]

Round-3 C1: the docstring said unlisted codes raise KeyError while the raise (and both suites' pins) is ValueError. One-word doc fix; behavior and pins untouched.

### F11 -- schema-keys constant ships unread -- consistency -- FIXED in `232c44d` (independent) [minor]

Round-3 C2: SCHEMA_KEYS sat defined and never read while both suites spelled the literal (the panel's line numbers drift, the claim holds). `refusal` now builds keys from the constant, so a drifted key fails there (KeyError) or in the suites' literal schema legs, which stay literal as the enforcement.

### F12 -- exit-code docs omit exit 2 and the runs map -- consistency -- FIXED in `232c44d` (independent) [minor]

Round-3 C3 plus the family sweep: the findings exit line omitted exit 2 and the `--check` exit-1 conditions, and the runs docstring stated no exits at all. Both now state the 0/1/2 map with conditions named and no code list (round-1 C1 registry-pointer rule).

### F13 -- live-green quotes stale at the candidate tip -- record -- FIXED in `232c44d` (independent) [minor]

Round-3 R1: the item-3 Done quoted `ledger current, 269 finding(s)` against a 277 ledger. Re-quoted at the round-3 fix (§21 F21/F34 precedent); the runs green dates from pre-review-file because mid-review `runs --check` fires RUN-004 for §22 itself (the run block lands at stamp, §21 precedent), so both quotes land fresh terminally at stamp, whose review verifies figures.

### F14 -- record omits the head record commit -- record -- FIXED in `232c44d` (independent) [minor]

Round-3 R2: the record closed the range at `3b7ed181` and named `c65a3700` sole record commit while the fenced head was `092801d1`. Range extends to `092801d1`, which joins the record-commits list; later record commits join as they land, terminally at stamp (the findings file rides the stamp set).

```text
Round-3 output (Opus result text), quoted verbatim:
RECEIPT sha=f63adb2850fa71be2f4499cfc5401517ce7a2cfb93300daf2db36ab96af0ff14 end=PANEL-b24784ce14281dce nonce=0ed6af29b3000823

**adversarial: needs-attention** (1)
1. `scripts/todo-runs.py:1777-1796`: `--format` (both the `=` and space forms) is popped out of `args` before the `--self-test` strictness gate at line 1796, so `todo-runs.py --self-test --format json` satisfies `sorted(args) == ["--self-test"]` and runs the suite instead of refusing with `[RUN-001]` exit 2; the `exit-selftest-surplus` leg only drives `--self-test --check`, leaving the hole unpinned while the item-3 record claims `--self-test` extras are tightened to RUN-001.

**consistency: needs-attention** (3)
1. `scripts/todo-diag.py:48`: `describe()`'s docstring says "Unlisted codes raise KeyError" while line 53 raises `ValueError`, which is also what both self-tests pin.
2. `scripts/todo-diag.py:44`: `SCHEMA_KEYS` is defined and never read anywhere in the tree (both suites spell `["code", "line", "message", "path"]` literally at `scripts/todo-findings.py:1018` and `scripts/todo-runs.py:1660`), so the new shared module ships a dead constant that cannot drift-fail.
3. `scripts/todo-findings.py:31`: "Exit codes: 0 everything parsed, 1 at least one heading could not be read." still omits the usage exit 2 this section pins via `_CodedParser.error`, and omits that `--check` exits 1 on a stale ledger or incomplete transitions.

**integration: approve**

**record: needs-attention** (2)
1. `todo/00-workspace/TODO-04-self-correction.md:979` quotes the live green as `todo-findings: ledger current, 269 finding(s)` while the ledger shipped in the same candidate (`docs/reviews/findings.md:7`) reads 277 findings across 37 sections, so the quoted live run is stale at the candidate tip.
2. `docs/reviews/00-workspace/D00-T04-s22.md:18,20` names `c65a3700` as the sole record commit and closes the range at `3b7ed181`, but the candidate head is `092801d1` (`docs/reviews/00-workspace/D00-T04-s22.md`, `docs/reviews/findings.md`, `docs/reviews/transitions.md`), a second record commit inside the reviewed range that the record does not account for.
```

## Opus panel Round 4

Round 4 over `5ac406b..9f481c80` (7 files, cross-check agrees). Opus medium, exit 0, latency 89s, cost 670218tokens (envelope usage: input 14, cache-create 82144, cache-read 572251, output 15809; cache-read dominated). Rundir `/tmp/review-s22` (`opus-r4.out` JSON envelope, stderr `opus-r4.err`), manifest `PANEL-748db2c41f7bbe7a` sha `bc10ca7e...` nonce `271678335a211550`, mirrored to `/tmp/s22r4-opus-out.bak`. check-panel: PASS four lenses, one verdict each.

`adversarial` needs-attention (A1 filed)
`consistency` needs-attention (C1, C2 filed)
`integration` approve
`record` approve

Confirmation bar: all three findings sit below it, so each files through `add-todo` and the panel closes with no round 5. A1 is pre-existing behavior (single-dash-as-path predates §22) outside the pinned contract (unknown `--flags`), and mistyping still fails loud in the wrong family rather than silently; C1 and C2 are doc wording the section's own Done already outlaws or outstates. On the "fourth shape" framing: rounds 1-3 closed the `--` family structurally (`_tokenize` plus raw gates); single-dash is a new sub-family (a different prefix convention), verified live and filed with its owner rather than patched as a fourth round on the unit.

### F15 -- single-dash tokens parse as paths, not usage -- adversarial -- FILED to D00 T04 §24 (independent) [major]

Round-4 A1: `-check` parses as a runs path (RUN-002 exit 1) and `--check-export -x` as an export path (RUN-007 exit 1) instead of the RUN-001 exit-2 usage refusal, while the sibling checker refuses `-check` as FIND-001 exit 2 (both shapes driven live). Pre-existing, outside the §22 contract, fails loud: filed as §24 item twenty-two, which pins the three shapes plus the double-dash regression legs.

### F16 -- format docstrings miss the report-mode carve-out -- consistency -- FILED to D00 T04 §24 (independent) [minor]

Round-4 C1: both docstrings state flatly that `--format json` renders one array, but report modes interleave prose (`findings --format json` prints tables, driven live) or print no array, and runs `--report`/`--export` ignore `json_mode`. The item-2 Done already carves report/export out; the docstrings never caught up. Filed as §24 item twenty-three (with F17).

### F17 -- refusal docstring overclaims the live mechanism -- consistency -- FILED to D00 T04 §24 (independent) [minor]

Round-4 C2: the sentence claims SCHEMA_KEYS order is the live property, but `dumps` sorts keys, so membership (KeyError on drift, literal legs on deletion) is what fires. Imprecise, not false (the constant is read; the ordering just never reaches output). Filed as §24 item twenty-three (with F16).

```text
Round-4 output (Opus result text), quoted verbatim:
RECEIPT sha=bc10ca7e0f9dedd3759484f2af9053d02a6e49485105a8f572790c13a887a762 end=PANEL-748db2c41f7bbe7a nonce=271678335a211550

**adversarial: needs-attention** (1)
1. `scripts/todo-runs.py:1882-1888` and `scripts/todo-runs.py:1835`: both unknown-option gates test only `a.startswith("--")`, so a single-dash token is never a usage error. `todo-runs.py -check docs/reviews/run-records.md` takes `-check` as a surplus positional only if a second path follows; `todo-runs.py -check` takes `-check` as the runs path and exits 1 with `[RUN-002] cannot read -check`, and `todo-runs.py --check-export -x` exits 1 with `[RUN-007] cannot read export`, instead of the RUN-001 exit 2 the item-3 contract pins for mistyped flags. The sibling checker refuses the same shape as usage (`scripts/todo-findings.py:1051` `_CodedParser` → `[FIND-001]` exit 2), so the two checkers disagree on the same input, and this is the fourth shape in the family the round-1/2/3 fixes closed.

**consistency: needs-attention** (2)
1. `scripts/todo-findings.py:36-41` and `scripts/todo-runs.py:26-31`: both docstrings state flatly that "`--format json` renders refusals as one JSON array of code/path/line/message objects", with the only exception carved out for usage errors, but the report modes interleave prose with the array. `python scripts/todo-findings.py --format json` over a corpus with an unreadable heading prints the category/disposition/severity tables (`scripts/todo-findings.py:456-486`), then the array (`scripts/todo-findings.py:489-492`), then `todo-findings: N parsed, ...` (`scripts/todo-findings.py:500`), so `json.loads(stdout)` raises; a green default run prints no array at all. `scripts/todo-runs.py:1908-1911` likewise ignores `json_mode` for `--report`/`--export`. The candidate's own record states the carve-out ("report/export artifacts print as usual"); the two docstrings rewritten by the round-3 C3 fix do not.
2. `scripts/todo-diag.py:63-65`: `refusal`'s docstring says "Keys ride SCHEMA_KEYS order, so the constant is live", but `dumps` (`scripts/todo-diag.py:74`) serializes with `sort_keys=True`, so the constant's order never reaches any output and the sole live property is key membership (a renamed or added key raises `KeyError`, a deleted key is caught only by the suites' literal legs). The stated mechanism is not the mechanism that fires.

**integration: approve**

**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §22, D00 T04 §16, D00 T04 §24]; dependents [D00 T04 §24]; bytes 34869; run 20260920-D00-T04-S22-gpt

Gpt rung failed before any review ran: the skill's argv prompt form (`codex exec ... "$(cat ...)"`, 34989 bytes) dies on Windows with `FileNotFoundError: [WinError 206] The filename or extension is too long` (the 32KB command-line ceiling, a sibling of the 48KB node-shim ceiling §24 item 8 already owns). Opus fallback over the same prompt via stdin: exit 0, latency 19s, cost 55359tokens (input 2, cache-create 38135, cache-read 15288, output 1934), manifest `PLAN-ea1cf381cfe8acb5` sha `14fbf64a...` nonce `a8c18435fc51060f`, check-plan PASS 15 findings. Fallback survivor, so the stamp marker owes `partial: gpt rung` plus `retry-owed (owner Derick Payne, due 2026-09-27)`: a later gpt rerun once §24 item 8 lands stdin prompts. Triaged 9 filed to §25 (as 9 items; §25 opens beside §24 at filing because the ninth item tripped the section-sizing cap: §24 at 33), 5 accepted pre-stamp (readiness notes, interleaved-shape leg, both-checker legs, item-7 extension, the PR15 split itself), 1 rejected, 0 deferred.

Ledger:
- [D00-T04-S22-PR1] [major] registry covers two checkers only; graph, claims, and review-prompt emit uncoded refusals -> filed D00 T04 §25 (registry-extension item)
- [D00-T04-S22-PR2] [major] code-to-phase stability is a docstring rule nothing enforces -> filed D00 T04 §25 (frozen-manifest item)
- [D00-T04-S22-PR3] [major] no retirement lifecycle for a family that goes away -> filed D00 T04 §25 (retirement item)
- [D00-T04-S22-PR4] [major] JSON refusal schema carries no version key -> filed D00 T04 §25 (schema-version item)
- [D00-T04-S22-PR5] [minor] JSON refusals carry no severity field -> rejected no warning class exists in the checker model; exit plus code determine handling
- [D00-T04-S22-PR6] [major] multi-refusal order, stability, and dedup unpinned -> filed D00 T04 §25 (multi-refusal item)
- [D00-T04-S22-PR7] [minor] §24 items 22 and 23 lack needs-§22-shipped readiness notes -> accepted corrected pre-stamp (notes added)
- [D00-T04-S22-PR8] [minor] §24 item 23 lacks a leg for the documented interleaved shape -> accepted corrected pre-stamp (leg in the Done)
- [D00-T04-S22-PR9] [minor] §24 item 22 pins the runs checker only -> accepted corrected pre-stamp (both-checker legs in the Done)
- [D00-T04-S22-PR10] [minor] §22's two deliberate unpinned behaviors have no owner -> filed D00 T04 §25 (deliberate-pins item)
- [D00-T04-S22-PR11] [minor] green-run quotes are non-simultaneous measurements -> accepted corrected pre-stamp (§24 item 7 extended to green-run quotes)
- [D00-T04-S22-PR12] [minor] no worked consumer proves the schema end to end -> filed D00 T04 §25 (worked-consumer item, premium win)
- [D00-T04-S22-PR13] [minor] no operator-readable code index exists -> filed D00 T04 §25 (code-index item, premium win)
- [D00-T04-S22-PR14] [major] internal crashes exit 1, colliding with problem reports -> filed D00 T04 §25 (crash-exit item)
- [D00-T04-S22-PR15] [minor] §24 is too wide to ship as one commit -> accepted split at filing (§25 opens for the nine plan-review items; validator warned at 33)
End of ledger

```text
Plan-review output (Opus result text), quoted verbatim:
RECEIPT sha=14fbf64a3c47192409c75c871754265fac5c89212757fd7f13c0186bf54f0a15 end=PLAN-ea1cf381cfe8acb5 nonce=a8c18435fc51060f

- The code registry in `scripts/todo-diag.py` is consumed by only two checkers, yet `todo-graph.py`, `todo-claims.py`, and the review-prompt tool emit refusals with no codes at all; §22 scoped codes to the two review checkers and no section owns extending the registry to the rest of `scripts/`, so a consumer still string-matches prose for the majority of the tooling surface.
- Nothing pins code stability across time: §22 documents "one code per checking phase, so reworded messages never renumber" as a docstring rule but no checker or self-test enforces it, so a future section can silently reuse or renumber `FIND-003` and every downstream matcher breaks quietly; the enforcement (a frozen code-to-phase manifest checked in the self-test) belongs in §24 beside the other machinery hardening.
- No section owns retirement or deprecation of a diagnostic code: §22's registry fails closed on unlisted codes but says nothing about a family that goes away, so removing a check silently deletes a public contract; the lifecycle (codes are append-only, retired codes stay listed as retired) belongs in `todo/README.md` beside the deferral contract §24 item 1 adds.
- The JSON refusal schema has no `version` field while the runs export already carries `export_version` (§22 pins `export_version must be an int`), so a consumer cannot detect a schema change in the refusal array the way it can for the export; adding the version key belongs in §24's docstring/carve-out item or its own follow-up, and matters because §22's own precedent proves the project treats versionless machine artifacts as a defect.
- §22's JSON form carries `code/path/line/message` only, with no `severity` field, so CI cannot distinguish a fatal refusal from a warning and must re-derive it from the exit code plus the code prefix; since §16 already established a severity vocabulary for findings, the omission is an inconsistency worth an item in §24.
- The JSON form is quoted only for gate modes and green `[]`; no section pins what a *multi-refusal* run emits (ordering, stability, dedup) even though the array shape implies many, so a consumer sorting or diffing runs has no contract; this belongs as a §24 item beside the docstring carve-out.
- §24 items 22 and 23 both extend §22 but §24 carries no `Depends On`-equivalent gate for them the way it states "needs §21 shipped" for items 9/10, and §22 is not yet stamped at §24's authoring; the missing readiness note belongs in §24's two §22-derived items so a runner does not start them against an unfrozen `_tokenize`.
- §24's `--format` docstring item (item 23) fixes the docstring but nothing adds a self-test leg that a report-mode `--format json` run actually produces the documented interleaved shape, so the corrected sentence is once again unfalsifiable prose; the leg belongs in that item's Done when.
- The `--format` flag is accepted in both space and `=` forms per §22, but the §24 single-dash item (22) covers only `todo-runs.py`; `todo-findings.py`'s own single-dash handling is never stated as already-correct with a drive, so the asymmetry the item exists to end may simply invert, and the item should pin both checkers.
- §22's third item admits two deliberate unpinned behaviors ("`--self-test` with a malformed `--format` reports the self-test usage" and "a doubled `--format` resolves last-wins") with no owner; unpinned-but-deliberate behavior is exactly what drifts, so a §24 item should pin both as legs or the §22 stamp should record them as accepted residual with a reason.
- The §22 green-run evidence is split across two different trees (findings re-quoted at stamp, runs quote "dates from pre-review-file" and lands at stamp), which means the checkpoint "both live checkers exit 0 quoted" is satisfied by two non-simultaneous measurements; §24's commit-time counts discipline item (7) addresses the analogous suite-count problem but not this one, and should extend to green-run quotes.
- No section owns a consumer-side proof that the structured form is actually usable: §22 proves `json.loads` validates, but nothing demonstrates the end-to-end case the section exists for (a CI step or IDE problem-matcher consuming the array and reporting a line), so the stated motivation is never exercised; a small worked-example artifact in §24 would be a real premium win and would catch schema mistakes that in-process validation cannot.
- The registry has no published human-readable index (a `docs/` table of code, family, meaning, exit, and remedy) despite §22 requiring the list live in "exactly one documented place"; the module docstring is the wrong surface for an operator, and generating the table from `CODES` into `docs/` belongs in §24 as a cheap, high-value win.
- Exit code 2 is documented as "usage errors" and 1 as "problem reports", but no code names the exit convention for an internal crash (an unhandled exception exits 1 on Python, colliding with "problem report"); no section owns distinguishing tool failure from tool finding, which is the single most consequential ambiguity for a CI consumer and belongs beside §22's exit map.
- §24's checkpoint lists twenty-three quoted proofs in one sentence for one commit, while the repo rule is "one section = one commit"; a section this wide makes a partial ship nearly certain and defeats the review granularity §22 itself demonstrates, so §24 should be split by owner (README/rule items, §21-extending items, §22-extending items) before it is run.
```
