# Review -- D00 T04 §39, CI Read-Back and Repair Completeness

**Reviewed:** 2026-09-26
**Stance:** post-implementation
**Rounds:** independent pass (6 findings: fixed); GPT 1 bulk (3 fixed); GPT 2 bulk (3 fixed); GPT 3 sign-off (3 fixed, the flow-list reader rethought after three rounds); GPT 4 depth (2 fixed, redaction rethought after three rounds); GPT 5 depth, the hard cap (2 filed in D00 T04 §41).
**Tier:** Full panel. The diff touches `scripts/review_prompt.py` and `scripts/campaign_guard.py` (gates in `scripts/check-all.ps1`), two skills (`review-todo-section`, `process-phase`: the run contract), and the durable oracle evidence under `docs/captures/ci-oracle/`.
**Attestation:** `docs/reviews/00-workspace/D00-T04-s39.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `34bc5ecb` | implementation: complete redaction and masked-input labels, the journalled repair lifecycle with GitHub-verified close, campaign binding, and the keyed ceiling, multi-failure classification by id, the push-exclusion check and retirement, the Windows drill and native re-run rendering, the durable oracle captures, and D00 T04 §35's acceptance paragraph |
| `f122556e` | answers the independent pass: no episode dropped without its terminal line, re-reserved abandons count, legacy attempts read pushed, retirement bound to the episode head, trigger comments and flow mappings refuse, Windows execution only on Windows |
| `f5c4bc06` | answers panel round 1: quoted list items keep their commas, jobs sharing a name are classified by id, suite counts recorded |
| `96221264` | answers panel round 2: plain scalars and escaped spaces mask to their end, quoted list items decode their escapes, repair fails closed without an established campaign |
| `2d337c78` | answers panel round 3: a scanner replaces the flow-list regex, block-scalar credentials mask their body, a present foreign episode refuses restore |
| `897d6426` | answers panel round 4: one continuation rule masks a credential value whole, a foreign journal line is never reconciled |

Record commits (filings only, no reviewed content): `4f7e8019` (the hard-cap and plan-review findings filed in D00 T04 §41 and D00 T01 §9).

Range `34bc5ecb^..897d6426` (base `354b9b35`), contiguous, no declaration owed. Each round fenced the range from `354b9b35` to its own head, the durable captures included (a 326 KB prompt, its receipt verified each round); the fenced contract was checked to open with this section's heading. The rundir is session scratch outside the tree (/tmp/review-xK1wSlxY, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation. Makes sense: each item maps to code (`redact` and `_redact_blocks`, `rerun_lines`, `failed_job_identities`, `cause_lines`, `push_trigger_filter` and `push_excluded`, the repair journal and `_ceiling` in `campaign_guard.py`, the `oracle_*` loaders) or to skill text, with legs that fail without it. Logical: the oracle's expectations are read from GitHub's own logs, never typed by the author, and the Windows drill found a real gap (GitHub's `Stop` error preference) that the printout now carries. Broke nothing: `check-all` ran `19 gate(s) ok` at the SHIP commit; the durable captures were verified against the constants they replace before the constants were removed; the Windows drill branch and its worktree were deleted after capture. The one pre-existing failure the independent review surfaced under WSL (`anchors-untracked-fires`) was measured against the pre-section tree (three WSL failures before, one after) and filed in D00 T01 §9.

`source-defect`: owed for GitHub's workflow semantics, and read from GitHub rather than from memory: the drills' logs are the source for the templates and outputs, and the trigger filter follows GitHub's documented `branches`, `branches-ignore`, `paths`, and `paths-ignore` (the unsupported shapes refuse). `design`: not owed (no rendered surface).

## Independent pass

`python scripts/panel_slots.py exec independent --commit 34bc5ecb...`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 293s, clock 2026-09-26T03:14:20Z, cost unresolved (the review runner printed no token figure). Output and stderr stay in the session scratch rundir outside the tree (ind39.out, ind39.err); the output is transcribed below.

### F1 -- a status call can drop an episode and reset its budget -- correctness -- FIXED in `f122556e` (independent) [major]

The caller's run file is checked first, and the episode file drops only on a terminal journal line for that episode.

### F2 -- a retried abandoned commit counts nothing -- correctness -- FIXED in `f122556e` (independent) [minor]

It is reserved again, journalled, under the bound.

### F3 -- legacy attempts fall out of close's ancestry -- correctness -- FIXED in `f122556e` (independent) [minor]

A missing attempt state reads as pushed.

### F4 -- retirement accepts any NOT GREEN line for the workflow -- adversarial -- FIXED in `f122556e` (independent) [minor]

It needs no open reservation, a range ending at the silent push, and that push descending from the last attempt.

### F5 -- a trigger comment or flow mapping reads as no push -- adversarial -- FIXED in `f122556e` (independent) [minor]

Comments are stripped and undecoded shapes refuse as unproven.

### F6 -- the Windows legs break the self-test off Windows -- integration -- FIXED in `f122556e` (independent) [minor]

Windows shells execute only on Windows; the pre-existing WSL-only failure is filed in D00 T01 §9.

```text
Both Windows self-test suites pass, but focused reproductions expose repair-budget resets and invalid episode termination. The review-prompt self-test also crashes under WSL, and valid trigger syntax can incorrectly authorize silence.

Full review comments:

- [P1] Validate the journal identity before deleting episode state — R:\GitHub\Resolute\scripts\campaign_guard.py:1082-1086
  Calling `repair status` with another run file deletes the existing episode because that file contains no open journal entry. A missing or unreadable journal has the same effect. This happens before the run-file and campaign identity checks, allowing an exhausted repair budget to reset. Validate the caller against the persisted episode first, and require a matching terminal journal event before deleting state.

- [P2] Re-reserve abandoned commits before allowing another push — R:\GitHub\Resolute\scripts\campaign_guard.py:1164-1168
  After a failed push is abandoned, retrying `repair attempt` for the same commit returns success and says it is reserved, but leaves its state `abandoned`. The subsequent `repair pushed` also returns success without changing it, so a repair that actually lands consumes zero attempts. Re-reserve abandoned commits subject to the current bound, and journal that transition so recovery preserves the count.

- [P2] Preserve legacy pushed attempts in the ancestry check — R:\GitHub\Resolute\scripts\campaign_guard.py:1218-1219
  Episode files produced before this commit have attempts without a `state` field. Journal reconciliation treats those as pushed and leaves them unchanged, but this lookup excludes them and falls back to the episode's original red commit. Consequently, rerunning that original commit successfully can close the episode without testing any repair commits. Default missing states to `pushed` here or normalize legacy records first, preserving the candidate-identity requirement in [AGENTS.md:198](R:/GitHub/Resolute/AGENTS.md#L198).

- [P2] Bind retirement evidence to the episode's repair head — R:\GitHub\Resolute\scripts\campaign_guard.py:1248-1252
  `repair retire` accepts an old or unrelated `NOT GREEN` line whenever its workflow name matches, even with an unrelated SHA and unresolved reservations. It then deletes the current episode and resets its budget although the current repair push was never shown to be excluded. Validate the evidence's approved head and ancestry against the episode before retiring it, consistent with [AGENTS.md:198](R:/GitHub/Resolute/AGENTS.md#L198).

- [P2] Do not interpret commented triggers as absent push events — R:\GitHub\Resolute\scripts\review_prompt.py:1182-1185
  A valid `on: # events` followed by an indented `push:` is parsed as having no push trigger because the comment is treated as an inline event value. Flow mappings such as `on: {push: null}` produce the same result. After an authorized trigger edit, this lets `--expect-no-run` accept silence even though the workflow still admits the push. Strip YAML comments correctly and refuse unsupported shapes rather than treating them as proof of exclusion.

- [P2] Keep Windows execution probes out of the portable self-test — R:\GitHub\Resolute\scripts\review_prompt.py:6480-6486
  On Linux with `pwsh` installed, the self-test now raises `FileNotFoundError` because `subprocess.run` receives the entire quoted command as one executable name; this reproduces under WSL. Without those shells, the new checks fail because Windows-only `powershell` and `cmd` are required. Separate platform-specific execution probes from portable oracle validation and use an argument vector on POSIX, preserving the portability promised by [AGENTS.md:28](R:/GitHub/Resolute/AGENTS.md#L28).
```

## GPT panel Round 1

Round 1 over `34bc5ecb^..f122556e` with the independent pass appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 46s, clock 2026-09-26T03:24:03Z, cost 115156tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-1.out, panel-1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F7 -- a quoted list item splits at its comma -- adversarial -- FIXED in `f5c4bc06` (independent) [minor]

Items split only outside quotes.

### F8 -- matrix jobs sharing a display name merge their signals -- integration -- FIXED in `f5c4bc06` (independent) [minor]

Colliding names fetch each job's log by id; a repeated step name reads ambiguous.

### F9 -- the run record lacks the suite counts -- record -- FIXED in `f5c4bc06` (independent) [minor]

Counts recorded in the run file; the Live proof quotes the final ones.

```text
RECEIPT sha=56920c23b790bf673d831fdbdbe7633dc52326006a9079e72e05b91932ae28d0 end=PANEL-fcc302b304b26bee nonce=0ded79ef25becea6
**adversarial: needs-attention** (1)

1. `scripts/review_prompt.py:1159-1166`: `_flow_list` splits quoted YAML values at every comma. Valid `branches: ['feature/foo,bar']` becomes two patterns, falsely proving that a push to `feature/foo,bar` is excluded and permitting authorized silence when GitHub should run the workflow. Decode quoted list elements correctly or refuse this shape.

**consistency: approve**

**integration: needs-attention** (1)

1. `scripts/review_prompt.py:1939-1953`: `cause_lines` still correlates failures by textual job and step names; its dictionary overwrites identities sharing those names. Matrix jobs with an explicit identical display name, or repeated step names, therefore merge signals and attribute them to one ID. Fetch and correlate logs by job ID and step number; the added fixture uses distinct job names and does not exercise this collision.

**record: needs-attention** (1)

1. `docs/phase-runs/2026-09-25-phase-0.md:207-210`: The §39 evidence records the Windows drill but provides neither required self-test command's execution result or suite count. The checklist's fixture descriptions do not establish that `review_prompt.py --self-test` and `campaign_guard.py --self-test` passed on this candidate, as the checkpoint requires.
```

## GPT panel Round 2

Round 2 over `34bc5ecb^..f5c4bc06` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 65s, clock 2026-09-26T03:30:02Z, cost 117661tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-2.out, panel-2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F10 -- a plain scalar with a space or an escaped space leaks its tail -- adversarial -- FIXED in `96221264` (independent) [major]

A credential key starting its line masks to the line end; bare values carry backslash escapes.

### F11 -- a double-quoted list item keeps its escapes -- adversarial -- FIXED in `96221264` (independent) [minor]

Items decode through `_decode_double`; a refused escape reads unproven.

### F12 -- an unreadable guard bypasses the campaign binding -- integration -- FIXED in `96221264` (independent) [major]

An unreadable guard refuses; mutating repair actions need a guard with a run id.

```text
RECEIPT sha=821767bcc255b82bae1d01be5ef00abaeed7f594aea6da9dbac373810b8f8dc7 end=PANEL-e03c5e687588fe0b nonce=74891b240e589d18
**adversarial: needs-attention** (2)

1. `scripts/review_prompt.py:1830`: Credential redaction still stops at whitespace inside valid YAML plain scalars and shell values containing escaped spaces. `password: abc defghi` and `API_TOKEN=abc\ defghi` leave `defghi` visible, violating the required masking through the credential's true end.
2. `scripts/review_prompt.py:1172-1173`: `_flow_list` decodes only escaped double quotes, leaving other YAML double-quoted escapes literal. Valid `branches: ["\u006daster"]` therefore fails to match `master`, falsely proving exclusion and permitting authorized silence. Decode supported YAML escapes or refuse such patterns as unproven.

**consistency: approve**

**integration: needs-attention** (1)

1. `scripts/campaign_guard.py:1090-1096`: Guard-read failures are swallowed and converted into an empty campaign identity. Subsequent campaign checks require both identities to be nonempty, so an unreadable guard bypasses campaign binding; opening an episode also permits `run=None`. Fail closed when the campaign identity cannot be established.

**record: approve**
```

## GPT panel Round 3

Round 3 over `34bc5ecb^..96221264` the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 100s, clock 2026-09-26T03:40:47Z, cost 120736tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-3.out, panel-3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F13 -- a hash inside a quoted list item is stripped -- adversarial -- FIXED in `2d337c78` (independent) [minor]

The flow-list reader, patched three rounds running, was rethought into a quote-aware scanner.

### F14 -- a block-scalar credential leaks its body -- adversarial -- FIXED in `2d337c78` (independent) [major]

Body lines indented deeper than the key are masked. Blocking (a credential reaching a record), so round 4 ran.

### F15 -- restore accepts a present foreign episode -- integration -- FIXED in `2d337c78` (independent) [major]

A persisted episode of another campaign refuses before any journal read or early return.

```text
RECEIPT sha=c91c6bd95caabeb92527dbe41db4effc304627289aeeb943aebffe4629357e0d end=PANEL-850b25476bbc1375 nonce=f6125a6facb09e9b
**adversarial: needs-attention** (2)

1. `scripts/review_prompt.py:1162-1167`: `_flow_list` strips comments before respecting quotes. Valid `paths: ['foo #bar/**']` is truncated at `#`, falsely excluding a push touching `foo #bar/test.txt` and permitting authorized silence.
2. `scripts/review_prompt.py:1852-1853`: YAML block credentials remain exposed: `password: |\n  abc\n  defghi` masks only the `|` indicator and leaves both credential body lines visible, violating masking through the value's true end.

**consistency: approve**

**integration: needs-attention** (1)

1. `scripts/campaign_guard.py:1131-1133`: `repair restore` returns success immediately when the episode state exists, before validating its campaign identity. With an episode belonging to campaign A and a live guard belonging to campaign B, restore accepts the foreign episode; preceding journal reconciliation can also modify it. Validate campaign and journal identities before reconciliation or this early return.

**record: approve**
```

## GPT panel Round 4

Round 4 over `34bc5ecb^..2d337c78` with prior verdicts appended (rounds past the sign-off run for blocking findings only). Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 95s, clock 2026-09-26T03:50:06Z, cost 136207tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-4.out, panel-4.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F16 -- a credential's continuation lines leak -- adversarial -- FIXED in `897d6426` (independent) [major]

Redaction, patched three rounds running, was rethought into one continuation rule: deeper-indented lines and lines after a trailing backslash are the value.

### F17 -- reconciliation adopts a foreign journal line -- integration -- FIXED in `897d6426` (independent) [major]

Every identity the journal's lines carry must be the persisted episode's, or the call escalates.

```text
RECEIPT sha=ff170aff673e184daecb3f765dff8d8e23b4557ab5961470188f2ba47b7357cc end=PANEL-527242d6dc57c1be nonce=d30851722bb7c01a
**adversarial: needs-attention** (1)
1. `scripts/review_prompt.py:1913-1914`: Credential redaction still exposes continuation lines. A YAML plain scalar `password: abc\n  defghi` leaves `defghi` visible; a shell assignment containing `API_TOKEN=abc` followed by backslash-newline and `defghi` does likewise because `\\.` cannot consume the newline. Both are single credential values whose tails remain unmasked.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/campaign_guard.py:1120-1134`: Journal reconciliation publishes recovered attempt states without validating the journal's campaign, repository, branch, or workflow identities. The preceding check validates only the persisted state's campaign. A foreign journal sharing the episode's red SHA and attempt commits can therefore change a current campaign's pushed attempts to abandoned, freeing its repair budget. Validate journal identities before reconciliation and before the existing-state return in `restore`.
**record: approve**
```

## GPT panel Round 5

Round 5 over `34bc5ecb^..897d6426` the hard cap, with prior verdicts appended. Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 71s, clock 2026-09-26T03:58:17Z, cost 129625tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-5.out, panel-5.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F18 -- a block scalar inside a trigger list reads as its indicator -- adversarial -- FILED to `D00 T04 §41` (independent) [minor]

Round 5 is the hard cap. The shape (a folded or literal scalar as a branch pattern) is rare in a trigger list, and the harm is a wrongly authorized silence the operator already approved the range for; it files rather than shipping unreviewed code.

### F19 -- pushed and abandoned lines carry no identity of their own -- integration -- FILED to `D00 T04 §41` (independent) [major]

Round 5 is the hard cap. Exploiting it needs a hand-written foreign transition in this campaign's own run file; filed as major with the strict-replay item.

```text
RECEIPT sha=aa4e3f2a9c4582443ccdd941aef4790b293fb95045f2e6a1fbdfc84ee225110f end=PANEL-1da55009a5b17d99 nonce=29a1d2b61fe0a4b8
**adversarial: needs-attention** (1)

1. `scripts/review_prompt.py:1175-1181`: Block-scalar filter items are silently misread. Valid `branches:\n  - >-\n      master` becomes `[">-"]`, ignoring the scalar body. `push_excluded` consequently proves exclusion for `master` and permits authorized silence although the workflow admits that branch. Decode these scalars or refuse them as unproven.

**consistency: approve**
**integration: needs-attention** (1)

1. `scripts/campaign_guard.py:976-991`: Journal identity validation still excludes `pushed` and `abandoned` transitions: only reservation and legacy identities enter `idents`, while other transitions update matching commits without checking their episode ID. Appending an `abandoned` line carrying a foreign campaign or repository therefore changes a current pushed attempt to abandoned during reconciliation and frees its repair budget. Validate every transition's episode and identity before applying it.

**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §39, D00 T04 §37, D00 T01 §9]; dependents [none]; bytes 40268; run 20260926-D00-T04-S39-astra

`bytes 40268` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency and XREF neighbor D00 T04 §37 and its XREF neighbor D00 T01 §9; no section depends on D00 T04 §39 yet. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. The first attempt (48s, clock 2026-09-26T03:59:21Z, 35595tokens) opened with a malformed receipt line before the real one, so `check-plan` failed it and it counts as a runner failure, not a record; the re-run of the same prompt is the record: exit 0, latency 57s, clock 2026-09-26T04:00:30Z, cost 17172tokens. Check: `PASS 18 findings, one per line`.

Ledger:
- [D00-T04-S39-PR1] [minor] close does not compare the run attempt -> filed D00 T04 §41
- [D00-T04-S39-PR2] [minor] retirement trusts a supplied NOT GREEN line -> filed D00 T04 §41
- [D00-T04-S39-PR3] [minor] push exclusion has no stated pattern semantics -> filed D00 T04 §41
- [D00-T04-S39-PR4] [minor] the changed-path source is unspecified per push event -> filed D00 T04 §41
- [D00-T04-S39-PR5] [minor] abandonment frees a place without a remote check -> filed D00 T04 §41
- [D00-T04-S39-PR6] [minor] interruption fixtures cover only a reservation -> filed D00 T04 §41
- [D00-T04-S39-PR7] [minor] the replay has no transition order or duplicate rule -> filed D00 T04 §41
- [D00-T04-S39-PR8] [minor] a re-reserved commit has no replay contract -> filed D00 T04 §41
- [D00-T04-S39-PR9] [minor] the ceiling survives a substituted journal -> filed D00 T04 §41
- [D00-T04-S39-PR10] [minor] the aggregation rule overlaps -> filed D00 T04 §41
- [D00-T04-S39-PR11] [minor] job-level outcomes have no classification -> filed D00 T04 §41
- [D00-T04-S39-PR12] [minor] redaction has no stated grammar -> filed D00 T04 §41
- [D00-T04-S39-PR13] [minor] oracle captures persist without a sanitization gate -> filed D00 T04 §41
- [D00-T04-S39-PR14] [minor] the severity rule leans on registered-secret masking -> filed D00 T04 §41
- [D00-T04-S39-PR15] [minor] the Windows drill proves no failure semantics -> filed D00 T04 §41
- [D00-T04-S39-PR16] [minor] the purge would rewrite stamped history -> filed D00 T01 §9
- [D00-T04-S39-PR17] [minor] the purge conflicts with retained cross-platform evidence -> filed D00 T01 §9
- [D00-T04-S39-PR18] [minor] repair outcomes have no structured receipt -> filed D00 T04 §41
End of ledger

18 findings, all filed (`4f7e8019`): sixteen in D00 T04 §41, PR16 and PR17 as one item in D00 T01 §9.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=d470ebed406dadebb37face28ad6aea54e1a2fdaf1c8e344d59c8de35727ff93 end=PLAN-bd1c9c0b7e45fcba nonce=fa0329feb6c77c3b
- §39 requires close evidence to match the GitHub run attempt, but its Done note and mismatch fixtures omit that comparison; put exact attempt verification in §39 close provenance so an earlier successful attempt cannot validate a later rerun.
- §39 retirement still relies on a supplied NOT GREEN line plus range and ancestry checks without explicitly revalidating authorization and trigger exclusion; put independent verification in `repair retire` so fabricated or stale evidence cannot terminate an episode.
- Push exclusion lacks a stated GitHub pattern-semantics contract; put ordered negation, branch/path conjunction, slash matching, and unsupported-pattern refusal fixtures in §39 so an approximate matcher cannot falsely authorize silence.
- The changed-path source is unspecified for multi-commit pushes, new branches, deletions, and force pushes; put event-specific comparison rules and conservative refusal in §39 because an incorrect path set invalidates exclusion proof.
- Abandonment frees an attempt without a stated check that the commit never reached the remote; put remote reconciliation in §39 failed-push recovery so a push that succeeded before its client disconnected cannot escape the repair budget.
- Crash safety promises interruption coverage for every transition, but the listed crash fixture proves only attempt reservation; put recovery fixtures for open, pushed, abandoned, closed, retired, and ceiling consumption in §39 to prove budgets and terminal states survive each interruption.
- Journal-first recovery does not specify truncated writes, duplicate events, invalid transition order, or concurrent invocations; put atomicity, serialization, and fail-closed replay rules in §39 so corruption cannot invent or erase attempts.
- §37 restores attempts by deduplicating commits while §39 allows an abandoned commit to be reserved again; put an explicit superseding replay contract and same-commit retry recovery fixture in §39 so reconstruction preserves the new lifecycle.
- Ceiling recovery covers a lost cache and a corrupt cache but not a missing, truncated, or substituted journal, and its key omits campaign identity; put authoritative journal binding in §39 so changing the run file cannot renew a consumed allowance.
- The aggregation rule says both “any repairable step” makes the run repairable and mixed platform/repository steps make it unknown; put an ordered decision table in §39 covering those overlapping cases and failed steps with no signal so routing is deterministic.
- Job and step classification covers failing steps but assigns no explicit treatment to job-level startup failures, cancellations, timeouts, or unavailable logs; put these outcomes and their bounded next actions in §39 so incomplete evidence cannot become a platform-only diagnosis.
- Redaction remains a growing collection of syntax heuristics without a defined supported grammar or conservative fallback; put that contract and adversarial fixtures for comments, escapes, continuations, and malformed input in §39 to prevent further suffix leaks.
- §39 archives full oracle logs while §37 requires records to contain only redacted output; put sanitization and secret scanning before capture persistence in §39 so durable evidence obeys the same confidentiality contract.
- §37 treats partial credential disclosure as blocking, while §39 downgrades it because GitHub masks registered secrets; put a consistent severity rule in §39 that accounts for unregistered and transformed credentials so the same exposure receives consistent release treatment.
- Windows reproduction proves output and template text but does not explicitly prove failure semantics or shell-sensitive environment values; put native-command failures, PowerShell exceptions, cmd expansion characters, and working-directory failures in §39’s Windows drill so a reproduction cannot falsely succeed.
- D00 T01 §9 demands no WSL references outside its own record, but §39 already contains historical WSL evidence and the rules preserve stamped history; narrow the purge to executable paths and active guidance so implementation does not require rewriting authoritative records.
- D00 T01 §9 removes Linux-gated paths while §39 deliberately retains cross-platform oracle checks and historical Linux captures; assign explicit retention rules and scope the portability decision to individual scripts so the purge preserves Windows Git Bash coverage and evidence.
- Repair outcomes rely on parsing human-readable evidence lines across commands; add a versioned structured receipt alongside readable output in §39, carrying campaign, repository, workflow, SHA, run, attempt, outcome, and remaining budget, to improve auditability and make recovery less fragile.
```

