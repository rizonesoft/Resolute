# Review -- D00 T04 §41, CI Read-Back Evidence and Replay Hardening

**Reviewed:** 2026-09-26
**Stance:** post-implementation
**Rounds:** independent pass (4 findings: fixed); GPT 1 bulk (4 fixed); GPT 2 bulk (3 fixed); GPT 3 sign-off (3 fixed); GPT 4 depth (2 fixed, the redaction opener unit rethought after four rounds); GPT 5 depth, the hard cap (3 filed in D00 T04 §42).
**Tier:** Full panel. The diff touches `scripts/campaign_guard.py` and `scripts/review_prompt.py` (gates in `scripts/check-all.ps1`), the CI oracle captures, and two skills (`process-phase`, `review-todo-section`: the read-back and repair contract).
**Attestation:** `docs/reviews/00-workspace/D00-T04-s41.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `83700a9e` | implementation: strict journal replay with receipts, remote-checked abandon, attempt-bound close, re-derived retirement, a campaign-bound ceiling, interruption fixtures, GitHub-ordered trigger filters, the decision table, the redaction grammar with `capture-run`, and the Windows failure drill |
| `2c60059f` | answers the independent pass: the quoted multiline value's first line, capture JSON and snapshots, refusal receipts |
| `d5be3d1d` | answers panel round 1 (code): retirement path binding, exact heredoc terminators, strict ceiling replay, drill error matching |
| `3a516394` | records the round 1 corrections in the section |
| `bd6d8427` | answers panel round 2: column-0 here-string close, strict ceiling receipts, recorded suite counts |
| `a779094c` | answers panel round 3: quoted brackets, unquoted NUL-delimited paths, whole-journal replay for every command |
| `de588a07` | answers panel round 4: the enclosing-quote scan, receipt evidence agreement |
| `1fc1cc0a` | rethinks the redaction opener unit: mask to the end of the block, no terminator search |

Record commits (filings only, no reviewed content): `d5f91b9b` (the hard-cap and plan-review findings folded into D00 T04 §42).

Range `83700a9e^..1fc1cc0a` (base `9a4d4231`), contiguous, no declaration owed. Each round fenced the range from `9a4d4231` to its own head, and `cross-check` agreed on 14 files every time. The rundir is session scratch outside the tree (/tmp/review-x03Eay1W, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation. Makes sense: each item maps to code (`_journal` and `_parse_prose`/`_parse_receipt`, `_delivered`, the close and retire checks, `_ceiling` and the interruption legs, `push_excluded` with `_filter_admits` and `changed_paths_for_push`, `DECISION_TABLE` with `decide`, the grammar above `_redact_blocks` with `secret_scan` and `capture_run`, `oracle_failures`, `_receipt`), with legs that fail without it. Logical: the one judgment the plan left open, whether a failing step with no log is repairable, now reads unknown (D9) with a local re-run as its next action, because a step's name proves nothing about its cause. Broke nothing: the D00 T04 §38 and §39 repair legs were rewritten to the strict line format and still pin their behavior, the committed captures were re-sanitized (only `persist-credentials` changed) and every oracle leg still reads them, and `scripts/check-all.ps1` ran `19 gate(s) ok` at the SHIP commit. Self-review also noted the unlocked read of the episode file for a generic receipt; it is read-only and can only lag a correct line.

`source-defect`: owed and done: GitHub's own run (36227133366) is the source for the Windows failure semantics, captured under `docs/captures/ci-oracle/`, and its exit codes and output are read from that log, never typed. `design`: not owed (no rendered surface).

## Independent pass

`python scripts/panel_slots.py exec independent --commit 83700a9e...`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 368s, clock 2026-09-26T07:55:22Z, cost unresolved (the review runner printed no token figure). Output and stderr stay in the build scratch outside the tree (build/rev41/independent.out, independent.err); the output is transcribed below.

### F1 -- a quoted multiline credential's first line stays unmasked -- adversarial -- FIXED in `2c60059f` (independent) [major]

The key line's value is masked with its continuation.

### F2 -- capture-run redacts serialized JSON and can break it -- correctness -- FIXED in `2c60059f` (independent) [minor]

Each record value is redacted and scanned alone, and the JSON must parse.

### F3 -- a second capture overwrites another drill's workflow snapshot -- correctness -- FIXED in `2c60059f` (independent) [minor]

A snapshot with different content refuses.

### F4 -- repair refusals print no receipt -- integration -- FIXED in `2c60059f` (independent) [minor]

A refusal answers with a `refused` receipt carrying the reason.

```text
Both self-test suites pass, but targeted probes reproduce credential exposure, invalid JSON captures, overwritten provenance, and missing refusal receipts. These are actionable correctness and security defects.

Full review comments:

- [P1] Mask the opening line of quoted multiline credentials — R:\GitHub\Resolute\scripts\review_prompt.py:2081-2085
  For `password: "TOPSECRET\nrest\nend"`, this loop removes the closing quote while leaving the opening line unchanged. The subsequent quoted-value regex can no longer match, so `TOPSECRET` remains exposed; `secret_scan()` nevertheless returns an empty list. The parent revision masks this input completely. This can leak credentials through CI output and captured evidence, contrary to [AGENTS.md:202](R:/GitHub/Resolute/AGENTS.md#L202). Mask the opening value together with its continuation lines.

- [P2] Sanitize record values before serializing them as JSON — R:\GitHub\Resolute\scripts\review_prompt.py:4305-4306
  When a job or step name contains text such as `verify token=placeholder`, applying the text redactor to serialized JSON treats the enclosing JSON quote as a multiline opener and replaces subsequent structural lines with `***`. A targeted capture returns success and passes the secret scan, but its `run-<id>.json` cannot be parsed. Sanitize string values before serialization and validate the resulting JSON before publishing it.

- [P2] Preserve earlier workflow snapshots when capturing another run — R:\GitHub\Resolute\scripts\review_prompt.py:4319-4323
  Capturing a second run after changing the same workflow overwrites `<workflow>.yml`, while the earlier oracle entry retains its original digest and head SHA. Both captures succeed, but the earlier evidence is lost and `capture-provenance-holds` subsequently fails. Store workflow snapshots per run, or reject conflicting content before writing anything. This also preserves the candidate-bound evidence required by [AGENTS.md:112](R:/GitHub/Resolute/AGENTS.md#L112).

- [P2] Emit structured receipts when repair commands refuse — R:\GitHub\Resolute\scripts\campaign_guard.py:1583-1585
  When `_repair()` raises `GuardError`, for example for a truncated journal, stale green evidence, or an unreadable remote during abandonment, execution never reaches receipt generation. `main()` catches the exception and prints only prose. Consequently, the updated workflow's receipt-based readers receive no structured outcome on these escalation paths. Handle refusals at the receipt boundary and emit a versioned failure receipt while preserving the nonzero exit status.
```

## GPT panel Round 1

Round 1 over `83700a9e^..2c60059f` with the independent pass answered. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 81s, clock 2026-09-26T08:13:11Z, cost 98951tokens. Output and stderr stay in the session scratch rundir outside the tree (r1.out, r1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F5 -- retirement trusts a made-up workflow path -- adversarial -- FIXED in `d5be3d1d` (independent) [major]

The path must name the episode's workflow at the range's base (its `name:` or file stem).

### F6 -- a space-indented heredoc terminator ends masking -- adversarial -- FIXED in `d5be3d1d` (independent) [major]

`<<WORD` ended only at an exact line (superseded by the round-4 rethink, which never looks for a terminator).

### F7 -- ceiling lines bypass strict replay -- integration -- FIXED in `d5be3d1d` (independent) [minor]

Ceiling lines replay strictly: prose and receipt agree, one use per key, one campaign.

### F8 -- the drill comparison checks only a stdout prefix -- record -- FIXED in `d5be3d1d` (independent) [minor]

It also requires each drill message in GitHub's error record in the local error, and none where GitHub shows none.

```text
RECEIPT sha=350c9c7d9cd930f3dbd64d69d4011462284f65773ae90849584450a44a366a5f end=PANEL-78c3cff716f55390 nonce=d61d312c6a565fd5
**adversarial: needs-attention** (2)
1. scripts/campaign_guard.py:1901-1938: Retirement trusts the supplied `workflow-path` without binding it to the episode’s workflow. Naming a nonexistent path makes `git show` fail, converts the workflow to `None`, and proves exclusion, allowing fabricated NOT GREEN evidence to retire an episode whose actual workflow still runs.
2. scripts/review_prompt.py:2082-2083: `body.strip() == end` accepts a space-prefixed heredoc terminator, although `<<EOF` requires an exact terminator and `<<-EOF` permits only leading tabs. `API_TOKEN=$(cat <<EOF\n EOF\nsensitive\nEOF\n)` therefore exposes `sensitive`; the subsequent scan passes because redaction removed the opener.
**consistency: approve**
**integration: needs-attention** (1)
1. scripts/campaign_guard.py:1639-1641,1973-1991: Ceiling commands bypass strict journal replay. Their separate reader ignores receipts, collapses duplicate keys, and never compares recorded campaign identities with the current campaign or cached identity. A substituted campaign or contradictory receipt is accepted, violating the required campaign binding and fail-closed replay.
**record: needs-attention** (1)
1. scripts/review_prompt.py:7230-7241: The Windows reproduction assertion checks only a stdout prefix and requires the expected error message only when local stderr already contains it. An unrelated exit-1 failure producing no stdout and neither expected message can pass the PowerShell cases. This does not prove the claimed matching output and failure semantics.
```

## GPT panel Round 2

Round 2 over `83700a9e^..3a516394` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 35s, clock 2026-09-26T08:21:10Z, cost 105931tokens. Output and stderr stay in the session scratch rundir outside the tree (r2.out, r2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

### F9 -- an indented here-string close ends masking -- adversarial -- FIXED in `bd6d8427` (independent) [major]

Column-0 closes only (superseded by the round-4 rethink).

### F10 -- a malformed ceiling receipt or a cut final line is accepted -- integration -- FIXED in `bd6d8427` (independent) [minor]

The ceiling replays the whole journal first and refuses both.

### F11 -- the candidate's suite counts are not recorded -- record -- FIXED in `bd6d8427` (independent) [minor]

The run file records `287` and `638` at that candidate; the Live proof quotes the final counts.

```text
RECEIPT sha=bed3d180a28dc11028e9a6dcea88bdf5c26d9cabb16afeb9b3065095c4b764e6 end=PANEL-72bc08d513b0ad96 nonce=f6025ce90470147e
**adversarial: needs-attention** (1)
1. scripts/review_prompt.py:2084-2085: Here-string redaction accepts an indented closing marker through `body.lstrip().startswith(end)`. PowerShell requires the closing marker at column zero. A credential here-string containing an indented `"@` followed by sensitive text therefore stops masking prematurely; the subsequent scan cannot recover the removed opener and accepts the exposed continuation.
**consistency: approve**
**integration: needs-attention** (1)
1. scripts/campaign_guard.py:1986-2013: Ceiling replay still ignores malformed receipts because `_parse_receipt(...) is None` is silently skipped, and `_journal_lines` discards whether the final line had its terminating newline. A valid ceiling prose line followed by a truncated receipt therefore permits another allowance instead of refusing the damaged journal. The prior strict-replay finding remains live.
**record: needs-attention** (1)
1. todo/00-workspace/TODO-04-self-correction.md:1711: The Test checkpoint requires quoted results with both self-test suite counts, but supplies only commands and expected coverage. The added phase-run entries likewise contain no suite results for this candidate, leaving the checked completion claims without the required recorded unit-test evidence.
```

## GPT panel Round 3

Round 3 over `83700a9e^..bd6d8427` the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 91s, clock 2026-09-26T08:31:33Z, cost 102821tokens. Output and stderr stay in the session scratch rundir outside the tree (r3.out, r3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F12 -- a quoted bracket ends bracket masking -- adversarial -- FIXED in `a779094c` (independent) [major]

Brackets counted outside quotes (superseded by the round-4 rethink). Blocking (a credential exposure), so round 4 ran.

### F13 -- `git diff --name-only` quotes non-ASCII paths, proving a false exclusion -- correctness -- FIXED in `a779094c` (independent) [major]

Paths are read NUL-delimited and git output decodes as UTF-8. Blocking (a false retirement).

### F14 -- only `ceiling` validates ceiling lines -- integration -- FIXED in `a779094c` (independent) [minor]

`_ceiling_replay` runs for every repair command before it reads or appends.

```text
RECEIPT sha=ebef9c15b697440cb96165539e5efd90c0191f2a930a3be5bb7abe8284977a8e end=PANEL-2c605d913e7ff0ae nonce=11d5d9c068e96a72
**adversarial: needs-attention** (2)

1. scripts/review_prompt.py:2095-2098: Bracket redaction counts delimiters inside quoted strings. For `token: [\n  "]",\n  "sensitive"\n]`, the quoted `]` prematurely ends masking, exposing the next credential element. The subsequent scan passes because the credential opener has already been removed.
2. scripts/review_prompt.py:1368-1371: `git diff --name-only` returns quoted and escaped filenames for non-ASCII characters, tabs, and newlines, but these lines are treated as literal paths. A changed `src/é.txt` can consequently miss `src/**`, falsely proving exclusion and allowing NOT GREEN retirement. Read NUL-delimited paths without Git pathname quoting.

**consistency: approve**
**integration: needs-attention** (1)

1. scripts/campaign_guard.py:1676,1985-2037: Strict ceiling validation runs only inside `_ceiling`; other repair commands call `_journal`, which skips ceiling prose and ceiling receipt events. Consequently, `repair attempt` accepts a journal ending in truncated ceiling prose and its append supplies the missing newline, while duplicate or foreign ceiling uses also escape validation. All repair commands must validate the complete journal before accepting or appending transitions.

**record: approve**
```

## GPT panel Round 4

Round 4 over `83700a9e^..a779094c` with prior verdicts appended (rounds past the sign-off run for blocking findings only). Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 119s, clock 2026-09-26T08:39:45Z, cost 105396tokens. Output and stderr stay in the session scratch rundir outside the tree (r4.out, r4.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F15 -- a double quote inside a single-quoted value ends masking -- adversarial -- FIXED in `de588a07`, then the unit rethought in `1fc1cc0a` (independent) [major]

The fourth early terminator in four rounds: the opener rule was rethought, not patched a fifth time. A credential value that opens any multiline form masks the rest of its block, and nothing looks for its end.

### F16 -- receipt agreement omits the red and terminal evidence -- integration -- FIXED in `de588a07` (independent) [major]

Agreement covers the red, and a close's green sha, run, attempt, and workflow id (a retirement's silent push).

```text
RECEIPT sha=6931ae55387ae0aea451d718d857203eacad7631f8ed8af02bcefd2932796177 end=PANEL-3519e3460d0d434d nonce=4aca8ac10c4251a8
**adversarial: needs-attention** (1)

1. scripts/review_prompt.py:2062-2064: Multiline quote detection tests double quotes before single quotes without respecting the enclosing delimiter. For `API_TOKEN='first "\nsecond "\nSENSITIVE\nlast'\necho done`, it selects the embedded double quote and stops masking after `second "`, exposing `SENSITIVE`. The subsequent secret scan passes because the credential opener has been removed and the remaining lines are unindented.

**consistency: approve**

**integration: needs-attention** (1)

1. scripts/campaign_guard.py:1366-1392,1434-1436: Receipt/prose agreement omits the repaired red SHA and terminal evidence fields. Terminal parsing discards the green SHA, run ID, run attempt, and workflow ID, so changing those fields in a closing receipt still passes replay and closes the episode. This violates the required refusal of contradictory receipts and undermines the structured terminal evidence.

**record: approve**
```

## GPT panel Round 5

Round 5 over `83700a9e^..1fc1cc0a` the hard cap, with prior verdicts appended. Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 153s, clock 2026-09-26T08:53:33Z, cost 108175tokens. Output and stderr stay in the session scratch rundir outside the tree (r5.out, r5.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F17 -- a PowerShell backtick escape reads as a closing quote -- adversarial -- FILED to `D00 T04 §42` (independent) [major]

Round 5 is the hard cap. It needs a credential value written as a PowerShell double-quoted string with an escaped quote and a line break; it folds into D00 T04 §42 (operator direction: no new section per review).

### F18 -- a re-reserved commit keeps its old order, so close can take an older push -- integration -- FILED to `D00 T04 §42` (independent) [major]

Round 5 is the hard cap. It needs an abandon, a later push, then the abandoned commit pushed again inside one episode; it folds into D00 T04 §42.

### F19 -- the capture gate scans serialized JSON while capture-run scans values -- integration -- FILED to `D00 T04 §42` (independent) [minor]

Round 5 is the hard cap. No committed capture trips it today (every committed capture scans clean); it folds into D00 T04 §42.

```text
RECEIPT sha=b9523eb1aea0565ef4981baf7713a75cd86bab5ac867e6347a5d1959ceee32bd end=PANEL-ea1c911f027dd360 nonce=d833a069ec7a79dd
**adversarial: needs-attention** (1)

1. scripts/review_prompt.py:2034-2038,2117-2130: Multiline opener detection ignores PowerShell’s backtick escape. For ``$secret = "first `"\nSENSITIVE\nlast"``, it treats the escaped quote as the closing delimiter, skips block masking, and exposes the unindented continuation. The subsequent scan cannot recover the removed credential opener and accepts the exposed text.

**consistency: approve**

**integration: needs-attention** (2)

1. scripts/campaign_guard.py:1798-1805,1868-1898,1935-1939: Re-reserving an abandoned commit changes its state without updating transition order, while terminal checks select the last pushed attempt by reversed list order. For commits R → B → A, reserving and abandoning A, pushing B, then re-reserving and pushing A leaves B selected as the latest push. A stale green run on B can therefore close the episode without covering A.
2. scripts/review_prompt.py:6957-6959: The committed-capture gate scans serialized JSON, although `capture_run` correctly scans individual string values. A captured step name such as the existing fixture’s `verify token=***` makes the serialized closing quotation mark look like a multiline opener, so `secret_scan` rejects the sanitized JSON. A capture accepted by `capture-run` can consequently fail the required self-test when committed.

**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §41, D00 T04 §39]; dependents [none]; bytes 47354; run 20260926-D00-T04-S41-astra

`bytes 47354` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency and XREF neighbor D00 T04 §39; no section depended on D00 T04 §41 when it ran. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 63s, clock 2026-09-26T08:56:58Z, cost 18866tokens. Check: `PASS 16 findings, one per line`.

Triage follows the operator direction of 2026-09-26 (the plan must not grow unconditionally): accepted findings fold into D00 T04 §42, an open section that already owns the unit, and the rest are rejected with a reason.

Ledger:
- [D00-T04-S41-PR1] [minor] a torn journal write has no recovery procedure -> filed D00 T04 §42
- [D00-T04-S41-PR2] [minor] uncommitted journal lines plus a lost episode file can erase a budget -> filed D00 T04 §42
- [D00-T04-S41-PR3] [minor] absence from the remote cannot prove non-delivery after a force push -> filed D00 T04 §42
- [D00-T04-S41-PR4] [minor] a newer attempt still running is unspecified -> rejected close already requires GitHub's listed run to be completed and successful, and a rerun reads in progress
- [D00-T04-S41-PR5] [minor] a retirement's before and after commits are not bound to the delivered push -> filed D00 T04 §42
- [D00-T04-S41-PR6] [major] `**` never matches zero directories, which can prove a false exclusion -> filed D00 T04 §42 (owner Claude Code, due 2026-10-03)
- [D00-T04-S41-PR7] [minor] a repairable step outranks an unavailable log -> rejected deliberate: the known cause is fixed first and the next read-back classifies again
- [D00-T04-S41-PR8] [minor] each new run attempt earns another ceiling allowance -> rejected attempts grow only by an operator rerun, which is a new wait by design
- [D00-T04-S41-PR9] [minor] forged job and step boundaries could end masking -> rejected gh prefixes the real columns on every line, so command output lands in the third field
- [D00-T04-S41-PR10] [minor] redaction and the scan share one grammar -> rejected the scan is defined against the stated grammar, and the severity rule covers unrecognized forms
- [D00-T04-S41-PR11] [minor] a capture's files are not published as one set -> filed D00 T04 §42
- [D00-T04-S41-PR12] [minor] receipts have no schema or derived-field check -> filed D00 T04 §42
- [D00-T04-S41-PR13] [minor] serial writes have no concurrency checkpoint -> filed D00 T04 §42
- [D00-T04-S41-PR14] [minor] D00 T04 §39 still states the rules D00 T04 §41 replaced -> filed D00 T04 §42
- [D00-T04-S41-PR15] [minor] Windows reproduction does not check the local revision -> rejected the re-run already names its commit and the earlier steps it may depend on
- [D00-T04-S41-PR16] [minor] no concise diagnostic view of an episode -> rejected repair status and its receipt already carry the episode, reservations, budget, and outcome
End of ledger

16 findings: 9 filed in D00 T04 §42 (`d5f91b9b`), 7 rejected.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=97de2439f70ee6e28aa1d4d754f4a6b5895438d83ca11a8d8cb48db18dad7cbd end=PLAN-0142887d725591be nonce=da37c40790219846
- Torn journal writes are refused without an owned recovery procedure; belongs in §41 replay and interruption hardening, with interruption during append and operator recovery fixtures, because the current crash tests start after the append and cannot prove recovery from partial writes.
- Append-only comparison against HEAD does not protect newly appended, uncommitted history if its cache also disappears; belongs in §41 journal durability, because losing both can erase consumed budget without violating the committed baseline.
- Current remote ancestry cannot prove a commit was never delivered and subsequently removed by a force push or branch deletion; belongs in §41 abandonment reconciliation, because treating present absence as non-delivery can refund an attempt that already triggered CI.
- “Latest completed attempt” leaves a newer queued or running attempt unspecified; belongs in §41 close evidence, explicitly requiring the latest attempt itself to be completed and green, because an earlier success must not close an episode during a rerun.
- The provenance of the push’s before/after commits is unspecified; belongs in §41 trigger evidence, binding both commits and the branch to the actual delivered push, because a correctly computed diff over the wrong range can falsely authorize silence.
- Wildcard semantics specify segment crossing but not zero-segment matching, escaping, or whole-path anchoring; belongs in §41 filter semantics and fixtures, because partially compatible matching can turn a workflow admission into a false exclusion.
- D7 “repairable” precedes D9 “unavailable or ambiguous logs,” leaving repairable-plus-unknown failures liable to classify as repairable; belongs in §41’s decision table, with combination fixtures and explicit precedence, because isolated row coverage does not prove safe aggregation.
- Giving every new run attempt another ceiling allowance has no stated campaign-wide bound; belongs in §39 ceiling recovery and §41 campaign binding, because repeated reruns can continually renew the supposedly bounded allowance.
- Conservative multiline masking depends on trustworthy job and step boundaries, but their provenance is unspecified; belongs in §41 redaction, with forged-boundary log fixtures, because command output resembling a boundary could end masking prematurely.
- Redaction and secret scanning share a stated set of recognized credential forms without an independent coverage requirement; belongs in §41 capture sanitization, because both checks can accept the same unrecognized secret and “scans clean” then overstates assurance.
- Atomic writes of individual capture files do not define atomic publication of the workflow, record, log, index, and provenance together; belongs in §41 capture persistence, with interruption and retry fixtures, because partial capture sets can become unusable or block recovery as duplicates.
- Receipt versioning lacks an explicit schema, unknown-version policy, and validation of derived fields such as remaining budget; belongs in §41 structured receipts, because valid JSON and prose agreement alone do not establish semantic validity.
- Concurrent-write serialization is asserted without a concurrent-process checkpoint; belongs in §41 journal testing, exercising simultaneous reservations and terminal transitions, because sequential crash fixtures cannot prove the budget survives competing commands.
- §39 retains the masking-based severity exemption and earlier aggregation rule that §41 replaces; belongs in dated acceptance-contract addenda to §39 and the consuming skills, because preserving history should not leave contradictory operational guidance active.
- Windows reproduction does not require verifying the local checkout revision or identifying prerequisites before execution; belongs in §39 reproduction context and §41 failure drills, because matching shell behavior on eight fixtures does not make a command reproduce against an arbitrary local tree.
- No section owns a concise diagnostic view combining episode identity, pending reservation, last verified CI attempt, budget, and recovery action; belongs in §41 status receipts and presentation, because operators otherwise have to reconstruct actionable state from journal prose during failures.
```
