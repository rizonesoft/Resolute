# D00 T04 §15 review -- Run-Record Vocabulary and Evidence Follow-Ups

Candidate chain: `d0b5690` (implementation, 6 files), `67a2f68` (round-1 answers), `0d5b50c` (round-2 answers), `b5c07df` (sign-off advisory guards). Full panel: rounds 1-2 on Sol at medium effort, round 3 Opus sign-off at medium effort. Research pass skipped: pure infrastructure, no tool plan or surface. Architecture gate skipped: review tooling touches none of the listed surfaces (framework state, settings, extension seams, tool contracts, elevation, restore/undo).

## The three questions

**Does it make sense?** Yes. Eleven vocabulary and evidence follow-ups land as one coherent tightening: the latency boundary, per-field coverage, transition bindings, the voids rule, export dispositions, the provider rename, record-time measurement, the revisit query, two skipped outcome shapes, and bound gate evidence. The sign-off passed with two advisories, both closed (one fixed directly, one closing at stamp).

**Is it logical?** Yes, after two fix-loop rounds. Round 1 split the as-of hole into generate-side (fixed by the corpus-clean leg) and consume-side (fixed in round 2 by the resolvability assertion plus the regeneration rule); the record thread moved from careless wording to disclosed deferral to stamp-time persistence. The provider rename (codex/claude to openai/anthropic) is the honest reading of its item: the serving company, confirmed by codex's own session banner.

**Did it break anything else?** No. All suites green at every commit (runs 65, findings 31, review-prompt 84, graph 511), validate 0 fatal, claims 112/112 hold. The export v1-to-v2 step is additive (one key per round line); no v1 consumer exists in code or on disk, and §18 consumes v2 natively.

## Self-review

Read the full candidate diff before the panel: no fixes needed. Two notes went to the panel explicitly: the item-7 proof rests on driven field values (the probe was not a review round and stays unpersisted) with the first real both-resolved lines landing in the S15 block this review, and export v2 rejects v1 loudly with mechanical co-evolution per the §14 precedent. The panel confirmed the first note as F2/F4/F6 and closed it through the stamp block; no panel finding touched the schema-version question.

## GPT panel Round 1

Round 1 over `d0b5690` (6 files, cross-check agrees).

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` needs-attention

```
RECEIPT sha=95aa8d5f2dbfd4c35c9ce27c0912c62e3b34ba68fa06c21042b26a9438baf108 end=PANEL-7ec91fffd7d52262 nonce=9469124ce14935bc
**adversarial: needs-attention** (1)
1. `scripts/todo-runs.py:623-630` combines live ledger dispositions with an `as_of` derived only from `run-records.md`; `scripts/todo-runs.py:683-690` checks only SHA syntax, and `scripts/todo-runs.py:810-816` then calls the export internally sound. Dirty or post-commit ledger dispositions, or an export carrying any fabricated 40-digit SHA, therefore pass under a commit that never contained them, defeating both the snapshot binding and the producing-commit evidence contract.
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:167-169` promises measured cost and latency on every new round but explicitly permits `cost: unresolved` and describes a timed invocation as “untimed”; `todo/00-workspace/TODO-04-self-correction.md:697,704` nevertheless marks the fresh resolved round complete, while the only added persisted round at `docs/reviews/run-records.md:248` has both fields unresolved and no committed round line contains the claimed `7435tokens` and `17s` values.
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost: 90,805 tokens (codex "tokens used"); latency 228s. Note: the round's stderr shows the reviewer roaming the tree with reads despite the prompt's no-tools framing; the receipt and verdicts are valid, the sandbox held read-only, and the cost honestly includes the roam.

## GPT panel Round 2

Fix-loop over `67a2f68` (2 files, cross-check agrees), round-1 verdicts appended.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` needs-attention

```
RECEIPT sha=c83e8a2d16123f9777770ff90a17d90f46c31ac84d118e29b50e6deaed3368e9 end=PANEL-d3d81b4f26c200a1 nonce=f969d765fe9ce107
**adversarial: needs-attention** (1)
1. `scripts/todo-runs.py:699-706` still validates an imported export's `as-of` as syntax only, and `scripts/todo-runs.py:826-832` consequently declares an export with any fabricated 40-hex commit internally sound; the new cleanliness check at `scripts/todo-runs.py:455-468` binds live export generation but does not assert that the commit carried the exported records and disposition corpus.
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:169` now clarifies when unresolved cost is permitted, but `todo/00-workspace/TODO-04-self-correction.md:697,704` still claims the fresh-round checkpoint complete without a persisted driven round resolving both fields; `docs/reviews/run-records.md:248` remains the only added round and records both `cost` and `latency` as unresolved instead of the claimed `7435tokens` and `17s`.
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost: 17,122 tokens (codex "tokens used"); latency 44s.

## Opus panel Round 3

Sign-off over `0d5b50c` (3 files, cross-check agrees), rounds 1-2 verdicts appended.

`adversarial` advisory
`consistency` approve
`integration` approve
`record` advisory

```
RECEIPT sha=aedfe62d3ee4d34fb7c8bd53bec16e46510b31bfc90b4121ef7d9f715894f0de end=PANEL-1a73681a8e7b71d5 nonce=8142683b64befe4d

**adversarial: advisory** (1)
1. `scripts/todo-runs.py:673-677` — `_commit_resolves` calls `subprocess.run` unguarded, so an `OSError` (git absent) or `subprocess.TimeoutExpired` (the 30s timeout expiring) propagates out of `check_export` at `scripts/todo-runs.py:715-717` as a traceback instead of a reported problem; the sibling git helpers in the same file guard exactly this (`as_of` at `scripts/todo-runs.py:477-492`, `_corpus_clean` at `scripts/todo-runs.py:460-466` both return a safe value on `(OSError, subprocess.SubprocessError)`). Round-2's as-of syntax hole is fixed: a fabricated `0`*40 binding now fails `--check-export` with `as_of commit resolves to no commit in this repository`, and the byte-carriage residual is stated in `scripts/todo-runs.py:687-690` and `docs/reviews/run-records.md:11`.

**consistency: approve**

**integration: approve**

**record: advisory** (1)
1. `todo/00-workspace/TODO-04-self-correction.md:697` claims the item's `Done when:` ("a round line written by following it resolves both fields") while no round line in the tree resolves `latency`: all 65 lines in `docs/reviews/run-records.md:17-292` read `latency: unresolved` (the section's own quoted report agrees, `latency: 0/65 recorded`), and the four values cited as proof (`7435tokens`/`17s` probe; `90805tokens latency: 228s`, `17122tokens latency: 44s` this review's rounds) exist nowhere in the repository, deferred to an S15 block the candidate does not yet contain. The round-2 text states the deferral and its reason explicitly, so the claim is disclosed rather than concealed, but the checkpoint leg is unverifiable from the tree until the stamp writes the block.
```

Transcript notes (the fenced output above is verbatim panel text, preserved unaltered): the `:11` citation points at the byte-carriage rule, which reads at `docs/reviews/run-records.md:9` in the candidate and staged trees; and the 65 round lines span lines 15-293 at `0d5b50c` (the cited 17-292 holds 63, verified), so the panel's count holds while its range endpoints miss the file's first and last round lines.

Output check: `PASS four lenses, one verdict each`, exit 0. Cost unresolved (no runner telemetry; 0-byte stderr); latency 53s.

### F1 -- as-of bound the runs file while dispositions floated from the live ledger -- correctness -- FIXED (independent) [major]

Round 1 adversarial. Fixed in `67a2f68`: `as_of` gains the corpus-clean leg (`_corpus_clean`), so any difference under `docs/reviews/` voids the binding; driven both ways (clean binds, untracked file voids).

### F2 -- skill called unmeasured cost untimed and claimed unpersisted values complete -- record -- FIXED (independent) [minor]

Round 1 record. Wording fixed in `67a2f68` ("honestly unmeasured rather than guessed", permission scoped to non-reporting runners); persistence completes in the S15 block with the stamp.

### F3 -- check-export blessed fabricated as-of bindings on syntax alone -- correctness -- FIXED (independent) [major]

Round 2 adversarial, extending F1 to consume-side. Fixed in `0d5b50c`: the checker asserts the as-of resolves (`resolves to no commit` on fabrications, pinned both ways), and the header requires consumers to regenerate rather than trust handed files. Byte-carriage rides §18 item 2's persisted hashes, stated in the checker docstring.

### F4 -- fresh-round proof quoted values no committed line carried -- record -- FIXED (independent) [minor]

Round 2 record, extending F2. The Done-7 note carries this review's driven values with the deferral stated and reasoned; the S15 block persists them with the stamp, which the stamp review verifies.

### F5 -- git resolvers crashed instead of reporting when git failed -- correctness -- FIXED (independent) [minor]

Round 3 adversarial advisory. Fixed directly in `b5c07df` without re-rounding (below the sign-off bar, same precedent as the §8 skill fixes): `_commit_resolves` and its `_transition_commit_resolves` sibling return False on `(OSError, subprocess.SubprocessError)`. The pre-existing `check_candidates` shape files to §20 rather than widening this candidate post-sign-off.

### F6 -- checkpoint leg unverifiable from the tree until the stamp writes the block -- record -- FIXED (independent) [minor]

Round 3 record advisory. Closes at stamp: the S15 block carries rounds 1-3 with measured cost and latency, verified by `--check` and the stamp review against the values quoted here.

## Plan review

gpt run over §15 plus §10: 13 findings, 1 filed to D00 T04 §20, 1 accepted direct, 9 rejected, 2 duplicate.

Manifest: sections [D00 T04 §15, D00 T04 §10]; dependents [D00 T04 §10, D00 T04 §7]; bytes 17788; run 20260920-D00-T04-S15-gpt

Dependents: nothing Depends on §15 (verified: §16 and §17 depend on §10, §18 on §8); §10 is the sole XREF consumer and §7 rides the transitive hop past it. Cost: 19,684 tokens (codex "tokens used"); latency 91s. Output check: `PASS 13 findings, one per line`, exit 0.

```
RECEIPT sha=6d4ad523b713d7314ab818adb402162e04ab3dafa36daa780a7d04cea555fb46 end=PLAN-9df1e676a2b573a0 nonce=3ca6ee74fb88c635
- §10 has an unchecked commit item despite a Verified stamp and three candidate commits; reconcile this in §10 because it contradicts both completion state and the one-section-one-commit rule.
- §15 marks its commit complete without naming the candidate and has no Verified, Review, CRUD, Duration, or Implementer record; add the lifecycle evidence to §15 because checked work is not yet auditable or review-stamp eligible.
- §15 quotes `latency: 0/65 recorded` and `cost: 6/65 recorded`, then records resolved cost and latency for later rounds while the final total remains 65; regenerate the final coverage report in §15 because its evidence cannot all describe the same snapshot.
- §15 establishes that bare counts are drift but repeatedly quotes `30 runs, 65 rounds` and uses `as-of <commit>` placeholders; replace these with exact bound output in §15 because the new evidence convention is not satisfied by its own checkpoint.
- §15 adds new round outcomes, provider semantics, and validation rules while the run-record schema remains version 1; add an explicit schema bump and migration test in §15 because semantic schema changes must be distinguishable from old records.
- §15 upgrades the export from version 1 to version 2 without proving that §8's consumer accepts v2 or rejects obsolete v1 input correctly; add a consumer compatibility gate spanning §15 and §8 because a sound producer does not prove the decision workflow still runs.
- `cost` is defined only as an aggregate token counter, with no input/output/cache/reasoning semantics or cross-provider normalization; define comparable cost accounting in §15 and consume it in §8 because value-per-cost rankings can otherwise be materially misleading.
- The provider rule infers the serving company from the runner, coupling `codex` to OpenAI and `claude` to Anthropic; store runner and actual serving provider independently in §15 because future gateways or model routing would silently misattribute usage.
- The latency boundary does not define retries, timeouts, cancellations, streaming completion, or multi-attempt rounds; extend the latency vocabulary in §15 because error and voided rounds otherwise remain incomparable.
- Per-finding dispositions are claimed as an as-of snapshot, but byte identity across the records, ledger, and transitions is deferred to §18; make atomic source binding a §15 completion gate because an internally sound export may still combine files from different commits.
- §15 materially references and affects §8, §14, and §18 but declares only an XREF to §10; add bidirectional dependency links to the affected sections because graph queries and change-impact review currently omit real consumers.
- Transition and gate bindings use abbreviated commit IDs; require full object IDs in §15 because abbreviations can become ambiguous as repository history grows.
- The report exposes completeness counts but no accepted-finding yield, tokens per accepted finding, latency percentiles, or unresolved-data confidence; add a premium analytics view after §15 because decision-makers need comparable efficiency and evidence quality, not only record coverage.
```

Ledger:
- [D00-T04-S15-PR1] [minor] §10 unchecked Commit despite stamp -> duplicate D00-T04-S13-PR16 (Commit representation rides D00 T04 §19 item 4)
- [D00-T04-S15-PR2] [minor] lifecycle evidence missing pre-stamp -> rejected (lifecycle order: the stamp lands after plan review; the candidate chain is named in the stamp's Review line)
- [D00-T04-S15-PR3] [minor] coverage quotes span two snapshots -> rejected (moot: counts are candidate-bound implementation evidence; the stamp's Live proof re-drives every count at the stamp tree)
- [D00-T04-S15-PR4] [minor] bare counts and as-of placeholders in Done notes -> rejected (placeholders mark pre-commit unknowns honestly; exact bound outputs ride the stamp's Live proof; the checkpoint describes legs, it quotes nothing)
- [D00-T04-S15-PR5] [minor] no records-schema bump for new vocabulary -> rejected (single records file migrated atomically with its parser; old readers fail loud, never silent; no v1 data survives to translate; compat policy owed with a second live shape per S10-PR8)
- [D00-T04-S15-PR6] [minor] no export consumer compatibility gate -> rejected (no v1 consumer exists in code or on disk; v2 is additive over v1; §8 consumed and shipped; §18 consumes v2 natively; strict co-evolution per S14-PR10)
- [D00-T04-S15-PR7] [minor] cost lacks comparable accounting semantics -> filed D00 T04 §20 (comparable cost accounting item)
- [D00-T04-S15-PR8] [minor] provider inferred from runner -> rejected (runner and provider already ride as independent fields on every line; the codex-to-openai mapping was evidenced migration, not a standing rule; a gateway round needs no schema change)
- [D00-T04-S15-PR9] [minor] latency edges undefined for retries and timeouts -> rejected (the boundary's literal reading covers every case: timeouts and cancels return output at end-of-wait, retries are separate invocations, rounds are attempts; no live case contradicts it)
- [D00-T04-S15-PR10] [minor] atomic source binding as completion gate -> rejected (litigated and closed in-panel: corpus-clean generation binding, consume-time existence, regeneration rule, §18 item 2 hashes; transitions.md rides the corpus leg)
- [D00-T04-S15-PR11] [minor] affected sections lack bidirectional links -> accepted (fixed directly: bidirectional XREF §15-§18; §8 and §14 already SOURCE-linked per the filings rule)
- [D00-T04-S15-PR12] [minor] abbreviated shas in bindings -> rejected (ambiguity fails loud through every resolver, reported never misresolved; full IDs add churn without safety at this history scale)
- [D00-T04-S15-PR13] [minor] no premium analytics view -> duplicate D00-T04-S8-PR21 (premium decision dashboard already filed to §18 item 6; percentiles fold into its latency rendering)
End of ledger

## Live proof

Section gates, each re-run after the last fix (candidate `2132070`, rundir `/tmp/s15-panel.5nB6Ni`), with the S15 block, ledger regen, stamp, and row flip in the worktree. Digests are sha256 over the quoted bytes as shown (no trailing newline). All quotes bind the review's marker run (the plan-review run; panel rounds mint no run IDs). The `as-of unresolved` below is the dirty worktree (the stamp uncommitted); the producing commit rides the provenance candidate, and a clean-tree re-drive binds the stamp commit.

- Suite: `review-prompt self-test: 84 cases, 0 failed`.

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command python scripts/review_prompt.py --self-test; exit 0; tool CPython 3.14.4; digest 000a962d6db147e36d149ed85c8c59b120f341d382ba6ff3e32faa9dc37dd6ff; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

- Graph suite: `todo-graph self-test: 511 cases, 0 failed`.

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command python scripts/todo-graph.py self-test; exit 0; tool CPython 3.14.4; digest adb430d6ff222a964ec7005d14045e3fffd00d831523e9b4747f23828af5177e; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

- Validate: `30 todos, 213 sections -- 0 fatal, 0 warning(s), 100 adjacency advisory`.

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command python scripts/todo-graph.py validate; exit 0; tool CPython 3.14.4; digest 9d0ccc36f8242b0f3d3049c5434d20f54daf65a9953aa9f80153f96cf08d5201; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

- Claims: `todo-claims: 112 claim(s) -- 112 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`.

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest 95616e39a4200e824612521c83820e14d00a5fdbbc56d3d7625c0c90c32a1366; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

- Findings ledger: `todo-findings: ledger current, 209 finding(s), transitions complete`.

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command python scripts/todo-findings.py --check; exit 0; tool CPython 3.14.4; digest fd53aed75f1602bc565f7a14386eab783c6acb69a47318a48e76f66230378f6c; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

- Runs suite: `todo-runs self-test: 65 cases, 0 failed`, covering every §15 pin (skipped outcomes, mapping skip, coverage, revisit open/met, dispositions carried/round-trip/missing/valued/homed/required, sound-line bound/foreign, as-of fabricated/resolving) plus the bad-outcome and bad-provider rejections.

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command python scripts/todo-runs.py --self-test; exit 0; tool CPython 3.14.4; digest 24204fcf0307a9cb58198001fd8d814af4a049490a6e24a47b0c9ae3c8a34495; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

- Runs check: `31 runs, 68 rounds: all resolve, all covered, counts agree`.

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command python scripts/todo-runs.py --check; exit 0; tool CPython 3.14.4; digest d0e639c4888cd43edbfac843f67a91a65067e22d483b58c0c49a1e8e38b7368f; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

- Report counts and coverage:
`31 review runs, 68 independent rounds.`
`Coverage (recorded vs unresolved, by field and model):`
`- cost: 8/68 recorded (gpt-5.6-sol 8/27, gpt-6-astra 0/21, opus 0/20)`
`- latency: 3/68 recorded (gpt-5.6-sol 2/27, gpt-6-astra 0/21, opus 1/20)`
`- version: 48/68 recorded (gpt-5.6-sol 27/27, gpt-6-astra 21/21, opus 0/20)`

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command python scripts/todo-runs.py --report; exit 0; tool CPython 3.14.4; digest f8b4f5e55e76952f41cf3321699a87c91f02695a8268343cfa91ff8c716186b0; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

- Revisit trigger: `- 6/5 panel-reviewed sections past the window (D00-T04-S8, D00-T04-S11, D00-T04-S12, D00-T04-S13, D00-T04-S14, D00-T04-S15): trigger met`, with the window named on the header line above it.

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command python scripts/todo-runs.py --report; exit 0; tool CPython 3.14.4; digest e35581faf016294c057eb02f52d5a6e66eba16d193c45e98352df7dd957bcc3c; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

- Export round-trip: `R:/tmp/s15-live-export.json: export version 2, 31 runs, as-of unresolved, internally sound`.

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command python scripts/todo-runs.py --export plus --check-export; exit 0; tool CPython 3.14.4; digest 0414e0b47863d52b1f0e4fd63156df75c9af3ef683f21f274f861b1de4a69a24; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

- Sweep: `check-all: 1 of 13 gate(s) FAILED`. Twelve green; the `graph self-test` gate fails on the pre-existing stderr hint while the suite itself passes exit 0, bisected to `ef362d6` and filed to D00 T04 §20.

Provenance: candidate 21320706c15b2dbec434938885870cd1ec0425cc; command powershell scripts/check-all.ps1; exit 1; tool check-all.ps1; digest 65164f3b69025369d987f87c97bb792bcaae2d524f94dba129a55f43f133d22d; path docs/reviews/00-workspace/D00-T04-s15.md; run 20260920-D00-T04-S15-gpt

## Stamp review

Attempt 1 (Sol high over staged tree `ae0afe9e`, exit 0, 118531 tokens, 535s) named two figures; both stand. `bytes 17788` is the assembled prompt's LF/UTF-8 count per the skill's Manifest grammar (the fence manifest's `bytes=16665` counts the fence only), the §14 attempt-1 precedent exactly. `docs/reviews/run-records.md:11` is the panel's own verbatim citation inside the fenced round-3 output, preserved unaltered for transcript fidelity; the byte-carriage rule reads at line 9 in the candidate and staged trees, and no stamp figure cites line 11.

Attempt 2 (Sol high over staged tree `7b44e019`, exit 0, 99183 tokens, 331s) accepted the bytes and re-named the `:11` cite plus the panel's `17-292` range (verified: the 65 round lines span 15-293 at `0d5b50c`, and 17-292 holds 63). Both live in verbatim transcript, so the correction rides a transcript note at the site (round-3 section above) with the fenced bytes untouched. Re-running over the re-staged tree.
