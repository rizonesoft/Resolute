# Review -- D00 T04 §35, CI Read-Back Follow-Ups

**Reviewed:** 2026-09-25
**Stance:** post-implementation
**Rounds:** independent pass (4 findings: fixed); GPT 1 bulk (3 fixed); GPT 2 bulk (2 fixed); GPT 3 sign-off (2 filed below the blocking bar, D00 T04 §37).
**Tier:** Full panel. The diff touches `scripts/review_prompt.py` and `scripts/campaign_guard.py` (review tooling and gates in `scripts/check-all.ps1`) and two skills (`review-todo-section`, `process-phase`: the shared run contract).
**Attestation:** `docs/reviews/00-workspace/D00-T04-s35.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `43a59fc6` | implementation: the GitHub-verified scalar decoder and re-run context, the ceiling outcome, the cause-evidence rules, the persisted repair episode, `--expect-no-run`, the D00 T04 §31 note, and the drill records |
| `b8d2246c` | answers the independent pass: an unverifiable no-run poll never passes; context renders as a quoted script, refuses unreadable env, and names the runner's real shell |
| `4daab1b9` | answers panel round 1: unreadable repair state refuses; empty or null run lists are unverifiable; block shell and directory refuse |
| `f5072a67` | answers panel round 2: a null episode refuses; container jobs refuse a host re-run |

Record commits (filings only, no reviewed content): `55a40f07` (the sign-off and plan-review findings filed in D00 T04 §37) and `8efff65a` (the D00 T04 §37 plan row that filing missed).

Range `43a59fc6^..f5072a67` (base `2e78343f`), contiguous, no declaration owed. Each round fenced the range from `2e78343f` to its own head; the fenced contract was checked to open with this section's heading. The rundir is session scratch outside the tree (/tmp/review-rZHPusYW, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation. The decoder's expected outputs are GitHub's own: a disposable `scalar-echo.yml` printed every `run:` form's script file under `shell: cat {0}` (run 36175449390), so the oracle is not the author's reading of YAML. Live checks: the real zero-job drill red `784d4fe6` reads repairable on GitHub's workflow-file note; the bound-exhaustion and stamped-owner drills ran on a disposable branch with fixture guard state. Mutations: dropping double-quote decoding fails its GitHub leg by name; forcing a failed no-run poll to read as silence fails its leg by name. The architecture gate is not triggered (review tooling, no framework state). `source-defect` approves in-session: the GitHub behaviours relied on were probed live (`shell: cat {0}` prints the script file; `gh run view` names a workflow file issue; a branch-only workflow resolves by name only after its first run). `design` is not owed.

## Independent pass

`python scripts/panel_slots.py exec independent --commit 43a59fc668a2feca8a5b553aefa74ee5610ab2d3`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 184s, cost unresolved (the review runner printed no token figure). Output and stderr stay in the session scratch rundir outside the tree (ind35.out, ind35.err); the output is transcribed below.

### F1 -- an unverifiable no-run poll reads as silence -- correctness -- FIXED in `b8d2246c` (independent) [major]

A failed or malformed `gh` poll returned the same result as a verified empty list, so `--expect-no-run` could pass without proof. Only polls that return a JSON list count now.

### F2 -- env forms the parser does not read are dropped or misread -- correctness -- FIXED in `b8d2246c` (independent) [minor]

A flow-mapping env was ignored and a block value read as `|`; unreadable env forms, including workflow-level flow env, now refuse.

### F3 -- context is interpolated unquoted and scoped to one command -- correctness -- FIXED in `b8d2246c` (independent) [minor]

The context now renders as a quoted script: `export KEY='value'` and `cd 'dir'` ahead of the command, so the env reaches the whole script.

### F4 -- the default shell is assumed to be Bash -- correctness -- FIXED in `b8d2246c` (independent) [minor]

The shell now comes from the job's `runs-on` (`bash -e {0}` on Linux and macOS, `pwsh` on Windows), refused when the runner is unknown.

```text
The no-run verification can falsely succeed when GitHub cannot be queried, and the fallback can recommend commands with incorrect environments or shells. The 411-case self-test passes, but targeted probes reproduce the uncovered failures.

Full review comments:

- [P1] Preserve an unverifiable outcome when no-run polling fails — R:\GitHub\Resolute\scripts\review_prompt.py:1720-1729
  When `gh` is missing, unauthenticated, returns HTTP errors, or produces malformed JSON, this function eventually returns `None`, just as it does for a verified empty run list. Consequently, `ci-wait --expect-no-run` exits 0 without establishing that no run occurred. Mocking either an HTTP 503 or invalid JSON reproduces this. Propagate an unverifiable result and reserve success for successful, valid polling responses.

- [P2] Refuse environment forms the context parser cannot decode — R:\GitHub\Resolute\scripts\review_prompt.py:1441-1447
  When logs are unavailable, a valid step containing `env: {MODE: required}` is reported as locally reproducible, but its environment is silently omitted because only child lines are inspected. A block-valued variable is instead decoded as the literal `|`, producing output such as `MODE=| python check.py`. Workflow-level flow mappings also bypass the global refusal. Decode these forms correctly or mark their context unsupported rather than recommending a different execution environment.

- [P2] Apply rerun context safely to the entire script — R:\GitHub\Resolute\scripts\review_prompt.py:1474-1481
  For supported context values, direct interpolation still changes execution: `MODE: "two words"` becomes `MODE=two words python check.py`, and a directory containing spaces produces an invalid `cd`. Even simple values have incorrect scope: `MODE=required echo "$MODE" && python check.py` neither supplies the value during shell expansion nor exports it to the second command, unlike GitHub's step environment. Quote paths and values for the selected shell and apply environment variables to the whole script, or refuse rendering that context.

- [P2] Resolve the runner's default shell instead of assuming Bash — R:\GitHub\Resolute\scripts\review_prompt.py:1478-1478
  For a Windows job without an explicit `shell`, the fallback now instructs the operator to use Bash even though GitHub runs the command in PowerShell. For Linux jobs, an unspecified shell also differs from the explicit Bash template used here: adding `pipefail` can change the script's outcome. Since `workflow_steps` does not retain runner information, derive the effective shell from job context or refuse to claim an exact local reproduction when it is unknown.
```

## GPT panel Round 1

Round 1 over `43a59fc6^..b8d2246c` with the independent pass appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 50s, clock 2026-09-25T19:29:30Z, cost 42332tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-1.out, panel-1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F5 -- an unreadable repair episode resets the bound -- adversarial -- FIXED in `4daab1b9` (independent) [major]

A corrupted or malformed episode file read as no episode, allowing a fresh count; it now refuses and escalates.

### F6 -- empty or null run lists read as silence -- adversarial -- FIXED in `4daab1b9` (independent) [minor]

Empty `gh` output and a list holding `null` now read as unverifiable.

### F7 -- block-valued shell and working-directory are read as their header -- integration -- FIXED in `4daab1b9` (independent) [minor]

Both refuse now instead of printing `cd '>-'`.


```text
RECEIPT sha=5931a6a0e4ca33107965d8f0a2da0b37827eda068f7e076a72d9be1cfa39623b end=PANEL-67aefc5167cf025b nonce=502dcfb620e82bcd
**adversarial: needs-attention** (2)
1. `scripts/campaign_guard.py:309-313`: Unreadable or malformed persisted repair state is treated as no episode. After three attempts, a corrupted state file permits another attempt with a reset count. Refuse unreadable state instead of silently resetting the bound.
2. `scripts/review_prompt.py:1768-1775`: No-run polling still accepts malformed responses as silence: `out or "[]"` converts empty output into success, and filtering non-dictionary list entries turns `[null]` into an empty run list. Both must return unverifiable.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/review_prompt.py:1440-1445`: Parsing `shell` and `working-directory` ignores their child lines and passes block headers through `_inline_scalar`. A valid `working-directory: >-` followed by `tools` consequently produces `cd '>-'`; block-valued `shell` is likewise misreported. Decode these context scalars or refuse them.
**record: approve**
```

## GPT panel Round 2

Round 2 over `43a59fc6^..4daab1b9` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 38s, clock 2026-09-25T19:33:17Z, cost 43358tokens (latency and clock measured from the rundir file times, the round's prompt copy to its output, because the printed timing line was cut from the session's view). Output and stderr stay in the session scratch rundir outside the tree (panel-2.out, panel-2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F8 -- a JSON null episode resets the bound -- adversarial -- FIXED in `f5072a67` (independent) [major]

Only a missing file means no episode now; a file holding `null` refuses.

### F9 -- container jobs are advertised with the host's shell -- integration -- FIXED in `f5072a67` (independent) [minor]

A job with `container:` or `services:` refuses a host re-run.


```text
RECEIPT sha=6349d84a4a8a43e60cca4c72963e4b8616ef656ad776eb8827409722b94e3dcf end=PANEL-2348620c5a3f979a nonce=355ac938ca035621
**adversarial: needs-attention** (1)
1. `scripts/campaign_guard.py:309-320`: A persisted JSON `null` bypasses validation because `ep is None`, then opens a fresh episode on the next attempt. Corrupting an exhausted episode to `null` therefore still resets the bound. Distinguish a missing file from an existing file containing an invalid episode.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/review_prompt.py:1498-1505`: Default-shell inference uses only `runs-on`, ignoring the job's `container`. An Ubuntu job running inside a container is consequently advertised with `bash -e {0}`, although container steps default to `sh`; the container's execution environment is also omitted. Detect container jobs and refuse local reproduction unless that context can be reproduced.
**record: approve**
```

## GPT panel Round 3

Round 3 over `43a59fc6^..f5072a67`, the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 80s, clock 2026-09-25T19:37:33Z, cost 45356tokens (latency and clock measured from the rundir file times, the round's prompt copy to its output, because the printed timing line was cut from the session's view). Output and stderr stay in the session scratch rundir outside the tree (panel-3.out, panel-3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` needs-attention
`record` approve

### F10 -- job context depends on key order -- integration -- FILED to `D00 T04 §37` (independent) [minor]

A job declaring `container:` or `runs-on:` after its steps is missed by the backward scan. Below the blocking bar (a diagnostic's printed command, no safety, data, or stamp effect), and the context parser was patched three rounds running, so the three-round rule files it (`55a40f07`). No round 4 runs.

### F11 -- quoted step keys are ignored -- integration -- FILED to `D00 T04 §37` (independent) [minor]

`"shell": pwsh` and other quoted context keys lose their context. Filed with F10 for the same reason.


```text
RECEIPT sha=6d455e089ca0017162160825c4a57df0907bf8ea4d8a11e4997f2b5665c3206c end=PANEL-7bca531b7d3cf091 nonce=a37b9fc608a11982
**adversarial: approve**
**consistency: approve**
**integration: needs-attention** (2)
1. `scripts/review_prompt.py:1444-1457`: Job context depends on key order. The container/services scan stops before the current step, so a valid job declaring `container:` after its `steps:` block still prints a host-shell rerun command. The backward `runs-on` search can also inherit a previous job’s runner when the current job declares its runner after its steps. Parse context within the complete owning job or refuse reproduction.
2. `scripts/review_prompt.py:1407-1416`: The key parser silently ignores quoted context keys. A valid step containing `"shell": pwsh`, `"env": {MODE: required}`, or `"working-directory": tools` alongside an ordinary `run:` loses that context and prints an incorrect rerun command. Decode quoted keys or refuse the unsupported mapping instead of treating omitted context as defaults.
**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §35, D00 T04 §33]; dependents [none]; bytes 29544; run 20260925-D00-T04-S35-astra

`bytes 29544` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency D00 T04 §33; no review dependents. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 43s, clock 2026-09-25T19:38:34Z, cost 19750tokens. Check: `PASS 13 findings, one per line`.

Ledger:
- [D00-T04-S35-PR1] [minor] the ending-signal rule reads line order, not the failed step -> filed D00 T04 §37
- [D00-T04-S35-PR2] [minor] cause: unknown has no next action -> filed D00 T04 §37
- [D00-T04-S35-PR3] [minor] earlier steps assumed run hides prerequisites -> filed D00 T04 §37
- [D00-T04-S35-PR4] [minor] a failing uses: step has no fallback contract -> filed D00 T04 §37
- [D00-T04-S35-PR5] [minor] the scalar oracle proves text, not execution context -> filed D00 T04 §37
- [D00-T04-S35-PR6] [minor] the repair episode carries no identity binding -> filed D00 T04 §37
- [D00-T04-S35-PR7] [minor] attempt accounting around a crash or failed push is undefined -> filed D00 T04 §37
- [D00-T04-S35-PR8] [minor] repair close accepts any sha -> filed D00 T04 §37
- [D00-T04-S35-PR9] [minor] a lost episode file silently starts over -> filed D00 T04 §37
- [D00-T04-S35-PR10] [minor] the ceiling re-run has no persisted count -> filed D00 T04 §37
- [D00-T04-S35-PR11] [minor] expect-no-run has no authorization record -> filed D00 T04 §37
- [D00-T04-S35-PR12] [minor] an authorized no-run reads like green -> filed D00 T04 §37
- [D00-T04-S35-PR13] [minor] no redaction policy for copied commands and logs -> filed D00 T04 §37
End of ledger

13 findings, all filed in D00 T04 §37 (`55a40f07`, plan row `8efff65a`).

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=b95f90cedb3b5879d111268a6778d14521ff6cf406153bfb6bc31e278c05e370 end=PLAN-dd177f9d7bf93636 nonce=939c61fbd3a72476
- §35 equates the last matching log line with the signal that ended the job; its cause-evidence item should correlate signals with the failed job and step, returning unknown when causality is ambiguous, because log ordering alone cannot establish the repair owner.
- Neither section defines the next action for `cause: unknown`; §35 should specify bounded evidence gathering followed by escalation, because the new classification otherwise leaves the repair loop without a decision.
- §35 permits “earlier steps assumed run” despite requiring unreproducible context to refuse; its rerun item should identify prerequisites and distinguish diagnostic scripts from reproducible commands, because checkout, generated files, installed tools, and inherited environment can change the result.
- Failures in `uses:` steps have no explicit fallback contract; §33’s diagnosis item and §35’s context item should identify the action and report unavailable local reproduction, because listing unrelated `run:` commands can misdirect repairs.
- §35’s scalar oracle proves decoding but not execution context; its checkpoint should cover supported shell, environment, and working-directory combinations against a disposable workflow, because correct command text can still execute differently.
- The persisted episode has no stated repository, branch, workflow, or campaign binding; §35’s persistence item should define those identities and mismatch handling, because stale state can constrain an unrelated repair or close the wrong episode.
- Repair accounting is not defined around a crash or failed push; §35 should specify an idempotent attempt lifecycle keyed to the repair commit and reconcile it after restart, because recording before or after publication creates different counting failures.
- `repair close --green <sha>` has no stated evidence or ancestry validation; §35 should require successful read-back for the episode’s expected candidate and workflow, because an unrelated or stale green must not reset the cap.
- Missing episode state is treated as a fresh start even though it resides under disposable `build/`; §35 should define recovery when an active campaign loses that file, because cleanup can silently defeat the persisted repair bound.
- “Re-run ci-wait once” has no persisted retry accounting or disposition for a run that remains in progress; §35’s ceiling item should cover both queued and running states across restart, because the current wording can permit repeated full-ceiling waits.
- `--expect-no-run` requires only a workflow edit and a reason, without a stated authorization record or trigger-change check; §35 should bind the exception to the approved range and expected event, because an ordinary workflow edit could otherwise bypass required CI.
- A successful no-run observation is indistinguishable by exit code from green CI; §35 should expose and preserve a distinct outcome in campaign records and repair-close handling, because authorized retirement is not evidence that a repair passed.
- The diagnostic fallback lacks an explicit redaction policy for workflow commands, environment values, and fetched logs; §35 should own safe output with redaction fixtures, because CI evidence is copied into durable review and phase records.
```

## Live proof

Section gates driven on the working tree at `732a3947` (the implementation `43a59fc6`, every review answer through `f5072a67`, the D00 T04 §37 filings, and the recorded findings and run records) with this stamp's own records added, so the record totals count this section. The candidate names `732a3947` because a staged stamp tree cannot name itself. The drills' live lines ride the run file, quoted in the Verified line.

- `pwsh -NoProfile -File scripts/check-all.ps1`: `check-all: 19 gate(s) ok`

Provenance: candidate 732a3947e7c2b8ead1c4ffcd0ee2f77647349912; command pwsh -NoProfile -File scripts/check-all.ps1; exit 0; tool PowerShell 7.6.6; digest 3a1cd3d7952837dc118a12277f4d4922bc875ce117431122f43ae85ad8460023; path docs/reviews/00-workspace/D00-T04-s35.md; run 20260925-D00-T04-S35-astra

- `python scripts/review_prompt.py --self-test`: `review-prompt self-test: 425 cases, 0 failed`

Provenance: candidate 732a3947e7c2b8ead1c4ffcd0ee2f77647349912; command python scripts/review_prompt.py --self-test; exit 0; tool CPython 3.14.4; digest 9f819432b396a705a82e2d45d18c1793c132cb51505c5ef819f021ebf13ffbd5; path docs/reviews/00-workspace/D00-T04-s35.md; run 20260925-D00-T04-S35-astra

- `python scripts/campaign_guard.py --self-test`: `campaign_guard self-test: 59 cases, 0 failed`

Provenance: candidate 732a3947e7c2b8ead1c4ffcd0ee2f77647349912; command python scripts/campaign_guard.py --self-test; exit 0; tool CPython 3.14.4; digest a8482c196e3b1cdb5418be382ea1c43c7871bfccad68464411c0b3cea35dfbab; path docs/reviews/00-workspace/D00-T04-s35.md; run 20260925-D00-T04-S35-astra

- `python scripts/todo-runs.py --check`: `47 runs, 135 rounds: all resolve, all covered, counts agree`

Provenance: candidate 732a3947e7c2b8ead1c4ffcd0ee2f77647349912; command python scripts/todo-runs.py --check; exit 0; tool CPython 3.14.4; digest a700efb54985a833bbfc342876a5b9cbd73f58acca8751068e7fce3100d6c76c; path docs/reviews/00-workspace/D00-T04-s35.md; run 20260925-D00-T04-S35-astra

- `python scripts/todo-graph.py validate`: `30 todos, 231 sections -- 0 fatal, 0 warning(s), 98 adjacency advisory`

Provenance: candidate 732a3947e7c2b8ead1c4ffcd0ee2f77647349912; command python scripts/todo-graph.py validate; exit 0; tool CPython 3.14.4; digest ecdc1de835ef58193f23007c04ba98ac6e843913cb7ab8dd3cef6190306d0215; path docs/reviews/00-workspace/D00-T04-s35.md; run 20260925-D00-T04-S35-astra

- `python scripts/todo-claims.py`: `todo-claims: 113 claim(s) -- 113 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`

Provenance: candidate 732a3947e7c2b8ead1c4ffcd0ee2f77647349912; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest 05e85bd8ef4b7d15e2e056b42646bc5a452af2963be54c05c0052e258feae8d7; path docs/reviews/00-workspace/D00-T04-s35.md; run 20260925-D00-T04-S35-astra

- `python scripts/todo-graph.py plan --check`: `implementation plan is current -- 47/228 sections complete (21%)`

Provenance: candidate 732a3947e7c2b8ead1c4ffcd0ee2f77647349912; command python scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.4; digest 4319ac9ee3e19d2db032ce95d68d384a87de2696dfa4ccfb9c59149fcebdd607; path docs/reviews/00-workspace/D00-T04-s35.md; run 20260925-D00-T04-S35-astra

- `python scripts/todo-findings.py --check`: `todo-findings: ledger current, 368 finding(s), transitions complete`

Provenance: candidate 732a3947e7c2b8ead1c4ffcd0ee2f77647349912; command python scripts/todo-findings.py --check; exit 0; tool CPython 3.14.4; digest df393c34d1bffccc50430c20b995e6bc39241deee1c0c9e97f9ad8526eb6d5f1; path docs/reviews/00-workspace/D00-T04-s35.md; run 20260925-D00-T04-S35-astra
