# Review -- D00 T04 §14, Bar Bool Versions from the Export Gate

**Reviewed:** 2026-09-20
**Stance:** post-implementation
**Rounds:** Sol 1-2, Opus sign-off 3, all clean; Sol plan review (genesis, first attempt passed)
**Tier:** Full panel. The diff touches `scripts/todo-runs.py`: the runs checker every review's records and exports pass through.
**Attestation:** `docs/reviews/00-workspace/D00-T04-s14.attest.json` (round-3 manifest and candidate, emitted after the panel closed, read back exit 0)

## Candidate

| Commit | What it did |
| --- | --- |
| `bc5f92a` | both version gates assert `_is_int` first with type-naming messages; `export-version-not-bool` and `export-schema-not-bool` pins; suite 49 green |

No range adaptation: a single commit on trunk, all three rounds fencing it (base `HEAD^`), rounds 2-3 with the prior verdicts appended as a third chunk (receipt lines stripped). Recorded adaptation: fence and cross-check path arguments ride cygpath-translated Windows paths (MSYS2 converts bare path arguments but not paths inside `TITLE=path`); fenced bytes are unaffected, and D00 T04 §20 owns the durable fix.

## The three questions

**Does it make sense?** Yes. Both version gates assert `_is_int` first, the checker's own idiom, failing bools (and every other non-int) with a type-naming message; wrong-integer values keep their existing messages byte-identical; two pins cover both gates. No widening: valid and wrong-int inputs behave exactly as before.

**Is it logical?** Yes. The `export_version` type failure returns early, matching that gate's existing early return; the `schema` type failure appends and the value check becomes `elif`, preserving single-message output and the gate's fall-through shape. `_is_int` is reused, not reimplemented.

**Did it break anything else?** No. `todo-runs self-test` runs 49 green (47 plus the 2 new pins) with every pre-existing case intact; `todo-graph self-test` 511 green; `validate` 0 fatal; `todo-claims` 112 hold; `todo-findings --check` current; `todo-runs --check` agrees. `check-all` reports 12/13 with the same single known-red `graph self-test` gate (pre-existing stderr hint, filed to D00 T04 §20; the suite itself passes exit 0).

**source-defect:** not owed (no Win32 contract, registry layout, or external behavior at stake). **design:** not a surface. **Research pass:** skipped (pure infrastructure).

## Self-review

No fixes needed. Verified before the panel: the early-return/`elif` shapes preserve each gate's control flow; the type message changes text only for inputs that already failed (bools, strings, nulls, missing fields), with no self-test pin on the old non-int text; the two new pins mirror the module's tamper-and-match style. Checked and cleared.

## Independent review

`codex review --commit bc5f92a` (gpt-5.6-sol, high), pass of record, clean: "The change correctly rejects boolean and other non-integer export/schema versions while preserving existing handling for unsupported integer versions. The added regression tests pass, and no breaking behavior was identified." No panel-mapping entry: the pass predates the §15 independent-pass record shape and rides here in prose rather than as a mislabeled round line.

## GPT panel Round 1

Round 1 over `bc5f92a` (2 files, cross-check agrees).

`adversarial` approve
`consistency` approve
`integration` approve
`record` approve

```
RECEIPT sha=adb6dd2114b2b5acc099d66f5250f70aeb78d1e9718f042278faba7d33c90560 end=PANEL-7ef116815bb6a8da nonce=e976220f44571fc0
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost: 13,041 tokens (codex "tokens used"); latency unresolved.

## GPT panel Round 2

Round 2 over `bc5f92a` (2 files, cross-check agrees), round-1 verdicts appended. Same candidate: round 1 was clean, so there was nothing to fix.

`adversarial` approve
`consistency` approve
`integration` approve
`record` approve

```
RECEIPT sha=099d5158c6b6179c1894fcfa43e4d1b2a96fd739bfcc86b5f203428dccb34f3c end=PANEL-5d3f3ad7af716575 nonce=a8339d3d45c08186
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost: 17,409 tokens (codex "tokens used"); latency unresolved.

## Opus panel Round 3

Sign-off over `bc5f92a` (2 files, cross-check agrees), rounds 1-2 verdicts appended.

`adversarial` approve
`consistency` approve
`integration` approve
`record` approve

```
RECEIPT sha=38d601c482da3d0fab695bd2daa8352a35dbdf55cb5e5d7cda8fe625e04e968c end=PANEL-cb6f77544c3d7d08 nonce=538056a9a3377a09

**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

Output check: `PASS four lenses, one verdict each`, exit 0. Cost and latency unresolved (no runner telemetry).

## Plan review

gpt run over §14 plus §10: 10 findings, 1 accepted into D00 T04 §15, 8 rejected, 1 duplicate.

Manifest: sections [D00 T04 §14, D00 T04 §10]; dependents [none]; bytes 10115; run 20260920-D00-T04-S14-gpt

Ledger:
- [D00-T04-S14-PR1] [minor] section lacks exact paths and commands -> rejected (demonstrated executable: implemented from the text alone; /tmp fixture paths are ephemeral and cannot be pinned in advance)
- [D00-T04-S14-PR2] [minor] items ticked without stamp -> rejected (process confusion: the stamp lands after plan review per the lifecycle; the row is still open)
- [D00-T04-S14-PR3] [minor] single driven case for two gates -> rejected (moot: both legs driven via CLI and quoted in the Done note, exceeding the checkpoint)
- [D00-T04-S14-PR4] [minor] no false cases -> rejected (False never passed since False != 1 failed before; the uniform _is_int gate leaves no branch to pin; True cases cover the previously-passing shape)
- [D00-T04-S14-PR5] [minor] 29-vs-24 unexplained growth -> filed D00 T04 §15 (gate-evidence binding item)
- [D00-T04-S14-PR6] [minor] no exporter-to-checker round-trip -> rejected (premise false: fresh --export piped to --check-export, quoted exit 0)
- [D00-T04-S14-PR7] [minor] no table-driven malformed-type cases -> rejected (below bar: uniform gate, wrong-int and bool shapes pinned; the section's scope was bools)
- [D00-T04-S14-PR8] [minor] §10 Verified with unchecked Commit -> duplicate D00-T04-S13-PR16 (Commit representation, filed to §19)
- [D00-T04-S14-PR9] [minor] §10 checkpoint omits export commands -> rejected (stamped text frozen; the §10 evidence exists and was reviewed; no live gap)
- [D00-T04-S14-PR10] [minor] no integration owner for gate consumers -> rejected (the version gate forces co-evolution mechanically: a bumped producer fails the old gate loudly)
End of ledger

```
RECEIPT sha=b2f56c0a4a684a60b337db986405a163931987ef178470000e5d655a2615fde9 end=PLAN-d66e775ecb904138 nonce=20487319c0155b88
- D00 T04 §14 lacks exact script paths, fixture paths, and runnable commands for `check_export`, `--check-export`, and the self-test; add them to the implementation item and checkpoint so the section is executable and its evidence reproducible without conversation context.
- D00 T04 §14 marks implementation and commit complete but has no `Verified:`, `Review:`, candidate identity, or review-record path; add the full review stamp before any Implementation Order row closes because an unreviewed commit cannot satisfy the repository lifecycle.
- D00 T04 §14 requires a driven failure for “a bool-versioned export” while changing two independent gates; require separate driven cases for boolean `export_version` and boolean `schema` so one successful rejection cannot masquerade as proof of both.
- D00 T04 §14 exercises only `true`; add `false` cases for both version fields because both JSON boolean values must fail through the type diagnostic rather than accidentally reaching the integer-value diagnostic.
- D00 T04 §14 reports the live export as 29 runs while §10’s recorded export proof reports 24 runs without explaining the growth; bind the §14 evidence to a commit and state that the changed count is expected so reviewers can distinguish legitimate new records from drift.
- D00 T04 §14 does not require regeneration of an export followed by validation of that exact artifact; add an exporter-to-checker round-trip because testing a pre-existing “live export” can miss incompatibility between current producer and current gate.
- D00 T04 §14 has no explicit regression check for malformed non-boolean version types or the preserved wrong-integer diagnostics; add table-driven cases for string, null, number, missing field, and unsupported integer so the premium-quality gate has stable, complete error behavior.
- D00 T04 §10 has a `Verified:` stamp despite its commit checklist item remaining unchecked; reconcile the checkbox with the recorded candidate commits because contradictory completion state makes plan queries and human handoff unreliable.
- D00 T04 §10’s checkpoint requires evidence from driven `--report` and `--check`, but its completion narrative also relies on `--export` and `--check-export`; expand the checkpoint to name every required command so the formal proof matches the behavior claimed as complete.
- D00 T04 §14 has no dependent or integration owner for consumers of the hardened export gate; add an explicit regression obligation in D00 T04 §8 or a new follow-up section so future schema-version changes are tested through the decision workflow, not only within the checker.
```

Plan manifest: b2f56c0a4a68 PLAN-d66e775ecb904138. Cost: 14,898 tokens (codex "tokens used"); latency unresolved.

## Live proof

Section gates, each re-run after the last fix (candidate `bc5f92a`, rundir `/tmp/review-1xjywmIJ`). Digests are sha256 over the quoted bytes as shown (no trailing newline). All seven quotes bind the review's marker run (the plan-review run; panel rounds mint no run IDs).

- Suite: `todo-runs self-test: 49 cases, 0 failed`, covering both new pins (`export-version-not-bool`, `export-schema-not-bool`).

Provenance: candidate bc5f92a1ffbb62412c85e3ab7997df4751615472; command python scripts/todo-runs.py --self-test; exit 0; tool CPython 3.14.4; digest 8de6e88c6b309c16fb4cb97170bdd0a7493721cdafc796e357415176ad648057; path docs/reviews/00-workspace/D00-T04-s14.md; run 20260920-D00-T04-S14-gpt

- Bool-version drive: `R:\tmp\bool-export.json: export_version must be an int, got bool`, exit 1.

Provenance: candidate bc5f92a1ffbb62412c85e3ab7997df4751615472; command python scripts/todo-runs.py --check-export R:\tmp\bool-export.json; exit 1; tool CPython 3.14.4; digest 966b2989b7d4e29b3237569911c15e82c31eeb55ebbacdbd813480809d9b4cec; path docs/reviews/00-workspace/D00-T04-s14.md; run 20260920-D00-T04-S14-gpt

- Live export drive: `R:\tmp\live-export.json: export version 1, 29 runs, internally sound`, exit 0.

Provenance: candidate bc5f92a1ffbb62412c85e3ab7997df4751615472; command python scripts/todo-runs.py --check-export R:\tmp\live-export.json; exit 0; tool CPython 3.14.4; digest 8d942e81e5c4eb658d8f103d603a895b3d67f7d70f95e85d885c57667ca7c604; path docs/reviews/00-workspace/D00-T04-s14.md; run 20260920-D00-T04-S14-gpt

- Validate: `30 todos, 213 sections -- 0 fatal, 0 warning(s), 100 adjacency advisory`.

Provenance: candidate bc5f92a1ffbb62412c85e3ab7997df4751615472; command python scripts/todo-graph.py validate; exit 0; tool CPython 3.14.4; digest 9d0ccc36f8242b0f3d3049c5434d20f54daf65a9953aa9f80153f96cf08d5201; path docs/reviews/00-workspace/D00-T04-s14.md; run 20260920-D00-T04-S14-gpt

- Claims: `todo-claims: 112 claim(s) -- 112 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`.

Provenance: candidate bc5f92a1ffbb62412c85e3ab7997df4751615472; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest 95616e39a4200e824612521c83820e14d00a5fdbbc56d3d7625c0c90c32a1366; path docs/reviews/00-workspace/D00-T04-s14.md; run 20260920-D00-T04-S14-gpt

- Findings ledger: `todo-findings: ledger current, 203 finding(s), transitions complete`.

Provenance: candidate bc5f92a1ffbb62412c85e3ab7997df4751615472; command python scripts/todo-findings.py --check; exit 0; tool CPython 3.14.4; digest c50eb76fe8bad412e22dc7acf6f95b3903c31a98c7d2404ec8289fa612533ed1; path docs/reviews/00-workspace/D00-T04-s14.md; run 20260920-D00-T04-S14-gpt

- Sweep: `check-all: 1 of 13 gate(s) FAILED`. Twelve green; the single red gate is the known `graph self-test` stderr failure (pre-existing, filed to D00 T04 §20; the suite itself passes exit 0).

Provenance: candidate bc5f92a1ffbb62412c85e3ab7997df4751615472; command powershell scripts/check-all.ps1; exit 1; tool check-all.ps1; digest 65164f3b69025369d987f87c97bb792bcaae2d524f94dba129a55f43f133d22d; path docs/reviews/00-workspace/D00-T04-s14.md; run 20260920-D00-T04-S14-gpt
