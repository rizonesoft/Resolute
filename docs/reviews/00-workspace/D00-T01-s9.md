# Review -- D00 T01 §9, Remove the Linux Execution Surface

**Reviewed:** 2026-09-26
**Stance:** post-implementation
**Rounds:** independent pass (1 finding, already fixed by self-review); GPT 1 bulk (1 fixed); GPT 2 bulk (1 fixed); GPT 3 sign-off (1 fixed, stamp-invalidating); GPT 4 depth (approve).
**Tier:** Full panel. The diff touches `.github/workflows/plan.yml` (the CI every read-back relies on), `scripts/check-all.ps1`, `scripts/review_prompt.py`, `AGENTS.md`, and the README.
**Attestation:** `docs/reviews/00-workspace/D00-T01-s9.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `072661e3` | implementation: `scripts/wsl.sh` and the README's WSL flow deleted, `plan-gates` on `windows-2025` through Git Bash with `python`, the interpreter and canonicalization comments reworded, the tooling declared Windows-only in `AGENTS.md`, the D00 T05 §4 correction |
| `797b907e` | self-review: `shell: bash` per step, since job-level run defaults made `ci-wait` refuse every step's re-run |
| `7aff0bc3` | repairs two backspace characters a patch script wrote into the §9 note |
| `5cb4e802` | records the Windows CI and Git Bash fence proofs in the run file |
| `0ec273d4` | answers panel round 1: the inventory quoted in the run file |
| `27d2f1fb` | answers panel round 2: the inventory output committed as a capture |
| `bdb01ffa` | answers panel round 3: the capture regenerated untruncated |

Record commits (filings only, no reviewed content): `ac119e1e` (the plan-review filings: three claims in D00 T01 §9, one drill item in D00 T04 §42).

Range `072661e3^..bdb01ffa` (base `73dbcf91`), contiguous, no declaration owed. Each round fenced the range from `73dbcf91` to its own head. The rundir is session scratch outside the tree (/tmp/review-M7hZ9m7Q, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Makes sense: each item maps to a change (the deleted bridge and README block, the workflow's runner and interpreter, the reworded comments, the `AGENTS.md` line) or to recorded evidence (the inventory capture, the first Windows run's `shell:` lines, the Git Bash fence round). Logical: the one judgment the plan left open, what to do with Linux-runner CI, is a recorded default (move it to `windows-2025` rather than delete the read-back every stamp relies on). Broke nothing: self-review caught that job-level run defaults would make `ci-wait` print "cannot reproduce locally" for every `plan-gates` step and fixed it in `797b907e` before the independent pass; `check-all: 20 gate(s) ok`; both Windows runs green. Self-review also caught two backspace characters a patch script's `\b` wrote into the section note (`7aff0bc3`).

`source-defect`: owed and done: GitHub's own log (run 36236461973) is the source for the Windows bash template, and every step's `shell:` line reads `C:\Program Files\Git\bin\bash.EXE --noprofile --norc -e -o pipefail {0}`, which normalizes to the printed template. `design`: not owed (no rendered surface).

## Independent pass

`python scripts/panel_slots.py exec independent --commit 072661e3...`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 81s, clock 2026-09-26T10:47:08Z, cost unresolved (the review runner printed no token figure). Output and stderr stay in the build scratch outside the tree (build/rev9/independent.out, independent.err); the output is transcribed below.

### F1 -- job-level run defaults drop every step's re-run printout -- integration -- FIXED in `797b907e` (independent) [minor]

Self-review had already found and fixed it before the pass ran; the pass confirms the defect on the SHIP commit.

```text
The workflow migration introduces a reproduced regression in CI failure diagnostics: every plan-gate step loses its local reproduction instructions. No other actionable defects were identified.

Review comment:

- [P2] Declare Bash on each step instead of using job defaults — R:\GitHub\Resolute\.github\workflows\plan.yml:19-21
  When a plan gate fails, `ci-wait` can no longer provide its local reproduction command: `workflow_steps()` in `scripts/review_prompt.py` explicitly rejects job-level `defaults`. Running the parser against this commit reproduces `cannot reproduce locally: the job sets run defaults` for all four run steps, whereas the parent produces commands and shell context. Set `shell: bash` on each run step, or extend and test the parser's defaults support.
```

## GPT panel Round 1

Round 1 over `072661e3^..5cb4e802` with the independent pass answered. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 28s, clock 2026-09-26T10:48:40Z, cost 33883tokens. Output and stderr stay in the session scratch rundir outside the tree (r1.out, r1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` approve
`record` needs-attention

### F2 -- the inventory and removal results are summarized, not quoted -- record -- FIXED in `0ec273d4` (independent) [minor]

The exact command, its exclusions, and the hits before and after are quoted in the run file.

```text
RECEIPT sha=ce1e3658d7627bee685d914f2340c43bcf69aae378911ef5a038430405e55d58 end=PANEL-f31dddf47b114a9f nonce=35fffad0a65ce449
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-toolchain-and-gates.md:868-873` marks the inventory and removal proofs complete but supplies narrative summaries instead of the required quoted grep results. The exclusion arguments and item-by-item post-removal output are absent; `docs/phase-runs/2026-09-25-phase-0.md:275-276` records CI, fence, and gate results only. Record the exact inventory and post-removal commands, exclusions, and output to substantiate the checkpoint.
```

## GPT panel Round 2

Round 2 over `072661e3^..0ec273d4` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 25s, clock 2026-09-26T10:50:03Z, cost 34738tokens. Output and stderr stay in the session scratch rundir outside the tree (r2.out, r2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` approve
`record` needs-attention

### F3 -- the broader inventory output is still not committed whole -- record -- FIXED in `27d2f1fb` (independent) [minor]

The full driven output at both revisions, with each per-name grep, is committed under `docs/captures/runs/`.

```text
RECEIPT sha=1ffb9472d16b58c1149fc87c9db9ba8b69b0cc8a4828c619ceeb2dad992e88db end=PANEL-84faaf68d8980f7f nonce=10b83339c1ddef77
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)
1. `docs/phase-runs/2026-09-25-phase-0.md:277` and `todo/00-workspace/TODO-01-toolchain-and-gates.md:868-873` still do not supply the required quoted inventory and removal output. The added record summarizes filenames and counts for a `wsl` search, omitting the broader `wsl|ubuntu|linux|python3 ` inventory output; the per-name checks likewise provide summaries rather than complete commands and quoted results. Record those driven outputs with exact revisions and exclusions to substantiate the completed checkpoint.
```

## GPT panel Round 3

Round 3 over `072661e3^..27d2f1fb` the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 66s, clock 2026-09-26T10:51:41Z, cost 31795tokens. Output and stderr stay in the session scratch rundir outside the tree (r3.out, r3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` approve
`record` needs-attention

### F4 -- the capture truncates each matching line -- record -- FIXED in `bdb01ffa` (independent) [minor]

Regenerated with complete lines and git's exit codes. Blocking: the stamp would have quoted a false "committed whole", so round 4 ran.

```text
RECEIPT sha=4d0fdb5be71150cfb40e2fa9a704cdff82905ea838dae4588fe767d76700ca66 end=PANEL-e6d3e61a7313a5c0 nonce=a546de01dbd3d73b
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)

1. `docs/captures/runs/2026-09-26-d00-t01-s9-linux-inventory.txt:5,10,79-84,88-92,94-95,99,139-144,148-152,154-155,166,168-169` contains truncated grep matches, including sentences ending mid-word and incomplete fixture strings. Consequently, `todo/00-workspace/TODO-01-toolchain-and-gates.md:868` incorrectly claims the full driven output is “committed whole”; the prior record finding remains unresolved. Capture the complete matching lines without display-width truncation.
```

## GPT panel Round 4

Round 4 over `072661e3^..bdb01ffa` with prior verdicts appended (rounds past the sign-off run for blocking findings only). Slot `depth`: `panel_slots: slot depth model gpt-6-astra effort high timeout 600s`. Exit 0, latency 22s, clock 2026-09-26T10:53:38Z, cost 44539tokens. Output and stderr stay in the session scratch rundir outside the tree (r4.out, r4.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` approve
`consistency` approve
`integration` approve
`record` approve

```text
RECEIPT sha=b1458580f7ec3224cb6f8dcb717652abd28e63fdfe7caefcb4e30a2cad1e2aed end=PANEL-a6b785191d19dffa nonce=0f420979d35c1e72
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Plan review

Manifest: sections [D00 T01 §9, D00 T01 §5, D00 T04 §39]; dependents [none]; bytes 48353; run 20260926-D00-T01-S9-astra

`bytes 48353` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency D00 T01 §5 and its XREF neighbor D00 T04 §39; no section depends on D00 T01 §9. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 33s, clock 2026-09-26T10:54:17Z, cost 23838tokens. Check: `PASS 8 findings, one per line`.

Triage follows the operator direction of 2026-09-26 (the plan must not grow unconditionally): one finding lands as claims in this section, two fold into one item of the open D00 T04 §42, and the rest are rejected with a reason.

Ledger:
- [D00-T01-S9-PR1] [minor] the checkpoint's empty-inventory wording contradicts the retained records -> rejected item 6's Done note exempts the kept records from the grep criteria, which is the acceptance contract
- [D00-T01-S9-PR2] [minor] the grep cannot find differently named Linux dependencies -> rejected the one workflow now runs on windows-2025, and every subprocess the scripts start (git, gh, pwsh, powershell, Git Bash by path, python) is Windows-native
- [D00-T01-S9-PR3] [minor] the host matrix does not name each entry point's interpreter -> rejected check-all resolves Python and fails naming it, and the skills name Git Bash by path
- [D00-T01-S9-PR4] [minor] no drill fails a step under the Windows runner's Git Bash -> filed D00 T04 §42
- [D00-T01-S9-PR5] [minor] the Windows bash re-run is not reproduced locally -> filed D00 T04 §42
- [D00-T01-S9-PR6] [minor] no clean-checkout smoke check -> duplicate this file's Verification line "A fresh clone into a different absolute path builds with no file edited"
- [D00-T01-S9-PR7] [minor] nothing keeps Linux runner configuration from returning -> filed D00 T01 §9
- [D00-T01-S9-PR8] [minor] the capture's revisions are not bound as acceptance fields -> rejected the capture's header names both revisions and each grep's exit code, and the stamp names the reviewed candidate
End of ledger

8 findings: 3 filed (PR7 as three claims in D00 T01 §9; PR4 and PR5 as one item in D00 T04 §42, `ac119e1e`), 1 duplicate, 4 rejected.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=e59a747e8271e833101b8e2a0f36af15c9815b9db574b9b28f41a9001945bf7c end=PLAN-b3fbe378a4183f57 nonce=d4b885b04c0044b5
- D00 T01 §9’s Test checkpoint still requires an empty inventory and removed-name matches only in its own record, contradicting its explicit retention exceptions; add a current acceptance contract distinguishing removed execution paths from retained evidence so completion is objectively reviewable.
- The inventory’s `wsl|ubuntu|linux|python3 ` search cannot establish that every Linux execution dependency was found; D00 T01 §9 should also enumerate workflow runners, shell entry points, subprocess calls, and their callers because differently named distributions, Unix utilities, and indirect wrappers can escape this search.
- The blanket Windows host matrix does not identify each entry point’s supported interpreter, shell, version requirements, or executable resolution; D00 T01 §9 should record these prerequisites because Windows PowerShell, pwsh, Git Bash, and Python installations are not interchangeable.
- The new Windows CI evidence proves a successful run but specifies no deliberately failing gate under the migrated shell; D00 T01 §9, connected to §5, should require a negative workflow drill proving that Python failures propagate through Git Bash to a failed job.
- Template normalization against the Windows runner’s logged Bash command does not prove the generated local re-run reproduces that runner’s behavior; D00 T04 §39’s oracle coverage should own a Windows Bash drill executed locally, with D00 T01 §9 linking its evidence.
- No acceptance step requires validation from a clean Windows checkout with declared prerequisites; D00 T01 §9 should own this migration smoke check because an established development tree can conceal dependencies on ignored files, inherited PATH entries, or previously generated outputs.
- No section owns preventing Linux execution guidance or runner configuration from returning; add a narrowly scoped regression gate through D00 T01 §5 with policy defined by §9, preserving historical records and oracle fixtures while rejecting newly active unsupported paths.
- The removal evidence names a capture but does not make its before revision, reviewed candidate, and retained exceptions explicit acceptance fields; D00 T01 §9 should require that binding so later reviewers can distinguish final-candidate proof from successful runs on earlier revisions.
```
