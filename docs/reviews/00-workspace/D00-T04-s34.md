# Review -- D00 T04 §34, Campaign Guard Follow-Ups

**Reviewed:** 2026-09-25
**Stance:** post-implementation
**Rounds:** independent pass (5 findings: fixed); GPT 1 bulk (2 fixed); GPT 2 bulk (3 fixed); GPT 3 sign-off (1 filed below the blocking bar, D00 T04 §36).
**Tier:** Full panel. The diff touches the Stop hook every campaign session loads (`.claude/hooks/campaign-stop.ps1`), `scripts/campaign_guard.py` (a gate in `scripts/check-all.ps1`), and three skills (`process-plan`, `process-phase`, `review-todo-section`: the shared run contract).
**Attestation:** `docs/reviews/00-workspace/D00-T04-s34.attest.json`

## Candidate

| Commit | What it did |
| --- | --- |
| `5b1d5eaf` | implementation: `acquire`, `end`, `hook-error`, the untracked-content and bookkeeping fingerprint, the recorded hook error, the escalation handover, the skill rules, and the drive records |
| `c51a519e` | answers the independent pass: locked guard changes, NUL names, full untracked coverage, the session in the escalation command, a refused acquire cancels its job |
| `070bf0f3` | answers panel round 1: an OS lock that never expires under a live holder; the heartbeat checks ownership first |
| `94c8b920` | answers panel round 2: `hook-error` under the lock and through a malformed guard; every end through `end`, stall included |

Record commits (filings only, no reviewed content): `ea1095f0` (the sign-off and plan-review findings filed in D00 T04 §36).

Range `5b1d5eaf^..94c8b920` (base `97912864`), contiguous, no declaration owed. Each round fenced the range from `97912864` to its own head (8 files, cross-check `8 file(s) agree`); the fenced contract was checked to open with this section's heading. The rundir is session scratch outside the tree (/tmp/review-9fZlJDhI, not committed); every output it held is transcribed below.

## Self-review (in-session, costs no round)

Worked the three questions over the implementation. Two real defects surfaced before any reviewer: a path list piped into a native command reached git with a BOM on its first line (under a pwsh 7 parent even with a BOM-free encoder), so untracked content was silently never hashed and the check-all run of the self-test failed where the direct run passed; the paths now ride the argument list. Mutations: dropping the Critical-events strip fails the bookkeeping leg, a BOM on the path list fails the untracked-content leg, reading names without `-z` fails the non-ASCII edit leg, and dropping the past-bound fallback fails its leg. Two live drives ran against real `CronCreate` jobs (the five end paths that existed at the drive, closeout, park, plan done, operator stop, and escalation, and the heartbeat's idle fire and resume), recorded in the run file; the `stall` end path added in panel round 2 has self-test legs only. The architecture gate is not triggered (review tooling and harness wiring, no framework state). `source-defect` approves in-session: the lock relies on `msvcrt.locking` byte-range semantics (per handle, released on close or process exit), probed by the live-holder and dead-holder legs. `design` is not owed.

## Independent pass

`python scripts/panel_slots.py exec independent --commit 5b1d5eafdb2d821fa5494486c04ca3866ee2d0ac`: `panel_slots: slot independent model gpt-6-astra effort high timeout 900s`. Exit 0, latency 179s, cost unresolved (the review runner printed no token figure). Output and stderr stay in the session scratch rundir outside the tree (ind34.out, ind34.err); the output is transcribed below.

### F1 -- the owner's re-point is not atomic -- correctness -- FIXED in `c51a519e` (independent) [major]

A handover could land between the re-read and the replace, and the old owner then overwrote it. Every guard change now holds an interprocess lock.

### F2 -- a refused acquire leaves its heartbeat alive -- integration -- FIXED in `c51a519e` (independent) [major]

The job created before a refused `acquire` survived and could resume another session's run. `process-plan` now cancels it, and the heartbeat checks ownership.

### F3 -- quoted git names drop non-ASCII files from the fingerprint -- correctness -- FIXED in `c51a519e` (independent) [minor]

The untracked list is now read NUL-delimited.

### F4 -- paths past the hash bound drop out of the fingerprint -- correctness -- FIXED in `c51a519e` (independent) [minor]

Every path now counts: content for the first 500, size and write time for the next 5000, the name after that.

### F5 -- the escalation command omits the session -- correctness -- FIXED in `c51a519e` (independent) [major]

The hook's advertised `end --reason escalation` failed without `--session`; it now names the owning session and the `CronDelete`.

```text
The 36-case self-test passes, but additional fixtures reproduce an ownership race, missed untracked-file progress, and a failing escalation command. The acquisition workflow also leaves a recovery heartbeat behind when ownership is refused.

Full review comments:

- [P2] Make the ownership check and replacement atomic — R:/GitHub/Resolute/scripts/campaign_guard.py:105-110
  A handover can complete between the second `read_guard()` and `os.replace()`, after which the old owner overwrites the new owner's guard without authorization. A fixture interleaving these operations reproduced both acquisitions reporting success while ownership reverted to the old session. Protect acquisition, replacement, and deletion with a shared interprocess lock rather than treating read-then-replace as compare-and-swap. This enforces the single-session ownership invariant in [AGENTS.md:131](R:/GitHub/Resolute/AGENTS.md#L131).

- [P2] Cancel the new heartbeat when guard acquisition fails — R:/GitHub/Resolute/.claude/skills/process-plan/SKILL.md:63-63
  If another session owns the guard, step 1 has already created a heartbeat before this command refuses acquisition. No failure branch cancels that job, and its canonical prompt checks only whether a guard exists, not whether this session owns it, before resuming the campaign. Cancel the newly created job on acquisition failure and validate ownership before heartbeat recovery; otherwise the refusal still leaves a second session capable of resuming work, contrary to [AGENTS.md:131](R:/GitHub/Resolute/AGENTS.md#L131).

- [P2] Decode Git filenames before accessing untracked files — R:/GitHub/Resolute/.claude/hooks/campaign-stop.ps1:89-95
  With Git's default quoting, a filename such as `café.txt` is returned as a quoted, octal-escaped string. `Get-Item` treats that representation literally, fails silently, and excludes the file entirely from the fingerprint. Creating this file after three blocks reproduced a false stall trip; previously its addition changed the untracked-name fingerprint. Read a NUL-delimited filename list without Git's display quoting before accessing and hashing files.

- [P2] Retain fingerprint coverage beyond the hashing limit — R:/GitHub/Resolute/.claude/hooks/campaign-stop.ps1:89-90
  Once 500 untracked files exist, `Select-Object -First` discards every later path before either hashing or metadata fallback. Adding a lexically later 501st file after three blocks reproduced a stall trip despite new work; additions and deletions beyond the cutoff were detectable by the previous implementation. Bound content hashing while retaining names and metadata for the remaining files instead of dropping them.

- [P2] Include the owning session in the escalation command — R:/GitHub/Resolute/.claude/hooks/campaign-stop.ps1:153-153
  The recovery command supplied by the hook omits `--session`, but `end()` defaults it to an empty string and rejects it against every valid guard owner. Executing the advertised command returns exit 1 and leaves the guard and heartbeat active. Include the session argument and the subsequent heartbeat cancellation so following this instruction actually ends the run before reporting.
```

## GPT panel Round 1

Round 1 over `5b1d5eaf^..c51a519e` with the independent pass appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 35s, clock 2026-09-25T18:15:34Z, cost 36560tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-1.out, panel-1.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F6 -- an age-broken lock can break a live holder's lock -- adversarial -- FIXED in `070bf0f3` (independent) [major]

A create-exclusive lock file broken after 60 seconds could be broken under a live `end(plan-done)`. The lock is now an OS lock on an open handle, released only when its holder closes it or dies, and the plan query runs before the lock.

### F7 -- the heartbeat clears the hook error before checking ownership -- integration -- FIXED in `070bf0f3` (independent) [minor]

The ownership check is now the heartbeat's first step, and `hook-error --session` refuses another session's state.


```text
RECEIPT sha=c472efce73d0d91ec3eed1c62c9ed82bdb3d8d5dbfd3b22cdc82d937657ac0c8 end=PANEL-434d976fdab5696f nonce=de9dc905bec1936f
**adversarial: needs-attention** (1)
1. `scripts/campaign_guard.py:85-86,95-97,174-175,198-205`: Lock age does not prove its holder crashed. `end(plan-done)` holds the lock during an unbounded subprocess; after 60 seconds another process can remove the lock and hand over ownership. The original process can then resume and delete the new owner's guard, and its unconditional unlock can remove the replacement lock. Use an ownership-safe lock that cannot expire while its holder remains active.
**consistency: approve**
**integration: needs-attention** (1)
1. `.claude/skills/process-plan/SKILL.md:85-86`: The heartbeat executes the destructive `hook-error` read-and-clear before checking session ownership. A former owner's heartbeat after handover can consume the current owner's failure record and append it to its own run file before replying `NOT THE OWNER`; the owning heartbeat then sees no error. Check ownership before reading or modifying campaign state.
**record: approve**
```

## GPT panel Round 2

Round 2 over `5b1d5eaf^..070bf0f3` with prior verdicts appended. Slot `bulk`: `panel_slots: slot bulk model gpt-6-astra effort medium timeout 600s`. Exit 0, latency 44s, clock 2026-09-25T18:19:28Z, cost 38312tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-2.out, panel-2.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

### F8 -- hook-error checks and clears outside the lock -- adversarial -- FIXED in `94c8b920` (independent) [minor]

`hook-error` now holds the guard lock through its ownership check and clear.

### F9 -- the heartbeat's end branches bypass end -- integration -- FIXED in `94c8b920` (independent) [major]

The heartbeat's plan-over and stall branches and the terminal acceptance deleted the guard directly; they now run `end --reason plan-done` or the new `end --reason stall`.

### F10 -- a malformed guard blocks the report of its own error -- integration -- FIXED in `94c8b920` (independent) [minor]

`hook-error` now reports through an unreadable guard, flagged, instead of refusing.


```text
RECEIPT sha=1db9b49309a42eaf671f1e6c49dd88c8e5d32a357abb9ca32af4a3c7eaacd956 end=PANEL-612906dfee3f5a0e nonce=d46d01e6f43c48ad
**adversarial: needs-attention** (1)
1. `scripts/campaign_guard.py:244-270`: `hook_error()` checks ownership and clears state without holding `_Lock`. A handover between those operations lets the former owner consume the new owner's error record. Hold the ownership lock through the state read and replacement.
**consistency: approve**
**integration: needs-attention** (2)
1. `.claude/skills/process-plan/SKILL.md:88-89,102`: Heartbeat termination and terminal acceptance still instruct direct guard/state deletion, bypassing the new locked `end` command. A handover after the heartbeat's initial ownership check can therefore have its replacement guard deleted by the former owner. Route these termination paths through session-checked `end`.
2. `scripts/campaign_guard.py:244-253,466-487`: A malformed guard produces a recorded hook error, but `hook-error --session` calls `read_guard()` and raises before reporting that error. The fixture replaces the malformed guard before exercising reporting, masking the broken recovery path. Provide an ownership-safe reporting path for malformed guards and test it without first repairing the guard.
**record: approve**
```

## GPT panel Round 3

Round 3 over `5b1d5eaf^..94c8b920`, the Full sign-off, with prior verdicts appended. Slot `signoff`: `panel_slots: slot signoff model gpt-6-astra effort high timeout 600s`. Exit 0, latency 67s, clock 2026-09-25T18:22:51Z, cost 45575tokens. Output and stderr stay in the session scratch rundir outside the tree (panel-3.out, panel-3.err); the output is transcribed below. Check: `PASS four lenses, one verdict each`.

`adversarial` needs-attention
`consistency` approve
`integration` approve
`record` approve

### F11 -- state deletes still happen outside the lock -- adversarial -- FILED to `D00 T04 §36` (independent) [minor]

The `acquire`-time state reset and the heartbeat's missing-guard cleanup delete the state file without the lock, so a concurrent handover or acquisition could lose its fresh breaker counters. Below the blocking bar (breaker counters only: no user data, safety, or stamp effect), and the guard lifecycle was patched in rounds 1, 2, and 3 running, so the three-round rule files it (`ea1095f0`) rather than patching a fourth time. No round 4 runs.


```text
RECEIPT sha=00ff32a0e5fa5792530c097608d2bf2dcc1ca8dd682f5b685221d5034bc3fde1 end=PANEL-49b52048ea4d7b02 nonce=867a41a823570eeb
**adversarial: needs-attention** (1)
1. `.claude/skills/process-plan/SKILL.md:63,87,102`: State deletion still occurs outside the ownership lock. A handover after either successful acquisition, or a new acquisition after the heartbeat observes a missing guard, can install another owner's state before these instructions delete it. Move acquisition-time state reset and missing-guard cleanup into locked commands that recheck ownership or absence before deleting.
**consistency: approve**
**integration: approve**
**record: approve**
```

## Plan review

Manifest: sections [D00 T04 §34, D00 T04 §32]; dependents [none]; bytes 20100; run 20260925-D00-T04-S34-astra

`bytes 20100` counts the whole plan prompt canonicalized to LF newlines and UTF-8, as the review skill defines it; the fence's own `MANIFEST bytes=` counts the fenced body alone.

Round over the section plus its dependency D00 T04 §32; no review dependents. Slot `plan-primary`: `panel_slots: slot plan-primary model gpt-6-astra effort high timeout 900s`. Exit 0, latency 37s, clock 2026-09-25T18:23:45Z, cost 16808tokens. Check: `PASS 11 findings, one per line`.

Ledger:
- [D00-T04-S34-PR1] [minor] Stop-hook state writes sit outside the guard lock -> filed D00 T04 §36
- [D00-T04-S34-PR2] [minor] an obsolete same-session heartbeat is indistinguishable from the current one -> filed D00 T04 §36
- [D00-T04-S34-PR3] [minor] a failed CronDelete after end leaves no record -> filed D00 T04 §36
- [D00-T04-S34-PR4] [minor] startup has no crash recovery -> filed D00 T04 §36
- [D00-T04-S34-PR5] [minor] the 7-day expiry and a new-session migration are undriven -> filed D00 T04 §36
- [D00-T04-S34-PR6] [minor] the malformed-guard report is unproven end to end -> filed D00 T04 §36
- [D00-T04-S34-PR7] [minor] hook-error clears before the record is durable -> filed D00 T04 §36
- [D00-T04-S34-PR8] [minor] coverage past the fingerprint bounds is unstated -> filed D00 T04 §36
- [D00-T04-S34-PR9] [minor] the whole Critical events section is excluded -> filed D00 T04 §36
- [D00-T04-S34-PR10] [minor] terminal markers are not bound to the run -> filed D00 T04 §36
- [D00-T04-S34-PR11] [minor] nothing checks the heartbeat's health after startup -> filed D00 T04 §36
End of ledger

11 findings, all filed in D00 T04 §36 (`ea1095f0`); PR1 merged with the sign-off finding F11, PR6 with PR7.

Plan transcript (fenced, strips from every scan):

```text
RECEIPT sha=ccd129fef20b45c86fc00f450953a960d590f7f92cb3f2864f35b639aff24224 end=PLAN-efa967d0f6d5bcfc nonce=a8efb251badf4d9a
- Stop-hook state writes are absent from the exclusive-lock contract; extend §34 guard ownership to serialize hook reads and writes with handover and cleanup, proving an in-flight hook cannot recreate deleted state or overwrite a successor’s counters.
- Session identity alone does not distinguish replacement heartbeats within the same session; extend §34 ownership to check the job ID or acquisition generation, so an obsolete job cannot resume or terminate the current run.
- Cleanup deletes the guard and state before external heartbeat cancellation succeeds; extend §34 run-end cleanup with durable pending-cancellation evidence and retry fixtures, so a failed `CronDelete` remains recoverable after the job ID disappears.
- Startup has no recovery contract for interruption between `CronCreate`, guard acquisition, and recording ownership; add crash recovery to §34 acquisition and §32 startup, preventing orphan jobs and guards without functioning heartbeats.
- The claimed heartbeat drive demonstrates immediate replacement, not the specified seven-day expiry or new-session rewrite; extend §34 lifecycle acceptance with expiry-boundary and session-migration evidence, proving long campaigns retain recovery coverage.
- Malformed-guard error reporting conflicts with the heartbeat’s requirement to establish ownership first; reconcile §34 ownership and hook-error handling with an end-to-end malformed-guard heartbeat fixture, proving the error reaches Critical events without consuming another run’s state.
- `hook-error` clears the diagnostic before the heartbeat records it; extend §34 failure reporting with acknowledgement after durable recording, so an interrupted heartbeat cannot permanently erase the only failure evidence.
- Beyond 5,500 untracked files, edits to existing paths never affect the fingerprint, while large-file metadata changes can count without content changes; extend §34 fingerprinting with an explicit degraded-coverage policy and boundary fixtures, preventing unsupported claims that real progress always resets the breaker.
- Excluding the entire Critical events section can hide substantive recovery evidence as well as retries; refine §34 bookkeeping exclusion to identify bookkeeping records explicitly, so meaningful work logged there can still reset the breaker.
- Terminal markers have no stated binding to the current run or acquisition; extend §32 marker semantics and §34 cleanup fixtures to cover reused run files and historical markers, preventing an old `PARKED` or Closeout from releasing a new campaign.
- Guard ownership has no bounded health check for a missing, expired, or mismatched heartbeat after startup; add reconciliation to §34 lifecycle handling, making recovery coverage observable and repairing a lost job before an idle campaign becomes stranded.
```

## Live proof

Section gates driven on the working tree at `6fdde4cd` (the implementation `5b1d5eaf`, every review answer through `94c8b920`, the D00 T04 §36 filings, the recorded findings and run records, and the final heartbeat record) with this stamp's own records added, so the record totals count this section. The candidate names `6fdde4cd` because a staged stamp tree cannot name itself. The live drives' lines ride the run file, quoted in the Verified line.

- `pwsh -NoProfile -File scripts/check-all.ps1`: `check-all: 19 gate(s) ok`

Provenance: candidate 6fdde4cdcf1f4508c1b812d2b3af0993496b6192; command pwsh -NoProfile -File scripts/check-all.ps1; exit 0; tool PowerShell 7.6.6; digest d3f7b5a76f5fa532dbc39b9dcb98681db10e7a0f2616d5d0d757a9a408b3a70d; path docs/reviews/00-workspace/D00-T04-s34.md; run 20260925-D00-T04-S34-astra

- `python scripts/campaign_guard.py --self-test`: `campaign_guard self-test: 52 cases, 0 failed`

Provenance: candidate 6fdde4cdcf1f4508c1b812d2b3af0993496b6192; command python scripts/campaign_guard.py --self-test; exit 0; tool CPython 3.14.4; digest 4a620d28b84b89cf8510de50e928134fb18aef3c0e88eab4849df814b45683ae; path docs/reviews/00-workspace/D00-T04-s34.md; run 20260925-D00-T04-S34-astra

- `python scripts/review_prompt.py --self-test`: `review-prompt self-test: 377 cases, 0 failed`

Provenance: candidate 6fdde4cdcf1f4508c1b812d2b3af0993496b6192; command python scripts/review_prompt.py --self-test; exit 0; tool CPython 3.14.4; digest 6479e1230c8565a7354940082239385da59e76f5d96843255f7e047aba50cfee; path docs/reviews/00-workspace/D00-T04-s34.md; run 20260925-D00-T04-S34-astra

- `python scripts/todo-runs.py --check`: `46 runs, 131 rounds: all resolve, all covered, counts agree`

Provenance: candidate 6fdde4cdcf1f4508c1b812d2b3af0993496b6192; command python scripts/todo-runs.py --check; exit 0; tool CPython 3.14.4; digest b15ff26a661cffb79395d56d7407e30fc45cc869a10ac3ae829a2259c1cffa0d; path docs/reviews/00-workspace/D00-T04-s34.md; run 20260925-D00-T04-S34-astra

- `python scripts/todo-graph.py validate`: `30 todos, 230 sections -- 0 fatal, 0 warning(s), 98 adjacency advisory`

Provenance: candidate 6fdde4cdcf1f4508c1b812d2b3af0993496b6192; command python scripts/todo-graph.py validate; exit 0; tool CPython 3.14.4; digest cca051a52225ec3a69886b43cca1656bd976f0509581b1386445319fdbd4dbaf; path docs/reviews/00-workspace/D00-T04-s34.md; run 20260925-D00-T04-S34-astra

- `python scripts/todo-claims.py`: `todo-claims: 113 claim(s) -- 113 hold, 0 stale, 0 malformed; coverage 4/20, floor 3`

Provenance: candidate 6fdde4cdcf1f4508c1b812d2b3af0993496b6192; command python scripts/todo-claims.py; exit 0; tool CPython 3.14.4; digest a15115277ca24891ffac49afdcdad24999ef5d2f669acc2a1db00759d44f18dd; path docs/reviews/00-workspace/D00-T04-s34.md; run 20260925-D00-T04-S34-astra

- `python scripts/todo-graph.py plan --check`: `implementation plan is current -- 46/227 sections complete (20%)`

Provenance: candidate 6fdde4cdcf1f4508c1b812d2b3af0993496b6192; command python scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.4; digest 2843cc15ca9c776e3229782de98f4cf3288606b6faedd284a25013de7aec9460; path docs/reviews/00-workspace/D00-T04-s34.md; run 20260925-D00-T04-S34-astra

- `python scripts/todo-findings.py --check`: `todo-findings: ledger current, 357 finding(s), transitions complete`

Provenance: candidate 6fdde4cdcf1f4508c1b812d2b3af0993496b6192; command python scripts/todo-findings.py --check; exit 0; tool CPython 3.14.4; digest e701e540648548d5ff70031beca3b72cc001f963e53c80313c26a06df5e1245c; path docs/reviews/00-workspace/D00-T04-s34.md; run 20260925-D00-T04-S34-astra
